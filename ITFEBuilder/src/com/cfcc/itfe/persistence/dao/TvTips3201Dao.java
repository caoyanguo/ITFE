    



package com.cfcc.itfe.persistence.dao;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.Statement;
import java.sql.SQLException;
import java.sql.Date;
import java.sql.Time;
import java.sql.Timestamp ;
import java.sql.Clob;

import com.cfcc.jaf.persistence.jaform.parent.* ;

import java.math.BigDecimal;

import java.util.List;
import java.util.ArrayList;
import java.util.HashMap;

import java.io.StringReader;
import java.io.Reader;
import java.io.IOException;

import com.cfcc.itfe.persistence.dto.TvTips3201Dto ;
import com.cfcc.itfe.persistence.pk.TvTips3201PK ;


/**
 * <p>Title: DAO of table: TV_TIPS_3201</p>
 * <p>Description: Data Access Object  </p>
 * <p>Copyright: Copyright (c) 2005</p>
 * <p>Company: CFCC </p>
 * <p>Generation Time: 2014-12-25 15:29:04 </p>
 *  Do NOT change this file by hand.
 *  This java source file is generated by JAF_ORM .
 *  dao.vm timestamp: 2007-04-06 08:30:00
 ******************************************************
 * <p> <b>change record</b><br>
 *  make dao methods simply.<br>
 *  especially make update,delete methods simply.<br>
 *  remove "..withCheck" method,withCheck will impl in JAFOrmTemplate.<br>
 *  remove "delete(IDto _dto,..)" method.<br></p>
 * @author win7
 */

public class TvTips3201Dao  implements IDao
{


    /* SQL to insert data */
    private static final String SQL_INSERT =
        "INSERT INTO TV_TIPS_3201 ("
          + "S_ORGCODE,S_CHKDATE,S_PACKNO,S_BILLORG,S_ORGTYPE"
          + ",S_ORIMSGNO,S_ORISENDORGCODE,S_ORIENTRUSTDATE,S_ORIPACKNO,I_ALLNUM"
          + ",F_ALLAMT,S_CHECK"
        + ") VALUES ("
        + "?,?,?,?,?,?,?,?,?,?,?,?)";


    private static final String SQL_INSERT_WITH_RESULT = "SELECT * FROM FINAL TABLE( " + SQL_INSERT + " )";
    
    /* SQL to select data */
    private static final String SQL_SELECT =
        "SELECT "
        + "TV_TIPS_3201.S_ORGCODE, TV_TIPS_3201.S_CHKDATE, TV_TIPS_3201.S_PACKNO, TV_TIPS_3201.S_BILLORG, TV_TIPS_3201.S_ORGTYPE, "
        + "TV_TIPS_3201.S_ORIMSGNO, TV_TIPS_3201.S_ORISENDORGCODE, TV_TIPS_3201.S_ORIENTRUSTDATE, TV_TIPS_3201.S_ORIPACKNO, TV_TIPS_3201.I_ALLNUM, "
        + "TV_TIPS_3201.F_ALLAMT, TV_TIPS_3201.S_CHECK "
        + "FROM TV_TIPS_3201 "
        +" WHERE " 
        + "S_ORGCODE = ? AND S_CHKDATE = ? AND S_PACKNO = ? AND S_ORIMSGNO = ? AND S_ORIPACKNO = ?"
        ;
    /* SQL to select for update data */
    private static final String SQL_SELECT_FOR_UPDATE =
        "SELECT "
        + "TV_TIPS_3201.S_ORGCODE, TV_TIPS_3201.S_CHKDATE, TV_TIPS_3201.S_PACKNO, TV_TIPS_3201.S_BILLORG, TV_TIPS_3201.S_ORGTYPE, "
        + "TV_TIPS_3201.S_ORIMSGNO, TV_TIPS_3201.S_ORISENDORGCODE, TV_TIPS_3201.S_ORIENTRUSTDATE, TV_TIPS_3201.S_ORIPACKNO, TV_TIPS_3201.I_ALLNUM, "
        + "TV_TIPS_3201.F_ALLAMT, TV_TIPS_3201.S_CHECK "
        + "FROM TV_TIPS_3201 "
        +" WHERE " 
        + "S_ORGCODE = ? AND S_CHKDATE = ? AND S_PACKNO = ? AND S_ORIMSGNO = ? AND S_ORIPACKNO = ? FOR UPDATE"
        ;

      /* SQL to select data (SCROLLABLE)*/
     private static final String SQL_SELECT_BATCH_SCROLLABLE =
        "SELECT "
        + "  TV_TIPS_3201.S_ORGCODE  , TV_TIPS_3201.S_CHKDATE  , TV_TIPS_3201.S_PACKNO  , TV_TIPS_3201.S_BILLORG  , TV_TIPS_3201.S_ORGTYPE "
        + " , TV_TIPS_3201.S_ORIMSGNO  , TV_TIPS_3201.S_ORISENDORGCODE  , TV_TIPS_3201.S_ORIENTRUSTDATE  , TV_TIPS_3201.S_ORIPACKNO  , TV_TIPS_3201.I_ALLNUM "
        + " , TV_TIPS_3201.F_ALLAMT  , TV_TIPS_3201.S_CHECK "
        + "FROM TV_TIPS_3201 ";



    /* SQL to select batch data */
    private static final String SQL_SELECT_BATCH =
        "SELECT "
        + "TV_TIPS_3201.S_ORGCODE, TV_TIPS_3201.S_CHKDATE, TV_TIPS_3201.S_PACKNO, TV_TIPS_3201.S_BILLORG, TV_TIPS_3201.S_ORGTYPE, "
        + "TV_TIPS_3201.S_ORIMSGNO, TV_TIPS_3201.S_ORISENDORGCODE, TV_TIPS_3201.S_ORIENTRUSTDATE, TV_TIPS_3201.S_ORIPACKNO, TV_TIPS_3201.I_ALLNUM, "
        + "TV_TIPS_3201.F_ALLAMT, TV_TIPS_3201.S_CHECK "
        + "FROM TV_TIPS_3201 " ;
        

   /* SQL to select batch data where condition  */
    private static final String SQL_SELECT_BATCH_WHERE =" ( "
        + "S_ORGCODE = ? AND S_CHKDATE = ? AND S_PACKNO = ? AND S_ORIMSGNO = ? AND S_ORIPACKNO = ?)"
        ;


    /* SQL to update data */
    private static final String SQL_UPDATE =
        "UPDATE TV_TIPS_3201 SET "
        + "S_BILLORG =?,S_ORGTYPE =?,S_ORISENDORGCODE =?,S_ORIENTRUSTDATE =?,I_ALLNUM =?, "
        + "F_ALLAMT =?,S_CHECK =? "
        + "WHERE "
        + "S_ORGCODE = ? AND S_CHKDATE = ? AND S_PACKNO = ? AND S_ORIMSGNO = ? AND S_ORIPACKNO = ?"
        ;

	/* SQL to update data, support LOB */
    private static final String SQL_UPDATE_LOB =
        "UPDATE TV_TIPS_3201 SET "
        + "S_BILLORG =?, S_ORGTYPE =?, S_ORISENDORGCODE =?, S_ORIENTRUSTDATE =?, I_ALLNUM =?,  "
        + "F_ALLAMT =?, S_CHECK =? "
        + "WHERE "
        + "S_ORGCODE = ? AND S_CHKDATE = ? AND S_PACKNO = ? AND S_ORIMSGNO = ? AND S_ORIPACKNO = ?"
        ;	
	
    /* SQL to delete data */
    private static final String SQL_DELETE =
        "DELETE FROM TV_TIPS_3201 " 
        + " WHERE "
        + "S_ORGCODE = ? AND S_CHKDATE = ? AND S_PACKNO = ? AND S_ORIMSGNO = ? AND S_ORIPACKNO = ?"
        ;


	/**
	*  批量查询 一次最多查询的参数数量
	*/
	
	public static final int FIND_BATCH_SIZE = 150  / 5;
	



   /**
   * Create a new record in Database.
   */
  public void create(IDto _dto,  Connection conn) throws SQLException
  {
     TvTips3201Dto dto = (TvTips3201Dto)_dto ;
     String msgValid = dto.checkValid() ;
     if (msgValid != null)
     	throw new SQLException("插入错误，"+msgValid) ;

     PreparedStatement ps = null;
     try
     {
         ps = conn.prepareStatement(SQL_INSERT);
          ps.setString(1, dto.getSorgcode());

          ps.setString(2, dto.getSchkdate());

          ps.setString(3, dto.getSpackno());

          ps.setString(4, dto.getSbillorg());

          ps.setString(5, dto.getSorgtype());

          ps.setString(6, dto.getSorimsgno());

          ps.setString(7, dto.getSorisendorgcode());

          ps.setString(8, dto.getSorientrustdate());

          ps.setString(9, dto.getSoripackno());

          if (dto.getIallnum()==null)
            ps.setNull(10, java.sql.Types.INTEGER);
         else
            ps.setInt(10, dto.getIallnum().intValue());
          ps.setBigDecimal(11, dto.getFallamt());

          ps.setString(12, dto.getScheck());

         ps.executeUpdate();
     }
     finally
     {
         close(ps);
     }
     return  ;
  }

   /**
   * Create a new record in Database. and return result, only used when having generated key
   */
   public IDto createWithResult(IDto _dto,  Connection conn) throws SQLException
   {
       TvTips3201Dto dto = (TvTips3201Dto)_dto ;
       String msgValid = dto.checkValid() ;
       if (msgValid != null)
           throw new SQLException("插入错误，"+msgValid) ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try
       {
           ps = conn.prepareStatement(SQL_INSERT_WITH_RESULT);
            ps.setString(1, dto.getSorgcode());
            ps.setString(2, dto.getSchkdate());
            ps.setString(3, dto.getSpackno());
            ps.setString(4, dto.getSbillorg());
            ps.setString(5, dto.getSorgtype());
            ps.setString(6, dto.getSorimsgno());
            ps.setString(7, dto.getSorisendorgcode());
            ps.setString(8, dto.getSorientrustdate());
            ps.setString(9, dto.getSoripackno());
            if (dto.getIallnum()==null)
              ps.setNull(10, java.sql.Types.INTEGER);
           else
              ps.setInt(10, dto.getIallnum().intValue());
            ps.setBigDecimal(11, dto.getFallamt());
            ps.setString(12, dto.getScheck());
           rs = ps.executeQuery();
           List results = getResults(rs);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }
        finally
        {
           close(ps);
           close(rs);
        }
   }

 	/**
	* Create batch new record in Database. it only create the first layer dto
	*/
    public void create(IDto[] _dtos, Connection conn)    throws SQLException
    {
        TvTips3201Dto dto  ;
     	for (int i= 0; i< _dtos.length ; i++)
 	    {
 		    dto  = (TvTips3201Dto)_dtos[i] ; 
    	    String msgValid = dto.checkValid() ;
        	if (msgValid != null)
     	    	throw new SQLException("插入错误，"+msgValid) ;
     	}
 	    PreparedStatement ps = null;
        try
        {
            ps = conn.prepareStatement(SQL_INSERT);
       
     	    for (int i= 0; i< _dtos.length ; i++)
     	    {
 	    	    dto  = (TvTips3201Dto)_dtos[i] ; 
  
               ps.setString(1, dto.getSorgcode());
  
               ps.setString(2, dto.getSchkdate());
  
               ps.setString(3, dto.getSpackno());
  
               ps.setString(4, dto.getSbillorg());
  
               ps.setString(5, dto.getSorgtype());
  
               ps.setString(6, dto.getSorimsgno());
  
               ps.setString(7, dto.getSorisendorgcode());
  
               ps.setString(8, dto.getSorientrustdate());
  
               ps.setString(9, dto.getSoripackno());
  
               if (dto.getIallnum()==null)
                  ps.setNull(10, java.sql.Types.INTEGER);
               else
                  ps.setInt(10, dto.getIallnum().intValue());
  
               ps.setBigDecimal(11, dto.getFallamt());
  
               ps.setString(12, dto.getScheck());
               ps.addBatch(); 
           }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }
        return  ;
   }




     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto find(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TvTips3201PK pk = (TvTips3201PK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT);
           ps.setString(1, pk.getSorgcode());

           ps.setString(2, pk.getSchkdate());

           ps.setString(3, pk.getSpackno());

           ps.setString(4, pk.getSorimsgno());

           ps.setString(5, pk.getSoripackno());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }

     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto findForUpdate(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TvTips3201PK pk = (TvTips3201PK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT_FOR_UPDATE);
           ps.setString(1, pk.getSorgcode());

           ps.setString(2, pk.getSchkdate());

           ps.setString(3, pk.getSpackno());

           ps.setString(4, pk.getSorimsgno());

           ps.setString(5, pk.getSoripackno());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }
  
     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto[] find(IPK[] _pks, Connection conn, boolean isLobSupport) throws SQLException
    {
        TvTips3201PK pk ;
        
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TvTips3201PK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 
        PreparedStatement ps = null;
        ResultSet rs = null;
        
         
        List results = new ArrayList();  
		for (int iBegin = 0; iBegin < pks.size(); iBegin += FIND_BATCH_SIZE)   //
		{
        
            try {
          		int iFindBatchSize = pks.size() - iBegin ;
	    		if (iFindBatchSize >FIND_BATCH_SIZE )
		    		iFindBatchSize = FIND_BATCH_SIZE ;
		  
        	    StringBuffer sb = new StringBuffer() ;
            	sb.append(SQL_SELECT_BATCH).append(" WHERE ").append(SQL_SELECT_BATCH_WHERE);

	    		for (int i = iBegin+1; i < iBegin + iFindBatchSize; i++)
		    	{
			    	sb.append("OR").append(SQL_SELECT_BATCH_WHERE) ;
    			}
        	
                ps = conn.prepareStatement(sb.toString());

                for (int i = iBegin;i < iBegin + iFindBatchSize; i++)
                {
                    pk  = (TvTips3201PK)(pks.get(i)) ; 
                   ps.setString((i-iBegin)*5+1, pk.getSorgcode());

                   ps.setString((i-iBegin)*5+2, pk.getSchkdate());

                   ps.setString((i-iBegin)*5+3, pk.getSpackno());

                   ps.setString((i-iBegin)*5+4, pk.getSorimsgno());

                   ps.setString((i-iBegin)*5+5, pk.getSoripackno());

			
	            }
                rs = ps.executeQuery();
                results.addAll(getResults(rs,0,isLobSupport)); //
            }finally {
                close(rs);
                close(ps);
            }
        }
        if (results!=null && results.size() > 0)
        {
            TvTips3201Dto[] dtos = new TvTips3201Dto[0];
		    dtos = (TvTips3201Dto[]) results.toArray(dtos) ;
		    return dtos ;
        }
        else
        {
            return null;
        }
    }

  
  
  



    /**
	    * 得到查询语句
	    */
    public String getSelectSQL() 
    {
	        return SQL_SELECT_BATCH_SCROLLABLE ;
    }
   

	public List getResults(ResultSet rs) throws SQLException
    {
    	return getResults(rs, 0) ;
    }
  

    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize) throws SQLException
    {
		return getResults(rs,maxSize,false);
	}
    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize, boolean isLobSupport) throws SQLException
    {
        List results = new ArrayList();
        String str ;
        Reader reader;
        Clob clob;
        char[] chars ;
        
        while (rs.next())
         {
             TvTips3201Dto  dto = new  TvTips3201Dto ();
             //S_ORGCODE
             str = rs.getString("S_ORGCODE");
             if (str == null)
                dto.setSorgcode(null);
             else
                dto.setSorgcode(str.trim());

             //S_CHKDATE
             str = rs.getString("S_CHKDATE");
             if (str == null)
                dto.setSchkdate(null);
             else
                dto.setSchkdate(str.trim());

             //S_PACKNO
             str = rs.getString("S_PACKNO");
             if (str == null)
                dto.setSpackno(null);
             else
                dto.setSpackno(str.trim());

             //S_BILLORG
             str = rs.getString("S_BILLORG");
             if (str == null)
                dto.setSbillorg(null);
             else
                dto.setSbillorg(str.trim());

             //S_ORGTYPE
             str = rs.getString("S_ORGTYPE");
             if (str == null)
                dto.setSorgtype(null);
             else
                dto.setSorgtype(str.trim());

             //S_ORIMSGNO
             str = rs.getString("S_ORIMSGNO");
             if (str == null)
                dto.setSorimsgno(null);
             else
                dto.setSorimsgno(str.trim());

             //S_ORISENDORGCODE
             str = rs.getString("S_ORISENDORGCODE");
             if (str == null)
                dto.setSorisendorgcode(null);
             else
                dto.setSorisendorgcode(str.trim());

             //S_ORIENTRUSTDATE
             str = rs.getString("S_ORIENTRUSTDATE");
             if (str == null)
                dto.setSorientrustdate(null);
             else
                dto.setSorientrustdate(str.trim());

             //S_ORIPACKNO
             str = rs.getString("S_ORIPACKNO");
             if (str == null)
                dto.setSoripackno(null);
             else
                dto.setSoripackno(str.trim());

             //I_ALLNUM
             str = rs.getString("I_ALLNUM");
             if(str!=null){
                dto.setIallnum(new Integer(str));
             }

             //F_ALLAMT
           dto.setFallamt(rs.getBigDecimal("F_ALLAMT"));

             //S_CHECK
             str = rs.getString("S_CHECK");
             if (str == null)
                dto.setScheck(null);
             else
                dto.setScheck(str.trim());



            results.add(dto);
            
            if(maxSize >0  && results.size() == maxSize)
            {
            	break ;
            }
            
         }
        
		return results ;
    }

  /**
   * populate resultSet as cclass
   */
  protected IDto[] getChildrenResults(ResultSet rs ,Class _dto) throws SQLException{

      throw new SQLException("本dto没有与其它dto相关联，无法应用此方法");
  }


     /**
     * Update a record in Database.
     */

    public void update(IDto _dto, Connection conn, boolean isLobSupport) throws SQLException
    {
        String msgValid = _dto.checkValid() ;
        if (msgValid != null)
            throw new SQLException("修改错误，"+msgValid) ;
        TvTips3201Dto dto = (TvTips3201Dto)_dto ;
        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            int pos = 1;
            //S_BILLORG
            ps.setString(pos, dto.getSbillorg());
            pos++;

            //S_ORGTYPE
            ps.setString(pos, dto.getSorgtype());
            pos++;

            //S_ORISENDORGCODE
            ps.setString(pos, dto.getSorisendorgcode());
            pos++;

            //S_ORIENTRUSTDATE
            ps.setString(pos, dto.getSorientrustdate());
            pos++;

            //I_ALLNUM
            if (dto.getIallnum()==null)
                ps.setNull(pos, java.sql.Types.INTEGER);
            else
                ps.setInt(pos, dto.getIallnum().intValue());
            pos++;

            //F_ALLAMT
            ps.setBigDecimal(pos, dto.getFallamt());
            pos++;

            //S_CHECK
            ps.setString(pos, dto.getScheck());
            pos++;


           //S_ORGCODE
           ps.setString(pos, dto.getSorgcode());
           pos++;
           //S_CHKDATE
           ps.setString(pos, dto.getSchkdate());
           pos++;
           //S_PACKNO
           ps.setString(pos, dto.getSpackno());
           pos++;
           //S_ORIMSGNO
           ps.setString(pos, dto.getSorimsgno());
           pos++;
           //S_ORIPACKNO
           ps.setString(pos, dto.getSoripackno());
           pos++;
           ps.executeUpdate();
        }
        finally
        {
           close(ps);
        }

        return ;
    }
    
    
    
      /**
     * Update batch record in Database.
     */

    public void update(IDto[] _dtos, Connection conn, boolean isLobSupport) throws SQLException
    {
     
     	 TvTips3201Dto dto  ;
         for (int i= 0; i< _dtos.length ; i++)
         {
            dto  = (TvTips3201Dto)_dtos[i] ; 
            String msgValid = dto.checkValid() ;
            if (msgValid != null)
                throw new SQLException("修改错误，"+msgValid) ;
         }

        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            
            for (int i= 0; i< _dtos.length ; i++)
            {
                dto  = (TvTips3201Dto)_dtos[i] ; 
                int pos = 1;
                //S_BILLORG
                 ps.setString(pos, dto.getSbillorg());
                pos++;

                //S_ORGTYPE
                 ps.setString(pos, dto.getSorgtype());
                pos++;

                //S_ORISENDORGCODE
                 ps.setString(pos, dto.getSorisendorgcode());
                pos++;

                //S_ORIENTRUSTDATE
                 ps.setString(pos, dto.getSorientrustdate());
                pos++;

                //I_ALLNUM
                 if (dto.getIallnum()==null)
                   ps.setNull(pos, java.sql.Types.INTEGER);
                else
                   ps.setInt(pos, dto.getIallnum().intValue());
                pos++ ;

                //F_ALLAMT
                 ps.setBigDecimal(pos, dto.getFallamt());
                pos++;

                //S_CHECK
                 ps.setString(pos, dto.getScheck());
                pos++;


               //S_ORGCODE
               ps.setString(pos, dto.getSorgcode());
               pos++;
               //S_CHKDATE
               ps.setString(pos, dto.getSchkdate());
               pos++;
               //S_PACKNO
               ps.setString(pos, dto.getSpackno());
               pos++;
               //S_ORIMSGNO
               ps.setString(pos, dto.getSorimsgno());
               pos++;
               //S_ORIPACKNO
               ps.setString(pos, dto.getSoripackno());
               pos++;
		       ps.addBatch(); 
	       }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }

        return ;
    }



    /**
     * delete a record in Database.
     */
    public void delete(IPK _pk, Connection conn) throws SQLException
    {
       
      
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return ;
        //	throw new SQLException("删除错误，"+msgValid) ;
       }
       TvTips3201PK pk = (TvTips3201PK)_pk ;


       PreparedStatement ps = null;
       try {
           ps = conn.prepareStatement(SQL_DELETE);
           ps.setString(1, pk.getSorgcode());
           ps.setString(2, pk.getSchkdate());
           ps.setString(3, pk.getSpackno());
           ps.setString(4, pk.getSorimsgno());
           ps.setString(5, pk.getSoripackno());
           ps.executeUpdate();
        }
        finally
        {
            close(ps);
        }
    }


     /**
     * delete batch record in Database.
     */
    public void delete(IPK[] _pks, Connection conn) throws SQLException
    {
        TvTips3201PK pk ;
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TvTips3201PK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 		
 		
        PreparedStatement ps = null;
        try
         {
            ps = conn.prepareStatement(SQL_DELETE);
             
       		for (int i= 0; i< pks.size() ; i++)
       		{
       			pk  = (TvTips3201PK)(pks.get(i)) ; 
                ps.setString(1, pk.getSorgcode());
                ps.setString(2, pk.getSchkdate());
                ps.setString(3, pk.getSpackno());
                ps.setString(4, pk.getSorimsgno());
                ps.setString(5, pk.getSoripackno());
		        ps.addBatch() ;
			}
            ps.executeBatch();
        }
        finally
        {
            close(ps);
        }      
    }



    /**
    * Check IChangerNumber or UpdateTimeStamp.
    */
    public void check(IDto _dto, Connection conn) throws SQLException
    {
                     		throw new SQLException("数据库表：TV_TIPS_3201没有检查修改的字段");
    }


	/**
	* synchronize values between parent and children
	*/
    public void syncToChildren(IDto _dto)     throws SQLException{
       throw new SQLException("本记录没有与其它记录相关联，无法进行主记录与明细记录之间的数据同步。");
  }

	/**
	*get children
	*/
    public IDto[] findChildren (IDto _dto, Connection conn, boolean isLobSupport)     throws SQLException{
         throw new SQLException("本dto没有与其它dto相关联，无法得到子dto");
  }    
    
  /**
   *return the children IDto class
   * @return Class
   */
  public Class getChildrenClass(){
	  return null;
  };    



    /**
     * Close JDBC Statement.
     * @param stmt  Statement to be closed.
     */
    private void close(Statement stmt)
    {
        if (stmt != null)
        {
            try
            {
                stmt.close();
            }
            catch(SQLException    e)
            {}
        }
    }

    /**
     * Close JDBC ResultSet.
     * @param rs  ResultSet to be closed.
     */
    private void close(ResultSet rs)
    {
        if (rs != null)
        {
            try
            {
                rs.close();
            }catch(SQLException  e){}
        }
    }
}
