    



package com.cfcc.itfe.persistence.dao;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.Statement;
import java.sql.SQLException;
import java.sql.Date;
import java.sql.Time;
import java.sql.Timestamp ;
import java.sql.Clob;

import com.cfcc.jaf.persistence.jaform.parent.* ;

import java.math.BigDecimal;

import java.util.List;
import java.util.ArrayList;
import java.util.HashMap;

import java.io.StringReader;
import java.io.Reader;
import java.io.IOException;

import com.cfcc.itfe.persistence.dto.TsUsersDto ;
import com.cfcc.itfe.persistence.pk.TsUsersPK ;


/**
 * <p>Title: DAO of table: TS_USERS</p>
 * <p>Description: Data Access Object  </p>
 * <p>Copyright: Copyright (c) 2005</p>
 * <p>Company: CFCC </p>
 * <p>Generation Time: 2014-12-25 15:29:01 </p>
 *  Do NOT change this file by hand.
 *  This java source file is generated by JAF_ORM .
 *  dao.vm timestamp: 2007-04-06 08:30:00
 ******************************************************
 * <p> <b>change record</b><br>
 *  make dao methods simply.<br>
 *  especially make update,delete methods simply.<br>
 *  remove "..withCheck" method,withCheck will impl in JAFOrmTemplate.<br>
 *  remove "delete(IDto _dto,..)" method.<br></p>
 * @author win7
 */

public class TsUsersDao  implements IDao
{


    /* SQL to insert data */
    private static final String SQL_INSERT =
        "INSERT INTO TS_USERS ("
          + "S_ORGCODE,S_USERCODE,S_USERNAME,S_PASSWORD,S_CACERTDN"
          + ",S_USERTYPE,S_USERSTATUS,I_MODICOUNT,S_PASSMODCYCLE,S_PASSMODDATE"
          + ",S_LOGINSTATUS,S_LASTLOGINTIME,S_LASTEXITTIME,S_CERTID,S_STAMPID"
          + ",S_HOLD1,S_HOLD2,S_STAMPPERMISSION"
        + ") VALUES ("
        + "?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)";


    private static final String SQL_INSERT_WITH_RESULT = "SELECT * FROM FINAL TABLE( " + SQL_INSERT + " )";
    
    /* SQL to select data */
    private static final String SQL_SELECT =
        "SELECT "
        + "TS_USERS.S_ORGCODE, TS_USERS.S_USERCODE, TS_USERS.S_USERNAME, TS_USERS.S_PASSWORD, TS_USERS.S_CACERTDN, "
        + "TS_USERS.S_USERTYPE, TS_USERS.S_USERSTATUS, TS_USERS.I_MODICOUNT, TS_USERS.S_PASSMODCYCLE, TS_USERS.S_PASSMODDATE, "
        + "TS_USERS.S_LOGINSTATUS, TS_USERS.S_LASTLOGINTIME, TS_USERS.S_LASTEXITTIME, TS_USERS.S_CERTID, TS_USERS.S_STAMPID, "
        + "TS_USERS.S_HOLD1, TS_USERS.S_HOLD2, TS_USERS.S_STAMPPERMISSION "
        + "FROM TS_USERS "
        +" WHERE " 
        + "S_ORGCODE = ? AND S_USERCODE = ?"
        ;
    /* SQL to select for update data */
    private static final String SQL_SELECT_FOR_UPDATE =
        "SELECT "
        + "TS_USERS.S_ORGCODE, TS_USERS.S_USERCODE, TS_USERS.S_USERNAME, TS_USERS.S_PASSWORD, TS_USERS.S_CACERTDN, "
        + "TS_USERS.S_USERTYPE, TS_USERS.S_USERSTATUS, TS_USERS.I_MODICOUNT, TS_USERS.S_PASSMODCYCLE, TS_USERS.S_PASSMODDATE, "
        + "TS_USERS.S_LOGINSTATUS, TS_USERS.S_LASTLOGINTIME, TS_USERS.S_LASTEXITTIME, TS_USERS.S_CERTID, TS_USERS.S_STAMPID, "
        + "TS_USERS.S_HOLD1, TS_USERS.S_HOLD2, TS_USERS.S_STAMPPERMISSION "
        + "FROM TS_USERS "
        +" WHERE " 
        + "S_ORGCODE = ? AND S_USERCODE = ? FOR UPDATE"
        ;

      /* SQL to select data (SCROLLABLE)*/
     private static final String SQL_SELECT_BATCH_SCROLLABLE =
        "SELECT "
        + "  TS_USERS.S_ORGCODE  , TS_USERS.S_USERCODE  , TS_USERS.S_USERNAME  , TS_USERS.S_PASSWORD  , TS_USERS.S_CACERTDN "
        + " , TS_USERS.S_USERTYPE  , TS_USERS.S_USERSTATUS  , TS_USERS.I_MODICOUNT  , TS_USERS.S_PASSMODCYCLE  , TS_USERS.S_PASSMODDATE "
        + " , TS_USERS.S_LOGINSTATUS  , TS_USERS.S_LASTLOGINTIME  , TS_USERS.S_LASTEXITTIME  , TS_USERS.S_CERTID  , TS_USERS.S_STAMPID "
        + " , TS_USERS.S_HOLD1  , TS_USERS.S_HOLD2  , TS_USERS.S_STAMPPERMISSION "
        + "FROM TS_USERS ";



    /* SQL to select batch data */
    private static final String SQL_SELECT_BATCH =
        "SELECT "
        + "TS_USERS.S_ORGCODE, TS_USERS.S_USERCODE, TS_USERS.S_USERNAME, TS_USERS.S_PASSWORD, TS_USERS.S_CACERTDN, "
        + "TS_USERS.S_USERTYPE, TS_USERS.S_USERSTATUS, TS_USERS.I_MODICOUNT, TS_USERS.S_PASSMODCYCLE, TS_USERS.S_PASSMODDATE, "
        + "TS_USERS.S_LOGINSTATUS, TS_USERS.S_LASTLOGINTIME, TS_USERS.S_LASTEXITTIME, TS_USERS.S_CERTID, TS_USERS.S_STAMPID, "
        + "TS_USERS.S_HOLD1, TS_USERS.S_HOLD2, TS_USERS.S_STAMPPERMISSION "
        + "FROM TS_USERS " ;
        

   /* SQL to select batch data where condition  */
    private static final String SQL_SELECT_BATCH_WHERE =" ( "
        + "S_ORGCODE = ? AND S_USERCODE = ?)"
        ;


    /* SQL to update data */
    private static final String SQL_UPDATE =
        "UPDATE TS_USERS SET "
        + "S_USERNAME =?,S_PASSWORD =?,S_CACERTDN =?,S_USERTYPE =?,S_USERSTATUS =?, "
        + "I_MODICOUNT =?,S_PASSMODCYCLE =?,S_PASSMODDATE =?,S_LOGINSTATUS =?,S_LASTLOGINTIME =?, "
        + "S_LASTEXITTIME =?,S_CERTID =?,S_STAMPID =?,S_HOLD1 =?,S_HOLD2 =?, "
        + "S_STAMPPERMISSION =? "
        + "WHERE "
        + "S_ORGCODE = ? AND S_USERCODE = ?"
        ;

	/* SQL to update data, support LOB */
    private static final String SQL_UPDATE_LOB =
        "UPDATE TS_USERS SET "
        + "S_USERNAME =?, S_PASSWORD =?, S_CACERTDN =?, S_USERTYPE =?, S_USERSTATUS =?,  "
        + "I_MODICOUNT =?, S_PASSMODCYCLE =?, S_PASSMODDATE =?, S_LOGINSTATUS =?, S_LASTLOGINTIME =?,  "
        + "S_LASTEXITTIME =?, S_CERTID =?, S_STAMPID =?, S_HOLD1 =?, S_HOLD2 =?,  "
        + "S_STAMPPERMISSION =? "
        + "WHERE "
        + "S_ORGCODE = ? AND S_USERCODE = ?"
        ;	
	
    /* SQL to delete data */
    private static final String SQL_DELETE =
        "DELETE FROM TS_USERS " 
        + " WHERE "
        + "S_ORGCODE = ? AND S_USERCODE = ?"
        ;


	/**
	*  批量查询 一次最多查询的参数数量
	*/
	
	public static final int FIND_BATCH_SIZE = 150  / 2;
	



   /**
   * Create a new record in Database.
   */
  public void create(IDto _dto,  Connection conn) throws SQLException
  {
     TsUsersDto dto = (TsUsersDto)_dto ;
     String msgValid = dto.checkValid() ;
     if (msgValid != null)
     	throw new SQLException("插入错误，"+msgValid) ;

     PreparedStatement ps = null;
     try
     {
         ps = conn.prepareStatement(SQL_INSERT);
          ps.setString(1, dto.getSorgcode());

          ps.setString(2, dto.getSusercode());

          ps.setString(3, dto.getSusername());

          ps.setString(4, dto.getSpassword());

          ps.setString(5, dto.getScacertdn());

          ps.setString(6, dto.getSusertype());

          ps.setString(7, dto.getSuserstatus());

          if (dto.getImodicount()==null)
            ps.setNull(8, java.sql.Types.INTEGER);
         else
            ps.setInt(8, dto.getImodicount().intValue());
          if (dto.getSpassmodcycle()==null)
            ps.setNull(9, java.sql.Types.INTEGER);
         else
            ps.setInt(9, dto.getSpassmodcycle().intValue());
          ps.setString(10, dto.getSpassmoddate());

          ps.setString(11, dto.getSloginstatus());

          ps.setTimestamp(12, dto.getSlastlogintime());

          ps.setTimestamp(13, dto.getSlastexittime());

          ps.setString(14, dto.getScertid());

          ps.setString(15, dto.getSstampid());

          ps.setString(16, dto.getShold1());

          ps.setString(17, dto.getShold2());

          ps.setString(18, dto.getSstamppermission());

         ps.executeUpdate();
     }
     finally
     {
         close(ps);
     }
     return  ;
  }

   /**
   * Create a new record in Database. and return result, only used when having generated key
   */
   public IDto createWithResult(IDto _dto,  Connection conn) throws SQLException
   {
       TsUsersDto dto = (TsUsersDto)_dto ;
       String msgValid = dto.checkValid() ;
       if (msgValid != null)
           throw new SQLException("插入错误，"+msgValid) ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try
       {
           ps = conn.prepareStatement(SQL_INSERT_WITH_RESULT);
            ps.setString(1, dto.getSorgcode());
            ps.setString(2, dto.getSusercode());
            ps.setString(3, dto.getSusername());
            ps.setString(4, dto.getSpassword());
            ps.setString(5, dto.getScacertdn());
            ps.setString(6, dto.getSusertype());
            ps.setString(7, dto.getSuserstatus());
            if (dto.getImodicount()==null)
              ps.setNull(8, java.sql.Types.INTEGER);
           else
              ps.setInt(8, dto.getImodicount().intValue());
            if (dto.getSpassmodcycle()==null)
              ps.setNull(9, java.sql.Types.INTEGER);
           else
              ps.setInt(9, dto.getSpassmodcycle().intValue());
            ps.setString(10, dto.getSpassmoddate());
            ps.setString(11, dto.getSloginstatus());
            ps.setTimestamp(12, dto.getSlastlogintime());
            ps.setTimestamp(13, dto.getSlastexittime());
            ps.setString(14, dto.getScertid());
            ps.setString(15, dto.getSstampid());
            ps.setString(16, dto.getShold1());
            ps.setString(17, dto.getShold2());
            ps.setString(18, dto.getSstamppermission());
           rs = ps.executeQuery();
           List results = getResults(rs);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }
        finally
        {
           close(ps);
           close(rs);
        }
   }

 	/**
	* Create batch new record in Database. it only create the first layer dto
	*/
    public void create(IDto[] _dtos, Connection conn)    throws SQLException
    {
        TsUsersDto dto  ;
     	for (int i= 0; i< _dtos.length ; i++)
 	    {
 		    dto  = (TsUsersDto)_dtos[i] ; 
    	    String msgValid = dto.checkValid() ;
        	if (msgValid != null)
     	    	throw new SQLException("插入错误，"+msgValid) ;
     	}
 	    PreparedStatement ps = null;
        try
        {
            ps = conn.prepareStatement(SQL_INSERT);
       
     	    for (int i= 0; i< _dtos.length ; i++)
     	    {
 	    	    dto  = (TsUsersDto)_dtos[i] ; 
  
               ps.setString(1, dto.getSorgcode());
  
               ps.setString(2, dto.getSusercode());
  
               ps.setString(3, dto.getSusername());
  
               ps.setString(4, dto.getSpassword());
  
               ps.setString(5, dto.getScacertdn());
  
               ps.setString(6, dto.getSusertype());
  
               ps.setString(7, dto.getSuserstatus());
  
               if (dto.getImodicount()==null)
                  ps.setNull(8, java.sql.Types.INTEGER);
               else
                  ps.setInt(8, dto.getImodicount().intValue());
  
               if (dto.getSpassmodcycle()==null)
                  ps.setNull(9, java.sql.Types.INTEGER);
               else
                  ps.setInt(9, dto.getSpassmodcycle().intValue());
  
               ps.setString(10, dto.getSpassmoddate());
  
               ps.setString(11, dto.getSloginstatus());
  
               ps.setTimestamp(12, dto.getSlastlogintime());
  
               ps.setTimestamp(13, dto.getSlastexittime());
  
               ps.setString(14, dto.getScertid());
  
               ps.setString(15, dto.getSstampid());
  
               ps.setString(16, dto.getShold1());
  
               ps.setString(17, dto.getShold2());
  
               ps.setString(18, dto.getSstamppermission());
               ps.addBatch(); 
           }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }
        return  ;
   }




     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto find(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TsUsersPK pk = (TsUsersPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT);
           ps.setString(1, pk.getSorgcode());

           ps.setString(2, pk.getSusercode());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }

     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto findForUpdate(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TsUsersPK pk = (TsUsersPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT_FOR_UPDATE);
           ps.setString(1, pk.getSorgcode());

           ps.setString(2, pk.getSusercode());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }
  
     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto[] find(IPK[] _pks, Connection conn, boolean isLobSupport) throws SQLException
    {
        TsUsersPK pk ;
        
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TsUsersPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 
        PreparedStatement ps = null;
        ResultSet rs = null;
        
         
        List results = new ArrayList();  
		for (int iBegin = 0; iBegin < pks.size(); iBegin += FIND_BATCH_SIZE)   //
		{
        
            try {
          		int iFindBatchSize = pks.size() - iBegin ;
	    		if (iFindBatchSize >FIND_BATCH_SIZE )
		    		iFindBatchSize = FIND_BATCH_SIZE ;
		  
        	    StringBuffer sb = new StringBuffer() ;
            	sb.append(SQL_SELECT_BATCH).append(" WHERE ").append(SQL_SELECT_BATCH_WHERE);

	    		for (int i = iBegin+1; i < iBegin + iFindBatchSize; i++)
		    	{
			    	sb.append("OR").append(SQL_SELECT_BATCH_WHERE) ;
    			}
        	
                ps = conn.prepareStatement(sb.toString());

                for (int i = iBegin;i < iBegin + iFindBatchSize; i++)
                {
                    pk  = (TsUsersPK)(pks.get(i)) ; 
                   ps.setString((i-iBegin)*2+1, pk.getSorgcode());

                   ps.setString((i-iBegin)*2+2, pk.getSusercode());

			
	            }
                rs = ps.executeQuery();
                results.addAll(getResults(rs,0,isLobSupport)); //
            }finally {
                close(rs);
                close(ps);
            }
        }
        if (results!=null && results.size() > 0)
        {
            TsUsersDto[] dtos = new TsUsersDto[0];
		    dtos = (TsUsersDto[]) results.toArray(dtos) ;
		    return dtos ;
        }
        else
        {
            return null;
        }
    }

  
  
  



    /**
	    * 得到查询语句
	    */
    public String getSelectSQL() 
    {
	        return SQL_SELECT_BATCH_SCROLLABLE ;
    }
   

	public List getResults(ResultSet rs) throws SQLException
    {
    	return getResults(rs, 0) ;
    }
  

    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize) throws SQLException
    {
		return getResults(rs,maxSize,false);
	}
    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize, boolean isLobSupport) throws SQLException
    {
        List results = new ArrayList();
        String str ;
        Reader reader;
        Clob clob;
        char[] chars ;
        
        while (rs.next())
         {
             TsUsersDto  dto = new  TsUsersDto ();
             //S_ORGCODE
             str = rs.getString("S_ORGCODE");
             if (str == null)
                dto.setSorgcode(null);
             else
                dto.setSorgcode(str.trim());

             //S_USERCODE
             str = rs.getString("S_USERCODE");
             if (str == null)
                dto.setSusercode(null);
             else
                dto.setSusercode(str.trim());

             //S_USERNAME
             str = rs.getString("S_USERNAME");
             if (str == null)
                dto.setSusername(null);
             else
                dto.setSusername(str.trim());

             //S_PASSWORD
             str = rs.getString("S_PASSWORD");
             if (str == null)
                dto.setSpassword(null);
             else
                dto.setSpassword(str.trim());

             //S_CACERTDN
             str = rs.getString("S_CACERTDN");
             if (str == null)
                dto.setScacertdn(null);
             else
                dto.setScacertdn(str.trim());

             //S_USERTYPE
             str = rs.getString("S_USERTYPE");
             if (str == null)
                dto.setSusertype(null);
             else
                dto.setSusertype(str.trim());

             //S_USERSTATUS
             str = rs.getString("S_USERSTATUS");
             if (str == null)
                dto.setSuserstatus(null);
             else
                dto.setSuserstatus(str.trim());

             //I_MODICOUNT
             str = rs.getString("I_MODICOUNT");
             if(str!=null){
                dto.setImodicount(new Integer(str));
             }

             //S_PASSMODCYCLE
             str = rs.getString("S_PASSMODCYCLE");
             if(str!=null){
                dto.setSpassmodcycle(new Integer(str));
             }

             //S_PASSMODDATE
             str = rs.getString("S_PASSMODDATE");
             if (str == null)
                dto.setSpassmoddate(null);
             else
                dto.setSpassmoddate(str.trim());

             //S_LOGINSTATUS
             str = rs.getString("S_LOGINSTATUS");
             if (str == null)
                dto.setSloginstatus(null);
             else
                dto.setSloginstatus(str.trim());

             //S_LASTLOGINTIME
           dto.setSlastlogintime(rs.getTimestamp("S_LASTLOGINTIME"));

             //S_LASTEXITTIME
           dto.setSlastexittime(rs.getTimestamp("S_LASTEXITTIME"));

             //S_CERTID
             str = rs.getString("S_CERTID");
             if (str == null)
                dto.setScertid(null);
             else
                dto.setScertid(str.trim());

             //S_STAMPID
             str = rs.getString("S_STAMPID");
             if (str == null)
                dto.setSstampid(null);
             else
                dto.setSstampid(str.trim());

             //S_HOLD1
             str = rs.getString("S_HOLD1");
             if (str == null)
                dto.setShold1(null);
             else
                dto.setShold1(str.trim());

             //S_HOLD2
             str = rs.getString("S_HOLD2");
             if (str == null)
                dto.setShold2(null);
             else
                dto.setShold2(str.trim());

             //S_STAMPPERMISSION
             str = rs.getString("S_STAMPPERMISSION");
             if (str == null)
                dto.setSstamppermission(null);
             else
                dto.setSstamppermission(str.trim());



            results.add(dto);
            
            if(maxSize >0  && results.size() == maxSize)
            {
            	break ;
            }
            
         }
        
		return results ;
    }

  /**
   * populate resultSet as cclass
   */
  protected IDto[] getChildrenResults(ResultSet rs ,Class _dto) throws SQLException{

      throw new SQLException("本dto没有与其它dto相关联，无法应用此方法");
  }


     /**
     * Update a record in Database.
     */

    public void update(IDto _dto, Connection conn, boolean isLobSupport) throws SQLException
    {
        String msgValid = _dto.checkValid() ;
        if (msgValid != null)
            throw new SQLException("修改错误，"+msgValid) ;
        TsUsersDto dto = (TsUsersDto)_dto ;
        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            int pos = 1;
            //S_USERNAME
            ps.setString(pos, dto.getSusername());
            pos++;

            //S_PASSWORD
            ps.setString(pos, dto.getSpassword());
            pos++;

            //S_CACERTDN
            ps.setString(pos, dto.getScacertdn());
            pos++;

            //S_USERTYPE
            ps.setString(pos, dto.getSusertype());
            pos++;

            //S_USERSTATUS
            ps.setString(pos, dto.getSuserstatus());
            pos++;

            //I_MODICOUNT
            if (dto.getImodicount()==null)
                ps.setNull(pos, java.sql.Types.INTEGER);
            else
                ps.setInt(pos, dto.getImodicount().intValue());
            pos++;

            //S_PASSMODCYCLE
            if (dto.getSpassmodcycle()==null)
                ps.setNull(pos, java.sql.Types.INTEGER);
            else
                ps.setInt(pos, dto.getSpassmodcycle().intValue());
            pos++;

            //S_PASSMODDATE
            ps.setString(pos, dto.getSpassmoddate());
            pos++;

            //S_LOGINSTATUS
            ps.setString(pos, dto.getSloginstatus());
            pos++;

            //S_LASTLOGINTIME
            ps.setTimestamp(pos, dto.getSlastlogintime());
            pos++;

            //S_LASTEXITTIME
            ps.setTimestamp(pos, dto.getSlastexittime());
            pos++;

            //S_CERTID
            ps.setString(pos, dto.getScertid());
            pos++;

            //S_STAMPID
            ps.setString(pos, dto.getSstampid());
            pos++;

            //S_HOLD1
            ps.setString(pos, dto.getShold1());
            pos++;

            //S_HOLD2
            ps.setString(pos, dto.getShold2());
            pos++;

            //S_STAMPPERMISSION
            ps.setString(pos, dto.getSstamppermission());
            pos++;


           //S_ORGCODE
           ps.setString(pos, dto.getSorgcode());
           pos++;
           //S_USERCODE
           ps.setString(pos, dto.getSusercode());
           pos++;
           ps.executeUpdate();
        }
        finally
        {
           close(ps);
        }

        return ;
    }
    
    
    
      /**
     * Update batch record in Database.
     */

    public void update(IDto[] _dtos, Connection conn, boolean isLobSupport) throws SQLException
    {
     
     	 TsUsersDto dto  ;
         for (int i= 0; i< _dtos.length ; i++)
         {
            dto  = (TsUsersDto)_dtos[i] ; 
            String msgValid = dto.checkValid() ;
            if (msgValid != null)
                throw new SQLException("修改错误，"+msgValid) ;
         }

        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            
            for (int i= 0; i< _dtos.length ; i++)
            {
                dto  = (TsUsersDto)_dtos[i] ; 
                int pos = 1;
                //S_USERNAME
                 ps.setString(pos, dto.getSusername());
                pos++;

                //S_PASSWORD
                 ps.setString(pos, dto.getSpassword());
                pos++;

                //S_CACERTDN
                 ps.setString(pos, dto.getScacertdn());
                pos++;

                //S_USERTYPE
                 ps.setString(pos, dto.getSusertype());
                pos++;

                //S_USERSTATUS
                 ps.setString(pos, dto.getSuserstatus());
                pos++;

                //I_MODICOUNT
                 if (dto.getImodicount()==null)
                   ps.setNull(pos, java.sql.Types.INTEGER);
                else
                   ps.setInt(pos, dto.getImodicount().intValue());
                pos++ ;

                //S_PASSMODCYCLE
                 if (dto.getSpassmodcycle()==null)
                   ps.setNull(pos, java.sql.Types.INTEGER);
                else
                   ps.setInt(pos, dto.getSpassmodcycle().intValue());
                pos++ ;

                //S_PASSMODDATE
                 ps.setString(pos, dto.getSpassmoddate());
                pos++;

                //S_LOGINSTATUS
                 ps.setString(pos, dto.getSloginstatus());
                pos++;

                //S_LASTLOGINTIME
                 ps.setTimestamp(pos, dto.getSlastlogintime());
                pos++;

                //S_LASTEXITTIME
                 ps.setTimestamp(pos, dto.getSlastexittime());
                pos++;

                //S_CERTID
                 ps.setString(pos, dto.getScertid());
                pos++;

                //S_STAMPID
                 ps.setString(pos, dto.getSstampid());
                pos++;

                //S_HOLD1
                 ps.setString(pos, dto.getShold1());
                pos++;

                //S_HOLD2
                 ps.setString(pos, dto.getShold2());
                pos++;

                //S_STAMPPERMISSION
                 ps.setString(pos, dto.getSstamppermission());
                pos++;


               //S_ORGCODE
               ps.setString(pos, dto.getSorgcode());
               pos++;
               //S_USERCODE
               ps.setString(pos, dto.getSusercode());
               pos++;
		       ps.addBatch(); 
	       }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }

        return ;
    }



    /**
     * delete a record in Database.
     */
    public void delete(IPK _pk, Connection conn) throws SQLException
    {
       
      
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return ;
        //	throw new SQLException("删除错误，"+msgValid) ;
       }
       TsUsersPK pk = (TsUsersPK)_pk ;


       PreparedStatement ps = null;
       try {
           ps = conn.prepareStatement(SQL_DELETE);
           ps.setString(1, pk.getSorgcode());
           ps.setString(2, pk.getSusercode());
           ps.executeUpdate();
        }
        finally
        {
            close(ps);
        }
    }


     /**
     * delete batch record in Database.
     */
    public void delete(IPK[] _pks, Connection conn) throws SQLException
    {
        TsUsersPK pk ;
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TsUsersPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 		
 		
        PreparedStatement ps = null;
        try
         {
            ps = conn.prepareStatement(SQL_DELETE);
             
       		for (int i= 0; i< pks.size() ; i++)
       		{
       			pk  = (TsUsersPK)(pks.get(i)) ; 
                ps.setString(1, pk.getSorgcode());
                ps.setString(2, pk.getSusercode());
		        ps.addBatch() ;
			}
            ps.executeBatch();
        }
        finally
        {
            close(ps);
        }      
    }



    /**
    * Check IChangerNumber or UpdateTimeStamp.
    */
    public void check(IDto _dto, Connection conn) throws SQLException
    {
                                                		throw new SQLException("数据库表：TS_USERS没有检查修改的字段");
    }


	/**
	* synchronize values between parent and children
	*/
    public void syncToChildren(IDto _dto)     throws SQLException{
       throw new SQLException("本记录没有与其它记录相关联，无法进行主记录与明细记录之间的数据同步。");
  }

	/**
	*get children
	*/
    public IDto[] findChildren (IDto _dto, Connection conn, boolean isLobSupport)     throws SQLException{
         throw new SQLException("本dto没有与其它dto相关联，无法得到子dto");
  }    
    
  /**
   *return the children IDto class
   * @return Class
   */
  public Class getChildrenClass(){
	  return null;
  };    



    /**
     * Close JDBC Statement.
     * @param stmt  Statement to be closed.
     */
    private void close(Statement stmt)
    {
        if (stmt != null)
        {
            try
            {
                stmt.close();
            }
            catch(SQLException    e)
            {}
        }
    }

    /**
     * Close JDBC ResultSet.
     * @param rs  ResultSet to be closed.
     */
    private void close(ResultSet rs)
    {
        if (rs != null)
        {
            try
            {
                rs.close();
            }catch(SQLException  e){}
        }
    }
}
