    



package com.cfcc.itfe.persistence.dao;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.Statement;
import java.sql.SQLException;
import java.sql.Date;
import java.sql.Time;
import java.sql.Timestamp ;
import java.sql.Clob;

import com.cfcc.jaf.persistence.jaform.parent.* ;

import java.math.BigDecimal;

import java.util.List;
import java.util.ArrayList;
import java.util.HashMap;

import java.io.StringReader;
import java.io.Reader;
import java.io.IOException;

import com.cfcc.itfe.persistence.dto.TvVoucherinfoAllocateIncomeDto ;
import com.cfcc.itfe.persistence.pk.TvVoucherinfoAllocateIncomePK ;


/**
 * <p>Title: DAO of table: TV_VOUCHERINFO_ALLOCATE_INCOME</p>
 * <p>Description: Data Access Object  </p>
 * <p>Copyright: Copyright (c) 2005</p>
 * <p>Company: CFCC </p>
 * <p>Generation Time: 2014-12-25 15:29:04 </p>
 *  Do NOT change this file by hand.
 *  This java source file is generated by JAF_ORM .
 *  dao.vm timestamp: 2007-04-06 08:30:00
 ******************************************************
 * <p> <b>change record</b><br>
 *  make dao methods simply.<br>
 *  especially make update,delete methods simply.<br>
 *  remove "..withCheck" method,withCheck will impl in JAFOrmTemplate.<br>
 *  remove "delete(IDto _dto,..)" method.<br></p>
 * @author win7
 */

public class TvVoucherinfoAllocateIncomeDao  implements IDao
{


    /* SQL to insert data */
    private static final String SQL_INSERT =
        "INSERT INTO TV_VOUCHERINFO_ALLOCATE_INCOME ("
          + "S_DEALNO,S_ORGCODE,S_TRECODE,S_ADMDIVCODE,S_VTCODE"
          + ",S_COMMITDATE,S_ORICOMMITDATE,S_RECEIVEDATE,N_MONEY,S_ESCORTMARKS"
          + ",S_DEMO,S_PAYEEACCTNO,S_PAYEEACCTNAME,S_PAYEEACCTBANKNAME,S_PAYACCTNO"
          + ",S_PAYACCTNAME,S_PAYACCTBANKNAME,S_FORWARDBANKNO,S_FORWARDBANKNAME,S_RECEIVEBANKNO"
          + ",S_PAYDEALNO,S_ORIPAYDEALNO,S_REPORTKIND,S_TRADEKIND,S_VTCODEKIND"
          + ",S_VTCODEKINDNO,S_VTCODEDES,S_SECRETSIGN,S_BORROW,S_LEND"
          + ",S_HOLD1,S_HOLD2,TS_SYSUPDATE"
        + ") VALUES ("
        + "?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,CURRENT TIMESTAMP )";


    private static final String SQL_INSERT_WITH_RESULT = "SELECT * FROM FINAL TABLE( " + SQL_INSERT + " )";
    
    /* SQL to select data */
    private static final String SQL_SELECT =
        "SELECT "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_DEALNO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_ORGCODE, TV_VOUCHERINFO_ALLOCATE_INCOME.S_TRECODE, TV_VOUCHERINFO_ALLOCATE_INCOME.S_ADMDIVCODE, TV_VOUCHERINFO_ALLOCATE_INCOME.S_VTCODE, "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_COMMITDATE, TV_VOUCHERINFO_ALLOCATE_INCOME.S_ORICOMMITDATE, TV_VOUCHERINFO_ALLOCATE_INCOME.S_RECEIVEDATE, TV_VOUCHERINFO_ALLOCATE_INCOME.N_MONEY, TV_VOUCHERINFO_ALLOCATE_INCOME.S_ESCORTMARKS, "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_DEMO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYEEACCTNO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYEEACCTNAME, TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYEEACCTBANKNAME, TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYACCTNO, "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYACCTNAME, TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYACCTBANKNAME, TV_VOUCHERINFO_ALLOCATE_INCOME.S_FORWARDBANKNO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_FORWARDBANKNAME, TV_VOUCHERINFO_ALLOCATE_INCOME.S_RECEIVEBANKNO, "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYDEALNO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_ORIPAYDEALNO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_REPORTKIND, TV_VOUCHERINFO_ALLOCATE_INCOME.S_TRADEKIND, TV_VOUCHERINFO_ALLOCATE_INCOME.S_VTCODEKIND, "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_VTCODEKINDNO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_VTCODEDES, TV_VOUCHERINFO_ALLOCATE_INCOME.S_SECRETSIGN, TV_VOUCHERINFO_ALLOCATE_INCOME.S_BORROW, TV_VOUCHERINFO_ALLOCATE_INCOME.S_LEND, "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_HOLD1, TV_VOUCHERINFO_ALLOCATE_INCOME.S_HOLD2, TV_VOUCHERINFO_ALLOCATE_INCOME.TS_SYSUPDATE "
        + "FROM TV_VOUCHERINFO_ALLOCATE_INCOME "
        +" WHERE " 
        + "S_DEALNO = ?"
        ;
    /* SQL to select for update data */
    private static final String SQL_SELECT_FOR_UPDATE =
        "SELECT "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_DEALNO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_ORGCODE, TV_VOUCHERINFO_ALLOCATE_INCOME.S_TRECODE, TV_VOUCHERINFO_ALLOCATE_INCOME.S_ADMDIVCODE, TV_VOUCHERINFO_ALLOCATE_INCOME.S_VTCODE, "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_COMMITDATE, TV_VOUCHERINFO_ALLOCATE_INCOME.S_ORICOMMITDATE, TV_VOUCHERINFO_ALLOCATE_INCOME.S_RECEIVEDATE, TV_VOUCHERINFO_ALLOCATE_INCOME.N_MONEY, TV_VOUCHERINFO_ALLOCATE_INCOME.S_ESCORTMARKS, "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_DEMO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYEEACCTNO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYEEACCTNAME, TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYEEACCTBANKNAME, TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYACCTNO, "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYACCTNAME, TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYACCTBANKNAME, TV_VOUCHERINFO_ALLOCATE_INCOME.S_FORWARDBANKNO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_FORWARDBANKNAME, TV_VOUCHERINFO_ALLOCATE_INCOME.S_RECEIVEBANKNO, "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYDEALNO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_ORIPAYDEALNO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_REPORTKIND, TV_VOUCHERINFO_ALLOCATE_INCOME.S_TRADEKIND, TV_VOUCHERINFO_ALLOCATE_INCOME.S_VTCODEKIND, "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_VTCODEKINDNO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_VTCODEDES, TV_VOUCHERINFO_ALLOCATE_INCOME.S_SECRETSIGN, TV_VOUCHERINFO_ALLOCATE_INCOME.S_BORROW, TV_VOUCHERINFO_ALLOCATE_INCOME.S_LEND, "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_HOLD1, TV_VOUCHERINFO_ALLOCATE_INCOME.S_HOLD2, TV_VOUCHERINFO_ALLOCATE_INCOME.TS_SYSUPDATE "
        + "FROM TV_VOUCHERINFO_ALLOCATE_INCOME "
        +" WHERE " 
        + "S_DEALNO = ? FOR UPDATE"
        ;

      /* SQL to select data (SCROLLABLE)*/
     private static final String SQL_SELECT_BATCH_SCROLLABLE =
        "SELECT "
        + "  TV_VOUCHERINFO_ALLOCATE_INCOME.S_DEALNO  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_ORGCODE  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_TRECODE  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_ADMDIVCODE  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_VTCODE "
        + " , TV_VOUCHERINFO_ALLOCATE_INCOME.S_COMMITDATE  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_ORICOMMITDATE  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_RECEIVEDATE  , TV_VOUCHERINFO_ALLOCATE_INCOME.N_MONEY  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_ESCORTMARKS "
        + " , TV_VOUCHERINFO_ALLOCATE_INCOME.S_DEMO  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYEEACCTNO  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYEEACCTNAME  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYEEACCTBANKNAME  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYACCTNO "
        + " , TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYACCTNAME  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYACCTBANKNAME  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_FORWARDBANKNO  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_FORWARDBANKNAME  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_RECEIVEBANKNO "
        + " , TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYDEALNO  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_ORIPAYDEALNO  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_REPORTKIND  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_TRADEKIND  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_VTCODEKIND "
        + " , TV_VOUCHERINFO_ALLOCATE_INCOME.S_VTCODEKINDNO  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_VTCODEDES  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_SECRETSIGN  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_BORROW  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_LEND "
        + " , TV_VOUCHERINFO_ALLOCATE_INCOME.S_HOLD1  , TV_VOUCHERINFO_ALLOCATE_INCOME.S_HOLD2  , TV_VOUCHERINFO_ALLOCATE_INCOME.TS_SYSUPDATE "
        + "FROM TV_VOUCHERINFO_ALLOCATE_INCOME ";



    /* SQL to select batch data */
    private static final String SQL_SELECT_BATCH =
        "SELECT "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_DEALNO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_ORGCODE, TV_VOUCHERINFO_ALLOCATE_INCOME.S_TRECODE, TV_VOUCHERINFO_ALLOCATE_INCOME.S_ADMDIVCODE, TV_VOUCHERINFO_ALLOCATE_INCOME.S_VTCODE, "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_COMMITDATE, TV_VOUCHERINFO_ALLOCATE_INCOME.S_ORICOMMITDATE, TV_VOUCHERINFO_ALLOCATE_INCOME.S_RECEIVEDATE, TV_VOUCHERINFO_ALLOCATE_INCOME.N_MONEY, TV_VOUCHERINFO_ALLOCATE_INCOME.S_ESCORTMARKS, "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_DEMO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYEEACCTNO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYEEACCTNAME, TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYEEACCTBANKNAME, TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYACCTNO, "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYACCTNAME, TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYACCTBANKNAME, TV_VOUCHERINFO_ALLOCATE_INCOME.S_FORWARDBANKNO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_FORWARDBANKNAME, TV_VOUCHERINFO_ALLOCATE_INCOME.S_RECEIVEBANKNO, "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_PAYDEALNO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_ORIPAYDEALNO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_REPORTKIND, TV_VOUCHERINFO_ALLOCATE_INCOME.S_TRADEKIND, TV_VOUCHERINFO_ALLOCATE_INCOME.S_VTCODEKIND, "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_VTCODEKINDNO, TV_VOUCHERINFO_ALLOCATE_INCOME.S_VTCODEDES, TV_VOUCHERINFO_ALLOCATE_INCOME.S_SECRETSIGN, TV_VOUCHERINFO_ALLOCATE_INCOME.S_BORROW, TV_VOUCHERINFO_ALLOCATE_INCOME.S_LEND, "
        + "TV_VOUCHERINFO_ALLOCATE_INCOME.S_HOLD1, TV_VOUCHERINFO_ALLOCATE_INCOME.S_HOLD2, TV_VOUCHERINFO_ALLOCATE_INCOME.TS_SYSUPDATE "
        + "FROM TV_VOUCHERINFO_ALLOCATE_INCOME " ;
        

   /* SQL to select batch data where condition  */
    private static final String SQL_SELECT_BATCH_WHERE =" ( "
        + "S_DEALNO = ?)"
        ;


    /* SQL to update data */
    private static final String SQL_UPDATE =
        "UPDATE TV_VOUCHERINFO_ALLOCATE_INCOME SET "
        + "S_ORGCODE =?,S_TRECODE =?,S_ADMDIVCODE =?,S_VTCODE =?,S_COMMITDATE =?, "
        + "S_ORICOMMITDATE =?,S_RECEIVEDATE =?,N_MONEY =?,S_ESCORTMARKS =?,S_DEMO =?, "
        + "S_PAYEEACCTNO =?,S_PAYEEACCTNAME =?,S_PAYEEACCTBANKNAME =?,S_PAYACCTNO =?,S_PAYACCTNAME =?, "
        + "S_PAYACCTBANKNAME =?,S_FORWARDBANKNO =?,S_FORWARDBANKNAME =?,S_RECEIVEBANKNO =?,S_PAYDEALNO =?, "
        + "S_ORIPAYDEALNO =?,S_REPORTKIND =?,S_TRADEKIND =?,S_VTCODEKIND =?,S_VTCODEKINDNO =?, "
        + "S_VTCODEDES =?,S_SECRETSIGN =?,S_BORROW =?,S_LEND =?,S_HOLD1 =?, "
        + "S_HOLD2 =?,TS_SYSUPDATE =CURRENT TIMESTAMP "
        + "WHERE "
        + "S_DEALNO = ?"
        ;

	/* SQL to update data, support LOB */
    private static final String SQL_UPDATE_LOB =
        "UPDATE TV_VOUCHERINFO_ALLOCATE_INCOME SET "
        + "S_ORGCODE =?, S_TRECODE =?, S_ADMDIVCODE =?, S_VTCODE =?, S_COMMITDATE =?,  "
        + "S_ORICOMMITDATE =?, S_RECEIVEDATE =?, N_MONEY =?, S_ESCORTMARKS =?, S_DEMO =?,  "
        + "S_PAYEEACCTNO =?, S_PAYEEACCTNAME =?, S_PAYEEACCTBANKNAME =?, S_PAYACCTNO =?, S_PAYACCTNAME =?,  "
        + "S_PAYACCTBANKNAME =?, S_FORWARDBANKNO =?, S_FORWARDBANKNAME =?, S_RECEIVEBANKNO =?, S_PAYDEALNO =?,  "
        + "S_ORIPAYDEALNO =?, S_REPORTKIND =?, S_TRADEKIND =?, S_VTCODEKIND =?, S_VTCODEKINDNO =?,  "
        + "S_VTCODEDES =?, S_SECRETSIGN =?, S_BORROW =?, S_LEND =?, S_HOLD1 =?,  "
        + "S_HOLD2 =?, TS_SYSUPDATE =CURRENT TIMESTAMP "
        + "WHERE "
        + "S_DEALNO = ?"
        ;	
	
    /* SQL to delete data */
    private static final String SQL_DELETE =
        "DELETE FROM TV_VOUCHERINFO_ALLOCATE_INCOME " 
        + " WHERE "
        + "S_DEALNO = ?"
        ;


	/**
	*  批量查询 一次最多查询的参数数量
	*/
	
	public static final int FIND_BATCH_SIZE = 150  / 1;
	



   /**
   * Create a new record in Database.
   */
  public void create(IDto _dto,  Connection conn) throws SQLException
  {
     TvVoucherinfoAllocateIncomeDto dto = (TvVoucherinfoAllocateIncomeDto)_dto ;
     String msgValid = dto.checkValid() ;
     if (msgValid != null)
     	throw new SQLException("插入错误，"+msgValid) ;

     PreparedStatement ps = null;
     try
     {
         ps = conn.prepareStatement(SQL_INSERT);
          ps.setString(1, dto.getSdealno());

          ps.setString(2, dto.getSorgcode());

          ps.setString(3, dto.getStrecode());

          ps.setString(4, dto.getSadmdivcode());

          ps.setString(5, dto.getSvtcode());

          ps.setString(6, dto.getScommitdate());

          ps.setString(7, dto.getSoricommitdate());

          ps.setString(8, dto.getSreceivedate());

          ps.setBigDecimal(9, dto.getNmoney());

          ps.setString(10, dto.getSescortmarks());

          ps.setString(11, dto.getSdemo());

          ps.setString(12, dto.getSpayeeacctno());

          ps.setString(13, dto.getSpayeeacctname());

          ps.setString(14, dto.getSpayeeacctbankname());

          ps.setString(15, dto.getSpayacctno());

          ps.setString(16, dto.getSpayacctname());

          ps.setString(17, dto.getSpayacctbankname());

          ps.setString(18, dto.getSforwardbankno());

          ps.setString(19, dto.getSforwardbankname());

          ps.setString(20, dto.getSreceivebankno());

          ps.setString(21, dto.getSpaydealno());

          ps.setString(22, dto.getSoripaydealno());

          ps.setString(23, dto.getSreportkind());

          ps.setString(24, dto.getStradekind());

          ps.setString(25, dto.getSvtcodekind());

          ps.setString(26, dto.getSvtcodekindno());

          ps.setString(27, dto.getSvtcodedes());

          ps.setString(28, dto.getSsecretsign());

          ps.setString(29, dto.getSborrow());

          ps.setString(30, dto.getSlend());

          ps.setString(31, dto.getShold1());

          ps.setString(32, dto.getShold2());

          ps.executeUpdate();
     }
     finally
     {
         close(ps);
     }
     return  ;
  }

   /**
   * Create a new record in Database. and return result, only used when having generated key
   */
   public IDto createWithResult(IDto _dto,  Connection conn) throws SQLException
   {
       TvVoucherinfoAllocateIncomeDto dto = (TvVoucherinfoAllocateIncomeDto)_dto ;
       String msgValid = dto.checkValid() ;
       if (msgValid != null)
           throw new SQLException("插入错误，"+msgValid) ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try
       {
           ps = conn.prepareStatement(SQL_INSERT_WITH_RESULT);
            ps.setString(1, dto.getSdealno());
            ps.setString(2, dto.getSorgcode());
            ps.setString(3, dto.getStrecode());
            ps.setString(4, dto.getSadmdivcode());
            ps.setString(5, dto.getSvtcode());
            ps.setString(6, dto.getScommitdate());
            ps.setString(7, dto.getSoricommitdate());
            ps.setString(8, dto.getSreceivedate());
            ps.setBigDecimal(9, dto.getNmoney());
            ps.setString(10, dto.getSescortmarks());
            ps.setString(11, dto.getSdemo());
            ps.setString(12, dto.getSpayeeacctno());
            ps.setString(13, dto.getSpayeeacctname());
            ps.setString(14, dto.getSpayeeacctbankname());
            ps.setString(15, dto.getSpayacctno());
            ps.setString(16, dto.getSpayacctname());
            ps.setString(17, dto.getSpayacctbankname());
            ps.setString(18, dto.getSforwardbankno());
            ps.setString(19, dto.getSforwardbankname());
            ps.setString(20, dto.getSreceivebankno());
            ps.setString(21, dto.getSpaydealno());
            ps.setString(22, dto.getSoripaydealno());
            ps.setString(23, dto.getSreportkind());
            ps.setString(24, dto.getStradekind());
            ps.setString(25, dto.getSvtcodekind());
            ps.setString(26, dto.getSvtcodekindno());
            ps.setString(27, dto.getSvtcodedes());
            ps.setString(28, dto.getSsecretsign());
            ps.setString(29, dto.getSborrow());
            ps.setString(30, dto.getSlend());
            ps.setString(31, dto.getShold1());
            ps.setString(32, dto.getShold2());
            rs = ps.executeQuery();
           List results = getResults(rs);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }
        finally
        {
           close(ps);
           close(rs);
        }
   }

 	/**
	* Create batch new record in Database. it only create the first layer dto
	*/
    public void create(IDto[] _dtos, Connection conn)    throws SQLException
    {
        TvVoucherinfoAllocateIncomeDto dto  ;
     	for (int i= 0; i< _dtos.length ; i++)
 	    {
 		    dto  = (TvVoucherinfoAllocateIncomeDto)_dtos[i] ; 
    	    String msgValid = dto.checkValid() ;
        	if (msgValid != null)
     	    	throw new SQLException("插入错误，"+msgValid) ;
     	}
 	    PreparedStatement ps = null;
        try
        {
            ps = conn.prepareStatement(SQL_INSERT);
       
     	    for (int i= 0; i< _dtos.length ; i++)
     	    {
 	    	    dto  = (TvVoucherinfoAllocateIncomeDto)_dtos[i] ; 
  
               ps.setString(1, dto.getSdealno());
  
               ps.setString(2, dto.getSorgcode());
  
               ps.setString(3, dto.getStrecode());
  
               ps.setString(4, dto.getSadmdivcode());
  
               ps.setString(5, dto.getSvtcode());
  
               ps.setString(6, dto.getScommitdate());
  
               ps.setString(7, dto.getSoricommitdate());
  
               ps.setString(8, dto.getSreceivedate());
  
               ps.setBigDecimal(9, dto.getNmoney());
  
               ps.setString(10, dto.getSescortmarks());
  
               ps.setString(11, dto.getSdemo());
  
               ps.setString(12, dto.getSpayeeacctno());
  
               ps.setString(13, dto.getSpayeeacctname());
  
               ps.setString(14, dto.getSpayeeacctbankname());
  
               ps.setString(15, dto.getSpayacctno());
  
               ps.setString(16, dto.getSpayacctname());
  
               ps.setString(17, dto.getSpayacctbankname());
  
               ps.setString(18, dto.getSforwardbankno());
  
               ps.setString(19, dto.getSforwardbankname());
  
               ps.setString(20, dto.getSreceivebankno());
  
               ps.setString(21, dto.getSpaydealno());
  
               ps.setString(22, dto.getSoripaydealno());
  
               ps.setString(23, dto.getSreportkind());
  
               ps.setString(24, dto.getStradekind());
  
               ps.setString(25, dto.getSvtcodekind());
  
               ps.setString(26, dto.getSvtcodekindno());
  
               ps.setString(27, dto.getSvtcodedes());
  
               ps.setString(28, dto.getSsecretsign());
  
               ps.setString(29, dto.getSborrow());
  
               ps.setString(30, dto.getSlend());
  
               ps.setString(31, dto.getShold1());
  
               ps.setString(32, dto.getShold2());
                ps.addBatch(); 
           }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }
        return  ;
   }




     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto find(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TvVoucherinfoAllocateIncomePK pk = (TvVoucherinfoAllocateIncomePK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT);
           ps.setString(1, pk.getSdealno());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }

     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto findForUpdate(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TvVoucherinfoAllocateIncomePK pk = (TvVoucherinfoAllocateIncomePK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT_FOR_UPDATE);
           ps.setString(1, pk.getSdealno());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }
  
     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto[] find(IPK[] _pks, Connection conn, boolean isLobSupport) throws SQLException
    {
        TvVoucherinfoAllocateIncomePK pk ;
        
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TvVoucherinfoAllocateIncomePK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 
        PreparedStatement ps = null;
        ResultSet rs = null;
        
         
        List results = new ArrayList();  
		for (int iBegin = 0; iBegin < pks.size(); iBegin += FIND_BATCH_SIZE)   //
		{
        
            try {
          		int iFindBatchSize = pks.size() - iBegin ;
	    		if (iFindBatchSize >FIND_BATCH_SIZE )
		    		iFindBatchSize = FIND_BATCH_SIZE ;
		  
        	    StringBuffer sb = new StringBuffer() ;
            	sb.append(SQL_SELECT_BATCH).append(" WHERE ").append(SQL_SELECT_BATCH_WHERE);

	    		for (int i = iBegin+1; i < iBegin + iFindBatchSize; i++)
		    	{
			    	sb.append("OR").append(SQL_SELECT_BATCH_WHERE) ;
    			}
        	
                ps = conn.prepareStatement(sb.toString());

                for (int i = iBegin;i < iBegin + iFindBatchSize; i++)
                {
                    pk  = (TvVoucherinfoAllocateIncomePK)(pks.get(i)) ; 
                   ps.setString((i-iBegin)*1+1, pk.getSdealno());

			
	            }
                rs = ps.executeQuery();
                results.addAll(getResults(rs,0,isLobSupport)); //
            }finally {
                close(rs);
                close(ps);
            }
        }
        if (results!=null && results.size() > 0)
        {
            TvVoucherinfoAllocateIncomeDto[] dtos = new TvVoucherinfoAllocateIncomeDto[0];
		    dtos = (TvVoucherinfoAllocateIncomeDto[]) results.toArray(dtos) ;
		    return dtos ;
        }
        else
        {
            return null;
        }
    }

  
  
  



    /**
	    * 得到查询语句
	    */
    public String getSelectSQL() 
    {
	        return SQL_SELECT_BATCH_SCROLLABLE ;
    }
   

	public List getResults(ResultSet rs) throws SQLException
    {
    	return getResults(rs, 0) ;
    }
  

    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize) throws SQLException
    {
		return getResults(rs,maxSize,false);
	}
    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize, boolean isLobSupport) throws SQLException
    {
        List results = new ArrayList();
        String str ;
        Reader reader;
        Clob clob;
        char[] chars ;
        
        while (rs.next())
         {
             TvVoucherinfoAllocateIncomeDto  dto = new  TvVoucherinfoAllocateIncomeDto ();
             //S_DEALNO
             str = rs.getString("S_DEALNO");
             if (str == null)
                dto.setSdealno(null);
             else
                dto.setSdealno(str.trim());

             //S_ORGCODE
             str = rs.getString("S_ORGCODE");
             if (str == null)
                dto.setSorgcode(null);
             else
                dto.setSorgcode(str.trim());

             //S_TRECODE
             str = rs.getString("S_TRECODE");
             if (str == null)
                dto.setStrecode(null);
             else
                dto.setStrecode(str.trim());

             //S_ADMDIVCODE
             str = rs.getString("S_ADMDIVCODE");
             if (str == null)
                dto.setSadmdivcode(null);
             else
                dto.setSadmdivcode(str.trim());

             //S_VTCODE
             str = rs.getString("S_VTCODE");
             if (str == null)
                dto.setSvtcode(null);
             else
                dto.setSvtcode(str.trim());

             //S_COMMITDATE
             str = rs.getString("S_COMMITDATE");
             if (str == null)
                dto.setScommitdate(null);
             else
                dto.setScommitdate(str.trim());

             //S_ORICOMMITDATE
             str = rs.getString("S_ORICOMMITDATE");
             if (str == null)
                dto.setSoricommitdate(null);
             else
                dto.setSoricommitdate(str.trim());

             //S_RECEIVEDATE
             str = rs.getString("S_RECEIVEDATE");
             if (str == null)
                dto.setSreceivedate(null);
             else
                dto.setSreceivedate(str.trim());

             //N_MONEY
           dto.setNmoney(rs.getBigDecimal("N_MONEY"));

             //S_ESCORTMARKS
             str = rs.getString("S_ESCORTMARKS");
             if (str == null)
                dto.setSescortmarks(null);
             else
                dto.setSescortmarks(str.trim());

             //S_DEMO
             str = rs.getString("S_DEMO");
             if (str == null)
                dto.setSdemo(null);
             else
                dto.setSdemo(str.trim());

             //S_PAYEEACCTNO
             str = rs.getString("S_PAYEEACCTNO");
             if (str == null)
                dto.setSpayeeacctno(null);
             else
                dto.setSpayeeacctno(str.trim());

             //S_PAYEEACCTNAME
             str = rs.getString("S_PAYEEACCTNAME");
             if (str == null)
                dto.setSpayeeacctname(null);
             else
                dto.setSpayeeacctname(str.trim());

             //S_PAYEEACCTBANKNAME
             str = rs.getString("S_PAYEEACCTBANKNAME");
             if (str == null)
                dto.setSpayeeacctbankname(null);
             else
                dto.setSpayeeacctbankname(str.trim());

             //S_PAYACCTNO
             str = rs.getString("S_PAYACCTNO");
             if (str == null)
                dto.setSpayacctno(null);
             else
                dto.setSpayacctno(str.trim());

             //S_PAYACCTNAME
             str = rs.getString("S_PAYACCTNAME");
             if (str == null)
                dto.setSpayacctname(null);
             else
                dto.setSpayacctname(str.trim());

             //S_PAYACCTBANKNAME
             str = rs.getString("S_PAYACCTBANKNAME");
             if (str == null)
                dto.setSpayacctbankname(null);
             else
                dto.setSpayacctbankname(str.trim());

             //S_FORWARDBANKNO
             str = rs.getString("S_FORWARDBANKNO");
             if (str == null)
                dto.setSforwardbankno(null);
             else
                dto.setSforwardbankno(str.trim());

             //S_FORWARDBANKNAME
             str = rs.getString("S_FORWARDBANKNAME");
             if (str == null)
                dto.setSforwardbankname(null);
             else
                dto.setSforwardbankname(str.trim());

             //S_RECEIVEBANKNO
             str = rs.getString("S_RECEIVEBANKNO");
             if (str == null)
                dto.setSreceivebankno(null);
             else
                dto.setSreceivebankno(str.trim());

             //S_PAYDEALNO
             str = rs.getString("S_PAYDEALNO");
             if (str == null)
                dto.setSpaydealno(null);
             else
                dto.setSpaydealno(str.trim());

             //S_ORIPAYDEALNO
             str = rs.getString("S_ORIPAYDEALNO");
             if (str == null)
                dto.setSoripaydealno(null);
             else
                dto.setSoripaydealno(str.trim());

             //S_REPORTKIND
             str = rs.getString("S_REPORTKIND");
             if (str == null)
                dto.setSreportkind(null);
             else
                dto.setSreportkind(str.trim());

             //S_TRADEKIND
             str = rs.getString("S_TRADEKIND");
             if (str == null)
                dto.setStradekind(null);
             else
                dto.setStradekind(str.trim());

             //S_VTCODEKIND
             str = rs.getString("S_VTCODEKIND");
             if (str == null)
                dto.setSvtcodekind(null);
             else
                dto.setSvtcodekind(str.trim());

             //S_VTCODEKINDNO
             str = rs.getString("S_VTCODEKINDNO");
             if (str == null)
                dto.setSvtcodekindno(null);
             else
                dto.setSvtcodekindno(str.trim());

             //S_VTCODEDES
             str = rs.getString("S_VTCODEDES");
             if (str == null)
                dto.setSvtcodedes(null);
             else
                dto.setSvtcodedes(str.trim());

             //S_SECRETSIGN
             str = rs.getString("S_SECRETSIGN");
             if (str == null)
                dto.setSsecretsign(null);
             else
                dto.setSsecretsign(str.trim());

             //S_BORROW
             str = rs.getString("S_BORROW");
             if (str == null)
                dto.setSborrow(null);
             else
                dto.setSborrow(str.trim());

             //S_LEND
             str = rs.getString("S_LEND");
             if (str == null)
                dto.setSlend(null);
             else
                dto.setSlend(str.trim());

             //S_HOLD1
             str = rs.getString("S_HOLD1");
             if (str == null)
                dto.setShold1(null);
             else
                dto.setShold1(str.trim());

             //S_HOLD2
             str = rs.getString("S_HOLD2");
             if (str == null)
                dto.setShold2(null);
             else
                dto.setShold2(str.trim());

             //TS_SYSUPDATE
           dto.setTssysupdate(rs.getTimestamp("TS_SYSUPDATE"));



            results.add(dto);
            
            if(maxSize >0  && results.size() == maxSize)
            {
            	break ;
            }
            
         }
        
		return results ;
    }

  /**
   * populate resultSet as cclass
   */
  protected IDto[] getChildrenResults(ResultSet rs ,Class _dto) throws SQLException{

      throw new SQLException("本dto没有与其它dto相关联，无法应用此方法");
  }


     /**
     * Update a record in Database.
     */

    public void update(IDto _dto, Connection conn, boolean isLobSupport) throws SQLException
    {
        String msgValid = _dto.checkValid() ;
        if (msgValid != null)
            throw new SQLException("修改错误，"+msgValid) ;
        TvVoucherinfoAllocateIncomeDto dto = (TvVoucherinfoAllocateIncomeDto)_dto ;
        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            int pos = 1;
            //S_ORGCODE
            ps.setString(pos, dto.getSorgcode());
            pos++;

            //S_TRECODE
            ps.setString(pos, dto.getStrecode());
            pos++;

            //S_ADMDIVCODE
            ps.setString(pos, dto.getSadmdivcode());
            pos++;

            //S_VTCODE
            ps.setString(pos, dto.getSvtcode());
            pos++;

            //S_COMMITDATE
            ps.setString(pos, dto.getScommitdate());
            pos++;

            //S_ORICOMMITDATE
            ps.setString(pos, dto.getSoricommitdate());
            pos++;

            //S_RECEIVEDATE
            ps.setString(pos, dto.getSreceivedate());
            pos++;

            //N_MONEY
            ps.setBigDecimal(pos, dto.getNmoney());
            pos++;

            //S_ESCORTMARKS
            ps.setString(pos, dto.getSescortmarks());
            pos++;

            //S_DEMO
            ps.setString(pos, dto.getSdemo());
            pos++;

            //S_PAYEEACCTNO
            ps.setString(pos, dto.getSpayeeacctno());
            pos++;

            //S_PAYEEACCTNAME
            ps.setString(pos, dto.getSpayeeacctname());
            pos++;

            //S_PAYEEACCTBANKNAME
            ps.setString(pos, dto.getSpayeeacctbankname());
            pos++;

            //S_PAYACCTNO
            ps.setString(pos, dto.getSpayacctno());
            pos++;

            //S_PAYACCTNAME
            ps.setString(pos, dto.getSpayacctname());
            pos++;

            //S_PAYACCTBANKNAME
            ps.setString(pos, dto.getSpayacctbankname());
            pos++;

            //S_FORWARDBANKNO
            ps.setString(pos, dto.getSforwardbankno());
            pos++;

            //S_FORWARDBANKNAME
            ps.setString(pos, dto.getSforwardbankname());
            pos++;

            //S_RECEIVEBANKNO
            ps.setString(pos, dto.getSreceivebankno());
            pos++;

            //S_PAYDEALNO
            ps.setString(pos, dto.getSpaydealno());
            pos++;

            //S_ORIPAYDEALNO
            ps.setString(pos, dto.getSoripaydealno());
            pos++;

            //S_REPORTKIND
            ps.setString(pos, dto.getSreportkind());
            pos++;

            //S_TRADEKIND
            ps.setString(pos, dto.getStradekind());
            pos++;

            //S_VTCODEKIND
            ps.setString(pos, dto.getSvtcodekind());
            pos++;

            //S_VTCODEKINDNO
            ps.setString(pos, dto.getSvtcodekindno());
            pos++;

            //S_VTCODEDES
            ps.setString(pos, dto.getSvtcodedes());
            pos++;

            //S_SECRETSIGN
            ps.setString(pos, dto.getSsecretsign());
            pos++;

            //S_BORROW
            ps.setString(pos, dto.getSborrow());
            pos++;

            //S_LEND
            ps.setString(pos, dto.getSlend());
            pos++;

            //S_HOLD1
            ps.setString(pos, dto.getShold1());
            pos++;

            //S_HOLD2
            ps.setString(pos, dto.getShold2());
            pos++;

            //TS_SYSUPDATE

           //S_DEALNO
           ps.setString(pos, dto.getSdealno());
           pos++;
           ps.executeUpdate();
        }
        finally
        {
           close(ps);
        }

        return ;
    }
    
    
    
      /**
     * Update batch record in Database.
     */

    public void update(IDto[] _dtos, Connection conn, boolean isLobSupport) throws SQLException
    {
     
     	 TvVoucherinfoAllocateIncomeDto dto  ;
         for (int i= 0; i< _dtos.length ; i++)
         {
            dto  = (TvVoucherinfoAllocateIncomeDto)_dtos[i] ; 
            String msgValid = dto.checkValid() ;
            if (msgValid != null)
                throw new SQLException("修改错误，"+msgValid) ;
         }

        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            
            for (int i= 0; i< _dtos.length ; i++)
            {
                dto  = (TvVoucherinfoAllocateIncomeDto)_dtos[i] ; 
                int pos = 1;
                //S_ORGCODE
                 ps.setString(pos, dto.getSorgcode());
                pos++;

                //S_TRECODE
                 ps.setString(pos, dto.getStrecode());
                pos++;

                //S_ADMDIVCODE
                 ps.setString(pos, dto.getSadmdivcode());
                pos++;

                //S_VTCODE
                 ps.setString(pos, dto.getSvtcode());
                pos++;

                //S_COMMITDATE
                 ps.setString(pos, dto.getScommitdate());
                pos++;

                //S_ORICOMMITDATE
                 ps.setString(pos, dto.getSoricommitdate());
                pos++;

                //S_RECEIVEDATE
                 ps.setString(pos, dto.getSreceivedate());
                pos++;

                //N_MONEY
                 ps.setBigDecimal(pos, dto.getNmoney());
                pos++;

                //S_ESCORTMARKS
                 ps.setString(pos, dto.getSescortmarks());
                pos++;

                //S_DEMO
                 ps.setString(pos, dto.getSdemo());
                pos++;

                //S_PAYEEACCTNO
                 ps.setString(pos, dto.getSpayeeacctno());
                pos++;

                //S_PAYEEACCTNAME
                 ps.setString(pos, dto.getSpayeeacctname());
                pos++;

                //S_PAYEEACCTBANKNAME
                 ps.setString(pos, dto.getSpayeeacctbankname());
                pos++;

                //S_PAYACCTNO
                 ps.setString(pos, dto.getSpayacctno());
                pos++;

                //S_PAYACCTNAME
                 ps.setString(pos, dto.getSpayacctname());
                pos++;

                //S_PAYACCTBANKNAME
                 ps.setString(pos, dto.getSpayacctbankname());
                pos++;

                //S_FORWARDBANKNO
                 ps.setString(pos, dto.getSforwardbankno());
                pos++;

                //S_FORWARDBANKNAME
                 ps.setString(pos, dto.getSforwardbankname());
                pos++;

                //S_RECEIVEBANKNO
                 ps.setString(pos, dto.getSreceivebankno());
                pos++;

                //S_PAYDEALNO
                 ps.setString(pos, dto.getSpaydealno());
                pos++;

                //S_ORIPAYDEALNO
                 ps.setString(pos, dto.getSoripaydealno());
                pos++;

                //S_REPORTKIND
                 ps.setString(pos, dto.getSreportkind());
                pos++;

                //S_TRADEKIND
                 ps.setString(pos, dto.getStradekind());
                pos++;

                //S_VTCODEKIND
                 ps.setString(pos, dto.getSvtcodekind());
                pos++;

                //S_VTCODEKINDNO
                 ps.setString(pos, dto.getSvtcodekindno());
                pos++;

                //S_VTCODEDES
                 ps.setString(pos, dto.getSvtcodedes());
                pos++;

                //S_SECRETSIGN
                 ps.setString(pos, dto.getSsecretsign());
                pos++;

                //S_BORROW
                 ps.setString(pos, dto.getSborrow());
                pos++;

                //S_LEND
                 ps.setString(pos, dto.getSlend());
                pos++;

                //S_HOLD1
                 ps.setString(pos, dto.getShold1());
                pos++;

                //S_HOLD2
                 ps.setString(pos, dto.getShold2());
                pos++;

                //TS_SYSUPDATE
 
               //S_DEALNO
               ps.setString(pos, dto.getSdealno());
               pos++;
		       ps.addBatch(); 
	       }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }

        return ;
    }



    /**
     * delete a record in Database.
     */
    public void delete(IPK _pk, Connection conn) throws SQLException
    {
       
      
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return ;
        //	throw new SQLException("删除错误，"+msgValid) ;
       }
       TvVoucherinfoAllocateIncomePK pk = (TvVoucherinfoAllocateIncomePK)_pk ;


       PreparedStatement ps = null;
       try {
           ps = conn.prepareStatement(SQL_DELETE);
           ps.setString(1, pk.getSdealno());
           ps.executeUpdate();
        }
        finally
        {
            close(ps);
        }
    }


     /**
     * delete batch record in Database.
     */
    public void delete(IPK[] _pks, Connection conn) throws SQLException
    {
        TvVoucherinfoAllocateIncomePK pk ;
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TvVoucherinfoAllocateIncomePK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 		
 		
        PreparedStatement ps = null;
        try
         {
            ps = conn.prepareStatement(SQL_DELETE);
             
       		for (int i= 0; i< pks.size() ; i++)
       		{
       			pk  = (TvVoucherinfoAllocateIncomePK)(pks.get(i)) ; 
                ps.setString(1, pk.getSdealno());
		        ps.addBatch() ;
			}
            ps.executeBatch();
        }
        finally
        {
            close(ps);
        }      
    }



    /**
    * Check IChangerNumber or UpdateTimeStamp.
    */
    public void check(IDto _dto, Connection conn) throws SQLException
    {
                                                                                                		throw new SQLException("数据库表：TV_VOUCHERINFO_ALLOCATE_INCOME没有检查修改的字段");
    }


	/**
	* synchronize values between parent and children
	*/
    public void syncToChildren(IDto _dto)     throws SQLException{
       throw new SQLException("本记录没有与其它记录相关联，无法进行主记录与明细记录之间的数据同步。");
  }

	/**
	*get children
	*/
    public IDto[] findChildren (IDto _dto, Connection conn, boolean isLobSupport)     throws SQLException{
         throw new SQLException("本dto没有与其它dto相关联，无法得到子dto");
  }    
    
  /**
   *return the children IDto class
   * @return Class
   */
  public Class getChildrenClass(){
	  return null;
  };    



    /**
     * Close JDBC Statement.
     * @param stmt  Statement to be closed.
     */
    private void close(Statement stmt)
    {
        if (stmt != null)
        {
            try
            {
                stmt.close();
            }
            catch(SQLException    e)
            {}
        }
    }

    /**
     * Close JDBC ResultSet.
     * @param rs  ResultSet to be closed.
     */
    private void close(ResultSet rs)
    {
        if (rs != null)
        {
            try
            {
                rs.close();
            }catch(SQLException  e){}
        }
    }
}
