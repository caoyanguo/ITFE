    



package com.cfcc.itfe.persistence.dao;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.Statement;
import java.sql.SQLException;
import java.sql.Date;
import java.sql.Time;
import java.sql.Timestamp ;
import java.sql.Clob;

import com.cfcc.jaf.persistence.jaform.parent.* ;

import java.math.BigDecimal;

import java.util.List;
import java.util.ArrayList;
import java.util.HashMap;

import java.io.StringReader;
import java.io.Reader;
import java.io.IOException;

import com.cfcc.itfe.persistence.dto.TvInfileDto ;
import com.cfcc.itfe.persistence.pk.TvInfilePK ;


/**
 * <p>Title: DAO of table: TV_INFILE</p>
 * <p>Description: Data Access Object  </p>
 * <p>Copyright: Copyright (c) 2005</p>
 * <p>Company: CFCC </p>
 * <p>Generation Time: 2014-12-25 15:29:02 </p>
 *  Do NOT change this file by hand.
 *  This java source file is generated by JAF_ORM .
 *  dao.vm timestamp: 2007-04-06 08:30:00
 ******************************************************
 * <p> <b>change record</b><br>
 *  make dao methods simply.<br>
 *  especially make update,delete methods simply.<br>
 *  remove "..withCheck" method,withCheck will impl in JAFOrmTemplate.<br>
 *  remove "delete(IDto _dto,..)" method.<br></p>
 * @author win7
 */

public class TvInfileDao  implements IDao
{


    /* SQL to insert data */
    private static final String SQL_INSERT =
        "INSERT INTO TV_INFILE ("
          + "S_ORGCODE,S_COMMITDATE,S_ACCDATE,S_PACKAGENO,S_TAXORGCODE"
          + ",S_TBS_TAXORGCODE,S_FILENAME,S_RECVTRECODE,S_RECVTRENAME,S_DEALNO"
          + ",S_TRASRLNO,N_MONEY,S_TAXTICKETNO,S_GENTICKETDATE,S_BUDGETTYPE"
          + ",S_TRIMFLAG,S_BUDGETSUBCODE,S_BUDGETLEVELCODE,S_ASSITSIGN,S_TBS_ASSITSIGN"
          + ",S_UNITCODE,S_PAYBOOKKIND,S_OPENACCBANKCODE,S_STATUS,S_USERCODE"
          + ",S_DEMO,S_TAXPAYCODE,S_TAXPAYNAME,S_CORPTYPE,S_LIMITDATE"
          + ",S_TAXTYPECODE,S_TAXTYPENAME,S_TAXSUBJECTNAME,S_PAYBNKNO,S_PAYACCT"
          + ",S_TAXSTARTDATE,S_TAXENDDATE,S_TAXNUMBER,N_TAXAMT,N_TAXRATE"
          + ",N_DISCOUNTTAXAMT,N_FACTTAXAMT,S_SOURCEFLAG"
        + ") VALUES ("
        + "?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)";


    private static final String SQL_INSERT_WITH_RESULT = "SELECT * FROM FINAL TABLE( " + SQL_INSERT + " )";
    
    /* SQL to select data */
    private static final String SQL_SELECT =
        "SELECT "
        + "TV_INFILE.S_ORGCODE, TV_INFILE.S_COMMITDATE, TV_INFILE.S_ACCDATE, TV_INFILE.S_PACKAGENO, TV_INFILE.S_TAXORGCODE, "
        + "TV_INFILE.S_TBS_TAXORGCODE, TV_INFILE.S_FILENAME, TV_INFILE.S_RECVTRECODE, TV_INFILE.S_RECVTRENAME, TV_INFILE.S_DEALNO, "
        + "TV_INFILE.S_TRASRLNO, TV_INFILE.N_MONEY, TV_INFILE.S_TAXTICKETNO, TV_INFILE.S_GENTICKETDATE, TV_INFILE.S_BUDGETTYPE, "
        + "TV_INFILE.S_TRIMFLAG, TV_INFILE.S_BUDGETSUBCODE, TV_INFILE.S_BUDGETLEVELCODE, TV_INFILE.S_ASSITSIGN, TV_INFILE.S_TBS_ASSITSIGN, "
        + "TV_INFILE.S_UNITCODE, TV_INFILE.S_PAYBOOKKIND, TV_INFILE.S_OPENACCBANKCODE, TV_INFILE.S_STATUS, TV_INFILE.S_USERCODE, "
        + "TV_INFILE.S_DEMO, TV_INFILE.S_TAXPAYCODE, TV_INFILE.S_TAXPAYNAME, TV_INFILE.S_CORPTYPE, TV_INFILE.S_LIMITDATE, "
        + "TV_INFILE.S_TAXTYPECODE, TV_INFILE.S_TAXTYPENAME, TV_INFILE.S_TAXSUBJECTNAME, TV_INFILE.S_PAYBNKNO, TV_INFILE.S_PAYACCT, "
        + "TV_INFILE.S_TAXSTARTDATE, TV_INFILE.S_TAXENDDATE, TV_INFILE.S_TAXNUMBER, TV_INFILE.N_TAXAMT, TV_INFILE.N_TAXRATE, "
        + "TV_INFILE.N_DISCOUNTTAXAMT, TV_INFILE.N_FACTTAXAMT, TV_INFILE.S_SOURCEFLAG "
        + "FROM TV_INFILE "
        +" WHERE " 
        + "S_ORGCODE = ? AND S_COMMITDATE = ? AND S_DEALNO = ?"
        ;
    /* SQL to select for update data */
    private static final String SQL_SELECT_FOR_UPDATE =
        "SELECT "
        + "TV_INFILE.S_ORGCODE, TV_INFILE.S_COMMITDATE, TV_INFILE.S_ACCDATE, TV_INFILE.S_PACKAGENO, TV_INFILE.S_TAXORGCODE, "
        + "TV_INFILE.S_TBS_TAXORGCODE, TV_INFILE.S_FILENAME, TV_INFILE.S_RECVTRECODE, TV_INFILE.S_RECVTRENAME, TV_INFILE.S_DEALNO, "
        + "TV_INFILE.S_TRASRLNO, TV_INFILE.N_MONEY, TV_INFILE.S_TAXTICKETNO, TV_INFILE.S_GENTICKETDATE, TV_INFILE.S_BUDGETTYPE, "
        + "TV_INFILE.S_TRIMFLAG, TV_INFILE.S_BUDGETSUBCODE, TV_INFILE.S_BUDGETLEVELCODE, TV_INFILE.S_ASSITSIGN, TV_INFILE.S_TBS_ASSITSIGN, "
        + "TV_INFILE.S_UNITCODE, TV_INFILE.S_PAYBOOKKIND, TV_INFILE.S_OPENACCBANKCODE, TV_INFILE.S_STATUS, TV_INFILE.S_USERCODE, "
        + "TV_INFILE.S_DEMO, TV_INFILE.S_TAXPAYCODE, TV_INFILE.S_TAXPAYNAME, TV_INFILE.S_CORPTYPE, TV_INFILE.S_LIMITDATE, "
        + "TV_INFILE.S_TAXTYPECODE, TV_INFILE.S_TAXTYPENAME, TV_INFILE.S_TAXSUBJECTNAME, TV_INFILE.S_PAYBNKNO, TV_INFILE.S_PAYACCT, "
        + "TV_INFILE.S_TAXSTARTDATE, TV_INFILE.S_TAXENDDATE, TV_INFILE.S_TAXNUMBER, TV_INFILE.N_TAXAMT, TV_INFILE.N_TAXRATE, "
        + "TV_INFILE.N_DISCOUNTTAXAMT, TV_INFILE.N_FACTTAXAMT, TV_INFILE.S_SOURCEFLAG "
        + "FROM TV_INFILE "
        +" WHERE " 
        + "S_ORGCODE = ? AND S_COMMITDATE = ? AND S_DEALNO = ? FOR UPDATE"
        ;

      /* SQL to select data (SCROLLABLE)*/
     private static final String SQL_SELECT_BATCH_SCROLLABLE =
        "SELECT "
        + "  TV_INFILE.S_ORGCODE  , TV_INFILE.S_COMMITDATE  , TV_INFILE.S_ACCDATE  , TV_INFILE.S_PACKAGENO  , TV_INFILE.S_TAXORGCODE "
        + " , TV_INFILE.S_TBS_TAXORGCODE  , TV_INFILE.S_FILENAME  , TV_INFILE.S_RECVTRECODE  , TV_INFILE.S_RECVTRENAME  , TV_INFILE.S_DEALNO "
        + " , TV_INFILE.S_TRASRLNO  , TV_INFILE.N_MONEY  , TV_INFILE.S_TAXTICKETNO  , TV_INFILE.S_GENTICKETDATE  , TV_INFILE.S_BUDGETTYPE "
        + " , TV_INFILE.S_TRIMFLAG  , TV_INFILE.S_BUDGETSUBCODE  , TV_INFILE.S_BUDGETLEVELCODE  , TV_INFILE.S_ASSITSIGN  , TV_INFILE.S_TBS_ASSITSIGN "
        + " , TV_INFILE.S_UNITCODE  , TV_INFILE.S_PAYBOOKKIND  , TV_INFILE.S_OPENACCBANKCODE  , TV_INFILE.S_STATUS  , TV_INFILE.S_USERCODE "
        + " , TV_INFILE.S_DEMO  , TV_INFILE.S_TAXPAYCODE  , TV_INFILE.S_TAXPAYNAME  , TV_INFILE.S_CORPTYPE  , TV_INFILE.S_LIMITDATE "
        + " , TV_INFILE.S_TAXTYPECODE  , TV_INFILE.S_TAXTYPENAME  , TV_INFILE.S_TAXSUBJECTNAME  , TV_INFILE.S_PAYBNKNO  , TV_INFILE.S_PAYACCT "
        + " , TV_INFILE.S_TAXSTARTDATE  , TV_INFILE.S_TAXENDDATE  , TV_INFILE.S_TAXNUMBER  , TV_INFILE.N_TAXAMT  , TV_INFILE.N_TAXRATE "
        + " , TV_INFILE.N_DISCOUNTTAXAMT  , TV_INFILE.N_FACTTAXAMT  , TV_INFILE.S_SOURCEFLAG "
        + "FROM TV_INFILE ";



    /* SQL to select batch data */
    private static final String SQL_SELECT_BATCH =
        "SELECT "
        + "TV_INFILE.S_ORGCODE, TV_INFILE.S_COMMITDATE, TV_INFILE.S_ACCDATE, TV_INFILE.S_PACKAGENO, TV_INFILE.S_TAXORGCODE, "
        + "TV_INFILE.S_TBS_TAXORGCODE, TV_INFILE.S_FILENAME, TV_INFILE.S_RECVTRECODE, TV_INFILE.S_RECVTRENAME, TV_INFILE.S_DEALNO, "
        + "TV_INFILE.S_TRASRLNO, TV_INFILE.N_MONEY, TV_INFILE.S_TAXTICKETNO, TV_INFILE.S_GENTICKETDATE, TV_INFILE.S_BUDGETTYPE, "
        + "TV_INFILE.S_TRIMFLAG, TV_INFILE.S_BUDGETSUBCODE, TV_INFILE.S_BUDGETLEVELCODE, TV_INFILE.S_ASSITSIGN, TV_INFILE.S_TBS_ASSITSIGN, "
        + "TV_INFILE.S_UNITCODE, TV_INFILE.S_PAYBOOKKIND, TV_INFILE.S_OPENACCBANKCODE, TV_INFILE.S_STATUS, TV_INFILE.S_USERCODE, "
        + "TV_INFILE.S_DEMO, TV_INFILE.S_TAXPAYCODE, TV_INFILE.S_TAXPAYNAME, TV_INFILE.S_CORPTYPE, TV_INFILE.S_LIMITDATE, "
        + "TV_INFILE.S_TAXTYPECODE, TV_INFILE.S_TAXTYPENAME, TV_INFILE.S_TAXSUBJECTNAME, TV_INFILE.S_PAYBNKNO, TV_INFILE.S_PAYACCT, "
        + "TV_INFILE.S_TAXSTARTDATE, TV_INFILE.S_TAXENDDATE, TV_INFILE.S_TAXNUMBER, TV_INFILE.N_TAXAMT, TV_INFILE.N_TAXRATE, "
        + "TV_INFILE.N_DISCOUNTTAXAMT, TV_INFILE.N_FACTTAXAMT, TV_INFILE.S_SOURCEFLAG "
        + "FROM TV_INFILE " ;
        

   /* SQL to select batch data where condition  */
    private static final String SQL_SELECT_BATCH_WHERE =" ( "
        + "S_ORGCODE = ? AND S_COMMITDATE = ? AND S_DEALNO = ?)"
        ;


    /* SQL to update data */
    private static final String SQL_UPDATE =
        "UPDATE TV_INFILE SET "
        + "S_ACCDATE =?,S_PACKAGENO =?,S_TAXORGCODE =?,S_TBS_TAXORGCODE =?,S_FILENAME =?, "
        + "S_RECVTRECODE =?,S_RECVTRENAME =?,S_TRASRLNO =?,N_MONEY =?,S_TAXTICKETNO =?, "
        + "S_GENTICKETDATE =?,S_BUDGETTYPE =?,S_TRIMFLAG =?,S_BUDGETSUBCODE =?,S_BUDGETLEVELCODE =?, "
        + "S_ASSITSIGN =?,S_TBS_ASSITSIGN =?,S_UNITCODE =?,S_PAYBOOKKIND =?,S_OPENACCBANKCODE =?, "
        + "S_STATUS =?,S_USERCODE =?,S_DEMO =?,S_TAXPAYCODE =?,S_TAXPAYNAME =?, "
        + "S_CORPTYPE =?,S_LIMITDATE =?,S_TAXTYPECODE =?,S_TAXTYPENAME =?,S_TAXSUBJECTNAME =?, "
        + "S_PAYBNKNO =?,S_PAYACCT =?,S_TAXSTARTDATE =?,S_TAXENDDATE =?,S_TAXNUMBER =?, "
        + "N_TAXAMT =?,N_TAXRATE =?,N_DISCOUNTTAXAMT =?,N_FACTTAXAMT =?,S_SOURCEFLAG =? "
        + "WHERE "
        + "S_ORGCODE = ? AND S_COMMITDATE = ? AND S_DEALNO = ?"
        ;

	/* SQL to update data, support LOB */
    private static final String SQL_UPDATE_LOB =
        "UPDATE TV_INFILE SET "
        + "S_ACCDATE =?, S_PACKAGENO =?, S_TAXORGCODE =?, S_TBS_TAXORGCODE =?, S_FILENAME =?,  "
        + "S_RECVTRECODE =?, S_RECVTRENAME =?, S_TRASRLNO =?, N_MONEY =?, S_TAXTICKETNO =?,  "
        + "S_GENTICKETDATE =?, S_BUDGETTYPE =?, S_TRIMFLAG =?, S_BUDGETSUBCODE =?, S_BUDGETLEVELCODE =?,  "
        + "S_ASSITSIGN =?, S_TBS_ASSITSIGN =?, S_UNITCODE =?, S_PAYBOOKKIND =?, S_OPENACCBANKCODE =?,  "
        + "S_STATUS =?, S_USERCODE =?, S_DEMO =?, S_TAXPAYCODE =?, S_TAXPAYNAME =?,  "
        + "S_CORPTYPE =?, S_LIMITDATE =?, S_TAXTYPECODE =?, S_TAXTYPENAME =?, S_TAXSUBJECTNAME =?,  "
        + "S_PAYBNKNO =?, S_PAYACCT =?, S_TAXSTARTDATE =?, S_TAXENDDATE =?, S_TAXNUMBER =?,  "
        + "N_TAXAMT =?, N_TAXRATE =?, N_DISCOUNTTAXAMT =?, N_FACTTAXAMT =?, S_SOURCEFLAG =? "
        + "WHERE "
        + "S_ORGCODE = ? AND S_COMMITDATE = ? AND S_DEALNO = ?"
        ;	
	
    /* SQL to delete data */
    private static final String SQL_DELETE =
        "DELETE FROM TV_INFILE " 
        + " WHERE "
        + "S_ORGCODE = ? AND S_COMMITDATE = ? AND S_DEALNO = ?"
        ;


	/**
	*  批量查询 一次最多查询的参数数量
	*/
	
	public static final int FIND_BATCH_SIZE = 150  / 3;
	



   /**
   * Create a new record in Database.
   */
  public void create(IDto _dto,  Connection conn) throws SQLException
  {
     TvInfileDto dto = (TvInfileDto)_dto ;
     String msgValid = dto.checkValid() ;
     if (msgValid != null)
     	throw new SQLException("插入错误，"+msgValid) ;

     PreparedStatement ps = null;
     try
     {
         ps = conn.prepareStatement(SQL_INSERT);
          ps.setString(1, dto.getSorgcode());

          ps.setString(2, dto.getScommitdate());

          ps.setString(3, dto.getSaccdate());

          ps.setString(4, dto.getSpackageno());

          ps.setString(5, dto.getStaxorgcode());

          ps.setString(6, dto.getStbstaxorgcode());

          ps.setString(7, dto.getSfilename());

          ps.setString(8, dto.getSrecvtrecode());

          ps.setString(9, dto.getSrecvtrename());

          ps.setString(10, dto.getSdealno());

          ps.setString(11, dto.getStrasrlno());

          ps.setBigDecimal(12, dto.getNmoney());

          ps.setString(13, dto.getStaxticketno());

          ps.setString(14, dto.getSgenticketdate());

          ps.setString(15, dto.getSbudgettype());

          ps.setString(16, dto.getStrimflag());

          ps.setString(17, dto.getSbudgetsubcode());

          ps.setString(18, dto.getSbudgetlevelcode());

          ps.setString(19, dto.getSassitsign());

          ps.setString(20, dto.getStbsassitsign());

          ps.setString(21, dto.getSunitcode());

          ps.setString(22, dto.getSpaybookkind());

          ps.setString(23, dto.getSopenaccbankcode());

          ps.setString(24, dto.getSstatus());

          ps.setString(25, dto.getSusercode());

          ps.setString(26, dto.getSdemo());

          ps.setString(27, dto.getStaxpaycode());

          ps.setString(28, dto.getStaxpayname());

          ps.setString(29, dto.getScorptype());

          ps.setString(30, dto.getSlimitdate());

          ps.setString(31, dto.getStaxtypecode());

          ps.setString(32, dto.getStaxtypename());

          ps.setString(33, dto.getStaxsubjectname());

          ps.setString(34, dto.getSpaybnkno());

          ps.setString(35, dto.getSpayacct());

          ps.setString(36, dto.getStaxstartdate());

          ps.setString(37, dto.getStaxenddate());

          ps.setString(38, dto.getStaxnumber());

          ps.setBigDecimal(39, dto.getNtaxamt());

          ps.setBigDecimal(40, dto.getNtaxrate());

          ps.setBigDecimal(41, dto.getNdiscounttaxamt());

          ps.setBigDecimal(42, dto.getNfacttaxamt());

          ps.setString(43, dto.getSsourceflag());

         ps.executeUpdate();
     }
     finally
     {
         close(ps);
     }
     return  ;
  }

   /**
   * Create a new record in Database. and return result, only used when having generated key
   */
   public IDto createWithResult(IDto _dto,  Connection conn) throws SQLException
   {
       TvInfileDto dto = (TvInfileDto)_dto ;
       String msgValid = dto.checkValid() ;
       if (msgValid != null)
           throw new SQLException("插入错误，"+msgValid) ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try
       {
           ps = conn.prepareStatement(SQL_INSERT_WITH_RESULT);
            ps.setString(1, dto.getSorgcode());
            ps.setString(2, dto.getScommitdate());
            ps.setString(3, dto.getSaccdate());
            ps.setString(4, dto.getSpackageno());
            ps.setString(5, dto.getStaxorgcode());
            ps.setString(6, dto.getStbstaxorgcode());
            ps.setString(7, dto.getSfilename());
            ps.setString(8, dto.getSrecvtrecode());
            ps.setString(9, dto.getSrecvtrename());
            ps.setString(10, dto.getSdealno());
            ps.setString(11, dto.getStrasrlno());
            ps.setBigDecimal(12, dto.getNmoney());
            ps.setString(13, dto.getStaxticketno());
            ps.setString(14, dto.getSgenticketdate());
            ps.setString(15, dto.getSbudgettype());
            ps.setString(16, dto.getStrimflag());
            ps.setString(17, dto.getSbudgetsubcode());
            ps.setString(18, dto.getSbudgetlevelcode());
            ps.setString(19, dto.getSassitsign());
            ps.setString(20, dto.getStbsassitsign());
            ps.setString(21, dto.getSunitcode());
            ps.setString(22, dto.getSpaybookkind());
            ps.setString(23, dto.getSopenaccbankcode());
            ps.setString(24, dto.getSstatus());
            ps.setString(25, dto.getSusercode());
            ps.setString(26, dto.getSdemo());
            ps.setString(27, dto.getStaxpaycode());
            ps.setString(28, dto.getStaxpayname());
            ps.setString(29, dto.getScorptype());
            ps.setString(30, dto.getSlimitdate());
            ps.setString(31, dto.getStaxtypecode());
            ps.setString(32, dto.getStaxtypename());
            ps.setString(33, dto.getStaxsubjectname());
            ps.setString(34, dto.getSpaybnkno());
            ps.setString(35, dto.getSpayacct());
            ps.setString(36, dto.getStaxstartdate());
            ps.setString(37, dto.getStaxenddate());
            ps.setString(38, dto.getStaxnumber());
            ps.setBigDecimal(39, dto.getNtaxamt());
            ps.setBigDecimal(40, dto.getNtaxrate());
            ps.setBigDecimal(41, dto.getNdiscounttaxamt());
            ps.setBigDecimal(42, dto.getNfacttaxamt());
            ps.setString(43, dto.getSsourceflag());
           rs = ps.executeQuery();
           List results = getResults(rs);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }
        finally
        {
           close(ps);
           close(rs);
        }
   }

 	/**
	* Create batch new record in Database. it only create the first layer dto
	*/
    public void create(IDto[] _dtos, Connection conn)    throws SQLException
    {
        TvInfileDto dto  ;
     	for (int i= 0; i< _dtos.length ; i++)
 	    {
 		    dto  = (TvInfileDto)_dtos[i] ; 
    	    String msgValid = dto.checkValid() ;
        	if (msgValid != null)
     	    	throw new SQLException("插入错误，"+msgValid) ;
     	}
 	    PreparedStatement ps = null;
        try
        {
            ps = conn.prepareStatement(SQL_INSERT);
       
     	    for (int i= 0; i< _dtos.length ; i++)
     	    {
 	    	    dto  = (TvInfileDto)_dtos[i] ; 
  
               ps.setString(1, dto.getSorgcode());
  
               ps.setString(2, dto.getScommitdate());
  
               ps.setString(3, dto.getSaccdate());
  
               ps.setString(4, dto.getSpackageno());
  
               ps.setString(5, dto.getStaxorgcode());
  
               ps.setString(6, dto.getStbstaxorgcode());
  
               ps.setString(7, dto.getSfilename());
  
               ps.setString(8, dto.getSrecvtrecode());
  
               ps.setString(9, dto.getSrecvtrename());
  
               ps.setString(10, dto.getSdealno());
  
               ps.setString(11, dto.getStrasrlno());
  
               ps.setBigDecimal(12, dto.getNmoney());
  
               ps.setString(13, dto.getStaxticketno());
  
               ps.setString(14, dto.getSgenticketdate());
  
               ps.setString(15, dto.getSbudgettype());
  
               ps.setString(16, dto.getStrimflag());
  
               ps.setString(17, dto.getSbudgetsubcode());
  
               ps.setString(18, dto.getSbudgetlevelcode());
  
               ps.setString(19, dto.getSassitsign());
  
               ps.setString(20, dto.getStbsassitsign());
  
               ps.setString(21, dto.getSunitcode());
  
               ps.setString(22, dto.getSpaybookkind());
  
               ps.setString(23, dto.getSopenaccbankcode());
  
               ps.setString(24, dto.getSstatus());
  
               ps.setString(25, dto.getSusercode());
  
               ps.setString(26, dto.getSdemo());
  
               ps.setString(27, dto.getStaxpaycode());
  
               ps.setString(28, dto.getStaxpayname());
  
               ps.setString(29, dto.getScorptype());
  
               ps.setString(30, dto.getSlimitdate());
  
               ps.setString(31, dto.getStaxtypecode());
  
               ps.setString(32, dto.getStaxtypename());
  
               ps.setString(33, dto.getStaxsubjectname());
  
               ps.setString(34, dto.getSpaybnkno());
  
               ps.setString(35, dto.getSpayacct());
  
               ps.setString(36, dto.getStaxstartdate());
  
               ps.setString(37, dto.getStaxenddate());
  
               ps.setString(38, dto.getStaxnumber());
  
               ps.setBigDecimal(39, dto.getNtaxamt());
  
               ps.setBigDecimal(40, dto.getNtaxrate());
  
               ps.setBigDecimal(41, dto.getNdiscounttaxamt());
  
               ps.setBigDecimal(42, dto.getNfacttaxamt());
  
               ps.setString(43, dto.getSsourceflag());
               ps.addBatch(); 
           }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }
        return  ;
   }




     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto find(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TvInfilePK pk = (TvInfilePK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT);
           ps.setString(1, pk.getSorgcode());

           ps.setString(2, pk.getScommitdate());

           ps.setString(3, pk.getSdealno());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }

     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto findForUpdate(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TvInfilePK pk = (TvInfilePK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT_FOR_UPDATE);
           ps.setString(1, pk.getSorgcode());

           ps.setString(2, pk.getScommitdate());

           ps.setString(3, pk.getSdealno());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }
  
     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto[] find(IPK[] _pks, Connection conn, boolean isLobSupport) throws SQLException
    {
        TvInfilePK pk ;
        
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TvInfilePK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 
        PreparedStatement ps = null;
        ResultSet rs = null;
        
         
        List results = new ArrayList();  
		for (int iBegin = 0; iBegin < pks.size(); iBegin += FIND_BATCH_SIZE)   //
		{
        
            try {
          		int iFindBatchSize = pks.size() - iBegin ;
	    		if (iFindBatchSize >FIND_BATCH_SIZE )
		    		iFindBatchSize = FIND_BATCH_SIZE ;
		  
        	    StringBuffer sb = new StringBuffer() ;
            	sb.append(SQL_SELECT_BATCH).append(" WHERE ").append(SQL_SELECT_BATCH_WHERE);

	    		for (int i = iBegin+1; i < iBegin + iFindBatchSize; i++)
		    	{
			    	sb.append("OR").append(SQL_SELECT_BATCH_WHERE) ;
    			}
        	
                ps = conn.prepareStatement(sb.toString());

                for (int i = iBegin;i < iBegin + iFindBatchSize; i++)
                {
                    pk  = (TvInfilePK)(pks.get(i)) ; 
                   ps.setString((i-iBegin)*3+1, pk.getSorgcode());

                   ps.setString((i-iBegin)*3+2, pk.getScommitdate());

                   ps.setString((i-iBegin)*3+3, pk.getSdealno());

			
	            }
                rs = ps.executeQuery();
                results.addAll(getResults(rs,0,isLobSupport)); //
            }finally {
                close(rs);
                close(ps);
            }
        }
        if (results!=null && results.size() > 0)
        {
            TvInfileDto[] dtos = new TvInfileDto[0];
		    dtos = (TvInfileDto[]) results.toArray(dtos) ;
		    return dtos ;
        }
        else
        {
            return null;
        }
    }

  
  
  



    /**
	    * 得到查询语句
	    */
    public String getSelectSQL() 
    {
	        return SQL_SELECT_BATCH_SCROLLABLE ;
    }
   

	public List getResults(ResultSet rs) throws SQLException
    {
    	return getResults(rs, 0) ;
    }
  

    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize) throws SQLException
    {
		return getResults(rs,maxSize,false);
	}
    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize, boolean isLobSupport) throws SQLException
    {
        List results = new ArrayList();
        String str ;
        Reader reader;
        Clob clob;
        char[] chars ;
        
        while (rs.next())
         {
             TvInfileDto  dto = new  TvInfileDto ();
             //S_ORGCODE
             str = rs.getString("S_ORGCODE");
             if (str == null)
                dto.setSorgcode(null);
             else
                dto.setSorgcode(str.trim());

             //S_COMMITDATE
             str = rs.getString("S_COMMITDATE");
             if (str == null)
                dto.setScommitdate(null);
             else
                dto.setScommitdate(str.trim());

             //S_ACCDATE
             str = rs.getString("S_ACCDATE");
             if (str == null)
                dto.setSaccdate(null);
             else
                dto.setSaccdate(str.trim());

             //S_PACKAGENO
             str = rs.getString("S_PACKAGENO");
             if (str == null)
                dto.setSpackageno(null);
             else
                dto.setSpackageno(str.trim());

             //S_TAXORGCODE
             str = rs.getString("S_TAXORGCODE");
             if (str == null)
                dto.setStaxorgcode(null);
             else
                dto.setStaxorgcode(str.trim());

             //S_TBS_TAXORGCODE
             str = rs.getString("S_TBS_TAXORGCODE");
             if (str == null)
                dto.setStbstaxorgcode(null);
             else
                dto.setStbstaxorgcode(str.trim());

             //S_FILENAME
             str = rs.getString("S_FILENAME");
             if (str == null)
                dto.setSfilename(null);
             else
                dto.setSfilename(str.trim());

             //S_RECVTRECODE
             str = rs.getString("S_RECVTRECODE");
             if (str == null)
                dto.setSrecvtrecode(null);
             else
                dto.setSrecvtrecode(str.trim());

             //S_RECVTRENAME
             str = rs.getString("S_RECVTRENAME");
             if (str == null)
                dto.setSrecvtrename(null);
             else
                dto.setSrecvtrename(str.trim());

             //S_DEALNO
             str = rs.getString("S_DEALNO");
             if (str == null)
                dto.setSdealno(null);
             else
                dto.setSdealno(str.trim());

             //S_TRASRLNO
             str = rs.getString("S_TRASRLNO");
             if (str == null)
                dto.setStrasrlno(null);
             else
                dto.setStrasrlno(str.trim());

             //N_MONEY
           dto.setNmoney(rs.getBigDecimal("N_MONEY"));

             //S_TAXTICKETNO
             str = rs.getString("S_TAXTICKETNO");
             if (str == null)
                dto.setStaxticketno(null);
             else
                dto.setStaxticketno(str.trim());

             //S_GENTICKETDATE
             str = rs.getString("S_GENTICKETDATE");
             if (str == null)
                dto.setSgenticketdate(null);
             else
                dto.setSgenticketdate(str.trim());

             //S_BUDGETTYPE
             str = rs.getString("S_BUDGETTYPE");
             if (str == null)
                dto.setSbudgettype(null);
             else
                dto.setSbudgettype(str.trim());

             //S_TRIMFLAG
             str = rs.getString("S_TRIMFLAG");
             if (str == null)
                dto.setStrimflag(null);
             else
                dto.setStrimflag(str.trim());

             //S_BUDGETSUBCODE
             str = rs.getString("S_BUDGETSUBCODE");
             if (str == null)
                dto.setSbudgetsubcode(null);
             else
                dto.setSbudgetsubcode(str.trim());

             //S_BUDGETLEVELCODE
             str = rs.getString("S_BUDGETLEVELCODE");
             if (str == null)
                dto.setSbudgetlevelcode(null);
             else
                dto.setSbudgetlevelcode(str.trim());

             //S_ASSITSIGN
             str = rs.getString("S_ASSITSIGN");
             if (str == null)
                dto.setSassitsign(null);
             else
                dto.setSassitsign(str.trim());

             //S_TBS_ASSITSIGN
             str = rs.getString("S_TBS_ASSITSIGN");
             if (str == null)
                dto.setStbsassitsign(null);
             else
                dto.setStbsassitsign(str.trim());

             //S_UNITCODE
             str = rs.getString("S_UNITCODE");
             if (str == null)
                dto.setSunitcode(null);
             else
                dto.setSunitcode(str.trim());

             //S_PAYBOOKKIND
             str = rs.getString("S_PAYBOOKKIND");
             if (str == null)
                dto.setSpaybookkind(null);
             else
                dto.setSpaybookkind(str.trim());

             //S_OPENACCBANKCODE
             str = rs.getString("S_OPENACCBANKCODE");
             if (str == null)
                dto.setSopenaccbankcode(null);
             else
                dto.setSopenaccbankcode(str.trim());

             //S_STATUS
             str = rs.getString("S_STATUS");
             if (str == null)
                dto.setSstatus(null);
             else
                dto.setSstatus(str.trim());

             //S_USERCODE
             str = rs.getString("S_USERCODE");
             if (str == null)
                dto.setSusercode(null);
             else
                dto.setSusercode(str.trim());

             //S_DEMO
             str = rs.getString("S_DEMO");
             if (str == null)
                dto.setSdemo(null);
             else
                dto.setSdemo(str.trim());

             //S_TAXPAYCODE
             str = rs.getString("S_TAXPAYCODE");
             if (str == null)
                dto.setStaxpaycode(null);
             else
                dto.setStaxpaycode(str.trim());

             //S_TAXPAYNAME
             str = rs.getString("S_TAXPAYNAME");
             if (str == null)
                dto.setStaxpayname(null);
             else
                dto.setStaxpayname(str.trim());

             //S_CORPTYPE
             str = rs.getString("S_CORPTYPE");
             if (str == null)
                dto.setScorptype(null);
             else
                dto.setScorptype(str.trim());

             //S_LIMITDATE
             str = rs.getString("S_LIMITDATE");
             if (str == null)
                dto.setSlimitdate(null);
             else
                dto.setSlimitdate(str.trim());

             //S_TAXTYPECODE
             str = rs.getString("S_TAXTYPECODE");
             if (str == null)
                dto.setStaxtypecode(null);
             else
                dto.setStaxtypecode(str.trim());

             //S_TAXTYPENAME
             str = rs.getString("S_TAXTYPENAME");
             if (str == null)
                dto.setStaxtypename(null);
             else
                dto.setStaxtypename(str.trim());

             //S_TAXSUBJECTNAME
             str = rs.getString("S_TAXSUBJECTNAME");
             if (str == null)
                dto.setStaxsubjectname(null);
             else
                dto.setStaxsubjectname(str.trim());

             //S_PAYBNKNO
             str = rs.getString("S_PAYBNKNO");
             if (str == null)
                dto.setSpaybnkno(null);
             else
                dto.setSpaybnkno(str.trim());

             //S_PAYACCT
             str = rs.getString("S_PAYACCT");
             if (str == null)
                dto.setSpayacct(null);
             else
                dto.setSpayacct(str.trim());

             //S_TAXSTARTDATE
             str = rs.getString("S_TAXSTARTDATE");
             if (str == null)
                dto.setStaxstartdate(null);
             else
                dto.setStaxstartdate(str.trim());

             //S_TAXENDDATE
             str = rs.getString("S_TAXENDDATE");
             if (str == null)
                dto.setStaxenddate(null);
             else
                dto.setStaxenddate(str.trim());

             //S_TAXNUMBER
             str = rs.getString("S_TAXNUMBER");
             if (str == null)
                dto.setStaxnumber(null);
             else
                dto.setStaxnumber(str.trim());

             //N_TAXAMT
           dto.setNtaxamt(rs.getBigDecimal("N_TAXAMT"));

             //N_TAXRATE
           dto.setNtaxrate(rs.getBigDecimal("N_TAXRATE"));

             //N_DISCOUNTTAXAMT
           dto.setNdiscounttaxamt(rs.getBigDecimal("N_DISCOUNTTAXAMT"));

             //N_FACTTAXAMT
           dto.setNfacttaxamt(rs.getBigDecimal("N_FACTTAXAMT"));

             //S_SOURCEFLAG
             str = rs.getString("S_SOURCEFLAG");
             if (str == null)
                dto.setSsourceflag(null);
             else
                dto.setSsourceflag(str.trim());



            results.add(dto);
            
            if(maxSize >0  && results.size() == maxSize)
            {
            	break ;
            }
            
         }
        
		return results ;
    }

  /**
   * populate resultSet as cclass
   */
  protected IDto[] getChildrenResults(ResultSet rs ,Class _dto) throws SQLException{

      throw new SQLException("本dto没有与其它dto相关联，无法应用此方法");
  }


     /**
     * Update a record in Database.
     */

    public void update(IDto _dto, Connection conn, boolean isLobSupport) throws SQLException
    {
        String msgValid = _dto.checkValid() ;
        if (msgValid != null)
            throw new SQLException("修改错误，"+msgValid) ;
        TvInfileDto dto = (TvInfileDto)_dto ;
        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            int pos = 1;
            //S_ACCDATE
            ps.setString(pos, dto.getSaccdate());
            pos++;

            //S_PACKAGENO
            ps.setString(pos, dto.getSpackageno());
            pos++;

            //S_TAXORGCODE
            ps.setString(pos, dto.getStaxorgcode());
            pos++;

            //S_TBS_TAXORGCODE
            ps.setString(pos, dto.getStbstaxorgcode());
            pos++;

            //S_FILENAME
            ps.setString(pos, dto.getSfilename());
            pos++;

            //S_RECVTRECODE
            ps.setString(pos, dto.getSrecvtrecode());
            pos++;

            //S_RECVTRENAME
            ps.setString(pos, dto.getSrecvtrename());
            pos++;

            //S_TRASRLNO
            ps.setString(pos, dto.getStrasrlno());
            pos++;

            //N_MONEY
            ps.setBigDecimal(pos, dto.getNmoney());
            pos++;

            //S_TAXTICKETNO
            ps.setString(pos, dto.getStaxticketno());
            pos++;

            //S_GENTICKETDATE
            ps.setString(pos, dto.getSgenticketdate());
            pos++;

            //S_BUDGETTYPE
            ps.setString(pos, dto.getSbudgettype());
            pos++;

            //S_TRIMFLAG
            ps.setString(pos, dto.getStrimflag());
            pos++;

            //S_BUDGETSUBCODE
            ps.setString(pos, dto.getSbudgetsubcode());
            pos++;

            //S_BUDGETLEVELCODE
            ps.setString(pos, dto.getSbudgetlevelcode());
            pos++;

            //S_ASSITSIGN
            ps.setString(pos, dto.getSassitsign());
            pos++;

            //S_TBS_ASSITSIGN
            ps.setString(pos, dto.getStbsassitsign());
            pos++;

            //S_UNITCODE
            ps.setString(pos, dto.getSunitcode());
            pos++;

            //S_PAYBOOKKIND
            ps.setString(pos, dto.getSpaybookkind());
            pos++;

            //S_OPENACCBANKCODE
            ps.setString(pos, dto.getSopenaccbankcode());
            pos++;

            //S_STATUS
            ps.setString(pos, dto.getSstatus());
            pos++;

            //S_USERCODE
            ps.setString(pos, dto.getSusercode());
            pos++;

            //S_DEMO
            ps.setString(pos, dto.getSdemo());
            pos++;

            //S_TAXPAYCODE
            ps.setString(pos, dto.getStaxpaycode());
            pos++;

            //S_TAXPAYNAME
            ps.setString(pos, dto.getStaxpayname());
            pos++;

            //S_CORPTYPE
            ps.setString(pos, dto.getScorptype());
            pos++;

            //S_LIMITDATE
            ps.setString(pos, dto.getSlimitdate());
            pos++;

            //S_TAXTYPECODE
            ps.setString(pos, dto.getStaxtypecode());
            pos++;

            //S_TAXTYPENAME
            ps.setString(pos, dto.getStaxtypename());
            pos++;

            //S_TAXSUBJECTNAME
            ps.setString(pos, dto.getStaxsubjectname());
            pos++;

            //S_PAYBNKNO
            ps.setString(pos, dto.getSpaybnkno());
            pos++;

            //S_PAYACCT
            ps.setString(pos, dto.getSpayacct());
            pos++;

            //S_TAXSTARTDATE
            ps.setString(pos, dto.getStaxstartdate());
            pos++;

            //S_TAXENDDATE
            ps.setString(pos, dto.getStaxenddate());
            pos++;

            //S_TAXNUMBER
            ps.setString(pos, dto.getStaxnumber());
            pos++;

            //N_TAXAMT
            ps.setBigDecimal(pos, dto.getNtaxamt());
            pos++;

            //N_TAXRATE
            ps.setBigDecimal(pos, dto.getNtaxrate());
            pos++;

            //N_DISCOUNTTAXAMT
            ps.setBigDecimal(pos, dto.getNdiscounttaxamt());
            pos++;

            //N_FACTTAXAMT
            ps.setBigDecimal(pos, dto.getNfacttaxamt());
            pos++;

            //S_SOURCEFLAG
            ps.setString(pos, dto.getSsourceflag());
            pos++;


           //S_ORGCODE
           ps.setString(pos, dto.getSorgcode());
           pos++;
           //S_COMMITDATE
           ps.setString(pos, dto.getScommitdate());
           pos++;
           //S_DEALNO
           ps.setString(pos, dto.getSdealno());
           pos++;
           ps.executeUpdate();
        }
        finally
        {
           close(ps);
        }

        return ;
    }
    
    
    
      /**
     * Update batch record in Database.
     */

    public void update(IDto[] _dtos, Connection conn, boolean isLobSupport) throws SQLException
    {
     
     	 TvInfileDto dto  ;
         for (int i= 0; i< _dtos.length ; i++)
         {
            dto  = (TvInfileDto)_dtos[i] ; 
            String msgValid = dto.checkValid() ;
            if (msgValid != null)
                throw new SQLException("修改错误，"+msgValid) ;
         }

        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            
            for (int i= 0; i< _dtos.length ; i++)
            {
                dto  = (TvInfileDto)_dtos[i] ; 
                int pos = 1;
                //S_ACCDATE
                 ps.setString(pos, dto.getSaccdate());
                pos++;

                //S_PACKAGENO
                 ps.setString(pos, dto.getSpackageno());
                pos++;

                //S_TAXORGCODE
                 ps.setString(pos, dto.getStaxorgcode());
                pos++;

                //S_TBS_TAXORGCODE
                 ps.setString(pos, dto.getStbstaxorgcode());
                pos++;

                //S_FILENAME
                 ps.setString(pos, dto.getSfilename());
                pos++;

                //S_RECVTRECODE
                 ps.setString(pos, dto.getSrecvtrecode());
                pos++;

                //S_RECVTRENAME
                 ps.setString(pos, dto.getSrecvtrename());
                pos++;

                //S_TRASRLNO
                 ps.setString(pos, dto.getStrasrlno());
                pos++;

                //N_MONEY
                 ps.setBigDecimal(pos, dto.getNmoney());
                pos++;

                //S_TAXTICKETNO
                 ps.setString(pos, dto.getStaxticketno());
                pos++;

                //S_GENTICKETDATE
                 ps.setString(pos, dto.getSgenticketdate());
                pos++;

                //S_BUDGETTYPE
                 ps.setString(pos, dto.getSbudgettype());
                pos++;

                //S_TRIMFLAG
                 ps.setString(pos, dto.getStrimflag());
                pos++;

                //S_BUDGETSUBCODE
                 ps.setString(pos, dto.getSbudgetsubcode());
                pos++;

                //S_BUDGETLEVELCODE
                 ps.setString(pos, dto.getSbudgetlevelcode());
                pos++;

                //S_ASSITSIGN
                 ps.setString(pos, dto.getSassitsign());
                pos++;

                //S_TBS_ASSITSIGN
                 ps.setString(pos, dto.getStbsassitsign());
                pos++;

                //S_UNITCODE
                 ps.setString(pos, dto.getSunitcode());
                pos++;

                //S_PAYBOOKKIND
                 ps.setString(pos, dto.getSpaybookkind());
                pos++;

                //S_OPENACCBANKCODE
                 ps.setString(pos, dto.getSopenaccbankcode());
                pos++;

                //S_STATUS
                 ps.setString(pos, dto.getSstatus());
                pos++;

                //S_USERCODE
                 ps.setString(pos, dto.getSusercode());
                pos++;

                //S_DEMO
                 ps.setString(pos, dto.getSdemo());
                pos++;

                //S_TAXPAYCODE
                 ps.setString(pos, dto.getStaxpaycode());
                pos++;

                //S_TAXPAYNAME
                 ps.setString(pos, dto.getStaxpayname());
                pos++;

                //S_CORPTYPE
                 ps.setString(pos, dto.getScorptype());
                pos++;

                //S_LIMITDATE
                 ps.setString(pos, dto.getSlimitdate());
                pos++;

                //S_TAXTYPECODE
                 ps.setString(pos, dto.getStaxtypecode());
                pos++;

                //S_TAXTYPENAME
                 ps.setString(pos, dto.getStaxtypename());
                pos++;

                //S_TAXSUBJECTNAME
                 ps.setString(pos, dto.getStaxsubjectname());
                pos++;

                //S_PAYBNKNO
                 ps.setString(pos, dto.getSpaybnkno());
                pos++;

                //S_PAYACCT
                 ps.setString(pos, dto.getSpayacct());
                pos++;

                //S_TAXSTARTDATE
                 ps.setString(pos, dto.getStaxstartdate());
                pos++;

                //S_TAXENDDATE
                 ps.setString(pos, dto.getStaxenddate());
                pos++;

                //S_TAXNUMBER
                 ps.setString(pos, dto.getStaxnumber());
                pos++;

                //N_TAXAMT
                 ps.setBigDecimal(pos, dto.getNtaxamt());
                pos++;

                //N_TAXRATE
                 ps.setBigDecimal(pos, dto.getNtaxrate());
                pos++;

                //N_DISCOUNTTAXAMT
                 ps.setBigDecimal(pos, dto.getNdiscounttaxamt());
                pos++;

                //N_FACTTAXAMT
                 ps.setBigDecimal(pos, dto.getNfacttaxamt());
                pos++;

                //S_SOURCEFLAG
                 ps.setString(pos, dto.getSsourceflag());
                pos++;


               //S_ORGCODE
               ps.setString(pos, dto.getSorgcode());
               pos++;
               //S_COMMITDATE
               ps.setString(pos, dto.getScommitdate());
               pos++;
               //S_DEALNO
               ps.setString(pos, dto.getSdealno());
               pos++;
		       ps.addBatch(); 
	       }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }

        return ;
    }



    /**
     * delete a record in Database.
     */
    public void delete(IPK _pk, Connection conn) throws SQLException
    {
       
      
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return ;
        //	throw new SQLException("删除错误，"+msgValid) ;
       }
       TvInfilePK pk = (TvInfilePK)_pk ;


       PreparedStatement ps = null;
       try {
           ps = conn.prepareStatement(SQL_DELETE);
           ps.setString(1, pk.getSorgcode());
           ps.setString(2, pk.getScommitdate());
           ps.setString(3, pk.getSdealno());
           ps.executeUpdate();
        }
        finally
        {
            close(ps);
        }
    }


     /**
     * delete batch record in Database.
     */
    public void delete(IPK[] _pks, Connection conn) throws SQLException
    {
        TvInfilePK pk ;
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TvInfilePK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 		
 		
        PreparedStatement ps = null;
        try
         {
            ps = conn.prepareStatement(SQL_DELETE);
             
       		for (int i= 0; i< pks.size() ; i++)
       		{
       			pk  = (TvInfilePK)(pks.get(i)) ; 
                ps.setString(1, pk.getSorgcode());
                ps.setString(2, pk.getScommitdate());
                ps.setString(3, pk.getSdealno());
		        ps.addBatch() ;
			}
            ps.executeBatch();
        }
        finally
        {
            close(ps);
        }      
    }



    /**
    * Check IChangerNumber or UpdateTimeStamp.
    */
    public void check(IDto _dto, Connection conn) throws SQLException
    {
                                                                                                                        		throw new SQLException("数据库表：TV_INFILE没有检查修改的字段");
    }


	/**
	* synchronize values between parent and children
	*/
    public void syncToChildren(IDto _dto)     throws SQLException{
       throw new SQLException("本记录没有与其它记录相关联，无法进行主记录与明细记录之间的数据同步。");
  }

	/**
	*get children
	*/
    public IDto[] findChildren (IDto _dto, Connection conn, boolean isLobSupport)     throws SQLException{
         throw new SQLException("本dto没有与其它dto相关联，无法得到子dto");
  }    
    
  /**
   *return the children IDto class
   * @return Class
   */
  public Class getChildrenClass(){
	  return null;
  };    



    /**
     * Close JDBC Statement.
     * @param stmt  Statement to be closed.
     */
    private void close(Statement stmt)
    {
        if (stmt != null)
        {
            try
            {
                stmt.close();
            }
            catch(SQLException    e)
            {}
        }
    }

    /**
     * Close JDBC ResultSet.
     * @param rs  ResultSet to be closed.
     */
    private void close(ResultSet rs)
    {
        if (rs != null)
        {
            try
            {
                rs.close();
            }catch(SQLException  e){}
        }
    }
}
