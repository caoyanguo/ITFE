    



package com.cfcc.itfe.persistence.dao;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.Statement;
import java.sql.SQLException;
import java.sql.Date;
import java.sql.Time;
import java.sql.Timestamp ;
import java.sql.Clob;

import com.cfcc.jaf.persistence.jaform.parent.* ;

import java.math.BigDecimal;

import java.util.List;
import java.util.ArrayList;
import java.util.HashMap;

import java.io.StringReader;
import java.io.Reader;
import java.io.IOException;

import com.cfcc.itfe.persistence.dto.TsPaybankDto ;
import com.cfcc.itfe.persistence.pk.TsPaybankPK ;


/**
 * <p>Title: DAO of table: TS_PAYBANK</p>
 * <p>Description: Data Access Object  </p>
 * <p>Copyright: Copyright (c) 2005</p>
 * <p>Company: CFCC </p>
 * <p>Generation Time: 2014-12-25 15:29:00 </p>
 *  Do NOT change this file by hand.
 *  This java source file is generated by JAF_ORM .
 *  dao.vm timestamp: 2007-04-06 08:30:00
 ******************************************************
 * <p> <b>change record</b><br>
 *  make dao methods simply.<br>
 *  especially make update,delete methods simply.<br>
 *  remove "..withCheck" method,withCheck will impl in JAFOrmTemplate.<br>
 *  remove "delete(IDto _dto,..)" method.<br></p>
 * @author win7
 */

public class TsPaybankDao  implements IDao
{


    /* SQL to insert data */
    private static final String SQL_INSERT =
        "INSERT INTO TS_PAYBANK ("
          + "S_ORGCODE,S_BANKNO,S_BANKNAME,S_PAYBANKNO,S_PAYBANKNAME"
          + ",I_MODICOUNT,S_STATE,D_AFFDATE,C_BNKCLASS,S_OFCITY"
          + ",C_BNKCLSNO,C_BNKACCTSTA,C_BNKPOSTCODE,S_BNKADDR,C_OPTTYPE"
          + ",S_BNKTEL,S_ADDCOLUMN1,S_ADDCOLUMN2"
        + ") VALUES ("
        + "?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)";


    private static final String SQL_INSERT_WITH_RESULT = "SELECT * FROM FINAL TABLE( " + SQL_INSERT + " )";
    
    /* SQL to select data */
    private static final String SQL_SELECT =
        "SELECT "
        + "TS_PAYBANK.S_ORGCODE, TS_PAYBANK.S_BANKNO, TS_PAYBANK.S_BANKNAME, TS_PAYBANK.S_PAYBANKNO, TS_PAYBANK.S_PAYBANKNAME, "
        + "TS_PAYBANK.I_MODICOUNT, TS_PAYBANK.S_STATE, TS_PAYBANK.D_AFFDATE, TS_PAYBANK.C_BNKCLASS, TS_PAYBANK.S_OFCITY, "
        + "TS_PAYBANK.C_BNKCLSNO, TS_PAYBANK.C_BNKACCTSTA, TS_PAYBANK.C_BNKPOSTCODE, TS_PAYBANK.S_BNKADDR, TS_PAYBANK.C_OPTTYPE, "
        + "TS_PAYBANK.S_BNKTEL, TS_PAYBANK.S_ADDCOLUMN1, TS_PAYBANK.S_ADDCOLUMN2 "
        + "FROM TS_PAYBANK "
        +" WHERE " 
        + "S_ORGCODE = ? AND S_BANKNO = ?"
        ;
    /* SQL to select for update data */
    private static final String SQL_SELECT_FOR_UPDATE =
        "SELECT "
        + "TS_PAYBANK.S_ORGCODE, TS_PAYBANK.S_BANKNO, TS_PAYBANK.S_BANKNAME, TS_PAYBANK.S_PAYBANKNO, TS_PAYBANK.S_PAYBANKNAME, "
        + "TS_PAYBANK.I_MODICOUNT, TS_PAYBANK.S_STATE, TS_PAYBANK.D_AFFDATE, TS_PAYBANK.C_BNKCLASS, TS_PAYBANK.S_OFCITY, "
        + "TS_PAYBANK.C_BNKCLSNO, TS_PAYBANK.C_BNKACCTSTA, TS_PAYBANK.C_BNKPOSTCODE, TS_PAYBANK.S_BNKADDR, TS_PAYBANK.C_OPTTYPE, "
        + "TS_PAYBANK.S_BNKTEL, TS_PAYBANK.S_ADDCOLUMN1, TS_PAYBANK.S_ADDCOLUMN2 "
        + "FROM TS_PAYBANK "
        +" WHERE " 
        + "S_ORGCODE = ? AND S_BANKNO = ? FOR UPDATE"
        ;

      /* SQL to select data (SCROLLABLE)*/
     private static final String SQL_SELECT_BATCH_SCROLLABLE =
        "SELECT "
        + "  TS_PAYBANK.S_ORGCODE  , TS_PAYBANK.S_BANKNO  , TS_PAYBANK.S_BANKNAME  , TS_PAYBANK.S_PAYBANKNO  , TS_PAYBANK.S_PAYBANKNAME "
        + " , TS_PAYBANK.I_MODICOUNT  , TS_PAYBANK.S_STATE  , TS_PAYBANK.D_AFFDATE  , TS_PAYBANK.C_BNKCLASS  , TS_PAYBANK.S_OFCITY "
        + " , TS_PAYBANK.C_BNKCLSNO  , TS_PAYBANK.C_BNKACCTSTA  , TS_PAYBANK.C_BNKPOSTCODE  , TS_PAYBANK.S_BNKADDR  , TS_PAYBANK.C_OPTTYPE "
        + " , TS_PAYBANK.S_BNKTEL  , TS_PAYBANK.S_ADDCOLUMN1  , TS_PAYBANK.S_ADDCOLUMN2 "
        + "FROM TS_PAYBANK ";



    /* SQL to select batch data */
    private static final String SQL_SELECT_BATCH =
        "SELECT "
        + "TS_PAYBANK.S_ORGCODE, TS_PAYBANK.S_BANKNO, TS_PAYBANK.S_BANKNAME, TS_PAYBANK.S_PAYBANKNO, TS_PAYBANK.S_PAYBANKNAME, "
        + "TS_PAYBANK.I_MODICOUNT, TS_PAYBANK.S_STATE, TS_PAYBANK.D_AFFDATE, TS_PAYBANK.C_BNKCLASS, TS_PAYBANK.S_OFCITY, "
        + "TS_PAYBANK.C_BNKCLSNO, TS_PAYBANK.C_BNKACCTSTA, TS_PAYBANK.C_BNKPOSTCODE, TS_PAYBANK.S_BNKADDR, TS_PAYBANK.C_OPTTYPE, "
        + "TS_PAYBANK.S_BNKTEL, TS_PAYBANK.S_ADDCOLUMN1, TS_PAYBANK.S_ADDCOLUMN2 "
        + "FROM TS_PAYBANK " ;
        

   /* SQL to select batch data where condition  */
    private static final String SQL_SELECT_BATCH_WHERE =" ( "
        + "S_ORGCODE = ? AND S_BANKNO = ?)"
        ;


    /* SQL to update data */
    private static final String SQL_UPDATE =
        "UPDATE TS_PAYBANK SET "
        + "S_BANKNAME =?,S_PAYBANKNO =?,S_PAYBANKNAME =?,I_MODICOUNT =?,S_STATE =?, "
        + "D_AFFDATE =?,C_BNKCLASS =?,S_OFCITY =?,C_BNKCLSNO =?,C_BNKACCTSTA =?, "
        + "C_BNKPOSTCODE =?,S_BNKADDR =?,C_OPTTYPE =?,S_BNKTEL =?,S_ADDCOLUMN1 =?, "
        + "S_ADDCOLUMN2 =? "
        + "WHERE "
        + "S_ORGCODE = ? AND S_BANKNO = ?"
        ;

	/* SQL to update data, support LOB */
    private static final String SQL_UPDATE_LOB =
        "UPDATE TS_PAYBANK SET "
        + "S_BANKNAME =?, S_PAYBANKNO =?, S_PAYBANKNAME =?, I_MODICOUNT =?, S_STATE =?,  "
        + "D_AFFDATE =?, C_BNKCLASS =?, S_OFCITY =?, C_BNKCLSNO =?, C_BNKACCTSTA =?,  "
        + "C_BNKPOSTCODE =?, S_BNKADDR =?, C_OPTTYPE =?, S_BNKTEL =?, S_ADDCOLUMN1 =?,  "
        + "S_ADDCOLUMN2 =? "
        + "WHERE "
        + "S_ORGCODE = ? AND S_BANKNO = ?"
        ;	
	
    /* SQL to delete data */
    private static final String SQL_DELETE =
        "DELETE FROM TS_PAYBANK " 
        + " WHERE "
        + "S_ORGCODE = ? AND S_BANKNO = ?"
        ;


	/**
	*  批量查询 一次最多查询的参数数量
	*/
	
	public static final int FIND_BATCH_SIZE = 150  / 2;
	



   /**
   * Create a new record in Database.
   */
  public void create(IDto _dto,  Connection conn) throws SQLException
  {
     TsPaybankDto dto = (TsPaybankDto)_dto ;
     String msgValid = dto.checkValid() ;
     if (msgValid != null)
     	throw new SQLException("插入错误，"+msgValid) ;

     PreparedStatement ps = null;
     try
     {
         ps = conn.prepareStatement(SQL_INSERT);
          ps.setString(1, dto.getSorgcode());

          ps.setString(2, dto.getSbankno());

          ps.setString(3, dto.getSbankname());

          ps.setString(4, dto.getSpaybankno());

          ps.setString(5, dto.getSpaybankname());

          if (dto.getImodicount()==null)
            ps.setNull(6, java.sql.Types.INTEGER);
         else
            ps.setInt(6, dto.getImodicount().intValue());
          ps.setString(7, dto.getSstate());

          ps.setDate(8, dto.getDaffdate());

          ps.setString(9, dto.getCbnkclass());

          ps.setString(10, dto.getSofcity());

          ps.setString(11, dto.getCbnkclsno());

          ps.setString(12, dto.getCbnkacctsta());

          ps.setString(13, dto.getCbnkpostcode());

          ps.setString(14, dto.getSbnkaddr());

          ps.setString(15, dto.getCopttype());

          ps.setString(16, dto.getSbnktel());

          ps.setString(17, dto.getSaddcolumn1());

          ps.setString(18, dto.getSaddcolumn2());

         ps.executeUpdate();
     }
     finally
     {
         close(ps);
     }
     return  ;
  }

   /**
   * Create a new record in Database. and return result, only used when having generated key
   */
   public IDto createWithResult(IDto _dto,  Connection conn) throws SQLException
   {
       TsPaybankDto dto = (TsPaybankDto)_dto ;
       String msgValid = dto.checkValid() ;
       if (msgValid != null)
           throw new SQLException("插入错误，"+msgValid) ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try
       {
           ps = conn.prepareStatement(SQL_INSERT_WITH_RESULT);
            ps.setString(1, dto.getSorgcode());
            ps.setString(2, dto.getSbankno());
            ps.setString(3, dto.getSbankname());
            ps.setString(4, dto.getSpaybankno());
            ps.setString(5, dto.getSpaybankname());
            if (dto.getImodicount()==null)
              ps.setNull(6, java.sql.Types.INTEGER);
           else
              ps.setInt(6, dto.getImodicount().intValue());
            ps.setString(7, dto.getSstate());
            ps.setDate(8, dto.getDaffdate());
            ps.setString(9, dto.getCbnkclass());
            ps.setString(10, dto.getSofcity());
            ps.setString(11, dto.getCbnkclsno());
            ps.setString(12, dto.getCbnkacctsta());
            ps.setString(13, dto.getCbnkpostcode());
            ps.setString(14, dto.getSbnkaddr());
            ps.setString(15, dto.getCopttype());
            ps.setString(16, dto.getSbnktel());
            ps.setString(17, dto.getSaddcolumn1());
            ps.setString(18, dto.getSaddcolumn2());
           rs = ps.executeQuery();
           List results = getResults(rs);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }
        finally
        {
           close(ps);
           close(rs);
        }
   }

 	/**
	* Create batch new record in Database. it only create the first layer dto
	*/
    public void create(IDto[] _dtos, Connection conn)    throws SQLException
    {
        TsPaybankDto dto  ;
     	for (int i= 0; i< _dtos.length ; i++)
 	    {
 		    dto  = (TsPaybankDto)_dtos[i] ; 
    	    String msgValid = dto.checkValid() ;
        	if (msgValid != null)
     	    	throw new SQLException("插入错误，"+msgValid) ;
     	}
 	    PreparedStatement ps = null;
        try
        {
            ps = conn.prepareStatement(SQL_INSERT);
       
     	    for (int i= 0; i< _dtos.length ; i++)
     	    {
 	    	    dto  = (TsPaybankDto)_dtos[i] ; 
  
               ps.setString(1, dto.getSorgcode());
  
               ps.setString(2, dto.getSbankno());
  
               ps.setString(3, dto.getSbankname());
  
               ps.setString(4, dto.getSpaybankno());
  
               ps.setString(5, dto.getSpaybankname());
  
               if (dto.getImodicount()==null)
                  ps.setNull(6, java.sql.Types.INTEGER);
               else
                  ps.setInt(6, dto.getImodicount().intValue());
  
               ps.setString(7, dto.getSstate());
  
               ps.setDate(8, dto.getDaffdate());
  
               ps.setString(9, dto.getCbnkclass());
  
               ps.setString(10, dto.getSofcity());
  
               ps.setString(11, dto.getCbnkclsno());
  
               ps.setString(12, dto.getCbnkacctsta());
  
               ps.setString(13, dto.getCbnkpostcode());
  
               ps.setString(14, dto.getSbnkaddr());
  
               ps.setString(15, dto.getCopttype());
  
               ps.setString(16, dto.getSbnktel());
  
               ps.setString(17, dto.getSaddcolumn1());
  
               ps.setString(18, dto.getSaddcolumn2());
               ps.addBatch(); 
           }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }
        return  ;
   }




     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto find(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TsPaybankPK pk = (TsPaybankPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT);
           ps.setString(1, pk.getSorgcode());

           ps.setString(2, pk.getSbankno());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }

     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto findForUpdate(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TsPaybankPK pk = (TsPaybankPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT_FOR_UPDATE);
           ps.setString(1, pk.getSorgcode());

           ps.setString(2, pk.getSbankno());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }
  
     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto[] find(IPK[] _pks, Connection conn, boolean isLobSupport) throws SQLException
    {
        TsPaybankPK pk ;
        
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TsPaybankPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 
        PreparedStatement ps = null;
        ResultSet rs = null;
        
         
        List results = new ArrayList();  
		for (int iBegin = 0; iBegin < pks.size(); iBegin += FIND_BATCH_SIZE)   //
		{
        
            try {
          		int iFindBatchSize = pks.size() - iBegin ;
	    		if (iFindBatchSize >FIND_BATCH_SIZE )
		    		iFindBatchSize = FIND_BATCH_SIZE ;
		  
        	    StringBuffer sb = new StringBuffer() ;
            	sb.append(SQL_SELECT_BATCH).append(" WHERE ").append(SQL_SELECT_BATCH_WHERE);

	    		for (int i = iBegin+1; i < iBegin + iFindBatchSize; i++)
		    	{
			    	sb.append("OR").append(SQL_SELECT_BATCH_WHERE) ;
    			}
        	
                ps = conn.prepareStatement(sb.toString());

                for (int i = iBegin;i < iBegin + iFindBatchSize; i++)
                {
                    pk  = (TsPaybankPK)(pks.get(i)) ; 
                   ps.setString((i-iBegin)*2+1, pk.getSorgcode());

                   ps.setString((i-iBegin)*2+2, pk.getSbankno());

			
	            }
                rs = ps.executeQuery();
                results.addAll(getResults(rs,0,isLobSupport)); //
            }finally {
                close(rs);
                close(ps);
            }
        }
        if (results!=null && results.size() > 0)
        {
            TsPaybankDto[] dtos = new TsPaybankDto[0];
		    dtos = (TsPaybankDto[]) results.toArray(dtos) ;
		    return dtos ;
        }
        else
        {
            return null;
        }
    }

  
  
  



    /**
	    * 得到查询语句
	    */
    public String getSelectSQL() 
    {
	        return SQL_SELECT_BATCH_SCROLLABLE ;
    }
   

	public List getResults(ResultSet rs) throws SQLException
    {
    	return getResults(rs, 0) ;
    }
  

    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize) throws SQLException
    {
		return getResults(rs,maxSize,false);
	}
    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize, boolean isLobSupport) throws SQLException
    {
        List results = new ArrayList();
        String str ;
        Reader reader;
        Clob clob;
        char[] chars ;
        
        while (rs.next())
         {
             TsPaybankDto  dto = new  TsPaybankDto ();
             //S_ORGCODE
             str = rs.getString("S_ORGCODE");
             if (str == null)
                dto.setSorgcode(null);
             else
                dto.setSorgcode(str.trim());

             //S_BANKNO
             str = rs.getString("S_BANKNO");
             if (str == null)
                dto.setSbankno(null);
             else
                dto.setSbankno(str.trim());

             //S_BANKNAME
             str = rs.getString("S_BANKNAME");
             if (str == null)
                dto.setSbankname(null);
             else
                dto.setSbankname(str.trim());

             //S_PAYBANKNO
             str = rs.getString("S_PAYBANKNO");
             if (str == null)
                dto.setSpaybankno(null);
             else
                dto.setSpaybankno(str.trim());

             //S_PAYBANKNAME
             str = rs.getString("S_PAYBANKNAME");
             if (str == null)
                dto.setSpaybankname(null);
             else
                dto.setSpaybankname(str.trim());

             //I_MODICOUNT
             str = rs.getString("I_MODICOUNT");
             if(str!=null){
                dto.setImodicount(new Integer(str));
             }

             //S_STATE
             str = rs.getString("S_STATE");
             if (str == null)
                dto.setSstate(null);
             else
                dto.setSstate(str.trim());

             //D_AFFDATE
           dto.setDaffdate(rs.getDate("D_AFFDATE"));

             //C_BNKCLASS
             str = rs.getString("C_BNKCLASS");
             if (str == null)
                dto.setCbnkclass(null);
             else
                dto.setCbnkclass(str.trim());

             //S_OFCITY
             str = rs.getString("S_OFCITY");
             if (str == null)
                dto.setSofcity(null);
             else
                dto.setSofcity(str.trim());

             //C_BNKCLSNO
             str = rs.getString("C_BNKCLSNO");
             if (str == null)
                dto.setCbnkclsno(null);
             else
                dto.setCbnkclsno(str.trim());

             //C_BNKACCTSTA
             str = rs.getString("C_BNKACCTSTA");
             if (str == null)
                dto.setCbnkacctsta(null);
             else
                dto.setCbnkacctsta(str.trim());

             //C_BNKPOSTCODE
             str = rs.getString("C_BNKPOSTCODE");
             if (str == null)
                dto.setCbnkpostcode(null);
             else
                dto.setCbnkpostcode(str.trim());

             //S_BNKADDR
             str = rs.getString("S_BNKADDR");
             if (str == null)
                dto.setSbnkaddr(null);
             else
                dto.setSbnkaddr(str.trim());

             //C_OPTTYPE
             str = rs.getString("C_OPTTYPE");
             if (str == null)
                dto.setCopttype(null);
             else
                dto.setCopttype(str.trim());

             //S_BNKTEL
             str = rs.getString("S_BNKTEL");
             if (str == null)
                dto.setSbnktel(null);
             else
                dto.setSbnktel(str.trim());

             //S_ADDCOLUMN1
             str = rs.getString("S_ADDCOLUMN1");
             if (str == null)
                dto.setSaddcolumn1(null);
             else
                dto.setSaddcolumn1(str.trim());

             //S_ADDCOLUMN2
             str = rs.getString("S_ADDCOLUMN2");
             if (str == null)
                dto.setSaddcolumn2(null);
             else
                dto.setSaddcolumn2(str.trim());



            results.add(dto);
            
            if(maxSize >0  && results.size() == maxSize)
            {
            	break ;
            }
            
         }
        
		return results ;
    }

  /**
   * populate resultSet as cclass
   */
  protected IDto[] getChildrenResults(ResultSet rs ,Class _dto) throws SQLException{

      throw new SQLException("本dto没有与其它dto相关联，无法应用此方法");
  }


     /**
     * Update a record in Database.
     */

    public void update(IDto _dto, Connection conn, boolean isLobSupport) throws SQLException
    {
        String msgValid = _dto.checkValid() ;
        if (msgValid != null)
            throw new SQLException("修改错误，"+msgValid) ;
        TsPaybankDto dto = (TsPaybankDto)_dto ;
        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            int pos = 1;
            //S_BANKNAME
            ps.setString(pos, dto.getSbankname());
            pos++;

            //S_PAYBANKNO
            ps.setString(pos, dto.getSpaybankno());
            pos++;

            //S_PAYBANKNAME
            ps.setString(pos, dto.getSpaybankname());
            pos++;

            //I_MODICOUNT
            if (dto.getImodicount()==null)
                ps.setNull(pos, java.sql.Types.INTEGER);
            else
                ps.setInt(pos, dto.getImodicount().intValue());
            pos++;

            //S_STATE
            ps.setString(pos, dto.getSstate());
            pos++;

            //D_AFFDATE
            ps.setDate(pos, dto.getDaffdate());
            pos++;

            //C_BNKCLASS
            ps.setString(pos, dto.getCbnkclass());
            pos++;

            //S_OFCITY
            ps.setString(pos, dto.getSofcity());
            pos++;

            //C_BNKCLSNO
            ps.setString(pos, dto.getCbnkclsno());
            pos++;

            //C_BNKACCTSTA
            ps.setString(pos, dto.getCbnkacctsta());
            pos++;

            //C_BNKPOSTCODE
            ps.setString(pos, dto.getCbnkpostcode());
            pos++;

            //S_BNKADDR
            ps.setString(pos, dto.getSbnkaddr());
            pos++;

            //C_OPTTYPE
            ps.setString(pos, dto.getCopttype());
            pos++;

            //S_BNKTEL
            ps.setString(pos, dto.getSbnktel());
            pos++;

            //S_ADDCOLUMN1
            ps.setString(pos, dto.getSaddcolumn1());
            pos++;

            //S_ADDCOLUMN2
            ps.setString(pos, dto.getSaddcolumn2());
            pos++;


           //S_ORGCODE
           ps.setString(pos, dto.getSorgcode());
           pos++;
           //S_BANKNO
           ps.setString(pos, dto.getSbankno());
           pos++;
           ps.executeUpdate();
        }
        finally
        {
           close(ps);
        }

        return ;
    }
    
    
    
      /**
     * Update batch record in Database.
     */

    public void update(IDto[] _dtos, Connection conn, boolean isLobSupport) throws SQLException
    {
     
     	 TsPaybankDto dto  ;
         for (int i= 0; i< _dtos.length ; i++)
         {
            dto  = (TsPaybankDto)_dtos[i] ; 
            String msgValid = dto.checkValid() ;
            if (msgValid != null)
                throw new SQLException("修改错误，"+msgValid) ;
         }

        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            
            for (int i= 0; i< _dtos.length ; i++)
            {
                dto  = (TsPaybankDto)_dtos[i] ; 
                int pos = 1;
                //S_BANKNAME
                 ps.setString(pos, dto.getSbankname());
                pos++;

                //S_PAYBANKNO
                 ps.setString(pos, dto.getSpaybankno());
                pos++;

                //S_PAYBANKNAME
                 ps.setString(pos, dto.getSpaybankname());
                pos++;

                //I_MODICOUNT
                 if (dto.getImodicount()==null)
                   ps.setNull(pos, java.sql.Types.INTEGER);
                else
                   ps.setInt(pos, dto.getImodicount().intValue());
                pos++ ;

                //S_STATE
                 ps.setString(pos, dto.getSstate());
                pos++;

                //D_AFFDATE
                 ps.setDate(pos, dto.getDaffdate());
                pos++;

                //C_BNKCLASS
                 ps.setString(pos, dto.getCbnkclass());
                pos++;

                //S_OFCITY
                 ps.setString(pos, dto.getSofcity());
                pos++;

                //C_BNKCLSNO
                 ps.setString(pos, dto.getCbnkclsno());
                pos++;

                //C_BNKACCTSTA
                 ps.setString(pos, dto.getCbnkacctsta());
                pos++;

                //C_BNKPOSTCODE
                 ps.setString(pos, dto.getCbnkpostcode());
                pos++;

                //S_BNKADDR
                 ps.setString(pos, dto.getSbnkaddr());
                pos++;

                //C_OPTTYPE
                 ps.setString(pos, dto.getCopttype());
                pos++;

                //S_BNKTEL
                 ps.setString(pos, dto.getSbnktel());
                pos++;

                //S_ADDCOLUMN1
                 ps.setString(pos, dto.getSaddcolumn1());
                pos++;

                //S_ADDCOLUMN2
                 ps.setString(pos, dto.getSaddcolumn2());
                pos++;


               //S_ORGCODE
               ps.setString(pos, dto.getSorgcode());
               pos++;
               //S_BANKNO
               ps.setString(pos, dto.getSbankno());
               pos++;
		       ps.addBatch(); 
	       }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }

        return ;
    }



    /**
     * delete a record in Database.
     */
    public void delete(IPK _pk, Connection conn) throws SQLException
    {
       
      
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return ;
        //	throw new SQLException("删除错误，"+msgValid) ;
       }
       TsPaybankPK pk = (TsPaybankPK)_pk ;


       PreparedStatement ps = null;
       try {
           ps = conn.prepareStatement(SQL_DELETE);
           ps.setString(1, pk.getSorgcode());
           ps.setString(2, pk.getSbankno());
           ps.executeUpdate();
        }
        finally
        {
            close(ps);
        }
    }


     /**
     * delete batch record in Database.
     */
    public void delete(IPK[] _pks, Connection conn) throws SQLException
    {
        TsPaybankPK pk ;
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TsPaybankPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 		
 		
        PreparedStatement ps = null;
        try
         {
            ps = conn.prepareStatement(SQL_DELETE);
             
       		for (int i= 0; i< pks.size() ; i++)
       		{
       			pk  = (TsPaybankPK)(pks.get(i)) ; 
                ps.setString(1, pk.getSorgcode());
                ps.setString(2, pk.getSbankno());
		        ps.addBatch() ;
			}
            ps.executeBatch();
        }
        finally
        {
            close(ps);
        }      
    }



    /**
    * Check IChangerNumber or UpdateTimeStamp.
    */
    public void check(IDto _dto, Connection conn) throws SQLException
    {
                                                		throw new SQLException("数据库表：TS_PAYBANK没有检查修改的字段");
    }


	/**
	* synchronize values between parent and children
	*/
    public void syncToChildren(IDto _dto)     throws SQLException{
       throw new SQLException("本记录没有与其它记录相关联，无法进行主记录与明细记录之间的数据同步。");
  }

	/**
	*get children
	*/
    public IDto[] findChildren (IDto _dto, Connection conn, boolean isLobSupport)     throws SQLException{
         throw new SQLException("本dto没有与其它dto相关联，无法得到子dto");
  }    
    
  /**
   *return the children IDto class
   * @return Class
   */
  public Class getChildrenClass(){
	  return null;
  };    



    /**
     * Close JDBC Statement.
     * @param stmt  Statement to be closed.
     */
    private void close(Statement stmt)
    {
        if (stmt != null)
        {
            try
            {
                stmt.close();
            }
            catch(SQLException    e)
            {}
        }
    }

    /**
     * Close JDBC ResultSet.
     * @param rs  ResultSet to be closed.
     */
    private void close(ResultSet rs)
    {
        if (rs != null)
        {
            try
            {
                rs.close();
            }catch(SQLException  e){}
        }
    }
}
