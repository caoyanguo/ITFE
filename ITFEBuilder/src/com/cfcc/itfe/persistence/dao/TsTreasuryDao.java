    



package com.cfcc.itfe.persistence.dao;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.Statement;
import java.sql.SQLException;
import java.sql.Date;
import java.sql.Time;
import java.sql.Timestamp ;
import java.sql.Clob;

import com.cfcc.jaf.persistence.jaform.parent.* ;

import java.math.BigDecimal;

import java.util.List;
import java.util.ArrayList;
import java.util.HashMap;

import java.io.StringReader;
import java.io.Reader;
import java.io.IOException;

import com.cfcc.itfe.persistence.dto.TsTreasuryDto ;
import com.cfcc.itfe.persistence.pk.TsTreasuryPK ;


/**
 * <p>Title: DAO of table: TS_TREASURY</p>
 * <p>Description: Data Access Object  </p>
 * <p>Copyright: Copyright (c) 2005</p>
 * <p>Company: CFCC </p>
 * <p>Generation Time: 2014-12-25 15:29:01 </p>
 *  Do NOT change this file by hand.
 *  This java source file is generated by JAF_ORM .
 *  dao.vm timestamp: 2007-04-06 08:30:00
 ******************************************************
 * <p> <b>change record</b><br>
 *  make dao methods simply.<br>
 *  especially make update,delete methods simply.<br>
 *  remove "..withCheck" method,withCheck will impl in JAFOrmTemplate.<br>
 *  remove "delete(IDto _dto,..)" method.<br></p>
 * @author win7
 */

public class TsTreasuryDao  implements IDao
{


    /* SQL to insert data */
    private static final String SQL_INSERT =
        "INSERT INTO TS_TREASURY ("
          + "S_ORGCODE,S_TRECODE,S_TRENAME,S_TRECODENATION,S_TRECODEAREA"
          + ",S_TRIMFLAG,S_PAYUNITNAME,S_FINANCENAME,S_TRELEVEL,S_TREATTRIB"
          + ",S_OFPROVTRECODE,S_OFCITYTRECODE,S_OFCOUNTRYTRECODE,S_GOVERNTRECODE,S_GOVERNTRELEVEL"
          + ",S_ISUNIONTRE,C_STARTZEROACCT,I_MODICOUNT,S_STAMPID,S_CERTID"
          + ",S_ROTARYID,S_ROTARYCERTID"
        + ") VALUES ("
        + "?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)";


    private static final String SQL_INSERT_WITH_RESULT = "SELECT * FROM FINAL TABLE( " + SQL_INSERT + " )";
    
    /* SQL to select data */
    private static final String SQL_SELECT =
        "SELECT "
        + "TS_TREASURY.S_ORGCODE, TS_TREASURY.S_TRECODE, TS_TREASURY.S_TRENAME, TS_TREASURY.S_TRECODENATION, TS_TREASURY.S_TRECODEAREA, "
        + "TS_TREASURY.S_TRIMFLAG, TS_TREASURY.S_PAYUNITNAME, TS_TREASURY.S_FINANCENAME, TS_TREASURY.S_TRELEVEL, TS_TREASURY.S_TREATTRIB, "
        + "TS_TREASURY.S_OFPROVTRECODE, TS_TREASURY.S_OFCITYTRECODE, TS_TREASURY.S_OFCOUNTRYTRECODE, TS_TREASURY.S_GOVERNTRECODE, TS_TREASURY.S_GOVERNTRELEVEL, "
        + "TS_TREASURY.S_ISUNIONTRE, TS_TREASURY.C_STARTZEROACCT, TS_TREASURY.I_MODICOUNT, TS_TREASURY.S_STAMPID, TS_TREASURY.S_CERTID, "
        + "TS_TREASURY.S_ROTARYID, TS_TREASURY.S_ROTARYCERTID "
        + "FROM TS_TREASURY "
        +" WHERE " 
        + "S_ORGCODE = ? AND S_TRECODE = ?"
        ;
    /* SQL to select for update data */
    private static final String SQL_SELECT_FOR_UPDATE =
        "SELECT "
        + "TS_TREASURY.S_ORGCODE, TS_TREASURY.S_TRECODE, TS_TREASURY.S_TRENAME, TS_TREASURY.S_TRECODENATION, TS_TREASURY.S_TRECODEAREA, "
        + "TS_TREASURY.S_TRIMFLAG, TS_TREASURY.S_PAYUNITNAME, TS_TREASURY.S_FINANCENAME, TS_TREASURY.S_TRELEVEL, TS_TREASURY.S_TREATTRIB, "
        + "TS_TREASURY.S_OFPROVTRECODE, TS_TREASURY.S_OFCITYTRECODE, TS_TREASURY.S_OFCOUNTRYTRECODE, TS_TREASURY.S_GOVERNTRECODE, TS_TREASURY.S_GOVERNTRELEVEL, "
        + "TS_TREASURY.S_ISUNIONTRE, TS_TREASURY.C_STARTZEROACCT, TS_TREASURY.I_MODICOUNT, TS_TREASURY.S_STAMPID, TS_TREASURY.S_CERTID, "
        + "TS_TREASURY.S_ROTARYID, TS_TREASURY.S_ROTARYCERTID "
        + "FROM TS_TREASURY "
        +" WHERE " 
        + "S_ORGCODE = ? AND S_TRECODE = ? FOR UPDATE"
        ;

      /* SQL to select data (SCROLLABLE)*/
     private static final String SQL_SELECT_BATCH_SCROLLABLE =
        "SELECT "
        + "  TS_TREASURY.S_ORGCODE  , TS_TREASURY.S_TRECODE  , TS_TREASURY.S_TRENAME  , TS_TREASURY.S_TRECODENATION  , TS_TREASURY.S_TRECODEAREA "
        + " , TS_TREASURY.S_TRIMFLAG  , TS_TREASURY.S_PAYUNITNAME  , TS_TREASURY.S_FINANCENAME  , TS_TREASURY.S_TRELEVEL  , TS_TREASURY.S_TREATTRIB "
        + " , TS_TREASURY.S_OFPROVTRECODE  , TS_TREASURY.S_OFCITYTRECODE  , TS_TREASURY.S_OFCOUNTRYTRECODE  , TS_TREASURY.S_GOVERNTRECODE  , TS_TREASURY.S_GOVERNTRELEVEL "
        + " , TS_TREASURY.S_ISUNIONTRE  , TS_TREASURY.C_STARTZEROACCT  , TS_TREASURY.I_MODICOUNT  , TS_TREASURY.S_STAMPID  , TS_TREASURY.S_CERTID "
        + " , TS_TREASURY.S_ROTARYID  , TS_TREASURY.S_ROTARYCERTID "
        + "FROM TS_TREASURY ";



    /* SQL to select batch data */
    private static final String SQL_SELECT_BATCH =
        "SELECT "
        + "TS_TREASURY.S_ORGCODE, TS_TREASURY.S_TRECODE, TS_TREASURY.S_TRENAME, TS_TREASURY.S_TRECODENATION, TS_TREASURY.S_TRECODEAREA, "
        + "TS_TREASURY.S_TRIMFLAG, TS_TREASURY.S_PAYUNITNAME, TS_TREASURY.S_FINANCENAME, TS_TREASURY.S_TRELEVEL, TS_TREASURY.S_TREATTRIB, "
        + "TS_TREASURY.S_OFPROVTRECODE, TS_TREASURY.S_OFCITYTRECODE, TS_TREASURY.S_OFCOUNTRYTRECODE, TS_TREASURY.S_GOVERNTRECODE, TS_TREASURY.S_GOVERNTRELEVEL, "
        + "TS_TREASURY.S_ISUNIONTRE, TS_TREASURY.C_STARTZEROACCT, TS_TREASURY.I_MODICOUNT, TS_TREASURY.S_STAMPID, TS_TREASURY.S_CERTID, "
        + "TS_TREASURY.S_ROTARYID, TS_TREASURY.S_ROTARYCERTID "
        + "FROM TS_TREASURY " ;
        

   /* SQL to select batch data where condition  */
    private static final String SQL_SELECT_BATCH_WHERE =" ( "
        + "S_ORGCODE = ? AND S_TRECODE = ?)"
        ;


    /* SQL to update data */
    private static final String SQL_UPDATE =
        "UPDATE TS_TREASURY SET "
        + "S_TRENAME =?,S_TRECODENATION =?,S_TRECODEAREA =?,S_TRIMFLAG =?,S_PAYUNITNAME =?, "
        + "S_FINANCENAME =?,S_TRELEVEL =?,S_TREATTRIB =?,S_OFPROVTRECODE =?,S_OFCITYTRECODE =?, "
        + "S_OFCOUNTRYTRECODE =?,S_GOVERNTRECODE =?,S_GOVERNTRELEVEL =?,S_ISUNIONTRE =?,C_STARTZEROACCT =?, "
        + "I_MODICOUNT =?,S_STAMPID =?,S_CERTID =?,S_ROTARYID =?,S_ROTARYCERTID =? "
        + "WHERE "
        + "S_ORGCODE = ? AND S_TRECODE = ?"
        ;

	/* SQL to update data, support LOB */
    private static final String SQL_UPDATE_LOB =
        "UPDATE TS_TREASURY SET "
        + "S_TRENAME =?, S_TRECODENATION =?, S_TRECODEAREA =?, S_TRIMFLAG =?, S_PAYUNITNAME =?,  "
        + "S_FINANCENAME =?, S_TRELEVEL =?, S_TREATTRIB =?, S_OFPROVTRECODE =?, S_OFCITYTRECODE =?,  "
        + "S_OFCOUNTRYTRECODE =?, S_GOVERNTRECODE =?, S_GOVERNTRELEVEL =?, S_ISUNIONTRE =?, C_STARTZEROACCT =?,  "
        + "I_MODICOUNT =?, S_STAMPID =?, S_CERTID =?, S_ROTARYID =?, S_ROTARYCERTID =? "
        + "WHERE "
        + "S_ORGCODE = ? AND S_TRECODE = ?"
        ;	
	
    /* SQL to delete data */
    private static final String SQL_DELETE =
        "DELETE FROM TS_TREASURY " 
        + " WHERE "
        + "S_ORGCODE = ? AND S_TRECODE = ?"
        ;


	/**
	*  批量查询 一次最多查询的参数数量
	*/
	
	public static final int FIND_BATCH_SIZE = 150  / 2;
	



   /**
   * Create a new record in Database.
   */
  public void create(IDto _dto,  Connection conn) throws SQLException
  {
     TsTreasuryDto dto = (TsTreasuryDto)_dto ;
     String msgValid = dto.checkValid() ;
     if (msgValid != null)
     	throw new SQLException("插入错误，"+msgValid) ;

     PreparedStatement ps = null;
     try
     {
         ps = conn.prepareStatement(SQL_INSERT);
          ps.setString(1, dto.getSorgcode());

          ps.setString(2, dto.getStrecode());

          ps.setString(3, dto.getStrename());

          ps.setString(4, dto.getStrecodenation());

          ps.setString(5, dto.getStrecodearea());

          ps.setString(6, dto.getStrimflag());

          ps.setString(7, dto.getSpayunitname());

          ps.setString(8, dto.getSfinancename());

          ps.setString(9, dto.getStrelevel());

          ps.setString(10, dto.getStreattrib());

          ps.setString(11, dto.getSofprovtrecode());

          ps.setString(12, dto.getSofcitytrecode());

          ps.setString(13, dto.getSofcountrytrecode());

          ps.setString(14, dto.getSgoverntrecode());

          ps.setString(15, dto.getSgoverntrelevel());

          ps.setString(16, dto.getSisuniontre());

          ps.setString(17, dto.getCstartzeroacct());

          if (dto.getImodicount()==null)
            ps.setNull(18, java.sql.Types.INTEGER);
         else
            ps.setInt(18, dto.getImodicount().intValue());
          ps.setString(19, dto.getSstampid());

          ps.setString(20, dto.getScertid());

          ps.setString(21, dto.getSrotaryid());

          ps.setString(22, dto.getSrotarycertid());

         ps.executeUpdate();
     }
     finally
     {
         close(ps);
     }
     return  ;
  }

   /**
   * Create a new record in Database. and return result, only used when having generated key
   */
   public IDto createWithResult(IDto _dto,  Connection conn) throws SQLException
   {
       TsTreasuryDto dto = (TsTreasuryDto)_dto ;
       String msgValid = dto.checkValid() ;
       if (msgValid != null)
           throw new SQLException("插入错误，"+msgValid) ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try
       {
           ps = conn.prepareStatement(SQL_INSERT_WITH_RESULT);
            ps.setString(1, dto.getSorgcode());
            ps.setString(2, dto.getStrecode());
            ps.setString(3, dto.getStrename());
            ps.setString(4, dto.getStrecodenation());
            ps.setString(5, dto.getStrecodearea());
            ps.setString(6, dto.getStrimflag());
            ps.setString(7, dto.getSpayunitname());
            ps.setString(8, dto.getSfinancename());
            ps.setString(9, dto.getStrelevel());
            ps.setString(10, dto.getStreattrib());
            ps.setString(11, dto.getSofprovtrecode());
            ps.setString(12, dto.getSofcitytrecode());
            ps.setString(13, dto.getSofcountrytrecode());
            ps.setString(14, dto.getSgoverntrecode());
            ps.setString(15, dto.getSgoverntrelevel());
            ps.setString(16, dto.getSisuniontre());
            ps.setString(17, dto.getCstartzeroacct());
            if (dto.getImodicount()==null)
              ps.setNull(18, java.sql.Types.INTEGER);
           else
              ps.setInt(18, dto.getImodicount().intValue());
            ps.setString(19, dto.getSstampid());
            ps.setString(20, dto.getScertid());
            ps.setString(21, dto.getSrotaryid());
            ps.setString(22, dto.getSrotarycertid());
           rs = ps.executeQuery();
           List results = getResults(rs);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }
        finally
        {
           close(ps);
           close(rs);
        }
   }

 	/**
	* Create batch new record in Database. it only create the first layer dto
	*/
    public void create(IDto[] _dtos, Connection conn)    throws SQLException
    {
        TsTreasuryDto dto  ;
     	for (int i= 0; i< _dtos.length ; i++)
 	    {
 		    dto  = (TsTreasuryDto)_dtos[i] ; 
    	    String msgValid = dto.checkValid() ;
        	if (msgValid != null)
     	    	throw new SQLException("插入错误，"+msgValid) ;
     	}
 	    PreparedStatement ps = null;
        try
        {
            ps = conn.prepareStatement(SQL_INSERT);
       
     	    for (int i= 0; i< _dtos.length ; i++)
     	    {
 	    	    dto  = (TsTreasuryDto)_dtos[i] ; 
  
               ps.setString(1, dto.getSorgcode());
  
               ps.setString(2, dto.getStrecode());
  
               ps.setString(3, dto.getStrename());
  
               ps.setString(4, dto.getStrecodenation());
  
               ps.setString(5, dto.getStrecodearea());
  
               ps.setString(6, dto.getStrimflag());
  
               ps.setString(7, dto.getSpayunitname());
  
               ps.setString(8, dto.getSfinancename());
  
               ps.setString(9, dto.getStrelevel());
  
               ps.setString(10, dto.getStreattrib());
  
               ps.setString(11, dto.getSofprovtrecode());
  
               ps.setString(12, dto.getSofcitytrecode());
  
               ps.setString(13, dto.getSofcountrytrecode());
  
               ps.setString(14, dto.getSgoverntrecode());
  
               ps.setString(15, dto.getSgoverntrelevel());
  
               ps.setString(16, dto.getSisuniontre());
  
               ps.setString(17, dto.getCstartzeroacct());
  
               if (dto.getImodicount()==null)
                  ps.setNull(18, java.sql.Types.INTEGER);
               else
                  ps.setInt(18, dto.getImodicount().intValue());
  
               ps.setString(19, dto.getSstampid());
  
               ps.setString(20, dto.getScertid());
  
               ps.setString(21, dto.getSrotaryid());
  
               ps.setString(22, dto.getSrotarycertid());
               ps.addBatch(); 
           }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }
        return  ;
   }




     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto find(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TsTreasuryPK pk = (TsTreasuryPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT);
           ps.setString(1, pk.getSorgcode());

           ps.setString(2, pk.getStrecode());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }

     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto findForUpdate(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TsTreasuryPK pk = (TsTreasuryPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT_FOR_UPDATE);
           ps.setString(1, pk.getSorgcode());

           ps.setString(2, pk.getStrecode());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }
  
     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto[] find(IPK[] _pks, Connection conn, boolean isLobSupport) throws SQLException
    {
        TsTreasuryPK pk ;
        
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TsTreasuryPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 
        PreparedStatement ps = null;
        ResultSet rs = null;
        
         
        List results = new ArrayList();  
		for (int iBegin = 0; iBegin < pks.size(); iBegin += FIND_BATCH_SIZE)   //
		{
        
            try {
          		int iFindBatchSize = pks.size() - iBegin ;
	    		if (iFindBatchSize >FIND_BATCH_SIZE )
		    		iFindBatchSize = FIND_BATCH_SIZE ;
		  
        	    StringBuffer sb = new StringBuffer() ;
            	sb.append(SQL_SELECT_BATCH).append(" WHERE ").append(SQL_SELECT_BATCH_WHERE);

	    		for (int i = iBegin+1; i < iBegin + iFindBatchSize; i++)
		    	{
			    	sb.append("OR").append(SQL_SELECT_BATCH_WHERE) ;
    			}
        	
                ps = conn.prepareStatement(sb.toString());

                for (int i = iBegin;i < iBegin + iFindBatchSize; i++)
                {
                    pk  = (TsTreasuryPK)(pks.get(i)) ; 
                   ps.setString((i-iBegin)*2+1, pk.getSorgcode());

                   ps.setString((i-iBegin)*2+2, pk.getStrecode());

			
	            }
                rs = ps.executeQuery();
                results.addAll(getResults(rs,0,isLobSupport)); //
            }finally {
                close(rs);
                close(ps);
            }
        }
        if (results!=null && results.size() > 0)
        {
            TsTreasuryDto[] dtos = new TsTreasuryDto[0];
		    dtos = (TsTreasuryDto[]) results.toArray(dtos) ;
		    return dtos ;
        }
        else
        {
            return null;
        }
    }

  
  
  



    /**
	    * 得到查询语句
	    */
    public String getSelectSQL() 
    {
	        return SQL_SELECT_BATCH_SCROLLABLE ;
    }
   

	public List getResults(ResultSet rs) throws SQLException
    {
    	return getResults(rs, 0) ;
    }
  

    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize) throws SQLException
    {
		return getResults(rs,maxSize,false);
	}
    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize, boolean isLobSupport) throws SQLException
    {
        List results = new ArrayList();
        String str ;
        Reader reader;
        Clob clob;
        char[] chars ;
        
        while (rs.next())
         {
             TsTreasuryDto  dto = new  TsTreasuryDto ();
             //S_ORGCODE
             str = rs.getString("S_ORGCODE");
             if (str == null)
                dto.setSorgcode(null);
             else
                dto.setSorgcode(str.trim());

             //S_TRECODE
             str = rs.getString("S_TRECODE");
             if (str == null)
                dto.setStrecode(null);
             else
                dto.setStrecode(str.trim());

             //S_TRENAME
             str = rs.getString("S_TRENAME");
             if (str == null)
                dto.setStrename(null);
             else
                dto.setStrename(str.trim());

             //S_TRECODENATION
             str = rs.getString("S_TRECODENATION");
             if (str == null)
                dto.setStrecodenation(null);
             else
                dto.setStrecodenation(str.trim());

             //S_TRECODEAREA
             str = rs.getString("S_TRECODEAREA");
             if (str == null)
                dto.setStrecodearea(null);
             else
                dto.setStrecodearea(str.trim());

             //S_TRIMFLAG
             str = rs.getString("S_TRIMFLAG");
             if (str == null)
                dto.setStrimflag(null);
             else
                dto.setStrimflag(str.trim());

             //S_PAYUNITNAME
             str = rs.getString("S_PAYUNITNAME");
             if (str == null)
                dto.setSpayunitname(null);
             else
                dto.setSpayunitname(str.trim());

             //S_FINANCENAME
             str = rs.getString("S_FINANCENAME");
             if (str == null)
                dto.setSfinancename(null);
             else
                dto.setSfinancename(str.trim());

             //S_TRELEVEL
             str = rs.getString("S_TRELEVEL");
             if (str == null)
                dto.setStrelevel(null);
             else
                dto.setStrelevel(str.trim());

             //S_TREATTRIB
             str = rs.getString("S_TREATTRIB");
             if (str == null)
                dto.setStreattrib(null);
             else
                dto.setStreattrib(str.trim());

             //S_OFPROVTRECODE
             str = rs.getString("S_OFPROVTRECODE");
             if (str == null)
                dto.setSofprovtrecode(null);
             else
                dto.setSofprovtrecode(str.trim());

             //S_OFCITYTRECODE
             str = rs.getString("S_OFCITYTRECODE");
             if (str == null)
                dto.setSofcitytrecode(null);
             else
                dto.setSofcitytrecode(str.trim());

             //S_OFCOUNTRYTRECODE
             str = rs.getString("S_OFCOUNTRYTRECODE");
             if (str == null)
                dto.setSofcountrytrecode(null);
             else
                dto.setSofcountrytrecode(str.trim());

             //S_GOVERNTRECODE
             str = rs.getString("S_GOVERNTRECODE");
             if (str == null)
                dto.setSgoverntrecode(null);
             else
                dto.setSgoverntrecode(str.trim());

             //S_GOVERNTRELEVEL
             str = rs.getString("S_GOVERNTRELEVEL");
             if (str == null)
                dto.setSgoverntrelevel(null);
             else
                dto.setSgoverntrelevel(str.trim());

             //S_ISUNIONTRE
             str = rs.getString("S_ISUNIONTRE");
             if (str == null)
                dto.setSisuniontre(null);
             else
                dto.setSisuniontre(str.trim());

             //C_STARTZEROACCT
             str = rs.getString("C_STARTZEROACCT");
             if (str == null)
                dto.setCstartzeroacct(null);
             else
                dto.setCstartzeroacct(str.trim());

             //I_MODICOUNT
             str = rs.getString("I_MODICOUNT");
             if(str!=null){
                dto.setImodicount(new Integer(str));
             }

             //S_STAMPID
             str = rs.getString("S_STAMPID");
             if (str == null)
                dto.setSstampid(null);
             else
                dto.setSstampid(str.trim());

             //S_CERTID
             str = rs.getString("S_CERTID");
             if (str == null)
                dto.setScertid(null);
             else
                dto.setScertid(str.trim());

             //S_ROTARYID
             str = rs.getString("S_ROTARYID");
             if (str == null)
                dto.setSrotaryid(null);
             else
                dto.setSrotaryid(str.trim());

             //S_ROTARYCERTID
             str = rs.getString("S_ROTARYCERTID");
             if (str == null)
                dto.setSrotarycertid(null);
             else
                dto.setSrotarycertid(str.trim());



            results.add(dto);
            
            if(maxSize >0  && results.size() == maxSize)
            {
            	break ;
            }
            
         }
        
		return results ;
    }

  /**
   * populate resultSet as cclass
   */
  protected IDto[] getChildrenResults(ResultSet rs ,Class _dto) throws SQLException{

      throw new SQLException("本dto没有与其它dto相关联，无法应用此方法");
  }


     /**
     * Update a record in Database.
     */

    public void update(IDto _dto, Connection conn, boolean isLobSupport) throws SQLException
    {
        String msgValid = _dto.checkValid() ;
        if (msgValid != null)
            throw new SQLException("修改错误，"+msgValid) ;
        TsTreasuryDto dto = (TsTreasuryDto)_dto ;
        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            int pos = 1;
            //S_TRENAME
            ps.setString(pos, dto.getStrename());
            pos++;

            //S_TRECODENATION
            ps.setString(pos, dto.getStrecodenation());
            pos++;

            //S_TRECODEAREA
            ps.setString(pos, dto.getStrecodearea());
            pos++;

            //S_TRIMFLAG
            ps.setString(pos, dto.getStrimflag());
            pos++;

            //S_PAYUNITNAME
            ps.setString(pos, dto.getSpayunitname());
            pos++;

            //S_FINANCENAME
            ps.setString(pos, dto.getSfinancename());
            pos++;

            //S_TRELEVEL
            ps.setString(pos, dto.getStrelevel());
            pos++;

            //S_TREATTRIB
            ps.setString(pos, dto.getStreattrib());
            pos++;

            //S_OFPROVTRECODE
            ps.setString(pos, dto.getSofprovtrecode());
            pos++;

            //S_OFCITYTRECODE
            ps.setString(pos, dto.getSofcitytrecode());
            pos++;

            //S_OFCOUNTRYTRECODE
            ps.setString(pos, dto.getSofcountrytrecode());
            pos++;

            //S_GOVERNTRECODE
            ps.setString(pos, dto.getSgoverntrecode());
            pos++;

            //S_GOVERNTRELEVEL
            ps.setString(pos, dto.getSgoverntrelevel());
            pos++;

            //S_ISUNIONTRE
            ps.setString(pos, dto.getSisuniontre());
            pos++;

            //C_STARTZEROACCT
            ps.setString(pos, dto.getCstartzeroacct());
            pos++;

            //I_MODICOUNT
            if (dto.getImodicount()==null)
                ps.setNull(pos, java.sql.Types.INTEGER);
            else
                ps.setInt(pos, dto.getImodicount().intValue());
            pos++;

            //S_STAMPID
            ps.setString(pos, dto.getSstampid());
            pos++;

            //S_CERTID
            ps.setString(pos, dto.getScertid());
            pos++;

            //S_ROTARYID
            ps.setString(pos, dto.getSrotaryid());
            pos++;

            //S_ROTARYCERTID
            ps.setString(pos, dto.getSrotarycertid());
            pos++;


           //S_ORGCODE
           ps.setString(pos, dto.getSorgcode());
           pos++;
           //S_TRECODE
           ps.setString(pos, dto.getStrecode());
           pos++;
           ps.executeUpdate();
        }
        finally
        {
           close(ps);
        }

        return ;
    }
    
    
    
      /**
     * Update batch record in Database.
     */

    public void update(IDto[] _dtos, Connection conn, boolean isLobSupport) throws SQLException
    {
     
     	 TsTreasuryDto dto  ;
         for (int i= 0; i< _dtos.length ; i++)
         {
            dto  = (TsTreasuryDto)_dtos[i] ; 
            String msgValid = dto.checkValid() ;
            if (msgValid != null)
                throw new SQLException("修改错误，"+msgValid) ;
         }

        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            
            for (int i= 0; i< _dtos.length ; i++)
            {
                dto  = (TsTreasuryDto)_dtos[i] ; 
                int pos = 1;
                //S_TRENAME
                 ps.setString(pos, dto.getStrename());
                pos++;

                //S_TRECODENATION
                 ps.setString(pos, dto.getStrecodenation());
                pos++;

                //S_TRECODEAREA
                 ps.setString(pos, dto.getStrecodearea());
                pos++;

                //S_TRIMFLAG
                 ps.setString(pos, dto.getStrimflag());
                pos++;

                //S_PAYUNITNAME
                 ps.setString(pos, dto.getSpayunitname());
                pos++;

                //S_FINANCENAME
                 ps.setString(pos, dto.getSfinancename());
                pos++;

                //S_TRELEVEL
                 ps.setString(pos, dto.getStrelevel());
                pos++;

                //S_TREATTRIB
                 ps.setString(pos, dto.getStreattrib());
                pos++;

                //S_OFPROVTRECODE
                 ps.setString(pos, dto.getSofprovtrecode());
                pos++;

                //S_OFCITYTRECODE
                 ps.setString(pos, dto.getSofcitytrecode());
                pos++;

                //S_OFCOUNTRYTRECODE
                 ps.setString(pos, dto.getSofcountrytrecode());
                pos++;

                //S_GOVERNTRECODE
                 ps.setString(pos, dto.getSgoverntrecode());
                pos++;

                //S_GOVERNTRELEVEL
                 ps.setString(pos, dto.getSgoverntrelevel());
                pos++;

                //S_ISUNIONTRE
                 ps.setString(pos, dto.getSisuniontre());
                pos++;

                //C_STARTZEROACCT
                 ps.setString(pos, dto.getCstartzeroacct());
                pos++;

                //I_MODICOUNT
                 if (dto.getImodicount()==null)
                   ps.setNull(pos, java.sql.Types.INTEGER);
                else
                   ps.setInt(pos, dto.getImodicount().intValue());
                pos++ ;

                //S_STAMPID
                 ps.setString(pos, dto.getSstampid());
                pos++;

                //S_CERTID
                 ps.setString(pos, dto.getScertid());
                pos++;

                //S_ROTARYID
                 ps.setString(pos, dto.getSrotaryid());
                pos++;

                //S_ROTARYCERTID
                 ps.setString(pos, dto.getSrotarycertid());
                pos++;


               //S_ORGCODE
               ps.setString(pos, dto.getSorgcode());
               pos++;
               //S_TRECODE
               ps.setString(pos, dto.getStrecode());
               pos++;
		       ps.addBatch(); 
	       }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }

        return ;
    }



    /**
     * delete a record in Database.
     */
    public void delete(IPK _pk, Connection conn) throws SQLException
    {
       
      
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return ;
        //	throw new SQLException("删除错误，"+msgValid) ;
       }
       TsTreasuryPK pk = (TsTreasuryPK)_pk ;


       PreparedStatement ps = null;
       try {
           ps = conn.prepareStatement(SQL_DELETE);
           ps.setString(1, pk.getSorgcode());
           ps.setString(2, pk.getStrecode());
           ps.executeUpdate();
        }
        finally
        {
            close(ps);
        }
    }


     /**
     * delete batch record in Database.
     */
    public void delete(IPK[] _pks, Connection conn) throws SQLException
    {
        TsTreasuryPK pk ;
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TsTreasuryPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 		
 		
        PreparedStatement ps = null;
        try
         {
            ps = conn.prepareStatement(SQL_DELETE);
             
       		for (int i= 0; i< pks.size() ; i++)
       		{
       			pk  = (TsTreasuryPK)(pks.get(i)) ; 
                ps.setString(1, pk.getSorgcode());
                ps.setString(2, pk.getStrecode());
		        ps.addBatch() ;
			}
            ps.executeBatch();
        }
        finally
        {
            close(ps);
        }      
    }



    /**
    * Check IChangerNumber or UpdateTimeStamp.
    */
    public void check(IDto _dto, Connection conn) throws SQLException
    {
                                                            		throw new SQLException("数据库表：TS_TREASURY没有检查修改的字段");
    }


	/**
	* synchronize values between parent and children
	*/
    public void syncToChildren(IDto _dto)     throws SQLException{
       throw new SQLException("本记录没有与其它记录相关联，无法进行主记录与明细记录之间的数据同步。");
  }

	/**
	*get children
	*/
    public IDto[] findChildren (IDto _dto, Connection conn, boolean isLobSupport)     throws SQLException{
         throw new SQLException("本dto没有与其它dto相关联，无法得到子dto");
  }    
    
  /**
   *return the children IDto class
   * @return Class
   */
  public Class getChildrenClass(){
	  return null;
  };    



    /**
     * Close JDBC Statement.
     * @param stmt  Statement to be closed.
     */
    private void close(Statement stmt)
    {
        if (stmt != null)
        {
            try
            {
                stmt.close();
            }
            catch(SQLException    e)
            {}
        }
    }

    /**
     * Close JDBC ResultSet.
     * @param rs  ResultSet to be closed.
     */
    private void close(ResultSet rs)
    {
        if (rs != null)
        {
            try
            {
                rs.close();
            }catch(SQLException  e){}
        }
    }
}
