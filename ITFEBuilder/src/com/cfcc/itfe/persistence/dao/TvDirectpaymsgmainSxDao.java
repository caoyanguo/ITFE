    



package com.cfcc.itfe.persistence.dao;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.Statement;
import java.sql.SQLException;
import java.sql.Date;
import java.sql.Time;
import java.sql.Timestamp ;
import java.sql.Clob;

import com.cfcc.jaf.persistence.jaform.parent.* ;

import java.math.BigDecimal;

import java.util.List;
import java.util.ArrayList;
import java.util.HashMap;

import java.io.StringReader;
import java.io.Reader;
import java.io.IOException;

import com.cfcc.itfe.persistence.dto.TvDirectpaymsgmainSxDto ;
import com.cfcc.itfe.persistence.pk.TvDirectpaymsgmainSxPK ;


/**
 * <p>Title: DAO of table: TV_DIRECTPAYMSGMAIN_SX</p>
 * <p>Description: Data Access Object  </p>
 * <p>Copyright: Copyright (c) 2005</p>
 * <p>Company: CFCC </p>
 * <p>Generation Time: 2014-12-25 15:29:01 </p>
 *  Do NOT change this file by hand.
 *  This java source file is generated by JAF_ORM .
 *  dao.vm timestamp: 2007-04-06 08:30:00
 ******************************************************
 * <p> <b>change record</b><br>
 *  make dao methods simply.<br>
 *  especially make update,delete methods simply.<br>
 *  remove "..withCheck" method,withCheck will impl in JAFOrmTemplate.<br>
 *  remove "delete(IDto _dto,..)" method.<br></p>
 * @author win7
 */

public class TvDirectpaymsgmainSxDao  implements IDao
{


    /* SQL to insert data */
    private static final String SQL_INSERT =
        "INSERT INTO TV_DIRECTPAYMSGMAIN_SX ("
          + "I_VOUSRLNO,S_ORGCODE,S_COMMITDATE,S_ACCDATE,S_FILENAME"
          + ",S_PACKAGENO,S_TRECODE,S_PAYUNIT,S_TRANSBANKCODE,S_DEALNO"
          + ",N_MONEY,S_TAXTICKETNO,S_PACKAGETICKETNO,S_GENTICKETDATE,S_BUDGETTYPE"
          + ",S_OFYEAR,S_TRANSACTUNIT,S_AMTTYPE,S_STATUS,S_USERCODE"
          + ",S_DEMO,TS_SYSUPDATE,S_ID,S_FUNDTYPECODE,S_FUNDTYPENAME"
          + ",S_PAYEEACCTNO,S_PAYEEACCTNAME,S_PAYEEACCTBANKNAME,S_PAYACCTNO,S_PAYACCTNAME"
          + ",S_PAYACCTBANKNAME,S_PAYBANKCODE,S_PAYBANKNAME,S_PAYBANKNO,S_XACCTDATE"
          + ",S_HOLD1,S_HOLD2"
        + ") VALUES ("
        + "?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,CURRENT TIMESTAMP ,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)";


    private static final String SQL_INSERT_WITH_RESULT = "SELECT * FROM FINAL TABLE( " + SQL_INSERT + " )";
    
    /* SQL to select data */
    private static final String SQL_SELECT =
        "SELECT "
        + "TV_DIRECTPAYMSGMAIN_SX.I_VOUSRLNO, TV_DIRECTPAYMSGMAIN_SX.S_ORGCODE, TV_DIRECTPAYMSGMAIN_SX.S_COMMITDATE, TV_DIRECTPAYMSGMAIN_SX.S_ACCDATE, TV_DIRECTPAYMSGMAIN_SX.S_FILENAME, "
        + "TV_DIRECTPAYMSGMAIN_SX.S_PACKAGENO, TV_DIRECTPAYMSGMAIN_SX.S_TRECODE, TV_DIRECTPAYMSGMAIN_SX.S_PAYUNIT, TV_DIRECTPAYMSGMAIN_SX.S_TRANSBANKCODE, TV_DIRECTPAYMSGMAIN_SX.S_DEALNO, "
        + "TV_DIRECTPAYMSGMAIN_SX.N_MONEY, TV_DIRECTPAYMSGMAIN_SX.S_TAXTICKETNO, TV_DIRECTPAYMSGMAIN_SX.S_PACKAGETICKETNO, TV_DIRECTPAYMSGMAIN_SX.S_GENTICKETDATE, TV_DIRECTPAYMSGMAIN_SX.S_BUDGETTYPE, "
        + "TV_DIRECTPAYMSGMAIN_SX.S_OFYEAR, TV_DIRECTPAYMSGMAIN_SX.S_TRANSACTUNIT, TV_DIRECTPAYMSGMAIN_SX.S_AMTTYPE, TV_DIRECTPAYMSGMAIN_SX.S_STATUS, TV_DIRECTPAYMSGMAIN_SX.S_USERCODE, "
        + "TV_DIRECTPAYMSGMAIN_SX.S_DEMO, TV_DIRECTPAYMSGMAIN_SX.TS_SYSUPDATE, TV_DIRECTPAYMSGMAIN_SX.S_ID, TV_DIRECTPAYMSGMAIN_SX.S_FUNDTYPECODE, TV_DIRECTPAYMSGMAIN_SX.S_FUNDTYPENAME, "
        + "TV_DIRECTPAYMSGMAIN_SX.S_PAYEEACCTNO, TV_DIRECTPAYMSGMAIN_SX.S_PAYEEACCTNAME, TV_DIRECTPAYMSGMAIN_SX.S_PAYEEACCTBANKNAME, TV_DIRECTPAYMSGMAIN_SX.S_PAYACCTNO, TV_DIRECTPAYMSGMAIN_SX.S_PAYACCTNAME, "
        + "TV_DIRECTPAYMSGMAIN_SX.S_PAYACCTBANKNAME, TV_DIRECTPAYMSGMAIN_SX.S_PAYBANKCODE, TV_DIRECTPAYMSGMAIN_SX.S_PAYBANKNAME, TV_DIRECTPAYMSGMAIN_SX.S_PAYBANKNO, TV_DIRECTPAYMSGMAIN_SX.S_XACCTDATE, "
        + "TV_DIRECTPAYMSGMAIN_SX.S_HOLD1, TV_DIRECTPAYMSGMAIN_SX.S_HOLD2 "
        + "FROM TV_DIRECTPAYMSGMAIN_SX "
        +" WHERE " 
        + "I_VOUSRLNO = ?"
        ;
    /* SQL to select for update data */
    private static final String SQL_SELECT_FOR_UPDATE =
        "SELECT "
        + "TV_DIRECTPAYMSGMAIN_SX.I_VOUSRLNO, TV_DIRECTPAYMSGMAIN_SX.S_ORGCODE, TV_DIRECTPAYMSGMAIN_SX.S_COMMITDATE, TV_DIRECTPAYMSGMAIN_SX.S_ACCDATE, TV_DIRECTPAYMSGMAIN_SX.S_FILENAME, "
        + "TV_DIRECTPAYMSGMAIN_SX.S_PACKAGENO, TV_DIRECTPAYMSGMAIN_SX.S_TRECODE, TV_DIRECTPAYMSGMAIN_SX.S_PAYUNIT, TV_DIRECTPAYMSGMAIN_SX.S_TRANSBANKCODE, TV_DIRECTPAYMSGMAIN_SX.S_DEALNO, "
        + "TV_DIRECTPAYMSGMAIN_SX.N_MONEY, TV_DIRECTPAYMSGMAIN_SX.S_TAXTICKETNO, TV_DIRECTPAYMSGMAIN_SX.S_PACKAGETICKETNO, TV_DIRECTPAYMSGMAIN_SX.S_GENTICKETDATE, TV_DIRECTPAYMSGMAIN_SX.S_BUDGETTYPE, "
        + "TV_DIRECTPAYMSGMAIN_SX.S_OFYEAR, TV_DIRECTPAYMSGMAIN_SX.S_TRANSACTUNIT, TV_DIRECTPAYMSGMAIN_SX.S_AMTTYPE, TV_DIRECTPAYMSGMAIN_SX.S_STATUS, TV_DIRECTPAYMSGMAIN_SX.S_USERCODE, "
        + "TV_DIRECTPAYMSGMAIN_SX.S_DEMO, TV_DIRECTPAYMSGMAIN_SX.TS_SYSUPDATE, TV_DIRECTPAYMSGMAIN_SX.S_ID, TV_DIRECTPAYMSGMAIN_SX.S_FUNDTYPECODE, TV_DIRECTPAYMSGMAIN_SX.S_FUNDTYPENAME, "
        + "TV_DIRECTPAYMSGMAIN_SX.S_PAYEEACCTNO, TV_DIRECTPAYMSGMAIN_SX.S_PAYEEACCTNAME, TV_DIRECTPAYMSGMAIN_SX.S_PAYEEACCTBANKNAME, TV_DIRECTPAYMSGMAIN_SX.S_PAYACCTNO, TV_DIRECTPAYMSGMAIN_SX.S_PAYACCTNAME, "
        + "TV_DIRECTPAYMSGMAIN_SX.S_PAYACCTBANKNAME, TV_DIRECTPAYMSGMAIN_SX.S_PAYBANKCODE, TV_DIRECTPAYMSGMAIN_SX.S_PAYBANKNAME, TV_DIRECTPAYMSGMAIN_SX.S_PAYBANKNO, TV_DIRECTPAYMSGMAIN_SX.S_XACCTDATE, "
        + "TV_DIRECTPAYMSGMAIN_SX.S_HOLD1, TV_DIRECTPAYMSGMAIN_SX.S_HOLD2 "
        + "FROM TV_DIRECTPAYMSGMAIN_SX "
        +" WHERE " 
        + "I_VOUSRLNO = ? FOR UPDATE"
        ;

      /* SQL to select data (SCROLLABLE)*/
     private static final String SQL_SELECT_BATCH_SCROLLABLE =
        "SELECT "
        + "  TV_DIRECTPAYMSGMAIN_SX.I_VOUSRLNO  , TV_DIRECTPAYMSGMAIN_SX.S_ORGCODE  , TV_DIRECTPAYMSGMAIN_SX.S_COMMITDATE  , TV_DIRECTPAYMSGMAIN_SX.S_ACCDATE  , TV_DIRECTPAYMSGMAIN_SX.S_FILENAME "
        + " , TV_DIRECTPAYMSGMAIN_SX.S_PACKAGENO  , TV_DIRECTPAYMSGMAIN_SX.S_TRECODE  , TV_DIRECTPAYMSGMAIN_SX.S_PAYUNIT  , TV_DIRECTPAYMSGMAIN_SX.S_TRANSBANKCODE  , TV_DIRECTPAYMSGMAIN_SX.S_DEALNO "
        + " , TV_DIRECTPAYMSGMAIN_SX.N_MONEY  , TV_DIRECTPAYMSGMAIN_SX.S_TAXTICKETNO  , TV_DIRECTPAYMSGMAIN_SX.S_PACKAGETICKETNO  , TV_DIRECTPAYMSGMAIN_SX.S_GENTICKETDATE  , TV_DIRECTPAYMSGMAIN_SX.S_BUDGETTYPE "
        + " , TV_DIRECTPAYMSGMAIN_SX.S_OFYEAR  , TV_DIRECTPAYMSGMAIN_SX.S_TRANSACTUNIT  , TV_DIRECTPAYMSGMAIN_SX.S_AMTTYPE  , TV_DIRECTPAYMSGMAIN_SX.S_STATUS  , TV_DIRECTPAYMSGMAIN_SX.S_USERCODE "
        + " , TV_DIRECTPAYMSGMAIN_SX.S_DEMO  , TV_DIRECTPAYMSGMAIN_SX.TS_SYSUPDATE  , TV_DIRECTPAYMSGMAIN_SX.S_ID  , TV_DIRECTPAYMSGMAIN_SX.S_FUNDTYPECODE  , TV_DIRECTPAYMSGMAIN_SX.S_FUNDTYPENAME "
        + " , TV_DIRECTPAYMSGMAIN_SX.S_PAYEEACCTNO  , TV_DIRECTPAYMSGMAIN_SX.S_PAYEEACCTNAME  , TV_DIRECTPAYMSGMAIN_SX.S_PAYEEACCTBANKNAME  , TV_DIRECTPAYMSGMAIN_SX.S_PAYACCTNO  , TV_DIRECTPAYMSGMAIN_SX.S_PAYACCTNAME "
        + " , TV_DIRECTPAYMSGMAIN_SX.S_PAYACCTBANKNAME  , TV_DIRECTPAYMSGMAIN_SX.S_PAYBANKCODE  , TV_DIRECTPAYMSGMAIN_SX.S_PAYBANKNAME  , TV_DIRECTPAYMSGMAIN_SX.S_PAYBANKNO  , TV_DIRECTPAYMSGMAIN_SX.S_XACCTDATE "
        + " , TV_DIRECTPAYMSGMAIN_SX.S_HOLD1  , TV_DIRECTPAYMSGMAIN_SX.S_HOLD2 "
        + "FROM TV_DIRECTPAYMSGMAIN_SX ";



    /* SQL to select batch data */
    private static final String SQL_SELECT_BATCH =
        "SELECT "
        + "TV_DIRECTPAYMSGMAIN_SX.I_VOUSRLNO, TV_DIRECTPAYMSGMAIN_SX.S_ORGCODE, TV_DIRECTPAYMSGMAIN_SX.S_COMMITDATE, TV_DIRECTPAYMSGMAIN_SX.S_ACCDATE, TV_DIRECTPAYMSGMAIN_SX.S_FILENAME, "
        + "TV_DIRECTPAYMSGMAIN_SX.S_PACKAGENO, TV_DIRECTPAYMSGMAIN_SX.S_TRECODE, TV_DIRECTPAYMSGMAIN_SX.S_PAYUNIT, TV_DIRECTPAYMSGMAIN_SX.S_TRANSBANKCODE, TV_DIRECTPAYMSGMAIN_SX.S_DEALNO, "
        + "TV_DIRECTPAYMSGMAIN_SX.N_MONEY, TV_DIRECTPAYMSGMAIN_SX.S_TAXTICKETNO, TV_DIRECTPAYMSGMAIN_SX.S_PACKAGETICKETNO, TV_DIRECTPAYMSGMAIN_SX.S_GENTICKETDATE, TV_DIRECTPAYMSGMAIN_SX.S_BUDGETTYPE, "
        + "TV_DIRECTPAYMSGMAIN_SX.S_OFYEAR, TV_DIRECTPAYMSGMAIN_SX.S_TRANSACTUNIT, TV_DIRECTPAYMSGMAIN_SX.S_AMTTYPE, TV_DIRECTPAYMSGMAIN_SX.S_STATUS, TV_DIRECTPAYMSGMAIN_SX.S_USERCODE, "
        + "TV_DIRECTPAYMSGMAIN_SX.S_DEMO, TV_DIRECTPAYMSGMAIN_SX.TS_SYSUPDATE, TV_DIRECTPAYMSGMAIN_SX.S_ID, TV_DIRECTPAYMSGMAIN_SX.S_FUNDTYPECODE, TV_DIRECTPAYMSGMAIN_SX.S_FUNDTYPENAME, "
        + "TV_DIRECTPAYMSGMAIN_SX.S_PAYEEACCTNO, TV_DIRECTPAYMSGMAIN_SX.S_PAYEEACCTNAME, TV_DIRECTPAYMSGMAIN_SX.S_PAYEEACCTBANKNAME, TV_DIRECTPAYMSGMAIN_SX.S_PAYACCTNO, TV_DIRECTPAYMSGMAIN_SX.S_PAYACCTNAME, "
        + "TV_DIRECTPAYMSGMAIN_SX.S_PAYACCTBANKNAME, TV_DIRECTPAYMSGMAIN_SX.S_PAYBANKCODE, TV_DIRECTPAYMSGMAIN_SX.S_PAYBANKNAME, TV_DIRECTPAYMSGMAIN_SX.S_PAYBANKNO, TV_DIRECTPAYMSGMAIN_SX.S_XACCTDATE, "
        + "TV_DIRECTPAYMSGMAIN_SX.S_HOLD1, TV_DIRECTPAYMSGMAIN_SX.S_HOLD2 "
        + "FROM TV_DIRECTPAYMSGMAIN_SX " ;
        

   /* SQL to select batch data where condition  */
    private static final String SQL_SELECT_BATCH_WHERE =" ( "
        + "I_VOUSRLNO = ?)"
        ;


    /* SQL to update data */
    private static final String SQL_UPDATE =
        "UPDATE TV_DIRECTPAYMSGMAIN_SX SET "
        + "S_ORGCODE =?,S_COMMITDATE =?,S_ACCDATE =?,S_FILENAME =?,S_PACKAGENO =?, "
        + "S_TRECODE =?,S_PAYUNIT =?,S_TRANSBANKCODE =?,S_DEALNO =?,N_MONEY =?, "
        + "S_TAXTICKETNO =?,S_PACKAGETICKETNO =?,S_GENTICKETDATE =?,S_BUDGETTYPE =?,S_OFYEAR =?, "
        + "S_TRANSACTUNIT =?,S_AMTTYPE =?,S_STATUS =?,S_USERCODE =?,S_DEMO =?, "
        + "TS_SYSUPDATE =CURRENT TIMESTAMP,S_ID =?,S_FUNDTYPECODE =?,S_FUNDTYPENAME =?,S_PAYEEACCTNO =?, "
        + "S_PAYEEACCTNAME =?,S_PAYEEACCTBANKNAME =?,S_PAYACCTNO =?,S_PAYACCTNAME =?,S_PAYACCTBANKNAME =?, "
        + "S_PAYBANKCODE =?,S_PAYBANKNAME =?,S_PAYBANKNO =?,S_XACCTDATE =?,S_HOLD1 =?, "
        + "S_HOLD2 =? "
        + "WHERE "
        + "I_VOUSRLNO = ?"
        ;

	/* SQL to update data, support LOB */
    private static final String SQL_UPDATE_LOB =
        "UPDATE TV_DIRECTPAYMSGMAIN_SX SET "
        + "S_ORGCODE =?, S_COMMITDATE =?, S_ACCDATE =?, S_FILENAME =?, S_PACKAGENO =?,  "
        + "S_TRECODE =?, S_PAYUNIT =?, S_TRANSBANKCODE =?, S_DEALNO =?, N_MONEY =?,  "
        + "S_TAXTICKETNO =?, S_PACKAGETICKETNO =?, S_GENTICKETDATE =?, S_BUDGETTYPE =?, S_OFYEAR =?,  "
        + "S_TRANSACTUNIT =?, S_AMTTYPE =?, S_STATUS =?, S_USERCODE =?, S_DEMO =?,  "
        + "TS_SYSUPDATE =CURRENT TIMESTAMP, S_ID =?, S_FUNDTYPECODE =?, S_FUNDTYPENAME =?, S_PAYEEACCTNO =?,  "
        + "S_PAYEEACCTNAME =?, S_PAYEEACCTBANKNAME =?, S_PAYACCTNO =?, S_PAYACCTNAME =?, S_PAYACCTBANKNAME =?,  "
        + "S_PAYBANKCODE =?, S_PAYBANKNAME =?, S_PAYBANKNO =?, S_XACCTDATE =?, S_HOLD1 =?,  "
        + "S_HOLD2 =? "
        + "WHERE "
        + "I_VOUSRLNO = ?"
        ;	
	
    /* SQL to delete data */
    private static final String SQL_DELETE =
        "DELETE FROM TV_DIRECTPAYMSGMAIN_SX " 
        + " WHERE "
        + "I_VOUSRLNO = ?"
        ;


	/**
	*  批量查询 一次最多查询的参数数量
	*/
	
	public static final int FIND_BATCH_SIZE = 150  / 1;
	



   /**
   * Create a new record in Database.
   */
  public void create(IDto _dto,  Connection conn) throws SQLException
  {
     TvDirectpaymsgmainSxDto dto = (TvDirectpaymsgmainSxDto)_dto ;
     String msgValid = dto.checkValid() ;
     if (msgValid != null)
     	throw new SQLException("插入错误，"+msgValid) ;

     PreparedStatement ps = null;
     try
     {
         ps = conn.prepareStatement(SQL_INSERT);
          if (dto.getIvousrlno()==null)
            ps.setNull(1, java.sql.Types.BIGINT);
         else
            ps.setLong(1, dto.getIvousrlno().longValue());
          ps.setString(2, dto.getSorgcode());

          ps.setString(3, dto.getScommitdate());

          ps.setString(4, dto.getSaccdate());

          ps.setString(5, dto.getSfilename());

          ps.setString(6, dto.getSpackageno());

          ps.setString(7, dto.getStrecode());

          ps.setString(8, dto.getSpayunit());

          ps.setString(9, dto.getStransbankcode());

          ps.setString(10, dto.getSdealno());

          ps.setBigDecimal(11, dto.getNmoney());

          ps.setString(12, dto.getStaxticketno());

          ps.setString(13, dto.getSpackageticketno());

          ps.setString(14, dto.getSgenticketdate());

          ps.setString(15, dto.getSbudgettype());

          ps.setString(16, dto.getSofyear());

          ps.setString(17, dto.getStransactunit());

          ps.setString(18, dto.getSamttype());

          ps.setString(19, dto.getSstatus());

          ps.setString(20, dto.getSusercode());

          ps.setString(21, dto.getSdemo());

           ps.setString(22, dto.getSid());

          ps.setString(23, dto.getSfundtypecode());

          ps.setString(24, dto.getSfundtypename());

          ps.setString(25, dto.getSpayeeacctno());

          ps.setString(26, dto.getSpayeeacctname());

          ps.setString(27, dto.getSpayeeacctbankname());

          ps.setString(28, dto.getSpayacctno());

          ps.setString(29, dto.getSpayacctname());

          ps.setString(30, dto.getSpayacctbankname());

          ps.setString(31, dto.getSpaybankcode());

          ps.setString(32, dto.getSpaybankname());

          ps.setString(33, dto.getSpaybankno());

          ps.setString(34, dto.getSxacctdate());

          ps.setString(35, dto.getShold1());

          ps.setString(36, dto.getShold2());

         ps.executeUpdate();
     }
     finally
     {
         close(ps);
     }
     return  ;
  }

   /**
   * Create a new record in Database. and return result, only used when having generated key
   */
   public IDto createWithResult(IDto _dto,  Connection conn) throws SQLException
   {
       TvDirectpaymsgmainSxDto dto = (TvDirectpaymsgmainSxDto)_dto ;
       String msgValid = dto.checkValid() ;
       if (msgValid != null)
           throw new SQLException("插入错误，"+msgValid) ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try
       {
           ps = conn.prepareStatement(SQL_INSERT_WITH_RESULT);
            if (dto.getIvousrlno()==null)
              ps.setNull(1, java.sql.Types.BIGINT);
           else
              ps.setLong(1, dto.getIvousrlno().longValue());
            ps.setString(2, dto.getSorgcode());
            ps.setString(3, dto.getScommitdate());
            ps.setString(4, dto.getSaccdate());
            ps.setString(5, dto.getSfilename());
            ps.setString(6, dto.getSpackageno());
            ps.setString(7, dto.getStrecode());
            ps.setString(8, dto.getSpayunit());
            ps.setString(9, dto.getStransbankcode());
            ps.setString(10, dto.getSdealno());
            ps.setBigDecimal(11, dto.getNmoney());
            ps.setString(12, dto.getStaxticketno());
            ps.setString(13, dto.getSpackageticketno());
            ps.setString(14, dto.getSgenticketdate());
            ps.setString(15, dto.getSbudgettype());
            ps.setString(16, dto.getSofyear());
            ps.setString(17, dto.getStransactunit());
            ps.setString(18, dto.getSamttype());
            ps.setString(19, dto.getSstatus());
            ps.setString(20, dto.getSusercode());
            ps.setString(21, dto.getSdemo());
             ps.setString(22, dto.getSid());
            ps.setString(23, dto.getSfundtypecode());
            ps.setString(24, dto.getSfundtypename());
            ps.setString(25, dto.getSpayeeacctno());
            ps.setString(26, dto.getSpayeeacctname());
            ps.setString(27, dto.getSpayeeacctbankname());
            ps.setString(28, dto.getSpayacctno());
            ps.setString(29, dto.getSpayacctname());
            ps.setString(30, dto.getSpayacctbankname());
            ps.setString(31, dto.getSpaybankcode());
            ps.setString(32, dto.getSpaybankname());
            ps.setString(33, dto.getSpaybankno());
            ps.setString(34, dto.getSxacctdate());
            ps.setString(35, dto.getShold1());
            ps.setString(36, dto.getShold2());
           rs = ps.executeQuery();
           List results = getResults(rs);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }
        finally
        {
           close(ps);
           close(rs);
        }
   }

 	/**
	* Create batch new record in Database. it only create the first layer dto
	*/
    public void create(IDto[] _dtos, Connection conn)    throws SQLException
    {
        TvDirectpaymsgmainSxDto dto  ;
     	for (int i= 0; i< _dtos.length ; i++)
 	    {
 		    dto  = (TvDirectpaymsgmainSxDto)_dtos[i] ; 
    	    String msgValid = dto.checkValid() ;
        	if (msgValid != null)
     	    	throw new SQLException("插入错误，"+msgValid) ;
     	}
 	    PreparedStatement ps = null;
        try
        {
            ps = conn.prepareStatement(SQL_INSERT);
       
     	    for (int i= 0; i< _dtos.length ; i++)
     	    {
 	    	    dto  = (TvDirectpaymsgmainSxDto)_dtos[i] ; 
  
               if (dto.getIvousrlno()==null)
                  ps.setNull(1, java.sql.Types.BIGINT);
               else
                  ps.setLong(1, dto.getIvousrlno().longValue());
  
               ps.setString(2, dto.getSorgcode());
  
               ps.setString(3, dto.getScommitdate());
  
               ps.setString(4, dto.getSaccdate());
  
               ps.setString(5, dto.getSfilename());
  
               ps.setString(6, dto.getSpackageno());
  
               ps.setString(7, dto.getStrecode());
  
               ps.setString(8, dto.getSpayunit());
  
               ps.setString(9, dto.getStransbankcode());
  
               ps.setString(10, dto.getSdealno());
  
               ps.setBigDecimal(11, dto.getNmoney());
  
               ps.setString(12, dto.getStaxticketno());
  
               ps.setString(13, dto.getSpackageticketno());
  
               ps.setString(14, dto.getSgenticketdate());
  
               ps.setString(15, dto.getSbudgettype());
  
               ps.setString(16, dto.getSofyear());
  
               ps.setString(17, dto.getStransactunit());
  
               ps.setString(18, dto.getSamttype());
  
               ps.setString(19, dto.getSstatus());
  
               ps.setString(20, dto.getSusercode());
  
               ps.setString(21, dto.getSdemo());
   
               ps.setString(22, dto.getSid());
  
               ps.setString(23, dto.getSfundtypecode());
  
               ps.setString(24, dto.getSfundtypename());
  
               ps.setString(25, dto.getSpayeeacctno());
  
               ps.setString(26, dto.getSpayeeacctname());
  
               ps.setString(27, dto.getSpayeeacctbankname());
  
               ps.setString(28, dto.getSpayacctno());
  
               ps.setString(29, dto.getSpayacctname());
  
               ps.setString(30, dto.getSpayacctbankname());
  
               ps.setString(31, dto.getSpaybankcode());
  
               ps.setString(32, dto.getSpaybankname());
  
               ps.setString(33, dto.getSpaybankno());
  
               ps.setString(34, dto.getSxacctdate());
  
               ps.setString(35, dto.getShold1());
  
               ps.setString(36, dto.getShold2());
               ps.addBatch(); 
           }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }
        return  ;
   }




     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto find(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TvDirectpaymsgmainSxPK pk = (TvDirectpaymsgmainSxPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT);
           if (pk.getIvousrlno()==null)
               ps.setNull(1, java.sql.Types.BIGINT);
           else
               ps.setLong(1, pk.getIvousrlno().longValue());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }

     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto findForUpdate(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TvDirectpaymsgmainSxPK pk = (TvDirectpaymsgmainSxPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT_FOR_UPDATE);
           if (pk.getIvousrlno()==null)
               ps.setNull(1, java.sql.Types.BIGINT);
           else
               ps.setLong(1, pk.getIvousrlno().longValue());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }
  
     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto[] find(IPK[] _pks, Connection conn, boolean isLobSupport) throws SQLException
    {
        TvDirectpaymsgmainSxPK pk ;
        
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TvDirectpaymsgmainSxPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 
        PreparedStatement ps = null;
        ResultSet rs = null;
        
         
        List results = new ArrayList();  
		for (int iBegin = 0; iBegin < pks.size(); iBegin += FIND_BATCH_SIZE)   //
		{
        
            try {
          		int iFindBatchSize = pks.size() - iBegin ;
	    		if (iFindBatchSize >FIND_BATCH_SIZE )
		    		iFindBatchSize = FIND_BATCH_SIZE ;
		  
        	    StringBuffer sb = new StringBuffer() ;
            	sb.append(SQL_SELECT_BATCH).append(" WHERE ").append(SQL_SELECT_BATCH_WHERE);

	    		for (int i = iBegin+1; i < iBegin + iFindBatchSize; i++)
		    	{
			    	sb.append("OR").append(SQL_SELECT_BATCH_WHERE) ;
    			}
        	
                ps = conn.prepareStatement(sb.toString());

                for (int i = iBegin;i < iBegin + iFindBatchSize; i++)
                {
                    pk  = (TvDirectpaymsgmainSxPK)(pks.get(i)) ; 
                   if (pk.getIvousrlno()==null)
                      ps.setNull((i-iBegin)*1+1, java.sql.Types.BIGINT);
                   else
                      ps.setLong((i-iBegin)*1+1, pk.getIvousrlno().longValue());

			
	            }
                rs = ps.executeQuery();
                results.addAll(getResults(rs,0,isLobSupport)); //
            }finally {
                close(rs);
                close(ps);
            }
        }
        if (results!=null && results.size() > 0)
        {
            TvDirectpaymsgmainSxDto[] dtos = new TvDirectpaymsgmainSxDto[0];
		    dtos = (TvDirectpaymsgmainSxDto[]) results.toArray(dtos) ;
		    return dtos ;
        }
        else
        {
            return null;
        }
    }

  
  
  



    /**
	    * 得到查询语句
	    */
    public String getSelectSQL() 
    {
	        return SQL_SELECT_BATCH_SCROLLABLE ;
    }
   

	public List getResults(ResultSet rs) throws SQLException
    {
    	return getResults(rs, 0) ;
    }
  

    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize) throws SQLException
    {
		return getResults(rs,maxSize,false);
	}
    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize, boolean isLobSupport) throws SQLException
    {
        List results = new ArrayList();
        String str ;
        Reader reader;
        Clob clob;
        char[] chars ;
        
        while (rs.next())
         {
             TvDirectpaymsgmainSxDto  dto = new  TvDirectpaymsgmainSxDto ();
             //I_VOUSRLNO
             str = rs.getString("I_VOUSRLNO");
             if(str!=null){
                dto.setIvousrlno(new Long(str));
             }

             //S_ORGCODE
             str = rs.getString("S_ORGCODE");
             if (str == null)
                dto.setSorgcode(null);
             else
                dto.setSorgcode(str.trim());

             //S_COMMITDATE
             str = rs.getString("S_COMMITDATE");
             if (str == null)
                dto.setScommitdate(null);
             else
                dto.setScommitdate(str.trim());

             //S_ACCDATE
             str = rs.getString("S_ACCDATE");
             if (str == null)
                dto.setSaccdate(null);
             else
                dto.setSaccdate(str.trim());

             //S_FILENAME
             str = rs.getString("S_FILENAME");
             if (str == null)
                dto.setSfilename(null);
             else
                dto.setSfilename(str.trim());

             //S_PACKAGENO
             str = rs.getString("S_PACKAGENO");
             if (str == null)
                dto.setSpackageno(null);
             else
                dto.setSpackageno(str.trim());

             //S_TRECODE
             str = rs.getString("S_TRECODE");
             if (str == null)
                dto.setStrecode(null);
             else
                dto.setStrecode(str.trim());

             //S_PAYUNIT
             str = rs.getString("S_PAYUNIT");
             if (str == null)
                dto.setSpayunit(null);
             else
                dto.setSpayunit(str.trim());

             //S_TRANSBANKCODE
             str = rs.getString("S_TRANSBANKCODE");
             if (str == null)
                dto.setStransbankcode(null);
             else
                dto.setStransbankcode(str.trim());

             //S_DEALNO
             str = rs.getString("S_DEALNO");
             if (str == null)
                dto.setSdealno(null);
             else
                dto.setSdealno(str.trim());

             //N_MONEY
           dto.setNmoney(rs.getBigDecimal("N_MONEY"));

             //S_TAXTICKETNO
             str = rs.getString("S_TAXTICKETNO");
             if (str == null)
                dto.setStaxticketno(null);
             else
                dto.setStaxticketno(str.trim());

             //S_PACKAGETICKETNO
             str = rs.getString("S_PACKAGETICKETNO");
             if (str == null)
                dto.setSpackageticketno(null);
             else
                dto.setSpackageticketno(str.trim());

             //S_GENTICKETDATE
             str = rs.getString("S_GENTICKETDATE");
             if (str == null)
                dto.setSgenticketdate(null);
             else
                dto.setSgenticketdate(str.trim());

             //S_BUDGETTYPE
             str = rs.getString("S_BUDGETTYPE");
             if (str == null)
                dto.setSbudgettype(null);
             else
                dto.setSbudgettype(str.trim());

             //S_OFYEAR
             str = rs.getString("S_OFYEAR");
             if (str == null)
                dto.setSofyear(null);
             else
                dto.setSofyear(str.trim());

             //S_TRANSACTUNIT
             str = rs.getString("S_TRANSACTUNIT");
             if (str == null)
                dto.setStransactunit(null);
             else
                dto.setStransactunit(str.trim());

             //S_AMTTYPE
             str = rs.getString("S_AMTTYPE");
             if (str == null)
                dto.setSamttype(null);
             else
                dto.setSamttype(str.trim());

             //S_STATUS
             str = rs.getString("S_STATUS");
             if (str == null)
                dto.setSstatus(null);
             else
                dto.setSstatus(str.trim());

             //S_USERCODE
             str = rs.getString("S_USERCODE");
             if (str == null)
                dto.setSusercode(null);
             else
                dto.setSusercode(str.trim());

             //S_DEMO
             str = rs.getString("S_DEMO");
             if (str == null)
                dto.setSdemo(null);
             else
                dto.setSdemo(str.trim());

             //TS_SYSUPDATE
           dto.setTssysupdate(rs.getTimestamp("TS_SYSUPDATE"));

             //S_ID
             str = rs.getString("S_ID");
             if (str == null)
                dto.setSid(null);
             else
                dto.setSid(str.trim());

             //S_FUNDTYPECODE
             str = rs.getString("S_FUNDTYPECODE");
             if (str == null)
                dto.setSfundtypecode(null);
             else
                dto.setSfundtypecode(str.trim());

             //S_FUNDTYPENAME
             str = rs.getString("S_FUNDTYPENAME");
             if (str == null)
                dto.setSfundtypename(null);
             else
                dto.setSfundtypename(str.trim());

             //S_PAYEEACCTNO
             str = rs.getString("S_PAYEEACCTNO");
             if (str == null)
                dto.setSpayeeacctno(null);
             else
                dto.setSpayeeacctno(str.trim());

             //S_PAYEEACCTNAME
             str = rs.getString("S_PAYEEACCTNAME");
             if (str == null)
                dto.setSpayeeacctname(null);
             else
                dto.setSpayeeacctname(str.trim());

             //S_PAYEEACCTBANKNAME
             str = rs.getString("S_PAYEEACCTBANKNAME");
             if (str == null)
                dto.setSpayeeacctbankname(null);
             else
                dto.setSpayeeacctbankname(str.trim());

             //S_PAYACCTNO
             str = rs.getString("S_PAYACCTNO");
             if (str == null)
                dto.setSpayacctno(null);
             else
                dto.setSpayacctno(str.trim());

             //S_PAYACCTNAME
             str = rs.getString("S_PAYACCTNAME");
             if (str == null)
                dto.setSpayacctname(null);
             else
                dto.setSpayacctname(str.trim());

             //S_PAYACCTBANKNAME
             str = rs.getString("S_PAYACCTBANKNAME");
             if (str == null)
                dto.setSpayacctbankname(null);
             else
                dto.setSpayacctbankname(str.trim());

             //S_PAYBANKCODE
             str = rs.getString("S_PAYBANKCODE");
             if (str == null)
                dto.setSpaybankcode(null);
             else
                dto.setSpaybankcode(str.trim());

             //S_PAYBANKNAME
             str = rs.getString("S_PAYBANKNAME");
             if (str == null)
                dto.setSpaybankname(null);
             else
                dto.setSpaybankname(str.trim());

             //S_PAYBANKNO
             str = rs.getString("S_PAYBANKNO");
             if (str == null)
                dto.setSpaybankno(null);
             else
                dto.setSpaybankno(str.trim());

             //S_XACCTDATE
             str = rs.getString("S_XACCTDATE");
             if (str == null)
                dto.setSxacctdate(null);
             else
                dto.setSxacctdate(str.trim());

             //S_HOLD1
             str = rs.getString("S_HOLD1");
             if (str == null)
                dto.setShold1(null);
             else
                dto.setShold1(str.trim());

             //S_HOLD2
             str = rs.getString("S_HOLD2");
             if (str == null)
                dto.setShold2(null);
             else
                dto.setShold2(str.trim());



            results.add(dto);
            
            if(maxSize >0  && results.size() == maxSize)
            {
            	break ;
            }
            
         }
        
		return results ;
    }

  /**
   * populate resultSet as cclass
   */
  protected IDto[] getChildrenResults(ResultSet rs ,Class _dto) throws SQLException{

      throw new SQLException("本dto没有与其它dto相关联，无法应用此方法");
  }


     /**
     * Update a record in Database.
     */

    public void update(IDto _dto, Connection conn, boolean isLobSupport) throws SQLException
    {
        String msgValid = _dto.checkValid() ;
        if (msgValid != null)
            throw new SQLException("修改错误，"+msgValid) ;
        TvDirectpaymsgmainSxDto dto = (TvDirectpaymsgmainSxDto)_dto ;
        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            int pos = 1;
            //S_ORGCODE
            ps.setString(pos, dto.getSorgcode());
            pos++;

            //S_COMMITDATE
            ps.setString(pos, dto.getScommitdate());
            pos++;

            //S_ACCDATE
            ps.setString(pos, dto.getSaccdate());
            pos++;

            //S_FILENAME
            ps.setString(pos, dto.getSfilename());
            pos++;

            //S_PACKAGENO
            ps.setString(pos, dto.getSpackageno());
            pos++;

            //S_TRECODE
            ps.setString(pos, dto.getStrecode());
            pos++;

            //S_PAYUNIT
            ps.setString(pos, dto.getSpayunit());
            pos++;

            //S_TRANSBANKCODE
            ps.setString(pos, dto.getStransbankcode());
            pos++;

            //S_DEALNO
            ps.setString(pos, dto.getSdealno());
            pos++;

            //N_MONEY
            ps.setBigDecimal(pos, dto.getNmoney());
            pos++;

            //S_TAXTICKETNO
            ps.setString(pos, dto.getStaxticketno());
            pos++;

            //S_PACKAGETICKETNO
            ps.setString(pos, dto.getSpackageticketno());
            pos++;

            //S_GENTICKETDATE
            ps.setString(pos, dto.getSgenticketdate());
            pos++;

            //S_BUDGETTYPE
            ps.setString(pos, dto.getSbudgettype());
            pos++;

            //S_OFYEAR
            ps.setString(pos, dto.getSofyear());
            pos++;

            //S_TRANSACTUNIT
            ps.setString(pos, dto.getStransactunit());
            pos++;

            //S_AMTTYPE
            ps.setString(pos, dto.getSamttype());
            pos++;

            //S_STATUS
            ps.setString(pos, dto.getSstatus());
            pos++;

            //S_USERCODE
            ps.setString(pos, dto.getSusercode());
            pos++;

            //S_DEMO
            ps.setString(pos, dto.getSdemo());
            pos++;

            //TS_SYSUPDATE
            //S_ID
            ps.setString(pos, dto.getSid());
            pos++;

            //S_FUNDTYPECODE
            ps.setString(pos, dto.getSfundtypecode());
            pos++;

            //S_FUNDTYPENAME
            ps.setString(pos, dto.getSfundtypename());
            pos++;

            //S_PAYEEACCTNO
            ps.setString(pos, dto.getSpayeeacctno());
            pos++;

            //S_PAYEEACCTNAME
            ps.setString(pos, dto.getSpayeeacctname());
            pos++;

            //S_PAYEEACCTBANKNAME
            ps.setString(pos, dto.getSpayeeacctbankname());
            pos++;

            //S_PAYACCTNO
            ps.setString(pos, dto.getSpayacctno());
            pos++;

            //S_PAYACCTNAME
            ps.setString(pos, dto.getSpayacctname());
            pos++;

            //S_PAYACCTBANKNAME
            ps.setString(pos, dto.getSpayacctbankname());
            pos++;

            //S_PAYBANKCODE
            ps.setString(pos, dto.getSpaybankcode());
            pos++;

            //S_PAYBANKNAME
            ps.setString(pos, dto.getSpaybankname());
            pos++;

            //S_PAYBANKNO
            ps.setString(pos, dto.getSpaybankno());
            pos++;

            //S_XACCTDATE
            ps.setString(pos, dto.getSxacctdate());
            pos++;

            //S_HOLD1
            ps.setString(pos, dto.getShold1());
            pos++;

            //S_HOLD2
            ps.setString(pos, dto.getShold2());
            pos++;


           //I_VOUSRLNO
           ps.setLong(pos, dto.getIvousrlno().longValue());
           pos++;
           ps.executeUpdate();
        }
        finally
        {
           close(ps);
        }

        return ;
    }
    
    
    
      /**
     * Update batch record in Database.
     */

    public void update(IDto[] _dtos, Connection conn, boolean isLobSupport) throws SQLException
    {
     
     	 TvDirectpaymsgmainSxDto dto  ;
         for (int i= 0; i< _dtos.length ; i++)
         {
            dto  = (TvDirectpaymsgmainSxDto)_dtos[i] ; 
            String msgValid = dto.checkValid() ;
            if (msgValid != null)
                throw new SQLException("修改错误，"+msgValid) ;
         }

        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            
            for (int i= 0; i< _dtos.length ; i++)
            {
                dto  = (TvDirectpaymsgmainSxDto)_dtos[i] ; 
                int pos = 1;
                //S_ORGCODE
                 ps.setString(pos, dto.getSorgcode());
                pos++;

                //S_COMMITDATE
                 ps.setString(pos, dto.getScommitdate());
                pos++;

                //S_ACCDATE
                 ps.setString(pos, dto.getSaccdate());
                pos++;

                //S_FILENAME
                 ps.setString(pos, dto.getSfilename());
                pos++;

                //S_PACKAGENO
                 ps.setString(pos, dto.getSpackageno());
                pos++;

                //S_TRECODE
                 ps.setString(pos, dto.getStrecode());
                pos++;

                //S_PAYUNIT
                 ps.setString(pos, dto.getSpayunit());
                pos++;

                //S_TRANSBANKCODE
                 ps.setString(pos, dto.getStransbankcode());
                pos++;

                //S_DEALNO
                 ps.setString(pos, dto.getSdealno());
                pos++;

                //N_MONEY
                 ps.setBigDecimal(pos, dto.getNmoney());
                pos++;

                //S_TAXTICKETNO
                 ps.setString(pos, dto.getStaxticketno());
                pos++;

                //S_PACKAGETICKETNO
                 ps.setString(pos, dto.getSpackageticketno());
                pos++;

                //S_GENTICKETDATE
                 ps.setString(pos, dto.getSgenticketdate());
                pos++;

                //S_BUDGETTYPE
                 ps.setString(pos, dto.getSbudgettype());
                pos++;

                //S_OFYEAR
                 ps.setString(pos, dto.getSofyear());
                pos++;

                //S_TRANSACTUNIT
                 ps.setString(pos, dto.getStransactunit());
                pos++;

                //S_AMTTYPE
                 ps.setString(pos, dto.getSamttype());
                pos++;

                //S_STATUS
                 ps.setString(pos, dto.getSstatus());
                pos++;

                //S_USERCODE
                 ps.setString(pos, dto.getSusercode());
                pos++;

                //S_DEMO
                 ps.setString(pos, dto.getSdemo());
                pos++;

                //TS_SYSUPDATE
                 //S_ID
                 ps.setString(pos, dto.getSid());
                pos++;

                //S_FUNDTYPECODE
                 ps.setString(pos, dto.getSfundtypecode());
                pos++;

                //S_FUNDTYPENAME
                 ps.setString(pos, dto.getSfundtypename());
                pos++;

                //S_PAYEEACCTNO
                 ps.setString(pos, dto.getSpayeeacctno());
                pos++;

                //S_PAYEEACCTNAME
                 ps.setString(pos, dto.getSpayeeacctname());
                pos++;

                //S_PAYEEACCTBANKNAME
                 ps.setString(pos, dto.getSpayeeacctbankname());
                pos++;

                //S_PAYACCTNO
                 ps.setString(pos, dto.getSpayacctno());
                pos++;

                //S_PAYACCTNAME
                 ps.setString(pos, dto.getSpayacctname());
                pos++;

                //S_PAYACCTBANKNAME
                 ps.setString(pos, dto.getSpayacctbankname());
                pos++;

                //S_PAYBANKCODE
                 ps.setString(pos, dto.getSpaybankcode());
                pos++;

                //S_PAYBANKNAME
                 ps.setString(pos, dto.getSpaybankname());
                pos++;

                //S_PAYBANKNO
                 ps.setString(pos, dto.getSpaybankno());
                pos++;

                //S_XACCTDATE
                 ps.setString(pos, dto.getSxacctdate());
                pos++;

                //S_HOLD1
                 ps.setString(pos, dto.getShold1());
                pos++;

                //S_HOLD2
                 ps.setString(pos, dto.getShold2());
                pos++;


               //I_VOUSRLNO
               ps.setLong(pos, dto.getIvousrlno().longValue());
               pos++;
		       ps.addBatch(); 
	       }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }

        return ;
    }



    /**
     * delete a record in Database.
     */
    public void delete(IPK _pk, Connection conn) throws SQLException
    {
       
      
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return ;
        //	throw new SQLException("删除错误，"+msgValid) ;
       }
       TvDirectpaymsgmainSxPK pk = (TvDirectpaymsgmainSxPK)_pk ;


       PreparedStatement ps = null;
       try {
           ps = conn.prepareStatement(SQL_DELETE);
           ps.setLong(1, pk.getIvousrlno().longValue());
           ps.executeUpdate();
        }
        finally
        {
            close(ps);
        }
    }


     /**
     * delete batch record in Database.
     */
    public void delete(IPK[] _pks, Connection conn) throws SQLException
    {
        TvDirectpaymsgmainSxPK pk ;
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TvDirectpaymsgmainSxPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 		
 		
        PreparedStatement ps = null;
        try
         {
            ps = conn.prepareStatement(SQL_DELETE);
             
       		for (int i= 0; i< pks.size() ; i++)
       		{
       			pk  = (TvDirectpaymsgmainSxPK)(pks.get(i)) ; 
                ps.setLong(1, pk.getIvousrlno().longValue());
		        ps.addBatch() ;
			}
            ps.executeBatch();
        }
        finally
        {
            close(ps);
        }      
    }



    /**
    * Check IChangerNumber or UpdateTimeStamp.
    */
    public void check(IDto _dto, Connection conn) throws SQLException
    {
                                                                                                            		throw new SQLException("数据库表：TV_DIRECTPAYMSGMAIN_SX没有检查修改的字段");
    }


	/**
	* synchronize values between parent and children
	*/
    public void syncToChildren(IDto _dto)     throws SQLException{
       throw new SQLException("本记录没有与其它记录相关联，无法进行主记录与明细记录之间的数据同步。");
  }

	/**
	*get children
	*/
    public IDto[] findChildren (IDto _dto, Connection conn, boolean isLobSupport)     throws SQLException{
         throw new SQLException("本dto没有与其它dto相关联，无法得到子dto");
  }    
    
  /**
   *return the children IDto class
   * @return Class
   */
  public Class getChildrenClass(){
	  return null;
  };    



    /**
     * Close JDBC Statement.
     * @param stmt  Statement to be closed.
     */
    private void close(Statement stmt)
    {
        if (stmt != null)
        {
            try
            {
                stmt.close();
            }
            catch(SQLException    e)
            {}
        }
    }

    /**
     * Close JDBC ResultSet.
     * @param rs  ResultSet to be closed.
     */
    private void close(ResultSet rs)
    {
        if (rs != null)
        {
            try
            {
                rs.close();
            }catch(SQLException  e){}
        }
    }
}
