    



package com.cfcc.itfe.persistence.dao;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.Statement;
import java.sql.SQLException;
import java.sql.Date;
import java.sql.Time;
import java.sql.Timestamp ;
import java.sql.Clob;

import com.cfcc.jaf.persistence.jaform.parent.* ;

import java.math.BigDecimal;

import java.util.List;
import java.util.ArrayList;
import java.util.HashMap;

import java.io.StringReader;
import java.io.Reader;
import java.io.IOException;

import com.cfcc.itfe.persistence.dto.HtfPaybankRefundmainDto ;
import com.cfcc.itfe.persistence.pk.HtfPaybankRefundmainPK ;


/**
 * <p>Title: DAO of table: HTF_PAYBANK_REFUNDMAIN</p>
 * <p>Description: Data Access Object  </p>
 * <p>Copyright: Copyright (c) 2005</p>
 * <p>Company: CFCC </p>
 * <p>Generation Time: 2014-12-25 15:28:54 </p>
 *  Do NOT change this file by hand.
 *  This java source file is generated by JAF_ORM .
 *  dao.vm timestamp: 2007-04-06 08:30:00
 ******************************************************
 * <p> <b>change record</b><br>
 *  make dao methods simply.<br>
 *  especially make update,delete methods simply.<br>
 *  remove "..withCheck" method,withCheck will impl in JAFOrmTemplate.<br>
 *  remove "delete(IDto _dto,..)" method.<br></p>
 * @author win7
 */

public class HtfPaybankRefundmainDao  implements IDao
{


    /* SQL to insert data */
    private static final String SQL_INSERT =
        "INSERT INTO HTF_PAYBANK_REFUNDMAIN ("
          + "I_VOUSRLNO,S_ORGCODE,S_TRECODE,S_COMMITDATE,S_DEALNO"
          + ",S_FINORGCODE,S_PACKAGENO,S_FILENAME,S_STATUS,S_DEMO"
          + ",TS_SYSUPDATE,S_ID,S_ADMDIVCODE,S_STYEAR,S_VTCODE"
          + ",S_VOUDATE,S_VOUCHERNO,S_ORIBILLNO,S_PAYTYPECODE,S_PAYTYPENAME"
          + ",S_ORIGINALVOUCHERNO,N_PAYAMT,S_PAYDICTATENO,S_PAYMSGNO,S_PAYENTRUSTDATE"
          + ",S_PAYSNDBNKNO,S_REMARK,S_PAYDATE,S_BACKFLAG,S_ISRETURN"
          + ",S_BCKREASON,S_REFUNDTYPE,N_BACKMONEY,S_HOLD1,S_HOLD2"
          + ",S_EXT1,S_EXT2,S_EXT3,S_EXT4,S_EXT5"
        + ") VALUES ("
        + "?,?,?,?,?,?,?,?,?,?,CURRENT TIMESTAMP ,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)";


    private static final String SQL_INSERT_WITH_RESULT = "SELECT * FROM FINAL TABLE( " + SQL_INSERT + " )";
    
    /* SQL to select data */
    private static final String SQL_SELECT =
        "SELECT "
        + "HTF_PAYBANK_REFUNDMAIN.I_VOUSRLNO, HTF_PAYBANK_REFUNDMAIN.S_ORGCODE, HTF_PAYBANK_REFUNDMAIN.S_TRECODE, HTF_PAYBANK_REFUNDMAIN.S_COMMITDATE, HTF_PAYBANK_REFUNDMAIN.S_DEALNO, "
        + "HTF_PAYBANK_REFUNDMAIN.S_FINORGCODE, HTF_PAYBANK_REFUNDMAIN.S_PACKAGENO, HTF_PAYBANK_REFUNDMAIN.S_FILENAME, HTF_PAYBANK_REFUNDMAIN.S_STATUS, HTF_PAYBANK_REFUNDMAIN.S_DEMO, "
        + "HTF_PAYBANK_REFUNDMAIN.TS_SYSUPDATE, HTF_PAYBANK_REFUNDMAIN.S_ID, HTF_PAYBANK_REFUNDMAIN.S_ADMDIVCODE, HTF_PAYBANK_REFUNDMAIN.S_STYEAR, HTF_PAYBANK_REFUNDMAIN.S_VTCODE, "
        + "HTF_PAYBANK_REFUNDMAIN.S_VOUDATE, HTF_PAYBANK_REFUNDMAIN.S_VOUCHERNO, HTF_PAYBANK_REFUNDMAIN.S_ORIBILLNO, HTF_PAYBANK_REFUNDMAIN.S_PAYTYPECODE, HTF_PAYBANK_REFUNDMAIN.S_PAYTYPENAME, "
        + "HTF_PAYBANK_REFUNDMAIN.S_ORIGINALVOUCHERNO, HTF_PAYBANK_REFUNDMAIN.N_PAYAMT, HTF_PAYBANK_REFUNDMAIN.S_PAYDICTATENO, HTF_PAYBANK_REFUNDMAIN.S_PAYMSGNO, HTF_PAYBANK_REFUNDMAIN.S_PAYENTRUSTDATE, "
        + "HTF_PAYBANK_REFUNDMAIN.S_PAYSNDBNKNO, HTF_PAYBANK_REFUNDMAIN.S_REMARK, HTF_PAYBANK_REFUNDMAIN.S_PAYDATE, HTF_PAYBANK_REFUNDMAIN.S_BACKFLAG, HTF_PAYBANK_REFUNDMAIN.S_ISRETURN, "
        + "HTF_PAYBANK_REFUNDMAIN.S_BCKREASON, HTF_PAYBANK_REFUNDMAIN.S_REFUNDTYPE, HTF_PAYBANK_REFUNDMAIN.N_BACKMONEY, HTF_PAYBANK_REFUNDMAIN.S_HOLD1, HTF_PAYBANK_REFUNDMAIN.S_HOLD2, "
        + "HTF_PAYBANK_REFUNDMAIN.S_EXT1, HTF_PAYBANK_REFUNDMAIN.S_EXT2, HTF_PAYBANK_REFUNDMAIN.S_EXT3, HTF_PAYBANK_REFUNDMAIN.S_EXT4, HTF_PAYBANK_REFUNDMAIN.S_EXT5 "
        + "FROM HTF_PAYBANK_REFUNDMAIN "
        +" WHERE " 
        + "I_VOUSRLNO = ?"
        ;
    /* SQL to select for update data */
    private static final String SQL_SELECT_FOR_UPDATE =
        "SELECT "
        + "HTF_PAYBANK_REFUNDMAIN.I_VOUSRLNO, HTF_PAYBANK_REFUNDMAIN.S_ORGCODE, HTF_PAYBANK_REFUNDMAIN.S_TRECODE, HTF_PAYBANK_REFUNDMAIN.S_COMMITDATE, HTF_PAYBANK_REFUNDMAIN.S_DEALNO, "
        + "HTF_PAYBANK_REFUNDMAIN.S_FINORGCODE, HTF_PAYBANK_REFUNDMAIN.S_PACKAGENO, HTF_PAYBANK_REFUNDMAIN.S_FILENAME, HTF_PAYBANK_REFUNDMAIN.S_STATUS, HTF_PAYBANK_REFUNDMAIN.S_DEMO, "
        + "HTF_PAYBANK_REFUNDMAIN.TS_SYSUPDATE, HTF_PAYBANK_REFUNDMAIN.S_ID, HTF_PAYBANK_REFUNDMAIN.S_ADMDIVCODE, HTF_PAYBANK_REFUNDMAIN.S_STYEAR, HTF_PAYBANK_REFUNDMAIN.S_VTCODE, "
        + "HTF_PAYBANK_REFUNDMAIN.S_VOUDATE, HTF_PAYBANK_REFUNDMAIN.S_VOUCHERNO, HTF_PAYBANK_REFUNDMAIN.S_ORIBILLNO, HTF_PAYBANK_REFUNDMAIN.S_PAYTYPECODE, HTF_PAYBANK_REFUNDMAIN.S_PAYTYPENAME, "
        + "HTF_PAYBANK_REFUNDMAIN.S_ORIGINALVOUCHERNO, HTF_PAYBANK_REFUNDMAIN.N_PAYAMT, HTF_PAYBANK_REFUNDMAIN.S_PAYDICTATENO, HTF_PAYBANK_REFUNDMAIN.S_PAYMSGNO, HTF_PAYBANK_REFUNDMAIN.S_PAYENTRUSTDATE, "
        + "HTF_PAYBANK_REFUNDMAIN.S_PAYSNDBNKNO, HTF_PAYBANK_REFUNDMAIN.S_REMARK, HTF_PAYBANK_REFUNDMAIN.S_PAYDATE, HTF_PAYBANK_REFUNDMAIN.S_BACKFLAG, HTF_PAYBANK_REFUNDMAIN.S_ISRETURN, "
        + "HTF_PAYBANK_REFUNDMAIN.S_BCKREASON, HTF_PAYBANK_REFUNDMAIN.S_REFUNDTYPE, HTF_PAYBANK_REFUNDMAIN.N_BACKMONEY, HTF_PAYBANK_REFUNDMAIN.S_HOLD1, HTF_PAYBANK_REFUNDMAIN.S_HOLD2, "
        + "HTF_PAYBANK_REFUNDMAIN.S_EXT1, HTF_PAYBANK_REFUNDMAIN.S_EXT2, HTF_PAYBANK_REFUNDMAIN.S_EXT3, HTF_PAYBANK_REFUNDMAIN.S_EXT4, HTF_PAYBANK_REFUNDMAIN.S_EXT5 "
        + "FROM HTF_PAYBANK_REFUNDMAIN "
        +" WHERE " 
        + "I_VOUSRLNO = ? FOR UPDATE"
        ;

      /* SQL to select data (SCROLLABLE)*/
     private static final String SQL_SELECT_BATCH_SCROLLABLE =
        "SELECT "
        + "  HTF_PAYBANK_REFUNDMAIN.I_VOUSRLNO  , HTF_PAYBANK_REFUNDMAIN.S_ORGCODE  , HTF_PAYBANK_REFUNDMAIN.S_TRECODE  , HTF_PAYBANK_REFUNDMAIN.S_COMMITDATE  , HTF_PAYBANK_REFUNDMAIN.S_DEALNO "
        + " , HTF_PAYBANK_REFUNDMAIN.S_FINORGCODE  , HTF_PAYBANK_REFUNDMAIN.S_PACKAGENO  , HTF_PAYBANK_REFUNDMAIN.S_FILENAME  , HTF_PAYBANK_REFUNDMAIN.S_STATUS  , HTF_PAYBANK_REFUNDMAIN.S_DEMO "
        + " , HTF_PAYBANK_REFUNDMAIN.TS_SYSUPDATE  , HTF_PAYBANK_REFUNDMAIN.S_ID  , HTF_PAYBANK_REFUNDMAIN.S_ADMDIVCODE  , HTF_PAYBANK_REFUNDMAIN.S_STYEAR  , HTF_PAYBANK_REFUNDMAIN.S_VTCODE "
        + " , HTF_PAYBANK_REFUNDMAIN.S_VOUDATE  , HTF_PAYBANK_REFUNDMAIN.S_VOUCHERNO  , HTF_PAYBANK_REFUNDMAIN.S_ORIBILLNO  , HTF_PAYBANK_REFUNDMAIN.S_PAYTYPECODE  , HTF_PAYBANK_REFUNDMAIN.S_PAYTYPENAME "
        + " , HTF_PAYBANK_REFUNDMAIN.S_ORIGINALVOUCHERNO  , HTF_PAYBANK_REFUNDMAIN.N_PAYAMT  , HTF_PAYBANK_REFUNDMAIN.S_PAYDICTATENO  , HTF_PAYBANK_REFUNDMAIN.S_PAYMSGNO  , HTF_PAYBANK_REFUNDMAIN.S_PAYENTRUSTDATE "
        + " , HTF_PAYBANK_REFUNDMAIN.S_PAYSNDBNKNO  , HTF_PAYBANK_REFUNDMAIN.S_REMARK  , HTF_PAYBANK_REFUNDMAIN.S_PAYDATE  , HTF_PAYBANK_REFUNDMAIN.S_BACKFLAG  , HTF_PAYBANK_REFUNDMAIN.S_ISRETURN "
        + " , HTF_PAYBANK_REFUNDMAIN.S_BCKREASON  , HTF_PAYBANK_REFUNDMAIN.S_REFUNDTYPE  , HTF_PAYBANK_REFUNDMAIN.N_BACKMONEY  , HTF_PAYBANK_REFUNDMAIN.S_HOLD1  , HTF_PAYBANK_REFUNDMAIN.S_HOLD2 "
        + " , HTF_PAYBANK_REFUNDMAIN.S_EXT1  , HTF_PAYBANK_REFUNDMAIN.S_EXT2  , HTF_PAYBANK_REFUNDMAIN.S_EXT3  , HTF_PAYBANK_REFUNDMAIN.S_EXT4  , HTF_PAYBANK_REFUNDMAIN.S_EXT5 "
        + "FROM HTF_PAYBANK_REFUNDMAIN ";



    /* SQL to select batch data */
    private static final String SQL_SELECT_BATCH =
        "SELECT "
        + "HTF_PAYBANK_REFUNDMAIN.I_VOUSRLNO, HTF_PAYBANK_REFUNDMAIN.S_ORGCODE, HTF_PAYBANK_REFUNDMAIN.S_TRECODE, HTF_PAYBANK_REFUNDMAIN.S_COMMITDATE, HTF_PAYBANK_REFUNDMAIN.S_DEALNO, "
        + "HTF_PAYBANK_REFUNDMAIN.S_FINORGCODE, HTF_PAYBANK_REFUNDMAIN.S_PACKAGENO, HTF_PAYBANK_REFUNDMAIN.S_FILENAME, HTF_PAYBANK_REFUNDMAIN.S_STATUS, HTF_PAYBANK_REFUNDMAIN.S_DEMO, "
        + "HTF_PAYBANK_REFUNDMAIN.TS_SYSUPDATE, HTF_PAYBANK_REFUNDMAIN.S_ID, HTF_PAYBANK_REFUNDMAIN.S_ADMDIVCODE, HTF_PAYBANK_REFUNDMAIN.S_STYEAR, HTF_PAYBANK_REFUNDMAIN.S_VTCODE, "
        + "HTF_PAYBANK_REFUNDMAIN.S_VOUDATE, HTF_PAYBANK_REFUNDMAIN.S_VOUCHERNO, HTF_PAYBANK_REFUNDMAIN.S_ORIBILLNO, HTF_PAYBANK_REFUNDMAIN.S_PAYTYPECODE, HTF_PAYBANK_REFUNDMAIN.S_PAYTYPENAME, "
        + "HTF_PAYBANK_REFUNDMAIN.S_ORIGINALVOUCHERNO, HTF_PAYBANK_REFUNDMAIN.N_PAYAMT, HTF_PAYBANK_REFUNDMAIN.S_PAYDICTATENO, HTF_PAYBANK_REFUNDMAIN.S_PAYMSGNO, HTF_PAYBANK_REFUNDMAIN.S_PAYENTRUSTDATE, "
        + "HTF_PAYBANK_REFUNDMAIN.S_PAYSNDBNKNO, HTF_PAYBANK_REFUNDMAIN.S_REMARK, HTF_PAYBANK_REFUNDMAIN.S_PAYDATE, HTF_PAYBANK_REFUNDMAIN.S_BACKFLAG, HTF_PAYBANK_REFUNDMAIN.S_ISRETURN, "
        + "HTF_PAYBANK_REFUNDMAIN.S_BCKREASON, HTF_PAYBANK_REFUNDMAIN.S_REFUNDTYPE, HTF_PAYBANK_REFUNDMAIN.N_BACKMONEY, HTF_PAYBANK_REFUNDMAIN.S_HOLD1, HTF_PAYBANK_REFUNDMAIN.S_HOLD2, "
        + "HTF_PAYBANK_REFUNDMAIN.S_EXT1, HTF_PAYBANK_REFUNDMAIN.S_EXT2, HTF_PAYBANK_REFUNDMAIN.S_EXT3, HTF_PAYBANK_REFUNDMAIN.S_EXT4, HTF_PAYBANK_REFUNDMAIN.S_EXT5 "
        + "FROM HTF_PAYBANK_REFUNDMAIN " ;
        

   /* SQL to select batch data where condition  */
    private static final String SQL_SELECT_BATCH_WHERE =" ( "
        + "I_VOUSRLNO = ?)"
        ;


    /* SQL to update data */
    private static final String SQL_UPDATE =
        "UPDATE HTF_PAYBANK_REFUNDMAIN SET "
        + "S_ORGCODE =?,S_TRECODE =?,S_COMMITDATE =?,S_DEALNO =?,S_FINORGCODE =?, "
        + "S_PACKAGENO =?,S_FILENAME =?,S_STATUS =?,S_DEMO =?,TS_SYSUPDATE =CURRENT TIMESTAMP, "
        + "S_ID =?,S_ADMDIVCODE =?,S_STYEAR =?,S_VTCODE =?,S_VOUDATE =?, "
        + "S_VOUCHERNO =?,S_ORIBILLNO =?,S_PAYTYPECODE =?,S_PAYTYPENAME =?,S_ORIGINALVOUCHERNO =?, "
        + "N_PAYAMT =?,S_PAYDICTATENO =?,S_PAYMSGNO =?,S_PAYENTRUSTDATE =?,S_PAYSNDBNKNO =?, "
        + "S_REMARK =?,S_PAYDATE =?,S_BACKFLAG =?,S_ISRETURN =?,S_BCKREASON =?, "
        + "S_REFUNDTYPE =?,N_BACKMONEY =?,S_HOLD1 =?,S_HOLD2 =?,S_EXT1 =?, "
        + "S_EXT2 =?,S_EXT3 =?,S_EXT4 =?,S_EXT5 =? "
        + "WHERE "
        + "I_VOUSRLNO = ?"
        ;

	/* SQL to update data, support LOB */
    private static final String SQL_UPDATE_LOB =
        "UPDATE HTF_PAYBANK_REFUNDMAIN SET "
        + "S_ORGCODE =?, S_TRECODE =?, S_COMMITDATE =?, S_DEALNO =?, S_FINORGCODE =?,  "
        + "S_PACKAGENO =?, S_FILENAME =?, S_STATUS =?, S_DEMO =?, TS_SYSUPDATE =CURRENT TIMESTAMP,  "
        + "S_ID =?, S_ADMDIVCODE =?, S_STYEAR =?, S_VTCODE =?, S_VOUDATE =?,  "
        + "S_VOUCHERNO =?, S_ORIBILLNO =?, S_PAYTYPECODE =?, S_PAYTYPENAME =?, S_ORIGINALVOUCHERNO =?,  "
        + "N_PAYAMT =?, S_PAYDICTATENO =?, S_PAYMSGNO =?, S_PAYENTRUSTDATE =?, S_PAYSNDBNKNO =?,  "
        + "S_REMARK =?, S_PAYDATE =?, S_BACKFLAG =?, S_ISRETURN =?, S_BCKREASON =?,  "
        + "S_REFUNDTYPE =?, N_BACKMONEY =?, S_HOLD1 =?, S_HOLD2 =?, S_EXT1 =?,  "
        + "S_EXT2 =?, S_EXT3 =?, S_EXT4 =?, S_EXT5 =? "
        + "WHERE "
        + "I_VOUSRLNO = ?"
        ;	
	
    /* SQL to delete data */
    private static final String SQL_DELETE =
        "DELETE FROM HTF_PAYBANK_REFUNDMAIN " 
        + " WHERE "
        + "I_VOUSRLNO = ?"
        ;


	/**
	*  批量查询 一次最多查询的参数数量
	*/
	
	public static final int FIND_BATCH_SIZE = 150  / 1;
	



   /**
   * Create a new record in Database.
   */
  public void create(IDto _dto,  Connection conn) throws SQLException
  {
     HtfPaybankRefundmainDto dto = (HtfPaybankRefundmainDto)_dto ;
     String msgValid = dto.checkValid() ;
     if (msgValid != null)
     	throw new SQLException("插入错误，"+msgValid) ;

     PreparedStatement ps = null;
     try
     {
         ps = conn.prepareStatement(SQL_INSERT);
          if (dto.getIvousrlno()==null)
            ps.setNull(1, java.sql.Types.BIGINT);
         else
            ps.setLong(1, dto.getIvousrlno().longValue());
          ps.setString(2, dto.getSorgcode());

          ps.setString(3, dto.getStrecode());

          ps.setString(4, dto.getScommitdate());

          ps.setString(5, dto.getSdealno());

          ps.setString(6, dto.getSfinorgcode());

          ps.setString(7, dto.getSpackageno());

          ps.setString(8, dto.getSfilename());

          ps.setString(9, dto.getSstatus());

          ps.setString(10, dto.getSdemo());

           ps.setString(11, dto.getSid());

          ps.setString(12, dto.getSadmdivcode());

          ps.setString(13, dto.getSstyear());

          ps.setString(14, dto.getSvtcode());

          ps.setString(15, dto.getSvoudate());

          ps.setString(16, dto.getSvoucherno());

          ps.setString(17, dto.getSoribillno());

          ps.setString(18, dto.getSpaytypecode());

          ps.setString(19, dto.getSpaytypename());

          ps.setString(20, dto.getSoriginalvoucherno());

          ps.setBigDecimal(21, dto.getNpayamt());

          ps.setString(22, dto.getSpaydictateno());

          ps.setString(23, dto.getSpaymsgno());

          ps.setString(24, dto.getSpayentrustdate());

          ps.setString(25, dto.getSpaysndbnkno());

          ps.setString(26, dto.getSremark());

          ps.setString(27, dto.getSpaydate());

          ps.setString(28, dto.getSbackflag());

          ps.setString(29, dto.getSisreturn());

          ps.setString(30, dto.getSbckreason());

          ps.setString(31, dto.getSrefundtype());

          ps.setBigDecimal(32, dto.getNbackmoney());

          ps.setString(33, dto.getShold1());

          ps.setString(34, dto.getShold2());

          ps.setString(35, dto.getSext1());

          ps.setString(36, dto.getSext2());

          ps.setString(37, dto.getSext3());

          ps.setString(38, dto.getSext4());

          ps.setString(39, dto.getSext5());

         ps.executeUpdate();
     }
     finally
     {
         close(ps);
     }
     return  ;
  }

   /**
   * Create a new record in Database. and return result, only used when having generated key
   */
   public IDto createWithResult(IDto _dto,  Connection conn) throws SQLException
   {
       HtfPaybankRefundmainDto dto = (HtfPaybankRefundmainDto)_dto ;
       String msgValid = dto.checkValid() ;
       if (msgValid != null)
           throw new SQLException("插入错误，"+msgValid) ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try
       {
           ps = conn.prepareStatement(SQL_INSERT_WITH_RESULT);
            if (dto.getIvousrlno()==null)
              ps.setNull(1, java.sql.Types.BIGINT);
           else
              ps.setLong(1, dto.getIvousrlno().longValue());
            ps.setString(2, dto.getSorgcode());
            ps.setString(3, dto.getStrecode());
            ps.setString(4, dto.getScommitdate());
            ps.setString(5, dto.getSdealno());
            ps.setString(6, dto.getSfinorgcode());
            ps.setString(7, dto.getSpackageno());
            ps.setString(8, dto.getSfilename());
            ps.setString(9, dto.getSstatus());
            ps.setString(10, dto.getSdemo());
             ps.setString(11, dto.getSid());
            ps.setString(12, dto.getSadmdivcode());
            ps.setString(13, dto.getSstyear());
            ps.setString(14, dto.getSvtcode());
            ps.setString(15, dto.getSvoudate());
            ps.setString(16, dto.getSvoucherno());
            ps.setString(17, dto.getSoribillno());
            ps.setString(18, dto.getSpaytypecode());
            ps.setString(19, dto.getSpaytypename());
            ps.setString(20, dto.getSoriginalvoucherno());
            ps.setBigDecimal(21, dto.getNpayamt());
            ps.setString(22, dto.getSpaydictateno());
            ps.setString(23, dto.getSpaymsgno());
            ps.setString(24, dto.getSpayentrustdate());
            ps.setString(25, dto.getSpaysndbnkno());
            ps.setString(26, dto.getSremark());
            ps.setString(27, dto.getSpaydate());
            ps.setString(28, dto.getSbackflag());
            ps.setString(29, dto.getSisreturn());
            ps.setString(30, dto.getSbckreason());
            ps.setString(31, dto.getSrefundtype());
            ps.setBigDecimal(32, dto.getNbackmoney());
            ps.setString(33, dto.getShold1());
            ps.setString(34, dto.getShold2());
            ps.setString(35, dto.getSext1());
            ps.setString(36, dto.getSext2());
            ps.setString(37, dto.getSext3());
            ps.setString(38, dto.getSext4());
            ps.setString(39, dto.getSext5());
           rs = ps.executeQuery();
           List results = getResults(rs);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }
        finally
        {
           close(ps);
           close(rs);
        }
   }

 	/**
	* Create batch new record in Database. it only create the first layer dto
	*/
    public void create(IDto[] _dtos, Connection conn)    throws SQLException
    {
        HtfPaybankRefundmainDto dto  ;
     	for (int i= 0; i< _dtos.length ; i++)
 	    {
 		    dto  = (HtfPaybankRefundmainDto)_dtos[i] ; 
    	    String msgValid = dto.checkValid() ;
        	if (msgValid != null)
     	    	throw new SQLException("插入错误，"+msgValid) ;
     	}
 	    PreparedStatement ps = null;
        try
        {
            ps = conn.prepareStatement(SQL_INSERT);
       
     	    for (int i= 0; i< _dtos.length ; i++)
     	    {
 	    	    dto  = (HtfPaybankRefundmainDto)_dtos[i] ; 
  
               if (dto.getIvousrlno()==null)
                  ps.setNull(1, java.sql.Types.BIGINT);
               else
                  ps.setLong(1, dto.getIvousrlno().longValue());
  
               ps.setString(2, dto.getSorgcode());
  
               ps.setString(3, dto.getStrecode());
  
               ps.setString(4, dto.getScommitdate());
  
               ps.setString(5, dto.getSdealno());
  
               ps.setString(6, dto.getSfinorgcode());
  
               ps.setString(7, dto.getSpackageno());
  
               ps.setString(8, dto.getSfilename());
  
               ps.setString(9, dto.getSstatus());
  
               ps.setString(10, dto.getSdemo());
   
               ps.setString(11, dto.getSid());
  
               ps.setString(12, dto.getSadmdivcode());
  
               ps.setString(13, dto.getSstyear());
  
               ps.setString(14, dto.getSvtcode());
  
               ps.setString(15, dto.getSvoudate());
  
               ps.setString(16, dto.getSvoucherno());
  
               ps.setString(17, dto.getSoribillno());
  
               ps.setString(18, dto.getSpaytypecode());
  
               ps.setString(19, dto.getSpaytypename());
  
               ps.setString(20, dto.getSoriginalvoucherno());
  
               ps.setBigDecimal(21, dto.getNpayamt());
  
               ps.setString(22, dto.getSpaydictateno());
  
               ps.setString(23, dto.getSpaymsgno());
  
               ps.setString(24, dto.getSpayentrustdate());
  
               ps.setString(25, dto.getSpaysndbnkno());
  
               ps.setString(26, dto.getSremark());
  
               ps.setString(27, dto.getSpaydate());
  
               ps.setString(28, dto.getSbackflag());
  
               ps.setString(29, dto.getSisreturn());
  
               ps.setString(30, dto.getSbckreason());
  
               ps.setString(31, dto.getSrefundtype());
  
               ps.setBigDecimal(32, dto.getNbackmoney());
  
               ps.setString(33, dto.getShold1());
  
               ps.setString(34, dto.getShold2());
  
               ps.setString(35, dto.getSext1());
  
               ps.setString(36, dto.getSext2());
  
               ps.setString(37, dto.getSext3());
  
               ps.setString(38, dto.getSext4());
  
               ps.setString(39, dto.getSext5());
               ps.addBatch(); 
           }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }
        return  ;
   }




     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto find(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       HtfPaybankRefundmainPK pk = (HtfPaybankRefundmainPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT);
           if (pk.getIvousrlno()==null)
               ps.setNull(1, java.sql.Types.BIGINT);
           else
               ps.setLong(1, pk.getIvousrlno().longValue());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }

     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto findForUpdate(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       HtfPaybankRefundmainPK pk = (HtfPaybankRefundmainPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT_FOR_UPDATE);
           if (pk.getIvousrlno()==null)
               ps.setNull(1, java.sql.Types.BIGINT);
           else
               ps.setLong(1, pk.getIvousrlno().longValue());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }
  
     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto[] find(IPK[] _pks, Connection conn, boolean isLobSupport) throws SQLException
    {
        HtfPaybankRefundmainPK pk ;
        
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (HtfPaybankRefundmainPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 
        PreparedStatement ps = null;
        ResultSet rs = null;
        
         
        List results = new ArrayList();  
		for (int iBegin = 0; iBegin < pks.size(); iBegin += FIND_BATCH_SIZE)   //
		{
        
            try {
          		int iFindBatchSize = pks.size() - iBegin ;
	    		if (iFindBatchSize >FIND_BATCH_SIZE )
		    		iFindBatchSize = FIND_BATCH_SIZE ;
		  
        	    StringBuffer sb = new StringBuffer() ;
            	sb.append(SQL_SELECT_BATCH).append(" WHERE ").append(SQL_SELECT_BATCH_WHERE);

	    		for (int i = iBegin+1; i < iBegin + iFindBatchSize; i++)
		    	{
			    	sb.append("OR").append(SQL_SELECT_BATCH_WHERE) ;
    			}
        	
                ps = conn.prepareStatement(sb.toString());

                for (int i = iBegin;i < iBegin + iFindBatchSize; i++)
                {
                    pk  = (HtfPaybankRefundmainPK)(pks.get(i)) ; 
                   if (pk.getIvousrlno()==null)
                      ps.setNull((i-iBegin)*1+1, java.sql.Types.BIGINT);
                   else
                      ps.setLong((i-iBegin)*1+1, pk.getIvousrlno().longValue());

			
	            }
                rs = ps.executeQuery();
                results.addAll(getResults(rs,0,isLobSupport)); //
            }finally {
                close(rs);
                close(ps);
            }
        }
        if (results!=null && results.size() > 0)
        {
            HtfPaybankRefundmainDto[] dtos = new HtfPaybankRefundmainDto[0];
		    dtos = (HtfPaybankRefundmainDto[]) results.toArray(dtos) ;
		    return dtos ;
        }
        else
        {
            return null;
        }
    }

  
  
  



    /**
	    * 得到查询语句
	    */
    public String getSelectSQL() 
    {
	        return SQL_SELECT_BATCH_SCROLLABLE ;
    }
   

	public List getResults(ResultSet rs) throws SQLException
    {
    	return getResults(rs, 0) ;
    }
  

    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize) throws SQLException
    {
		return getResults(rs,maxSize,false);
	}
    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize, boolean isLobSupport) throws SQLException
    {
        List results = new ArrayList();
        String str ;
        Reader reader;
        Clob clob;
        char[] chars ;
        
        while (rs.next())
         {
             HtfPaybankRefundmainDto  dto = new  HtfPaybankRefundmainDto ();
             //I_VOUSRLNO
             str = rs.getString("I_VOUSRLNO");
             if(str!=null){
                dto.setIvousrlno(new Long(str));
             }

             //S_ORGCODE
             str = rs.getString("S_ORGCODE");
             if (str == null)
                dto.setSorgcode(null);
             else
                dto.setSorgcode(str.trim());

             //S_TRECODE
             str = rs.getString("S_TRECODE");
             if (str == null)
                dto.setStrecode(null);
             else
                dto.setStrecode(str.trim());

             //S_COMMITDATE
             str = rs.getString("S_COMMITDATE");
             if (str == null)
                dto.setScommitdate(null);
             else
                dto.setScommitdate(str.trim());

             //S_DEALNO
             str = rs.getString("S_DEALNO");
             if (str == null)
                dto.setSdealno(null);
             else
                dto.setSdealno(str.trim());

             //S_FINORGCODE
             str = rs.getString("S_FINORGCODE");
             if (str == null)
                dto.setSfinorgcode(null);
             else
                dto.setSfinorgcode(str.trim());

             //S_PACKAGENO
             str = rs.getString("S_PACKAGENO");
             if (str == null)
                dto.setSpackageno(null);
             else
                dto.setSpackageno(str.trim());

             //S_FILENAME
             str = rs.getString("S_FILENAME");
             if (str == null)
                dto.setSfilename(null);
             else
                dto.setSfilename(str.trim());

             //S_STATUS
             str = rs.getString("S_STATUS");
             if (str == null)
                dto.setSstatus(null);
             else
                dto.setSstatus(str.trim());

             //S_DEMO
             str = rs.getString("S_DEMO");
             if (str == null)
                dto.setSdemo(null);
             else
                dto.setSdemo(str.trim());

             //TS_SYSUPDATE
           dto.setTssysupdate(rs.getTimestamp("TS_SYSUPDATE"));

             //S_ID
             str = rs.getString("S_ID");
             if (str == null)
                dto.setSid(null);
             else
                dto.setSid(str.trim());

             //S_ADMDIVCODE
             str = rs.getString("S_ADMDIVCODE");
             if (str == null)
                dto.setSadmdivcode(null);
             else
                dto.setSadmdivcode(str.trim());

             //S_STYEAR
             str = rs.getString("S_STYEAR");
             if (str == null)
                dto.setSstyear(null);
             else
                dto.setSstyear(str.trim());

             //S_VTCODE
             str = rs.getString("S_VTCODE");
             if (str == null)
                dto.setSvtcode(null);
             else
                dto.setSvtcode(str.trim());

             //S_VOUDATE
             str = rs.getString("S_VOUDATE");
             if (str == null)
                dto.setSvoudate(null);
             else
                dto.setSvoudate(str.trim());

             //S_VOUCHERNO
             str = rs.getString("S_VOUCHERNO");
             if (str == null)
                dto.setSvoucherno(null);
             else
                dto.setSvoucherno(str.trim());

             //S_ORIBILLNO
             str = rs.getString("S_ORIBILLNO");
             if (str == null)
                dto.setSoribillno(null);
             else
                dto.setSoribillno(str.trim());

             //S_PAYTYPECODE
             str = rs.getString("S_PAYTYPECODE");
             if (str == null)
                dto.setSpaytypecode(null);
             else
                dto.setSpaytypecode(str.trim());

             //S_PAYTYPENAME
             str = rs.getString("S_PAYTYPENAME");
             if (str == null)
                dto.setSpaytypename(null);
             else
                dto.setSpaytypename(str.trim());

             //S_ORIGINALVOUCHERNO
             str = rs.getString("S_ORIGINALVOUCHERNO");
             if (str == null)
                dto.setSoriginalvoucherno(null);
             else
                dto.setSoriginalvoucherno(str.trim());

             //N_PAYAMT
           dto.setNpayamt(rs.getBigDecimal("N_PAYAMT"));

             //S_PAYDICTATENO
             str = rs.getString("S_PAYDICTATENO");
             if (str == null)
                dto.setSpaydictateno(null);
             else
                dto.setSpaydictateno(str.trim());

             //S_PAYMSGNO
             str = rs.getString("S_PAYMSGNO");
             if (str == null)
                dto.setSpaymsgno(null);
             else
                dto.setSpaymsgno(str.trim());

             //S_PAYENTRUSTDATE
             str = rs.getString("S_PAYENTRUSTDATE");
             if (str == null)
                dto.setSpayentrustdate(null);
             else
                dto.setSpayentrustdate(str.trim());

             //S_PAYSNDBNKNO
             str = rs.getString("S_PAYSNDBNKNO");
             if (str == null)
                dto.setSpaysndbnkno(null);
             else
                dto.setSpaysndbnkno(str.trim());

             //S_REMARK
             str = rs.getString("S_REMARK");
             if (str == null)
                dto.setSremark(null);
             else
                dto.setSremark(str.trim());

             //S_PAYDATE
             str = rs.getString("S_PAYDATE");
             if (str == null)
                dto.setSpaydate(null);
             else
                dto.setSpaydate(str.trim());

             //S_BACKFLAG
             str = rs.getString("S_BACKFLAG");
             if (str == null)
                dto.setSbackflag(null);
             else
                dto.setSbackflag(str.trim());

             //S_ISRETURN
             str = rs.getString("S_ISRETURN");
             if (str == null)
                dto.setSisreturn(null);
             else
                dto.setSisreturn(str.trim());

             //S_BCKREASON
             str = rs.getString("S_BCKREASON");
             if (str == null)
                dto.setSbckreason(null);
             else
                dto.setSbckreason(str.trim());

             //S_REFUNDTYPE
             str = rs.getString("S_REFUNDTYPE");
             if (str == null)
                dto.setSrefundtype(null);
             else
                dto.setSrefundtype(str.trim());

             //N_BACKMONEY
           dto.setNbackmoney(rs.getBigDecimal("N_BACKMONEY"));

             //S_HOLD1
             str = rs.getString("S_HOLD1");
             if (str == null)
                dto.setShold1(null);
             else
                dto.setShold1(str.trim());

             //S_HOLD2
             str = rs.getString("S_HOLD2");
             if (str == null)
                dto.setShold2(null);
             else
                dto.setShold2(str.trim());

             //S_EXT1
             str = rs.getString("S_EXT1");
             if (str == null)
                dto.setSext1(null);
             else
                dto.setSext1(str.trim());

             //S_EXT2
             str = rs.getString("S_EXT2");
             if (str == null)
                dto.setSext2(null);
             else
                dto.setSext2(str.trim());

             //S_EXT3
             str = rs.getString("S_EXT3");
             if (str == null)
                dto.setSext3(null);
             else
                dto.setSext3(str.trim());

             //S_EXT4
             str = rs.getString("S_EXT4");
             if (str == null)
                dto.setSext4(null);
             else
                dto.setSext4(str.trim());

             //S_EXT5
             str = rs.getString("S_EXT5");
             if (str == null)
                dto.setSext5(null);
             else
                dto.setSext5(str.trim());



            results.add(dto);
            
            if(maxSize >0  && results.size() == maxSize)
            {
            	break ;
            }
            
         }
        
		return results ;
    }

  /**
   * populate resultSet as cclass
   */
  protected IDto[] getChildrenResults(ResultSet rs ,Class _dto) throws SQLException{

      throw new SQLException("本dto没有与其它dto相关联，无法应用此方法");
  }


     /**
     * Update a record in Database.
     */

    public void update(IDto _dto, Connection conn, boolean isLobSupport) throws SQLException
    {
        String msgValid = _dto.checkValid() ;
        if (msgValid != null)
            throw new SQLException("修改错误，"+msgValid) ;
        HtfPaybankRefundmainDto dto = (HtfPaybankRefundmainDto)_dto ;
        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            int pos = 1;
            //S_ORGCODE
            ps.setString(pos, dto.getSorgcode());
            pos++;

            //S_TRECODE
            ps.setString(pos, dto.getStrecode());
            pos++;

            //S_COMMITDATE
            ps.setString(pos, dto.getScommitdate());
            pos++;

            //S_DEALNO
            ps.setString(pos, dto.getSdealno());
            pos++;

            //S_FINORGCODE
            ps.setString(pos, dto.getSfinorgcode());
            pos++;

            //S_PACKAGENO
            ps.setString(pos, dto.getSpackageno());
            pos++;

            //S_FILENAME
            ps.setString(pos, dto.getSfilename());
            pos++;

            //S_STATUS
            ps.setString(pos, dto.getSstatus());
            pos++;

            //S_DEMO
            ps.setString(pos, dto.getSdemo());
            pos++;

            //TS_SYSUPDATE
            //S_ID
            ps.setString(pos, dto.getSid());
            pos++;

            //S_ADMDIVCODE
            ps.setString(pos, dto.getSadmdivcode());
            pos++;

            //S_STYEAR
            ps.setString(pos, dto.getSstyear());
            pos++;

            //S_VTCODE
            ps.setString(pos, dto.getSvtcode());
            pos++;

            //S_VOUDATE
            ps.setString(pos, dto.getSvoudate());
            pos++;

            //S_VOUCHERNO
            ps.setString(pos, dto.getSvoucherno());
            pos++;

            //S_ORIBILLNO
            ps.setString(pos, dto.getSoribillno());
            pos++;

            //S_PAYTYPECODE
            ps.setString(pos, dto.getSpaytypecode());
            pos++;

            //S_PAYTYPENAME
            ps.setString(pos, dto.getSpaytypename());
            pos++;

            //S_ORIGINALVOUCHERNO
            ps.setString(pos, dto.getSoriginalvoucherno());
            pos++;

            //N_PAYAMT
            ps.setBigDecimal(pos, dto.getNpayamt());
            pos++;

            //S_PAYDICTATENO
            ps.setString(pos, dto.getSpaydictateno());
            pos++;

            //S_PAYMSGNO
            ps.setString(pos, dto.getSpaymsgno());
            pos++;

            //S_PAYENTRUSTDATE
            ps.setString(pos, dto.getSpayentrustdate());
            pos++;

            //S_PAYSNDBNKNO
            ps.setString(pos, dto.getSpaysndbnkno());
            pos++;

            //S_REMARK
            ps.setString(pos, dto.getSremark());
            pos++;

            //S_PAYDATE
            ps.setString(pos, dto.getSpaydate());
            pos++;

            //S_BACKFLAG
            ps.setString(pos, dto.getSbackflag());
            pos++;

            //S_ISRETURN
            ps.setString(pos, dto.getSisreturn());
            pos++;

            //S_BCKREASON
            ps.setString(pos, dto.getSbckreason());
            pos++;

            //S_REFUNDTYPE
            ps.setString(pos, dto.getSrefundtype());
            pos++;

            //N_BACKMONEY
            ps.setBigDecimal(pos, dto.getNbackmoney());
            pos++;

            //S_HOLD1
            ps.setString(pos, dto.getShold1());
            pos++;

            //S_HOLD2
            ps.setString(pos, dto.getShold2());
            pos++;

            //S_EXT1
            ps.setString(pos, dto.getSext1());
            pos++;

            //S_EXT2
            ps.setString(pos, dto.getSext2());
            pos++;

            //S_EXT3
            ps.setString(pos, dto.getSext3());
            pos++;

            //S_EXT4
            ps.setString(pos, dto.getSext4());
            pos++;

            //S_EXT5
            ps.setString(pos, dto.getSext5());
            pos++;


           //I_VOUSRLNO
           ps.setLong(pos, dto.getIvousrlno().longValue());
           pos++;
           ps.executeUpdate();
        }
        finally
        {
           close(ps);
        }

        return ;
    }
    
    
    
      /**
     * Update batch record in Database.
     */

    public void update(IDto[] _dtos, Connection conn, boolean isLobSupport) throws SQLException
    {
     
     	 HtfPaybankRefundmainDto dto  ;
         for (int i= 0; i< _dtos.length ; i++)
         {
            dto  = (HtfPaybankRefundmainDto)_dtos[i] ; 
            String msgValid = dto.checkValid() ;
            if (msgValid != null)
                throw new SQLException("修改错误，"+msgValid) ;
         }

        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            
            for (int i= 0; i< _dtos.length ; i++)
            {
                dto  = (HtfPaybankRefundmainDto)_dtos[i] ; 
                int pos = 1;
                //S_ORGCODE
                 ps.setString(pos, dto.getSorgcode());
                pos++;

                //S_TRECODE
                 ps.setString(pos, dto.getStrecode());
                pos++;

                //S_COMMITDATE
                 ps.setString(pos, dto.getScommitdate());
                pos++;

                //S_DEALNO
                 ps.setString(pos, dto.getSdealno());
                pos++;

                //S_FINORGCODE
                 ps.setString(pos, dto.getSfinorgcode());
                pos++;

                //S_PACKAGENO
                 ps.setString(pos, dto.getSpackageno());
                pos++;

                //S_FILENAME
                 ps.setString(pos, dto.getSfilename());
                pos++;

                //S_STATUS
                 ps.setString(pos, dto.getSstatus());
                pos++;

                //S_DEMO
                 ps.setString(pos, dto.getSdemo());
                pos++;

                //TS_SYSUPDATE
                 //S_ID
                 ps.setString(pos, dto.getSid());
                pos++;

                //S_ADMDIVCODE
                 ps.setString(pos, dto.getSadmdivcode());
                pos++;

                //S_STYEAR
                 ps.setString(pos, dto.getSstyear());
                pos++;

                //S_VTCODE
                 ps.setString(pos, dto.getSvtcode());
                pos++;

                //S_VOUDATE
                 ps.setString(pos, dto.getSvoudate());
                pos++;

                //S_VOUCHERNO
                 ps.setString(pos, dto.getSvoucherno());
                pos++;

                //S_ORIBILLNO
                 ps.setString(pos, dto.getSoribillno());
                pos++;

                //S_PAYTYPECODE
                 ps.setString(pos, dto.getSpaytypecode());
                pos++;

                //S_PAYTYPENAME
                 ps.setString(pos, dto.getSpaytypename());
                pos++;

                //S_ORIGINALVOUCHERNO
                 ps.setString(pos, dto.getSoriginalvoucherno());
                pos++;

                //N_PAYAMT
                 ps.setBigDecimal(pos, dto.getNpayamt());
                pos++;

                //S_PAYDICTATENO
                 ps.setString(pos, dto.getSpaydictateno());
                pos++;

                //S_PAYMSGNO
                 ps.setString(pos, dto.getSpaymsgno());
                pos++;

                //S_PAYENTRUSTDATE
                 ps.setString(pos, dto.getSpayentrustdate());
                pos++;

                //S_PAYSNDBNKNO
                 ps.setString(pos, dto.getSpaysndbnkno());
                pos++;

                //S_REMARK
                 ps.setString(pos, dto.getSremark());
                pos++;

                //S_PAYDATE
                 ps.setString(pos, dto.getSpaydate());
                pos++;

                //S_BACKFLAG
                 ps.setString(pos, dto.getSbackflag());
                pos++;

                //S_ISRETURN
                 ps.setString(pos, dto.getSisreturn());
                pos++;

                //S_BCKREASON
                 ps.setString(pos, dto.getSbckreason());
                pos++;

                //S_REFUNDTYPE
                 ps.setString(pos, dto.getSrefundtype());
                pos++;

                //N_BACKMONEY
                 ps.setBigDecimal(pos, dto.getNbackmoney());
                pos++;

                //S_HOLD1
                 ps.setString(pos, dto.getShold1());
                pos++;

                //S_HOLD2
                 ps.setString(pos, dto.getShold2());
                pos++;

                //S_EXT1
                 ps.setString(pos, dto.getSext1());
                pos++;

                //S_EXT2
                 ps.setString(pos, dto.getSext2());
                pos++;

                //S_EXT3
                 ps.setString(pos, dto.getSext3());
                pos++;

                //S_EXT4
                 ps.setString(pos, dto.getSext4());
                pos++;

                //S_EXT5
                 ps.setString(pos, dto.getSext5());
                pos++;


               //I_VOUSRLNO
               ps.setLong(pos, dto.getIvousrlno().longValue());
               pos++;
		       ps.addBatch(); 
	       }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }

        return ;
    }



    /**
     * delete a record in Database.
     */
    public void delete(IPK _pk, Connection conn) throws SQLException
    {
       
      
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return ;
        //	throw new SQLException("删除错误，"+msgValid) ;
       }
       HtfPaybankRefundmainPK pk = (HtfPaybankRefundmainPK)_pk ;


       PreparedStatement ps = null;
       try {
           ps = conn.prepareStatement(SQL_DELETE);
           ps.setLong(1, pk.getIvousrlno().longValue());
           ps.executeUpdate();
        }
        finally
        {
            close(ps);
        }
    }


     /**
     * delete batch record in Database.
     */
    public void delete(IPK[] _pks, Connection conn) throws SQLException
    {
        HtfPaybankRefundmainPK pk ;
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (HtfPaybankRefundmainPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 		
 		
        PreparedStatement ps = null;
        try
         {
            ps = conn.prepareStatement(SQL_DELETE);
             
       		for (int i= 0; i< pks.size() ; i++)
       		{
       			pk  = (HtfPaybankRefundmainPK)(pks.get(i)) ; 
                ps.setLong(1, pk.getIvousrlno().longValue());
		        ps.addBatch() ;
			}
            ps.executeBatch();
        }
        finally
        {
            close(ps);
        }      
    }



    /**
    * Check IChangerNumber or UpdateTimeStamp.
    */
    public void check(IDto _dto, Connection conn) throws SQLException
    {
                                                                                                                     		throw new SQLException("数据库表：HTF_PAYBANK_REFUNDMAIN没有检查修改的字段");
    }


	/**
	* synchronize values between parent and children
	*/
    public void syncToChildren(IDto _dto)     throws SQLException{
       throw new SQLException("本记录没有与其它记录相关联，无法进行主记录与明细记录之间的数据同步。");
  }

	/**
	*get children
	*/
    public IDto[] findChildren (IDto _dto, Connection conn, boolean isLobSupport)     throws SQLException{
         throw new SQLException("本dto没有与其它dto相关联，无法得到子dto");
  }    
    
  /**
   *return the children IDto class
   * @return Class
   */
  public Class getChildrenClass(){
	  return null;
  };    



    /**
     * Close JDBC Statement.
     * @param stmt  Statement to be closed.
     */
    private void close(Statement stmt)
    {
        if (stmt != null)
        {
            try
            {
                stmt.close();
            }
            catch(SQLException    e)
            {}
        }
    }

    /**
     * Close JDBC ResultSet.
     * @param rs  ResultSet to be closed.
     */
    private void close(ResultSet rs)
    {
        if (rs != null)
        {
            try
            {
                rs.close();
            }catch(SQLException  e){}
        }
    }
}
