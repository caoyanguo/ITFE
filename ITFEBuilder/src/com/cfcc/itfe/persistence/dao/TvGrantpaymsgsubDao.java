    



package com.cfcc.itfe.persistence.dao;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.Statement;
import java.sql.SQLException;
import java.sql.Date;
import java.sql.Time;
import java.sql.Timestamp ;
import java.sql.Clob;

import com.cfcc.jaf.persistence.jaform.parent.* ;

import java.math.BigDecimal;

import java.util.List;
import java.util.ArrayList;
import java.util.HashMap;

import java.io.StringReader;
import java.io.Reader;
import java.io.IOException;

import com.cfcc.itfe.persistence.dto.TvGrantpaymsgsubDto ;
import com.cfcc.itfe.persistence.pk.TvGrantpaymsgsubPK ;


/**
 * <p>Title: DAO of table: TV_GRANTPAYMSGSUB</p>
 * <p>Description: Data Access Object  </p>
 * <p>Copyright: Copyright (c) 2005</p>
 * <p>Company: CFCC </p>
 * <p>Generation Time: 2014-12-25 15:29:02 </p>
 *  Do NOT change this file by hand.
 *  This java source file is generated by JAF_ORM .
 *  dao.vm timestamp: 2007-04-06 08:30:00
 ******************************************************
 * <p> <b>change record</b><br>
 *  make dao methods simply.<br>
 *  especially make update,delete methods simply.<br>
 *  remove "..withCheck" method,withCheck will impl in JAFOrmTemplate.<br>
 *  remove "delete(IDto _dto,..)" method.<br></p>
 * @author win7
 */

public class TvGrantpaymsgsubDao  implements IDao
{


    /* SQL to insert data */
    private static final String SQL_INSERT =
        "INSERT INTO TV_GRANTPAYMSGSUB ("
          + "I_VOUSRLNO,I_DETAILSEQNO,S_ORGCODE,S_FILENAME,S_LIMITID"
          + ",S_OFYEAR,S_LINE,S_PACKAGENO,S_DEALNO,S_PACKAGETICKETNO"
          + ",S_BUDGETUNITCODE,S_BUDGETTYPE,S_FUNSUBJECTCODE,S_ECOSUBJECTCODE,N_MONEY"
          + ",S_ACCATTRIB,S_ACCDATE,S_STATUS,S_USERCODE,TS_SYSUPDATE"
          + ",S_DEMO,S_SUPDEPNAME,S_EXPFUNCNAME,S_EXPFUNCCODE1,S_EXPFUNCNAME1"
          + ",S_EXPFUNCCODE2,S_EXPFUNCNAME2,S_EXPFUNCCODE3,S_EXPFUNCNAME3,S_PROCATCODE"
          + ",S_PROCATNAME,S_HOLD1,S_HOLD2,S_HOLD3,S_HOLD4"
          + ",S_ID"
        + ") VALUES ("
        + "?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,CURRENT TIMESTAMP ,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)";


    private static final String SQL_INSERT_WITH_RESULT = "SELECT * FROM FINAL TABLE( " + SQL_INSERT + " )";
    
    /* SQL to select data */
    private static final String SQL_SELECT =
        "SELECT "
        + "TV_GRANTPAYMSGSUB.I_VOUSRLNO, TV_GRANTPAYMSGSUB.I_DETAILSEQNO, TV_GRANTPAYMSGSUB.S_ORGCODE, TV_GRANTPAYMSGSUB.S_FILENAME, TV_GRANTPAYMSGSUB.S_LIMITID, "
        + "TV_GRANTPAYMSGSUB.S_OFYEAR, TV_GRANTPAYMSGSUB.S_LINE, TV_GRANTPAYMSGSUB.S_PACKAGENO, TV_GRANTPAYMSGSUB.S_DEALNO, TV_GRANTPAYMSGSUB.S_PACKAGETICKETNO, "
        + "TV_GRANTPAYMSGSUB.S_BUDGETUNITCODE, TV_GRANTPAYMSGSUB.S_BUDGETTYPE, TV_GRANTPAYMSGSUB.S_FUNSUBJECTCODE, TV_GRANTPAYMSGSUB.S_ECOSUBJECTCODE, TV_GRANTPAYMSGSUB.N_MONEY, "
        + "TV_GRANTPAYMSGSUB.S_ACCATTRIB, TV_GRANTPAYMSGSUB.S_ACCDATE, TV_GRANTPAYMSGSUB.S_STATUS, TV_GRANTPAYMSGSUB.S_USERCODE, TV_GRANTPAYMSGSUB.TS_SYSUPDATE, "
        + "TV_GRANTPAYMSGSUB.S_DEMO, TV_GRANTPAYMSGSUB.S_SUPDEPNAME, TV_GRANTPAYMSGSUB.S_EXPFUNCNAME, TV_GRANTPAYMSGSUB.S_EXPFUNCCODE1, TV_GRANTPAYMSGSUB.S_EXPFUNCNAME1, "
        + "TV_GRANTPAYMSGSUB.S_EXPFUNCCODE2, TV_GRANTPAYMSGSUB.S_EXPFUNCNAME2, TV_GRANTPAYMSGSUB.S_EXPFUNCCODE3, TV_GRANTPAYMSGSUB.S_EXPFUNCNAME3, TV_GRANTPAYMSGSUB.S_PROCATCODE, "
        + "TV_GRANTPAYMSGSUB.S_PROCATNAME, TV_GRANTPAYMSGSUB.S_HOLD1, TV_GRANTPAYMSGSUB.S_HOLD2, TV_GRANTPAYMSGSUB.S_HOLD3, TV_GRANTPAYMSGSUB.S_HOLD4, "
        + "TV_GRANTPAYMSGSUB.S_ID "
        + "FROM TV_GRANTPAYMSGSUB "
        +" WHERE " 
        + "I_VOUSRLNO = ? AND I_DETAILSEQNO = ? AND S_PACKAGETICKETNO = ?"
        ;
    /* SQL to select for update data */
    private static final String SQL_SELECT_FOR_UPDATE =
        "SELECT "
        + "TV_GRANTPAYMSGSUB.I_VOUSRLNO, TV_GRANTPAYMSGSUB.I_DETAILSEQNO, TV_GRANTPAYMSGSUB.S_ORGCODE, TV_GRANTPAYMSGSUB.S_FILENAME, TV_GRANTPAYMSGSUB.S_LIMITID, "
        + "TV_GRANTPAYMSGSUB.S_OFYEAR, TV_GRANTPAYMSGSUB.S_LINE, TV_GRANTPAYMSGSUB.S_PACKAGENO, TV_GRANTPAYMSGSUB.S_DEALNO, TV_GRANTPAYMSGSUB.S_PACKAGETICKETNO, "
        + "TV_GRANTPAYMSGSUB.S_BUDGETUNITCODE, TV_GRANTPAYMSGSUB.S_BUDGETTYPE, TV_GRANTPAYMSGSUB.S_FUNSUBJECTCODE, TV_GRANTPAYMSGSUB.S_ECOSUBJECTCODE, TV_GRANTPAYMSGSUB.N_MONEY, "
        + "TV_GRANTPAYMSGSUB.S_ACCATTRIB, TV_GRANTPAYMSGSUB.S_ACCDATE, TV_GRANTPAYMSGSUB.S_STATUS, TV_GRANTPAYMSGSUB.S_USERCODE, TV_GRANTPAYMSGSUB.TS_SYSUPDATE, "
        + "TV_GRANTPAYMSGSUB.S_DEMO, TV_GRANTPAYMSGSUB.S_SUPDEPNAME, TV_GRANTPAYMSGSUB.S_EXPFUNCNAME, TV_GRANTPAYMSGSUB.S_EXPFUNCCODE1, TV_GRANTPAYMSGSUB.S_EXPFUNCNAME1, "
        + "TV_GRANTPAYMSGSUB.S_EXPFUNCCODE2, TV_GRANTPAYMSGSUB.S_EXPFUNCNAME2, TV_GRANTPAYMSGSUB.S_EXPFUNCCODE3, TV_GRANTPAYMSGSUB.S_EXPFUNCNAME3, TV_GRANTPAYMSGSUB.S_PROCATCODE, "
        + "TV_GRANTPAYMSGSUB.S_PROCATNAME, TV_GRANTPAYMSGSUB.S_HOLD1, TV_GRANTPAYMSGSUB.S_HOLD2, TV_GRANTPAYMSGSUB.S_HOLD3, TV_GRANTPAYMSGSUB.S_HOLD4, "
        + "TV_GRANTPAYMSGSUB.S_ID "
        + "FROM TV_GRANTPAYMSGSUB "
        +" WHERE " 
        + "I_VOUSRLNO = ? AND I_DETAILSEQNO = ? AND S_PACKAGETICKETNO = ? FOR UPDATE"
        ;

      /* SQL to select data (SCROLLABLE)*/
     private static final String SQL_SELECT_BATCH_SCROLLABLE =
        "SELECT "
        + "  TV_GRANTPAYMSGSUB.I_VOUSRLNO  , TV_GRANTPAYMSGSUB.I_DETAILSEQNO  , TV_GRANTPAYMSGSUB.S_ORGCODE  , TV_GRANTPAYMSGSUB.S_FILENAME  , TV_GRANTPAYMSGSUB.S_LIMITID "
        + " , TV_GRANTPAYMSGSUB.S_OFYEAR  , TV_GRANTPAYMSGSUB.S_LINE  , TV_GRANTPAYMSGSUB.S_PACKAGENO  , TV_GRANTPAYMSGSUB.S_DEALNO  , TV_GRANTPAYMSGSUB.S_PACKAGETICKETNO "
        + " , TV_GRANTPAYMSGSUB.S_BUDGETUNITCODE  , TV_GRANTPAYMSGSUB.S_BUDGETTYPE  , TV_GRANTPAYMSGSUB.S_FUNSUBJECTCODE  , TV_GRANTPAYMSGSUB.S_ECOSUBJECTCODE  , TV_GRANTPAYMSGSUB.N_MONEY "
        + " , TV_GRANTPAYMSGSUB.S_ACCATTRIB  , TV_GRANTPAYMSGSUB.S_ACCDATE  , TV_GRANTPAYMSGSUB.S_STATUS  , TV_GRANTPAYMSGSUB.S_USERCODE  , TV_GRANTPAYMSGSUB.TS_SYSUPDATE "
        + " , TV_GRANTPAYMSGSUB.S_DEMO  , TV_GRANTPAYMSGSUB.S_SUPDEPNAME  , TV_GRANTPAYMSGSUB.S_EXPFUNCNAME  , TV_GRANTPAYMSGSUB.S_EXPFUNCCODE1  , TV_GRANTPAYMSGSUB.S_EXPFUNCNAME1 "
        + " , TV_GRANTPAYMSGSUB.S_EXPFUNCCODE2  , TV_GRANTPAYMSGSUB.S_EXPFUNCNAME2  , TV_GRANTPAYMSGSUB.S_EXPFUNCCODE3  , TV_GRANTPAYMSGSUB.S_EXPFUNCNAME3  , TV_GRANTPAYMSGSUB.S_PROCATCODE "
        + " , TV_GRANTPAYMSGSUB.S_PROCATNAME  , TV_GRANTPAYMSGSUB.S_HOLD1  , TV_GRANTPAYMSGSUB.S_HOLD2  , TV_GRANTPAYMSGSUB.S_HOLD3  , TV_GRANTPAYMSGSUB.S_HOLD4 "
        + " , TV_GRANTPAYMSGSUB.S_ID "
        + "FROM TV_GRANTPAYMSGSUB ";



    /* SQL to select batch data */
    private static final String SQL_SELECT_BATCH =
        "SELECT "
        + "TV_GRANTPAYMSGSUB.I_VOUSRLNO, TV_GRANTPAYMSGSUB.I_DETAILSEQNO, TV_GRANTPAYMSGSUB.S_ORGCODE, TV_GRANTPAYMSGSUB.S_FILENAME, TV_GRANTPAYMSGSUB.S_LIMITID, "
        + "TV_GRANTPAYMSGSUB.S_OFYEAR, TV_GRANTPAYMSGSUB.S_LINE, TV_GRANTPAYMSGSUB.S_PACKAGENO, TV_GRANTPAYMSGSUB.S_DEALNO, TV_GRANTPAYMSGSUB.S_PACKAGETICKETNO, "
        + "TV_GRANTPAYMSGSUB.S_BUDGETUNITCODE, TV_GRANTPAYMSGSUB.S_BUDGETTYPE, TV_GRANTPAYMSGSUB.S_FUNSUBJECTCODE, TV_GRANTPAYMSGSUB.S_ECOSUBJECTCODE, TV_GRANTPAYMSGSUB.N_MONEY, "
        + "TV_GRANTPAYMSGSUB.S_ACCATTRIB, TV_GRANTPAYMSGSUB.S_ACCDATE, TV_GRANTPAYMSGSUB.S_STATUS, TV_GRANTPAYMSGSUB.S_USERCODE, TV_GRANTPAYMSGSUB.TS_SYSUPDATE, "
        + "TV_GRANTPAYMSGSUB.S_DEMO, TV_GRANTPAYMSGSUB.S_SUPDEPNAME, TV_GRANTPAYMSGSUB.S_EXPFUNCNAME, TV_GRANTPAYMSGSUB.S_EXPFUNCCODE1, TV_GRANTPAYMSGSUB.S_EXPFUNCNAME1, "
        + "TV_GRANTPAYMSGSUB.S_EXPFUNCCODE2, TV_GRANTPAYMSGSUB.S_EXPFUNCNAME2, TV_GRANTPAYMSGSUB.S_EXPFUNCCODE3, TV_GRANTPAYMSGSUB.S_EXPFUNCNAME3, TV_GRANTPAYMSGSUB.S_PROCATCODE, "
        + "TV_GRANTPAYMSGSUB.S_PROCATNAME, TV_GRANTPAYMSGSUB.S_HOLD1, TV_GRANTPAYMSGSUB.S_HOLD2, TV_GRANTPAYMSGSUB.S_HOLD3, TV_GRANTPAYMSGSUB.S_HOLD4, "
        + "TV_GRANTPAYMSGSUB.S_ID "
        + "FROM TV_GRANTPAYMSGSUB " ;
        

   /* SQL to select batch data where condition  */
    private static final String SQL_SELECT_BATCH_WHERE =" ( "
        + "I_VOUSRLNO = ? AND I_DETAILSEQNO = ? AND S_PACKAGETICKETNO = ?)"
        ;


    /* SQL to update data */
    private static final String SQL_UPDATE =
        "UPDATE TV_GRANTPAYMSGSUB SET "
        + "S_ORGCODE =?,S_FILENAME =?,S_LIMITID =?,S_OFYEAR =?,S_LINE =?, "
        + "S_PACKAGENO =?,S_DEALNO =?,S_BUDGETUNITCODE =?,S_BUDGETTYPE =?,S_FUNSUBJECTCODE =?, "
        + "S_ECOSUBJECTCODE =?,N_MONEY =?,S_ACCATTRIB =?,S_ACCDATE =?,S_STATUS =?, "
        + "S_USERCODE =?,TS_SYSUPDATE =CURRENT TIMESTAMP,S_DEMO =?,S_SUPDEPNAME =?,S_EXPFUNCNAME =?, "
        + "S_EXPFUNCCODE1 =?,S_EXPFUNCNAME1 =?,S_EXPFUNCCODE2 =?,S_EXPFUNCNAME2 =?,S_EXPFUNCCODE3 =?, "
        + "S_EXPFUNCNAME3 =?,S_PROCATCODE =?,S_PROCATNAME =?,S_HOLD1 =?,S_HOLD2 =?, "
        + "S_HOLD3 =?,S_HOLD4 =?,S_ID =? "
        + "WHERE "
        + "I_VOUSRLNO = ? AND I_DETAILSEQNO = ? AND S_PACKAGETICKETNO = ?"
        ;

	/* SQL to update data, support LOB */
    private static final String SQL_UPDATE_LOB =
        "UPDATE TV_GRANTPAYMSGSUB SET "
        + "S_ORGCODE =?, S_FILENAME =?, S_LIMITID =?, S_OFYEAR =?, S_LINE =?,  "
        + "S_PACKAGENO =?, S_DEALNO =?, S_BUDGETUNITCODE =?, S_BUDGETTYPE =?, S_FUNSUBJECTCODE =?,  "
        + "S_ECOSUBJECTCODE =?, N_MONEY =?, S_ACCATTRIB =?, S_ACCDATE =?, S_STATUS =?,  "
        + "S_USERCODE =?, TS_SYSUPDATE =CURRENT TIMESTAMP, S_DEMO =?, S_SUPDEPNAME =?, S_EXPFUNCNAME =?,  "
        + "S_EXPFUNCCODE1 =?, S_EXPFUNCNAME1 =?, S_EXPFUNCCODE2 =?, S_EXPFUNCNAME2 =?, S_EXPFUNCCODE3 =?,  "
        + "S_EXPFUNCNAME3 =?, S_PROCATCODE =?, S_PROCATNAME =?, S_HOLD1 =?, S_HOLD2 =?,  "
        + "S_HOLD3 =?, S_HOLD4 =?, S_ID =? "
        + "WHERE "
        + "I_VOUSRLNO = ? AND I_DETAILSEQNO = ? AND S_PACKAGETICKETNO = ?"
        ;	
	
    /* SQL to delete data */
    private static final String SQL_DELETE =
        "DELETE FROM TV_GRANTPAYMSGSUB " 
        + " WHERE "
        + "I_VOUSRLNO = ? AND I_DETAILSEQNO = ? AND S_PACKAGETICKETNO = ?"
        ;


	/**
	*  批量查询 一次最多查询的参数数量
	*/
	
	public static final int FIND_BATCH_SIZE = 150  / 3;
	



   /**
   * Create a new record in Database.
   */
  public void create(IDto _dto,  Connection conn) throws SQLException
  {
     TvGrantpaymsgsubDto dto = (TvGrantpaymsgsubDto)_dto ;
     String msgValid = dto.checkValid() ;
     if (msgValid != null)
     	throw new SQLException("插入错误，"+msgValid) ;

     PreparedStatement ps = null;
     try
     {
         ps = conn.prepareStatement(SQL_INSERT);
          if (dto.getIvousrlno()==null)
            ps.setNull(1, java.sql.Types.BIGINT);
         else
            ps.setLong(1, dto.getIvousrlno().longValue());
          if (dto.getIdetailseqno()==null)
            ps.setNull(2, java.sql.Types.INTEGER);
         else
            ps.setInt(2, dto.getIdetailseqno().intValue());
          ps.setString(3, dto.getSorgcode());

          ps.setString(4, dto.getSfilename());

          ps.setString(5, dto.getSlimitid());

          ps.setString(6, dto.getSofyear());

          ps.setString(7, dto.getSline());

          ps.setString(8, dto.getSpackageno());

          ps.setString(9, dto.getSdealno());

          ps.setString(10, dto.getSpackageticketno());

          ps.setString(11, dto.getSbudgetunitcode());

          ps.setString(12, dto.getSbudgettype());

          ps.setString(13, dto.getSfunsubjectcode());

          ps.setString(14, dto.getSecosubjectcode());

          ps.setBigDecimal(15, dto.getNmoney());

          ps.setString(16, dto.getSaccattrib());

          ps.setString(17, dto.getSaccdate());

          ps.setString(18, dto.getSstatus());

          ps.setString(19, dto.getSusercode());

           ps.setString(20, dto.getSdemo());

          ps.setString(21, dto.getSsupdepname());

          ps.setString(22, dto.getSexpfuncname());

          ps.setString(23, dto.getSexpfunccode1());

          ps.setString(24, dto.getSexpfuncname1());

          ps.setString(25, dto.getSexpfunccode2());

          ps.setString(26, dto.getSexpfuncname2());

          ps.setString(27, dto.getSexpfunccode3());

          ps.setString(28, dto.getSexpfuncname3());

          ps.setString(29, dto.getSprocatcode());

          ps.setString(30, dto.getSprocatname());

          ps.setString(31, dto.getShold1());

          ps.setString(32, dto.getShold2());

          ps.setString(33, dto.getShold3());

          ps.setString(34, dto.getShold4());

          ps.setString(35, dto.getSid());

         ps.executeUpdate();
     }
     finally
     {
         close(ps);
     }
     return  ;
  }

   /**
   * Create a new record in Database. and return result, only used when having generated key
   */
   public IDto createWithResult(IDto _dto,  Connection conn) throws SQLException
   {
       TvGrantpaymsgsubDto dto = (TvGrantpaymsgsubDto)_dto ;
       String msgValid = dto.checkValid() ;
       if (msgValid != null)
           throw new SQLException("插入错误，"+msgValid) ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try
       {
           ps = conn.prepareStatement(SQL_INSERT_WITH_RESULT);
            if (dto.getIvousrlno()==null)
              ps.setNull(1, java.sql.Types.BIGINT);
           else
              ps.setLong(1, dto.getIvousrlno().longValue());
            if (dto.getIdetailseqno()==null)
              ps.setNull(2, java.sql.Types.INTEGER);
           else
              ps.setInt(2, dto.getIdetailseqno().intValue());
            ps.setString(3, dto.getSorgcode());
            ps.setString(4, dto.getSfilename());
            ps.setString(5, dto.getSlimitid());
            ps.setString(6, dto.getSofyear());
            ps.setString(7, dto.getSline());
            ps.setString(8, dto.getSpackageno());
            ps.setString(9, dto.getSdealno());
            ps.setString(10, dto.getSpackageticketno());
            ps.setString(11, dto.getSbudgetunitcode());
            ps.setString(12, dto.getSbudgettype());
            ps.setString(13, dto.getSfunsubjectcode());
            ps.setString(14, dto.getSecosubjectcode());
            ps.setBigDecimal(15, dto.getNmoney());
            ps.setString(16, dto.getSaccattrib());
            ps.setString(17, dto.getSaccdate());
            ps.setString(18, dto.getSstatus());
            ps.setString(19, dto.getSusercode());
             ps.setString(20, dto.getSdemo());
            ps.setString(21, dto.getSsupdepname());
            ps.setString(22, dto.getSexpfuncname());
            ps.setString(23, dto.getSexpfunccode1());
            ps.setString(24, dto.getSexpfuncname1());
            ps.setString(25, dto.getSexpfunccode2());
            ps.setString(26, dto.getSexpfuncname2());
            ps.setString(27, dto.getSexpfunccode3());
            ps.setString(28, dto.getSexpfuncname3());
            ps.setString(29, dto.getSprocatcode());
            ps.setString(30, dto.getSprocatname());
            ps.setString(31, dto.getShold1());
            ps.setString(32, dto.getShold2());
            ps.setString(33, dto.getShold3());
            ps.setString(34, dto.getShold4());
            ps.setString(35, dto.getSid());
           rs = ps.executeQuery();
           List results = getResults(rs);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }
        finally
        {
           close(ps);
           close(rs);
        }
   }

 	/**
	* Create batch new record in Database. it only create the first layer dto
	*/
    public void create(IDto[] _dtos, Connection conn)    throws SQLException
    {
        TvGrantpaymsgsubDto dto  ;
     	for (int i= 0; i< _dtos.length ; i++)
 	    {
 		    dto  = (TvGrantpaymsgsubDto)_dtos[i] ; 
    	    String msgValid = dto.checkValid() ;
        	if (msgValid != null)
     	    	throw new SQLException("插入错误，"+msgValid) ;
     	}
 	    PreparedStatement ps = null;
        try
        {
            ps = conn.prepareStatement(SQL_INSERT);
       
     	    for (int i= 0; i< _dtos.length ; i++)
     	    {
 	    	    dto  = (TvGrantpaymsgsubDto)_dtos[i] ; 
  
               if (dto.getIvousrlno()==null)
                  ps.setNull(1, java.sql.Types.BIGINT);
               else
                  ps.setLong(1, dto.getIvousrlno().longValue());
  
               if (dto.getIdetailseqno()==null)
                  ps.setNull(2, java.sql.Types.INTEGER);
               else
                  ps.setInt(2, dto.getIdetailseqno().intValue());
  
               ps.setString(3, dto.getSorgcode());
  
               ps.setString(4, dto.getSfilename());
  
               ps.setString(5, dto.getSlimitid());
  
               ps.setString(6, dto.getSofyear());
  
               ps.setString(7, dto.getSline());
  
               ps.setString(8, dto.getSpackageno());
  
               ps.setString(9, dto.getSdealno());
  
               ps.setString(10, dto.getSpackageticketno());
  
               ps.setString(11, dto.getSbudgetunitcode());
  
               ps.setString(12, dto.getSbudgettype());
  
               ps.setString(13, dto.getSfunsubjectcode());
  
               ps.setString(14, dto.getSecosubjectcode());
  
               ps.setBigDecimal(15, dto.getNmoney());
  
               ps.setString(16, dto.getSaccattrib());
  
               ps.setString(17, dto.getSaccdate());
  
               ps.setString(18, dto.getSstatus());
  
               ps.setString(19, dto.getSusercode());
   
               ps.setString(20, dto.getSdemo());
  
               ps.setString(21, dto.getSsupdepname());
  
               ps.setString(22, dto.getSexpfuncname());
  
               ps.setString(23, dto.getSexpfunccode1());
  
               ps.setString(24, dto.getSexpfuncname1());
  
               ps.setString(25, dto.getSexpfunccode2());
  
               ps.setString(26, dto.getSexpfuncname2());
  
               ps.setString(27, dto.getSexpfunccode3());
  
               ps.setString(28, dto.getSexpfuncname3());
  
               ps.setString(29, dto.getSprocatcode());
  
               ps.setString(30, dto.getSprocatname());
  
               ps.setString(31, dto.getShold1());
  
               ps.setString(32, dto.getShold2());
  
               ps.setString(33, dto.getShold3());
  
               ps.setString(34, dto.getShold4());
  
               ps.setString(35, dto.getSid());
               ps.addBatch(); 
           }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }
        return  ;
   }




     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto find(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TvGrantpaymsgsubPK pk = (TvGrantpaymsgsubPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT);
           if (pk.getIvousrlno()==null)
               ps.setNull(1, java.sql.Types.BIGINT);
           else
               ps.setLong(1, pk.getIvousrlno().longValue());


           if (pk.getIdetailseqno()==null)
               ps.setNull(2, java.sql.Types.INTEGER);
           else
               ps.setInt(2, pk.getIdetailseqno().intValue());

           ps.setString(3, pk.getSpackageticketno());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }

     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto findForUpdate(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TvGrantpaymsgsubPK pk = (TvGrantpaymsgsubPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT_FOR_UPDATE);
           if (pk.getIvousrlno()==null)
               ps.setNull(1, java.sql.Types.BIGINT);
           else
               ps.setLong(1, pk.getIvousrlno().longValue());


           if (pk.getIdetailseqno()==null)
               ps.setNull(2, java.sql.Types.INTEGER);
           else
               ps.setInt(2, pk.getIdetailseqno().intValue());

           ps.setString(3, pk.getSpackageticketno());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }
  
     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto[] find(IPK[] _pks, Connection conn, boolean isLobSupport) throws SQLException
    {
        TvGrantpaymsgsubPK pk ;
        
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TvGrantpaymsgsubPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 
        PreparedStatement ps = null;
        ResultSet rs = null;
        
         
        List results = new ArrayList();  
		for (int iBegin = 0; iBegin < pks.size(); iBegin += FIND_BATCH_SIZE)   //
		{
        
            try {
          		int iFindBatchSize = pks.size() - iBegin ;
	    		if (iFindBatchSize >FIND_BATCH_SIZE )
		    		iFindBatchSize = FIND_BATCH_SIZE ;
		  
        	    StringBuffer sb = new StringBuffer() ;
            	sb.append(SQL_SELECT_BATCH).append(" WHERE ").append(SQL_SELECT_BATCH_WHERE);

	    		for (int i = iBegin+1; i < iBegin + iFindBatchSize; i++)
		    	{
			    	sb.append("OR").append(SQL_SELECT_BATCH_WHERE) ;
    			}
        	
                ps = conn.prepareStatement(sb.toString());

                for (int i = iBegin;i < iBegin + iFindBatchSize; i++)
                {
                    pk  = (TvGrantpaymsgsubPK)(pks.get(i)) ; 
                   if (pk.getIvousrlno()==null)
                      ps.setNull((i-iBegin)*3+1, java.sql.Types.BIGINT);
                   else
                      ps.setLong((i-iBegin)*3+1, pk.getIvousrlno().longValue());

                   if (pk.getIdetailseqno()==null)
                      ps.setNull((i-iBegin)*3+ 2 , java.sql.Types.INTEGER);
                   else
                     ps.setInt((i-iBegin)*3+2, pk.getIdetailseqno().intValue());

                   ps.setString((i-iBegin)*3+3, pk.getSpackageticketno());

			
	            }
                rs = ps.executeQuery();
                results.addAll(getResults(rs,0,isLobSupport)); //
            }finally {
                close(rs);
                close(ps);
            }
        }
        if (results!=null && results.size() > 0)
        {
            TvGrantpaymsgsubDto[] dtos = new TvGrantpaymsgsubDto[0];
		    dtos = (TvGrantpaymsgsubDto[]) results.toArray(dtos) ;
		    return dtos ;
        }
        else
        {
            return null;
        }
    }

  
  
  



    /**
	    * 得到查询语句
	    */
    public String getSelectSQL() 
    {
	        return SQL_SELECT_BATCH_SCROLLABLE ;
    }
   

	public List getResults(ResultSet rs) throws SQLException
    {
    	return getResults(rs, 0) ;
    }
  

    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize) throws SQLException
    {
		return getResults(rs,maxSize,false);
	}
    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize, boolean isLobSupport) throws SQLException
    {
        List results = new ArrayList();
        String str ;
        Reader reader;
        Clob clob;
        char[] chars ;
        
        while (rs.next())
         {
             TvGrantpaymsgsubDto  dto = new  TvGrantpaymsgsubDto ();
             //I_VOUSRLNO
             str = rs.getString("I_VOUSRLNO");
             if(str!=null){
                dto.setIvousrlno(new Long(str));
             }

             //I_DETAILSEQNO
             str = rs.getString("I_DETAILSEQNO");
             if(str!=null){
                dto.setIdetailseqno(new Integer(str));
             }

             //S_ORGCODE
             str = rs.getString("S_ORGCODE");
             if (str == null)
                dto.setSorgcode(null);
             else
                dto.setSorgcode(str.trim());

             //S_FILENAME
             str = rs.getString("S_FILENAME");
             if (str == null)
                dto.setSfilename(null);
             else
                dto.setSfilename(str.trim());

             //S_LIMITID
             str = rs.getString("S_LIMITID");
             if (str == null)
                dto.setSlimitid(null);
             else
                dto.setSlimitid(str.trim());

             //S_OFYEAR
             str = rs.getString("S_OFYEAR");
             if (str == null)
                dto.setSofyear(null);
             else
                dto.setSofyear(str.trim());

             //S_LINE
             str = rs.getString("S_LINE");
             if (str == null)
                dto.setSline(null);
             else
                dto.setSline(str.trim());

             //S_PACKAGENO
             str = rs.getString("S_PACKAGENO");
             if (str == null)
                dto.setSpackageno(null);
             else
                dto.setSpackageno(str.trim());

             //S_DEALNO
             str = rs.getString("S_DEALNO");
             if (str == null)
                dto.setSdealno(null);
             else
                dto.setSdealno(str.trim());

             //S_PACKAGETICKETNO
             str = rs.getString("S_PACKAGETICKETNO");
             if (str == null)
                dto.setSpackageticketno(null);
             else
                dto.setSpackageticketno(str.trim());

             //S_BUDGETUNITCODE
             str = rs.getString("S_BUDGETUNITCODE");
             if (str == null)
                dto.setSbudgetunitcode(null);
             else
                dto.setSbudgetunitcode(str.trim());

             //S_BUDGETTYPE
             str = rs.getString("S_BUDGETTYPE");
             if (str == null)
                dto.setSbudgettype(null);
             else
                dto.setSbudgettype(str.trim());

             //S_FUNSUBJECTCODE
             str = rs.getString("S_FUNSUBJECTCODE");
             if (str == null)
                dto.setSfunsubjectcode(null);
             else
                dto.setSfunsubjectcode(str.trim());

             //S_ECOSUBJECTCODE
             str = rs.getString("S_ECOSUBJECTCODE");
             if (str == null)
                dto.setSecosubjectcode(null);
             else
                dto.setSecosubjectcode(str.trim());

             //N_MONEY
           dto.setNmoney(rs.getBigDecimal("N_MONEY"));

             //S_ACCATTRIB
             str = rs.getString("S_ACCATTRIB");
             if (str == null)
                dto.setSaccattrib(null);
             else
                dto.setSaccattrib(str.trim());

             //S_ACCDATE
             str = rs.getString("S_ACCDATE");
             if (str == null)
                dto.setSaccdate(null);
             else
                dto.setSaccdate(str.trim());

             //S_STATUS
             str = rs.getString("S_STATUS");
             if (str == null)
                dto.setSstatus(null);
             else
                dto.setSstatus(str.trim());

             //S_USERCODE
             str = rs.getString("S_USERCODE");
             if (str == null)
                dto.setSusercode(null);
             else
                dto.setSusercode(str.trim());

             //TS_SYSUPDATE
           dto.setTssysupdate(rs.getTimestamp("TS_SYSUPDATE"));

             //S_DEMO
             str = rs.getString("S_DEMO");
             if (str == null)
                dto.setSdemo(null);
             else
                dto.setSdemo(str.trim());

             //S_SUPDEPNAME
             str = rs.getString("S_SUPDEPNAME");
             if (str == null)
                dto.setSsupdepname(null);
             else
                dto.setSsupdepname(str.trim());

             //S_EXPFUNCNAME
             str = rs.getString("S_EXPFUNCNAME");
             if (str == null)
                dto.setSexpfuncname(null);
             else
                dto.setSexpfuncname(str.trim());

             //S_EXPFUNCCODE1
             str = rs.getString("S_EXPFUNCCODE1");
             if (str == null)
                dto.setSexpfunccode1(null);
             else
                dto.setSexpfunccode1(str.trim());

             //S_EXPFUNCNAME1
             str = rs.getString("S_EXPFUNCNAME1");
             if (str == null)
                dto.setSexpfuncname1(null);
             else
                dto.setSexpfuncname1(str.trim());

             //S_EXPFUNCCODE2
             str = rs.getString("S_EXPFUNCCODE2");
             if (str == null)
                dto.setSexpfunccode2(null);
             else
                dto.setSexpfunccode2(str.trim());

             //S_EXPFUNCNAME2
             str = rs.getString("S_EXPFUNCNAME2");
             if (str == null)
                dto.setSexpfuncname2(null);
             else
                dto.setSexpfuncname2(str.trim());

             //S_EXPFUNCCODE3
             str = rs.getString("S_EXPFUNCCODE3");
             if (str == null)
                dto.setSexpfunccode3(null);
             else
                dto.setSexpfunccode3(str.trim());

             //S_EXPFUNCNAME3
             str = rs.getString("S_EXPFUNCNAME3");
             if (str == null)
                dto.setSexpfuncname3(null);
             else
                dto.setSexpfuncname3(str.trim());

             //S_PROCATCODE
             str = rs.getString("S_PROCATCODE");
             if (str == null)
                dto.setSprocatcode(null);
             else
                dto.setSprocatcode(str.trim());

             //S_PROCATNAME
             str = rs.getString("S_PROCATNAME");
             if (str == null)
                dto.setSprocatname(null);
             else
                dto.setSprocatname(str.trim());

             //S_HOLD1
             str = rs.getString("S_HOLD1");
             if (str == null)
                dto.setShold1(null);
             else
                dto.setShold1(str.trim());

             //S_HOLD2
             str = rs.getString("S_HOLD2");
             if (str == null)
                dto.setShold2(null);
             else
                dto.setShold2(str.trim());

             //S_HOLD3
             str = rs.getString("S_HOLD3");
             if (str == null)
                dto.setShold3(null);
             else
                dto.setShold3(str.trim());

             //S_HOLD4
             str = rs.getString("S_HOLD4");
             if (str == null)
                dto.setShold4(null);
             else
                dto.setShold4(str.trim());

             //S_ID
             str = rs.getString("S_ID");
             if (str == null)
                dto.setSid(null);
             else
                dto.setSid(str.trim());



            results.add(dto);
            
            if(maxSize >0  && results.size() == maxSize)
            {
            	break ;
            }
            
         }
        
		return results ;
    }

  /**
   * populate resultSet as cclass
   */
  protected IDto[] getChildrenResults(ResultSet rs ,Class _dto) throws SQLException{

      throw new SQLException("本dto没有与其它dto相关联，无法应用此方法");
  }


     /**
     * Update a record in Database.
     */

    public void update(IDto _dto, Connection conn, boolean isLobSupport) throws SQLException
    {
        String msgValid = _dto.checkValid() ;
        if (msgValid != null)
            throw new SQLException("修改错误，"+msgValid) ;
        TvGrantpaymsgsubDto dto = (TvGrantpaymsgsubDto)_dto ;
        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            int pos = 1;
            //S_ORGCODE
            ps.setString(pos, dto.getSorgcode());
            pos++;

            //S_FILENAME
            ps.setString(pos, dto.getSfilename());
            pos++;

            //S_LIMITID
            ps.setString(pos, dto.getSlimitid());
            pos++;

            //S_OFYEAR
            ps.setString(pos, dto.getSofyear());
            pos++;

            //S_LINE
            ps.setString(pos, dto.getSline());
            pos++;

            //S_PACKAGENO
            ps.setString(pos, dto.getSpackageno());
            pos++;

            //S_DEALNO
            ps.setString(pos, dto.getSdealno());
            pos++;

            //S_BUDGETUNITCODE
            ps.setString(pos, dto.getSbudgetunitcode());
            pos++;

            //S_BUDGETTYPE
            ps.setString(pos, dto.getSbudgettype());
            pos++;

            //S_FUNSUBJECTCODE
            ps.setString(pos, dto.getSfunsubjectcode());
            pos++;

            //S_ECOSUBJECTCODE
            ps.setString(pos, dto.getSecosubjectcode());
            pos++;

            //N_MONEY
            ps.setBigDecimal(pos, dto.getNmoney());
            pos++;

            //S_ACCATTRIB
            ps.setString(pos, dto.getSaccattrib());
            pos++;

            //S_ACCDATE
            ps.setString(pos, dto.getSaccdate());
            pos++;

            //S_STATUS
            ps.setString(pos, dto.getSstatus());
            pos++;

            //S_USERCODE
            ps.setString(pos, dto.getSusercode());
            pos++;

            //TS_SYSUPDATE
            //S_DEMO
            ps.setString(pos, dto.getSdemo());
            pos++;

            //S_SUPDEPNAME
            ps.setString(pos, dto.getSsupdepname());
            pos++;

            //S_EXPFUNCNAME
            ps.setString(pos, dto.getSexpfuncname());
            pos++;

            //S_EXPFUNCCODE1
            ps.setString(pos, dto.getSexpfunccode1());
            pos++;

            //S_EXPFUNCNAME1
            ps.setString(pos, dto.getSexpfuncname1());
            pos++;

            //S_EXPFUNCCODE2
            ps.setString(pos, dto.getSexpfunccode2());
            pos++;

            //S_EXPFUNCNAME2
            ps.setString(pos, dto.getSexpfuncname2());
            pos++;

            //S_EXPFUNCCODE3
            ps.setString(pos, dto.getSexpfunccode3());
            pos++;

            //S_EXPFUNCNAME3
            ps.setString(pos, dto.getSexpfuncname3());
            pos++;

            //S_PROCATCODE
            ps.setString(pos, dto.getSprocatcode());
            pos++;

            //S_PROCATNAME
            ps.setString(pos, dto.getSprocatname());
            pos++;

            //S_HOLD1
            ps.setString(pos, dto.getShold1());
            pos++;

            //S_HOLD2
            ps.setString(pos, dto.getShold2());
            pos++;

            //S_HOLD3
            ps.setString(pos, dto.getShold3());
            pos++;

            //S_HOLD4
            ps.setString(pos, dto.getShold4());
            pos++;

            //S_ID
            ps.setString(pos, dto.getSid());
            pos++;


           //I_VOUSRLNO
           ps.setLong(pos, dto.getIvousrlno().longValue());
           pos++;
           //I_DETAILSEQNO
           ps.setInt(pos, dto.getIdetailseqno().intValue());
           pos++;
           //S_PACKAGETICKETNO
           ps.setString(pos, dto.getSpackageticketno());
           pos++;
           ps.executeUpdate();
        }
        finally
        {
           close(ps);
        }

        return ;
    }
    
    
    
      /**
     * Update batch record in Database.
     */

    public void update(IDto[] _dtos, Connection conn, boolean isLobSupport) throws SQLException
    {
     
     	 TvGrantpaymsgsubDto dto  ;
         for (int i= 0; i< _dtos.length ; i++)
         {
            dto  = (TvGrantpaymsgsubDto)_dtos[i] ; 
            String msgValid = dto.checkValid() ;
            if (msgValid != null)
                throw new SQLException("修改错误，"+msgValid) ;
         }

        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            
            for (int i= 0; i< _dtos.length ; i++)
            {
                dto  = (TvGrantpaymsgsubDto)_dtos[i] ; 
                int pos = 1;
                //S_ORGCODE
                 ps.setString(pos, dto.getSorgcode());
                pos++;

                //S_FILENAME
                 ps.setString(pos, dto.getSfilename());
                pos++;

                //S_LIMITID
                 ps.setString(pos, dto.getSlimitid());
                pos++;

                //S_OFYEAR
                 ps.setString(pos, dto.getSofyear());
                pos++;

                //S_LINE
                 ps.setString(pos, dto.getSline());
                pos++;

                //S_PACKAGENO
                 ps.setString(pos, dto.getSpackageno());
                pos++;

                //S_DEALNO
                 ps.setString(pos, dto.getSdealno());
                pos++;

                //S_BUDGETUNITCODE
                 ps.setString(pos, dto.getSbudgetunitcode());
                pos++;

                //S_BUDGETTYPE
                 ps.setString(pos, dto.getSbudgettype());
                pos++;

                //S_FUNSUBJECTCODE
                 ps.setString(pos, dto.getSfunsubjectcode());
                pos++;

                //S_ECOSUBJECTCODE
                 ps.setString(pos, dto.getSecosubjectcode());
                pos++;

                //N_MONEY
                 ps.setBigDecimal(pos, dto.getNmoney());
                pos++;

                //S_ACCATTRIB
                 ps.setString(pos, dto.getSaccattrib());
                pos++;

                //S_ACCDATE
                 ps.setString(pos, dto.getSaccdate());
                pos++;

                //S_STATUS
                 ps.setString(pos, dto.getSstatus());
                pos++;

                //S_USERCODE
                 ps.setString(pos, dto.getSusercode());
                pos++;

                //TS_SYSUPDATE
                 //S_DEMO
                 ps.setString(pos, dto.getSdemo());
                pos++;

                //S_SUPDEPNAME
                 ps.setString(pos, dto.getSsupdepname());
                pos++;

                //S_EXPFUNCNAME
                 ps.setString(pos, dto.getSexpfuncname());
                pos++;

                //S_EXPFUNCCODE1
                 ps.setString(pos, dto.getSexpfunccode1());
                pos++;

                //S_EXPFUNCNAME1
                 ps.setString(pos, dto.getSexpfuncname1());
                pos++;

                //S_EXPFUNCCODE2
                 ps.setString(pos, dto.getSexpfunccode2());
                pos++;

                //S_EXPFUNCNAME2
                 ps.setString(pos, dto.getSexpfuncname2());
                pos++;

                //S_EXPFUNCCODE3
                 ps.setString(pos, dto.getSexpfunccode3());
                pos++;

                //S_EXPFUNCNAME3
                 ps.setString(pos, dto.getSexpfuncname3());
                pos++;

                //S_PROCATCODE
                 ps.setString(pos, dto.getSprocatcode());
                pos++;

                //S_PROCATNAME
                 ps.setString(pos, dto.getSprocatname());
                pos++;

                //S_HOLD1
                 ps.setString(pos, dto.getShold1());
                pos++;

                //S_HOLD2
                 ps.setString(pos, dto.getShold2());
                pos++;

                //S_HOLD3
                 ps.setString(pos, dto.getShold3());
                pos++;

                //S_HOLD4
                 ps.setString(pos, dto.getShold4());
                pos++;

                //S_ID
                 ps.setString(pos, dto.getSid());
                pos++;


               //I_VOUSRLNO
               ps.setLong(pos, dto.getIvousrlno().longValue());
               pos++;
               //I_DETAILSEQNO
               ps.setInt(pos, dto.getIdetailseqno().intValue());
               pos++;
               //S_PACKAGETICKETNO
               ps.setString(pos, dto.getSpackageticketno());
               pos++;
		       ps.addBatch(); 
	       }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }

        return ;
    }



    /**
     * delete a record in Database.
     */
    public void delete(IPK _pk, Connection conn) throws SQLException
    {
       
      
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return ;
        //	throw new SQLException("删除错误，"+msgValid) ;
       }
       TvGrantpaymsgsubPK pk = (TvGrantpaymsgsubPK)_pk ;


       PreparedStatement ps = null;
       try {
           ps = conn.prepareStatement(SQL_DELETE);
           ps.setLong(1, pk.getIvousrlno().longValue());
           ps.setInt(2, pk.getIdetailseqno().intValue());
           ps.setString(3, pk.getSpackageticketno());
           ps.executeUpdate();
        }
        finally
        {
            close(ps);
        }
    }


     /**
     * delete batch record in Database.
     */
    public void delete(IPK[] _pks, Connection conn) throws SQLException
    {
        TvGrantpaymsgsubPK pk ;
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TvGrantpaymsgsubPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 		
 		
        PreparedStatement ps = null;
        try
         {
            ps = conn.prepareStatement(SQL_DELETE);
             
       		for (int i= 0; i< pks.size() ; i++)
       		{
       			pk  = (TvGrantpaymsgsubPK)(pks.get(i)) ; 
                ps.setLong(1, pk.getIvousrlno().longValue());
                ps.setInt(2, pk.getIdetailseqno().intValue());
                ps.setString(3, pk.getSpackageticketno());
		        ps.addBatch() ;
			}
            ps.executeBatch();
        }
        finally
        {
            close(ps);
        }      
    }



    /**
    * Check IChangerNumber or UpdateTimeStamp.
    */
    public void check(IDto _dto, Connection conn) throws SQLException
    {
                                                                                                   		throw new SQLException("数据库表：TV_GRANTPAYMSGSUB没有检查修改的字段");
    }


	/**
	* synchronize values between parent and children
	*/
    public void syncToChildren(IDto _dto)     throws SQLException{
       throw new SQLException("本记录没有与其它记录相关联，无法进行主记录与明细记录之间的数据同步。");
  }

	/**
	*get children
	*/
    public IDto[] findChildren (IDto _dto, Connection conn, boolean isLobSupport)     throws SQLException{
         throw new SQLException("本dto没有与其它dto相关联，无法得到子dto");
  }    
    
  /**
   *return the children IDto class
   * @return Class
   */
  public Class getChildrenClass(){
	  return null;
  };    



    /**
     * Close JDBC Statement.
     * @param stmt  Statement to be closed.
     */
    private void close(Statement stmt)
    {
        if (stmt != null)
        {
            try
            {
                stmt.close();
            }
            catch(SQLException    e)
            {}
        }
    }

    /**
     * Close JDBC ResultSet.
     * @param rs  ResultSet to be closed.
     */
    private void close(ResultSet rs)
    {
        if (rs != null)
        {
            try
            {
                rs.close();
            }catch(SQLException  e){}
        }
    }
}
