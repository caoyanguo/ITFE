    



package com.cfcc.itfe.persistence.dao;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.Statement;
import java.sql.SQLException;
import java.sql.Date;
import java.sql.Time;
import java.sql.Timestamp ;
import java.sql.Clob;

import com.cfcc.jaf.persistence.jaform.parent.* ;

import java.math.BigDecimal;

import java.util.List;
import java.util.ArrayList;
import java.util.HashMap;

import java.io.StringReader;
import java.io.Reader;
import java.io.IOException;

import com.cfcc.itfe.persistence.dto.TfPaymentDetailsmainDto ;
import com.cfcc.itfe.persistence.pk.TfPaymentDetailsmainPK ;


/**
 * <p>Title: DAO of table: TF_PAYMENT_DETAILSMAIN</p>
 * <p>Description:财政批量业务支付明细8207主表 Data Access Object  </p>
 * <p>Copyright: Copyright (c) 2005</p>
 * <p>Company: CFCC </p>
 * <p>Generation Time: 2014-12-25 15:28:58 </p>
 *  Do NOT change this file by hand.
 *  This java source file is generated by JAF_ORM .
 *  dao.vm timestamp: 2007-04-06 08:30:00
 ******************************************************
 * <p> <b>change record</b><br>
 *  make dao methods simply.<br>
 *  especially make update,delete methods simply.<br>
 *  remove "..withCheck" method,withCheck will impl in JAFOrmTemplate.<br>
 *  remove "delete(IDto _dto,..)" method.<br></p>
 * @author win7
 */

public class TfPaymentDetailsmainDao  implements IDao
{


    /* SQL to insert data */
    private static final String SQL_INSERT =
        "INSERT INTO TF_PAYMENT_DETAILSMAIN ("
          + "I_VOUSRLNO,S_ORGCODE,S_TRECODE,S_STATUS,S_DEMO"
          + ",S_FILENAME,TS_SYSUPDATE,S_PACKAGENO,S_ADMDIVCODE,S_STYEAR"
          + ",S_VTCODE,S_VOUDATE,S_VOUCHERNO,S_ORIGINALVTCODE,S_ORIGINALVOUCHERNO"
          + ",S_FUNDTYPECODE,S_FUNDTYPENAME,S_PAYDICTATENO,S_PAYMSGNO,S_PAYENTRUSTDATE"
          + ",S_PAYSNDBNKNO,N_SUMAMT,S_AGENCYCODE,S_AGENCYNAME,S_PAYACCTNO"
          + ",S_PAYACCTNAME,S_PAYACCTBANKNAME,S_PAYBANKCODE,S_PAYBANKNAME,S_BUSINESSTYPECODE"
          + ",S_BUSINESSTYPENAME,S_PAYTYPECODE,S_PAYTYPENAME,S_XPAYDATE,N_XSUMAMT"
          + ",S_HOLD1,S_HOLD2,S_EXT1,S_EXT2,S_EXT3"
          + ",S_EXT4,S_EXT5"
        + ") VALUES ("
        + "?,?,?,?,?,?,CURRENT TIMESTAMP ,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)";


    private static final String SQL_INSERT_WITH_RESULT = "SELECT * FROM FINAL TABLE( " + SQL_INSERT + " )";
    
    /* SQL to select data */
    private static final String SQL_SELECT =
        "SELECT "
        + "TF_PAYMENT_DETAILSMAIN.I_VOUSRLNO, TF_PAYMENT_DETAILSMAIN.S_ORGCODE, TF_PAYMENT_DETAILSMAIN.S_TRECODE, TF_PAYMENT_DETAILSMAIN.S_STATUS, TF_PAYMENT_DETAILSMAIN.S_DEMO, "
        + "TF_PAYMENT_DETAILSMAIN.S_FILENAME, TF_PAYMENT_DETAILSMAIN.TS_SYSUPDATE, TF_PAYMENT_DETAILSMAIN.S_PACKAGENO, TF_PAYMENT_DETAILSMAIN.S_ADMDIVCODE, TF_PAYMENT_DETAILSMAIN.S_STYEAR, "
        + "TF_PAYMENT_DETAILSMAIN.S_VTCODE, TF_PAYMENT_DETAILSMAIN.S_VOUDATE, TF_PAYMENT_DETAILSMAIN.S_VOUCHERNO, TF_PAYMENT_DETAILSMAIN.S_ORIGINALVTCODE, TF_PAYMENT_DETAILSMAIN.S_ORIGINALVOUCHERNO, "
        + "TF_PAYMENT_DETAILSMAIN.S_FUNDTYPECODE, TF_PAYMENT_DETAILSMAIN.S_FUNDTYPENAME, TF_PAYMENT_DETAILSMAIN.S_PAYDICTATENO, TF_PAYMENT_DETAILSMAIN.S_PAYMSGNO, TF_PAYMENT_DETAILSMAIN.S_PAYENTRUSTDATE, "
        + "TF_PAYMENT_DETAILSMAIN.S_PAYSNDBNKNO, TF_PAYMENT_DETAILSMAIN.N_SUMAMT, TF_PAYMENT_DETAILSMAIN.S_AGENCYCODE, TF_PAYMENT_DETAILSMAIN.S_AGENCYNAME, TF_PAYMENT_DETAILSMAIN.S_PAYACCTNO, "
        + "TF_PAYMENT_DETAILSMAIN.S_PAYACCTNAME, TF_PAYMENT_DETAILSMAIN.S_PAYACCTBANKNAME, TF_PAYMENT_DETAILSMAIN.S_PAYBANKCODE, TF_PAYMENT_DETAILSMAIN.S_PAYBANKNAME, TF_PAYMENT_DETAILSMAIN.S_BUSINESSTYPECODE, "
        + "TF_PAYMENT_DETAILSMAIN.S_BUSINESSTYPENAME, TF_PAYMENT_DETAILSMAIN.S_PAYTYPECODE, TF_PAYMENT_DETAILSMAIN.S_PAYTYPENAME, TF_PAYMENT_DETAILSMAIN.S_XPAYDATE, TF_PAYMENT_DETAILSMAIN.N_XSUMAMT, "
        + "TF_PAYMENT_DETAILSMAIN.S_HOLD1, TF_PAYMENT_DETAILSMAIN.S_HOLD2, TF_PAYMENT_DETAILSMAIN.S_EXT1, TF_PAYMENT_DETAILSMAIN.S_EXT2, TF_PAYMENT_DETAILSMAIN.S_EXT3, "
        + "TF_PAYMENT_DETAILSMAIN.S_EXT4, TF_PAYMENT_DETAILSMAIN.S_EXT5 "
        + "FROM TF_PAYMENT_DETAILSMAIN "
        +" WHERE " 
        + "I_VOUSRLNO = ?"
        ;
    /* SQL to select for update data */
    private static final String SQL_SELECT_FOR_UPDATE =
        "SELECT "
        + "TF_PAYMENT_DETAILSMAIN.I_VOUSRLNO, TF_PAYMENT_DETAILSMAIN.S_ORGCODE, TF_PAYMENT_DETAILSMAIN.S_TRECODE, TF_PAYMENT_DETAILSMAIN.S_STATUS, TF_PAYMENT_DETAILSMAIN.S_DEMO, "
        + "TF_PAYMENT_DETAILSMAIN.S_FILENAME, TF_PAYMENT_DETAILSMAIN.TS_SYSUPDATE, TF_PAYMENT_DETAILSMAIN.S_PACKAGENO, TF_PAYMENT_DETAILSMAIN.S_ADMDIVCODE, TF_PAYMENT_DETAILSMAIN.S_STYEAR, "
        + "TF_PAYMENT_DETAILSMAIN.S_VTCODE, TF_PAYMENT_DETAILSMAIN.S_VOUDATE, TF_PAYMENT_DETAILSMAIN.S_VOUCHERNO, TF_PAYMENT_DETAILSMAIN.S_ORIGINALVTCODE, TF_PAYMENT_DETAILSMAIN.S_ORIGINALVOUCHERNO, "
        + "TF_PAYMENT_DETAILSMAIN.S_FUNDTYPECODE, TF_PAYMENT_DETAILSMAIN.S_FUNDTYPENAME, TF_PAYMENT_DETAILSMAIN.S_PAYDICTATENO, TF_PAYMENT_DETAILSMAIN.S_PAYMSGNO, TF_PAYMENT_DETAILSMAIN.S_PAYENTRUSTDATE, "
        + "TF_PAYMENT_DETAILSMAIN.S_PAYSNDBNKNO, TF_PAYMENT_DETAILSMAIN.N_SUMAMT, TF_PAYMENT_DETAILSMAIN.S_AGENCYCODE, TF_PAYMENT_DETAILSMAIN.S_AGENCYNAME, TF_PAYMENT_DETAILSMAIN.S_PAYACCTNO, "
        + "TF_PAYMENT_DETAILSMAIN.S_PAYACCTNAME, TF_PAYMENT_DETAILSMAIN.S_PAYACCTBANKNAME, TF_PAYMENT_DETAILSMAIN.S_PAYBANKCODE, TF_PAYMENT_DETAILSMAIN.S_PAYBANKNAME, TF_PAYMENT_DETAILSMAIN.S_BUSINESSTYPECODE, "
        + "TF_PAYMENT_DETAILSMAIN.S_BUSINESSTYPENAME, TF_PAYMENT_DETAILSMAIN.S_PAYTYPECODE, TF_PAYMENT_DETAILSMAIN.S_PAYTYPENAME, TF_PAYMENT_DETAILSMAIN.S_XPAYDATE, TF_PAYMENT_DETAILSMAIN.N_XSUMAMT, "
        + "TF_PAYMENT_DETAILSMAIN.S_HOLD1, TF_PAYMENT_DETAILSMAIN.S_HOLD2, TF_PAYMENT_DETAILSMAIN.S_EXT1, TF_PAYMENT_DETAILSMAIN.S_EXT2, TF_PAYMENT_DETAILSMAIN.S_EXT3, "
        + "TF_PAYMENT_DETAILSMAIN.S_EXT4, TF_PAYMENT_DETAILSMAIN.S_EXT5 "
        + "FROM TF_PAYMENT_DETAILSMAIN "
        +" WHERE " 
        + "I_VOUSRLNO = ? FOR UPDATE"
        ;

      /* SQL to select data (SCROLLABLE)*/
     private static final String SQL_SELECT_BATCH_SCROLLABLE =
        "SELECT "
        + "  TF_PAYMENT_DETAILSMAIN.I_VOUSRLNO  , TF_PAYMENT_DETAILSMAIN.S_ORGCODE  , TF_PAYMENT_DETAILSMAIN.S_TRECODE  , TF_PAYMENT_DETAILSMAIN.S_STATUS  , TF_PAYMENT_DETAILSMAIN.S_DEMO "
        + " , TF_PAYMENT_DETAILSMAIN.S_FILENAME  , TF_PAYMENT_DETAILSMAIN.TS_SYSUPDATE  , TF_PAYMENT_DETAILSMAIN.S_PACKAGENO  , TF_PAYMENT_DETAILSMAIN.S_ADMDIVCODE  , TF_PAYMENT_DETAILSMAIN.S_STYEAR "
        + " , TF_PAYMENT_DETAILSMAIN.S_VTCODE  , TF_PAYMENT_DETAILSMAIN.S_VOUDATE  , TF_PAYMENT_DETAILSMAIN.S_VOUCHERNO  , TF_PAYMENT_DETAILSMAIN.S_ORIGINALVTCODE  , TF_PAYMENT_DETAILSMAIN.S_ORIGINALVOUCHERNO "
        + " , TF_PAYMENT_DETAILSMAIN.S_FUNDTYPECODE  , TF_PAYMENT_DETAILSMAIN.S_FUNDTYPENAME  , TF_PAYMENT_DETAILSMAIN.S_PAYDICTATENO  , TF_PAYMENT_DETAILSMAIN.S_PAYMSGNO  , TF_PAYMENT_DETAILSMAIN.S_PAYENTRUSTDATE "
        + " , TF_PAYMENT_DETAILSMAIN.S_PAYSNDBNKNO  , TF_PAYMENT_DETAILSMAIN.N_SUMAMT  , TF_PAYMENT_DETAILSMAIN.S_AGENCYCODE  , TF_PAYMENT_DETAILSMAIN.S_AGENCYNAME  , TF_PAYMENT_DETAILSMAIN.S_PAYACCTNO "
        + " , TF_PAYMENT_DETAILSMAIN.S_PAYACCTNAME  , TF_PAYMENT_DETAILSMAIN.S_PAYACCTBANKNAME  , TF_PAYMENT_DETAILSMAIN.S_PAYBANKCODE  , TF_PAYMENT_DETAILSMAIN.S_PAYBANKNAME  , TF_PAYMENT_DETAILSMAIN.S_BUSINESSTYPECODE "
        + " , TF_PAYMENT_DETAILSMAIN.S_BUSINESSTYPENAME  , TF_PAYMENT_DETAILSMAIN.S_PAYTYPECODE  , TF_PAYMENT_DETAILSMAIN.S_PAYTYPENAME  , TF_PAYMENT_DETAILSMAIN.S_XPAYDATE  , TF_PAYMENT_DETAILSMAIN.N_XSUMAMT "
        + " , TF_PAYMENT_DETAILSMAIN.S_HOLD1  , TF_PAYMENT_DETAILSMAIN.S_HOLD2  , TF_PAYMENT_DETAILSMAIN.S_EXT1  , TF_PAYMENT_DETAILSMAIN.S_EXT2  , TF_PAYMENT_DETAILSMAIN.S_EXT3 "
        + " , TF_PAYMENT_DETAILSMAIN.S_EXT4  , TF_PAYMENT_DETAILSMAIN.S_EXT5 "
        + "FROM TF_PAYMENT_DETAILSMAIN ";



    /* SQL to select batch data */
    private static final String SQL_SELECT_BATCH =
        "SELECT "
        + "TF_PAYMENT_DETAILSMAIN.I_VOUSRLNO, TF_PAYMENT_DETAILSMAIN.S_ORGCODE, TF_PAYMENT_DETAILSMAIN.S_TRECODE, TF_PAYMENT_DETAILSMAIN.S_STATUS, TF_PAYMENT_DETAILSMAIN.S_DEMO, "
        + "TF_PAYMENT_DETAILSMAIN.S_FILENAME, TF_PAYMENT_DETAILSMAIN.TS_SYSUPDATE, TF_PAYMENT_DETAILSMAIN.S_PACKAGENO, TF_PAYMENT_DETAILSMAIN.S_ADMDIVCODE, TF_PAYMENT_DETAILSMAIN.S_STYEAR, "
        + "TF_PAYMENT_DETAILSMAIN.S_VTCODE, TF_PAYMENT_DETAILSMAIN.S_VOUDATE, TF_PAYMENT_DETAILSMAIN.S_VOUCHERNO, TF_PAYMENT_DETAILSMAIN.S_ORIGINALVTCODE, TF_PAYMENT_DETAILSMAIN.S_ORIGINALVOUCHERNO, "
        + "TF_PAYMENT_DETAILSMAIN.S_FUNDTYPECODE, TF_PAYMENT_DETAILSMAIN.S_FUNDTYPENAME, TF_PAYMENT_DETAILSMAIN.S_PAYDICTATENO, TF_PAYMENT_DETAILSMAIN.S_PAYMSGNO, TF_PAYMENT_DETAILSMAIN.S_PAYENTRUSTDATE, "
        + "TF_PAYMENT_DETAILSMAIN.S_PAYSNDBNKNO, TF_PAYMENT_DETAILSMAIN.N_SUMAMT, TF_PAYMENT_DETAILSMAIN.S_AGENCYCODE, TF_PAYMENT_DETAILSMAIN.S_AGENCYNAME, TF_PAYMENT_DETAILSMAIN.S_PAYACCTNO, "
        + "TF_PAYMENT_DETAILSMAIN.S_PAYACCTNAME, TF_PAYMENT_DETAILSMAIN.S_PAYACCTBANKNAME, TF_PAYMENT_DETAILSMAIN.S_PAYBANKCODE, TF_PAYMENT_DETAILSMAIN.S_PAYBANKNAME, TF_PAYMENT_DETAILSMAIN.S_BUSINESSTYPECODE, "
        + "TF_PAYMENT_DETAILSMAIN.S_BUSINESSTYPENAME, TF_PAYMENT_DETAILSMAIN.S_PAYTYPECODE, TF_PAYMENT_DETAILSMAIN.S_PAYTYPENAME, TF_PAYMENT_DETAILSMAIN.S_XPAYDATE, TF_PAYMENT_DETAILSMAIN.N_XSUMAMT, "
        + "TF_PAYMENT_DETAILSMAIN.S_HOLD1, TF_PAYMENT_DETAILSMAIN.S_HOLD2, TF_PAYMENT_DETAILSMAIN.S_EXT1, TF_PAYMENT_DETAILSMAIN.S_EXT2, TF_PAYMENT_DETAILSMAIN.S_EXT3, "
        + "TF_PAYMENT_DETAILSMAIN.S_EXT4, TF_PAYMENT_DETAILSMAIN.S_EXT5 "
        + "FROM TF_PAYMENT_DETAILSMAIN " ;
        

   /* SQL to select batch data where condition  */
    private static final String SQL_SELECT_BATCH_WHERE =" ( "
        + "I_VOUSRLNO = ?)"
        ;


    /* SQL to update data */
    private static final String SQL_UPDATE =
        "UPDATE TF_PAYMENT_DETAILSMAIN SET "
        + "S_ORGCODE =?,S_TRECODE =?,S_STATUS =?,S_DEMO =?,S_FILENAME =?, "
        + "TS_SYSUPDATE =CURRENT TIMESTAMP,S_PACKAGENO =?,S_ADMDIVCODE =?,S_STYEAR =?,S_VTCODE =?, "
        + "S_VOUDATE =?,S_VOUCHERNO =?,S_ORIGINALVTCODE =?,S_ORIGINALVOUCHERNO =?,S_FUNDTYPECODE =?, "
        + "S_FUNDTYPENAME =?,S_PAYDICTATENO =?,S_PAYMSGNO =?,S_PAYENTRUSTDATE =?,S_PAYSNDBNKNO =?, "
        + "N_SUMAMT =?,S_AGENCYCODE =?,S_AGENCYNAME =?,S_PAYACCTNO =?,S_PAYACCTNAME =?, "
        + "S_PAYACCTBANKNAME =?,S_PAYBANKCODE =?,S_PAYBANKNAME =?,S_BUSINESSTYPECODE =?,S_BUSINESSTYPENAME =?, "
        + "S_PAYTYPECODE =?,S_PAYTYPENAME =?,S_XPAYDATE =?,N_XSUMAMT =?,S_HOLD1 =?, "
        + "S_HOLD2 =?,S_EXT1 =?,S_EXT2 =?,S_EXT3 =?,S_EXT4 =?, "
        + "S_EXT5 =? "
        + "WHERE "
        + "I_VOUSRLNO = ?"
        ;

	/* SQL to update data, support LOB */
    private static final String SQL_UPDATE_LOB =
        "UPDATE TF_PAYMENT_DETAILSMAIN SET "
        + "S_ORGCODE =?, S_TRECODE =?, S_STATUS =?, S_DEMO =?, S_FILENAME =?,  "
        + "TS_SYSUPDATE =CURRENT TIMESTAMP, S_PACKAGENO =?, S_ADMDIVCODE =?, S_STYEAR =?, S_VTCODE =?,  "
        + "S_VOUDATE =?, S_VOUCHERNO =?, S_ORIGINALVTCODE =?, S_ORIGINALVOUCHERNO =?, S_FUNDTYPECODE =?,  "
        + "S_FUNDTYPENAME =?, S_PAYDICTATENO =?, S_PAYMSGNO =?, S_PAYENTRUSTDATE =?, S_PAYSNDBNKNO =?,  "
        + "N_SUMAMT =?, S_AGENCYCODE =?, S_AGENCYNAME =?, S_PAYACCTNO =?, S_PAYACCTNAME =?,  "
        + "S_PAYACCTBANKNAME =?, S_PAYBANKCODE =?, S_PAYBANKNAME =?, S_BUSINESSTYPECODE =?, S_BUSINESSTYPENAME =?,  "
        + "S_PAYTYPECODE =?, S_PAYTYPENAME =?, S_XPAYDATE =?, N_XSUMAMT =?, S_HOLD1 =?,  "
        + "S_HOLD2 =?, S_EXT1 =?, S_EXT2 =?, S_EXT3 =?, S_EXT4 =?,  "
        + "S_EXT5 =? "
        + "WHERE "
        + "I_VOUSRLNO = ?"
        ;	
	
    /* SQL to delete data */
    private static final String SQL_DELETE =
        "DELETE FROM TF_PAYMENT_DETAILSMAIN " 
        + " WHERE "
        + "I_VOUSRLNO = ?"
        ;


	/**
	*  批量查询 一次最多查询的参数数量
	*/
	
	public static final int FIND_BATCH_SIZE = 150  / 1;
	



   /**
   * Create a new record in Database.
   */
  public void create(IDto _dto,  Connection conn) throws SQLException
  {
     TfPaymentDetailsmainDto dto = (TfPaymentDetailsmainDto)_dto ;
     String msgValid = dto.checkValid() ;
     if (msgValid != null)
     	throw new SQLException("插入错误，"+msgValid) ;

     PreparedStatement ps = null;
     try
     {
         ps = conn.prepareStatement(SQL_INSERT);
          if (dto.getIvousrlno()==null)
            ps.setNull(1, java.sql.Types.BIGINT);
         else
            ps.setLong(1, dto.getIvousrlno().longValue());
          ps.setString(2, dto.getSorgcode());

          ps.setString(3, dto.getStrecode());

          ps.setString(4, dto.getSstatus());

          ps.setString(5, dto.getSdemo());

          ps.setString(6, dto.getSfilename());

           ps.setString(7, dto.getSpackageno());

          ps.setString(8, dto.getSadmdivcode());

          ps.setString(9, dto.getSstyear());

          ps.setString(10, dto.getSvtcode());

          ps.setString(11, dto.getSvoudate());

          ps.setString(12, dto.getSvoucherno());

          ps.setString(13, dto.getSoriginalvtcode());

          ps.setString(14, dto.getSoriginalvoucherno());

          ps.setString(15, dto.getSfundtypecode());

          ps.setString(16, dto.getSfundtypename());

          ps.setString(17, dto.getSpaydictateno());

          ps.setString(18, dto.getSpaymsgno());

          ps.setString(19, dto.getSpayentrustdate());

          ps.setString(20, dto.getSpaysndbnkno());

          ps.setBigDecimal(21, dto.getNsumamt());

          ps.setString(22, dto.getSagencycode());

          ps.setString(23, dto.getSagencyname());

          ps.setString(24, dto.getSpayacctno());

          ps.setString(25, dto.getSpayacctname());

          ps.setString(26, dto.getSpayacctbankname());

          ps.setString(27, dto.getSpaybankcode());

          ps.setString(28, dto.getSpaybankname());

          ps.setString(29, dto.getSbusinesstypecode());

          ps.setString(30, dto.getSbusinesstypename());

          ps.setString(31, dto.getSpaytypecode());

          ps.setString(32, dto.getSpaytypename());

          ps.setString(33, dto.getSxpaydate());

          ps.setBigDecimal(34, dto.getNxsumamt());

          ps.setString(35, dto.getShold1());

          ps.setString(36, dto.getShold2());

          ps.setString(37, dto.getSext1());

          ps.setString(38, dto.getSext2());

          ps.setString(39, dto.getSext3());

          ps.setString(40, dto.getSext4());

          ps.setString(41, dto.getSext5());

         ps.executeUpdate();
     }
     finally
     {
         close(ps);
     }
     return  ;
  }

   /**
   * Create a new record in Database. and return result, only used when having generated key
   */
   public IDto createWithResult(IDto _dto,  Connection conn) throws SQLException
   {
       TfPaymentDetailsmainDto dto = (TfPaymentDetailsmainDto)_dto ;
       String msgValid = dto.checkValid() ;
       if (msgValid != null)
           throw new SQLException("插入错误，"+msgValid) ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try
       {
           ps = conn.prepareStatement(SQL_INSERT_WITH_RESULT);
            if (dto.getIvousrlno()==null)
              ps.setNull(1, java.sql.Types.BIGINT);
           else
              ps.setLong(1, dto.getIvousrlno().longValue());
            ps.setString(2, dto.getSorgcode());
            ps.setString(3, dto.getStrecode());
            ps.setString(4, dto.getSstatus());
            ps.setString(5, dto.getSdemo());
            ps.setString(6, dto.getSfilename());
             ps.setString(7, dto.getSpackageno());
            ps.setString(8, dto.getSadmdivcode());
            ps.setString(9, dto.getSstyear());
            ps.setString(10, dto.getSvtcode());
            ps.setString(11, dto.getSvoudate());
            ps.setString(12, dto.getSvoucherno());
            ps.setString(13, dto.getSoriginalvtcode());
            ps.setString(14, dto.getSoriginalvoucherno());
            ps.setString(15, dto.getSfundtypecode());
            ps.setString(16, dto.getSfundtypename());
            ps.setString(17, dto.getSpaydictateno());
            ps.setString(18, dto.getSpaymsgno());
            ps.setString(19, dto.getSpayentrustdate());
            ps.setString(20, dto.getSpaysndbnkno());
            ps.setBigDecimal(21, dto.getNsumamt());
            ps.setString(22, dto.getSagencycode());
            ps.setString(23, dto.getSagencyname());
            ps.setString(24, dto.getSpayacctno());
            ps.setString(25, dto.getSpayacctname());
            ps.setString(26, dto.getSpayacctbankname());
            ps.setString(27, dto.getSpaybankcode());
            ps.setString(28, dto.getSpaybankname());
            ps.setString(29, dto.getSbusinesstypecode());
            ps.setString(30, dto.getSbusinesstypename());
            ps.setString(31, dto.getSpaytypecode());
            ps.setString(32, dto.getSpaytypename());
            ps.setString(33, dto.getSxpaydate());
            ps.setBigDecimal(34, dto.getNxsumamt());
            ps.setString(35, dto.getShold1());
            ps.setString(36, dto.getShold2());
            ps.setString(37, dto.getSext1());
            ps.setString(38, dto.getSext2());
            ps.setString(39, dto.getSext3());
            ps.setString(40, dto.getSext4());
            ps.setString(41, dto.getSext5());
           rs = ps.executeQuery();
           List results = getResults(rs);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }
        finally
        {
           close(ps);
           close(rs);
        }
   }

 	/**
	* Create batch new record in Database. it only create the first layer dto
	*/
    public void create(IDto[] _dtos, Connection conn)    throws SQLException
    {
        TfPaymentDetailsmainDto dto  ;
     	for (int i= 0; i< _dtos.length ; i++)
 	    {
 		    dto  = (TfPaymentDetailsmainDto)_dtos[i] ; 
    	    String msgValid = dto.checkValid() ;
        	if (msgValid != null)
     	    	throw new SQLException("插入错误，"+msgValid) ;
     	}
 	    PreparedStatement ps = null;
        try
        {
            ps = conn.prepareStatement(SQL_INSERT);
       
     	    for (int i= 0; i< _dtos.length ; i++)
     	    {
 	    	    dto  = (TfPaymentDetailsmainDto)_dtos[i] ; 
  
               if (dto.getIvousrlno()==null)
                  ps.setNull(1, java.sql.Types.BIGINT);
               else
                  ps.setLong(1, dto.getIvousrlno().longValue());
  
               ps.setString(2, dto.getSorgcode());
  
               ps.setString(3, dto.getStrecode());
  
               ps.setString(4, dto.getSstatus());
  
               ps.setString(5, dto.getSdemo());
  
               ps.setString(6, dto.getSfilename());
   
               ps.setString(7, dto.getSpackageno());
  
               ps.setString(8, dto.getSadmdivcode());
  
               ps.setString(9, dto.getSstyear());
  
               ps.setString(10, dto.getSvtcode());
  
               ps.setString(11, dto.getSvoudate());
  
               ps.setString(12, dto.getSvoucherno());
  
               ps.setString(13, dto.getSoriginalvtcode());
  
               ps.setString(14, dto.getSoriginalvoucherno());
  
               ps.setString(15, dto.getSfundtypecode());
  
               ps.setString(16, dto.getSfundtypename());
  
               ps.setString(17, dto.getSpaydictateno());
  
               ps.setString(18, dto.getSpaymsgno());
  
               ps.setString(19, dto.getSpayentrustdate());
  
               ps.setString(20, dto.getSpaysndbnkno());
  
               ps.setBigDecimal(21, dto.getNsumamt());
  
               ps.setString(22, dto.getSagencycode());
  
               ps.setString(23, dto.getSagencyname());
  
               ps.setString(24, dto.getSpayacctno());
  
               ps.setString(25, dto.getSpayacctname());
  
               ps.setString(26, dto.getSpayacctbankname());
  
               ps.setString(27, dto.getSpaybankcode());
  
               ps.setString(28, dto.getSpaybankname());
  
               ps.setString(29, dto.getSbusinesstypecode());
  
               ps.setString(30, dto.getSbusinesstypename());
  
               ps.setString(31, dto.getSpaytypecode());
  
               ps.setString(32, dto.getSpaytypename());
  
               ps.setString(33, dto.getSxpaydate());
  
               ps.setBigDecimal(34, dto.getNxsumamt());
  
               ps.setString(35, dto.getShold1());
  
               ps.setString(36, dto.getShold2());
  
               ps.setString(37, dto.getSext1());
  
               ps.setString(38, dto.getSext2());
  
               ps.setString(39, dto.getSext3());
  
               ps.setString(40, dto.getSext4());
  
               ps.setString(41, dto.getSext5());
               ps.addBatch(); 
           }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }
        return  ;
   }




     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto find(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TfPaymentDetailsmainPK pk = (TfPaymentDetailsmainPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT);
           if (pk.getIvousrlno()==null)
               ps.setNull(1, java.sql.Types.BIGINT);
           else
               ps.setLong(1, pk.getIvousrlno().longValue());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }

     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto findForUpdate(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TfPaymentDetailsmainPK pk = (TfPaymentDetailsmainPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT_FOR_UPDATE);
           if (pk.getIvousrlno()==null)
               ps.setNull(1, java.sql.Types.BIGINT);
           else
               ps.setLong(1, pk.getIvousrlno().longValue());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }
  
     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto[] find(IPK[] _pks, Connection conn, boolean isLobSupport) throws SQLException
    {
        TfPaymentDetailsmainPK pk ;
        
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TfPaymentDetailsmainPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 
        PreparedStatement ps = null;
        ResultSet rs = null;
        
         
        List results = new ArrayList();  
		for (int iBegin = 0; iBegin < pks.size(); iBegin += FIND_BATCH_SIZE)   //
		{
        
            try {
          		int iFindBatchSize = pks.size() - iBegin ;
	    		if (iFindBatchSize >FIND_BATCH_SIZE )
		    		iFindBatchSize = FIND_BATCH_SIZE ;
		  
        	    StringBuffer sb = new StringBuffer() ;
            	sb.append(SQL_SELECT_BATCH).append(" WHERE ").append(SQL_SELECT_BATCH_WHERE);

	    		for (int i = iBegin+1; i < iBegin + iFindBatchSize; i++)
		    	{
			    	sb.append("OR").append(SQL_SELECT_BATCH_WHERE) ;
    			}
        	
                ps = conn.prepareStatement(sb.toString());

                for (int i = iBegin;i < iBegin + iFindBatchSize; i++)
                {
                    pk  = (TfPaymentDetailsmainPK)(pks.get(i)) ; 
                   if (pk.getIvousrlno()==null)
                      ps.setNull((i-iBegin)*1+1, java.sql.Types.BIGINT);
                   else
                      ps.setLong((i-iBegin)*1+1, pk.getIvousrlno().longValue());

			
	            }
                rs = ps.executeQuery();
                results.addAll(getResults(rs,0,isLobSupport)); //
            }finally {
                close(rs);
                close(ps);
            }
        }
        if (results!=null && results.size() > 0)
        {
            TfPaymentDetailsmainDto[] dtos = new TfPaymentDetailsmainDto[0];
		    dtos = (TfPaymentDetailsmainDto[]) results.toArray(dtos) ;
		    return dtos ;
        }
        else
        {
            return null;
        }
    }

  
  
  



    /**
	    * 得到查询语句
	    */
    public String getSelectSQL() 
    {
	        return SQL_SELECT_BATCH_SCROLLABLE ;
    }
   

	public List getResults(ResultSet rs) throws SQLException
    {
    	return getResults(rs, 0) ;
    }
  

    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize) throws SQLException
    {
		return getResults(rs,maxSize,false);
	}
    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize, boolean isLobSupport) throws SQLException
    {
        List results = new ArrayList();
        String str ;
        Reader reader;
        Clob clob;
        char[] chars ;
        
        while (rs.next())
         {
             TfPaymentDetailsmainDto  dto = new  TfPaymentDetailsmainDto ();
             //I_VOUSRLNO
             str = rs.getString("I_VOUSRLNO");
             if(str!=null){
                dto.setIvousrlno(new Long(str));
             }

             //S_ORGCODE
             str = rs.getString("S_ORGCODE");
             if (str == null)
                dto.setSorgcode(null);
             else
                dto.setSorgcode(str.trim());

             //S_TRECODE
             str = rs.getString("S_TRECODE");
             if (str == null)
                dto.setStrecode(null);
             else
                dto.setStrecode(str.trim());

             //S_STATUS
             str = rs.getString("S_STATUS");
             if (str == null)
                dto.setSstatus(null);
             else
                dto.setSstatus(str.trim());

             //S_DEMO
             str = rs.getString("S_DEMO");
             if (str == null)
                dto.setSdemo(null);
             else
                dto.setSdemo(str.trim());

             //S_FILENAME
             str = rs.getString("S_FILENAME");
             if (str == null)
                dto.setSfilename(null);
             else
                dto.setSfilename(str.trim());

             //TS_SYSUPDATE
           dto.setTssysupdate(rs.getTimestamp("TS_SYSUPDATE"));

             //S_PACKAGENO
             str = rs.getString("S_PACKAGENO");
             if (str == null)
                dto.setSpackageno(null);
             else
                dto.setSpackageno(str.trim());

             //S_ADMDIVCODE
             str = rs.getString("S_ADMDIVCODE");
             if (str == null)
                dto.setSadmdivcode(null);
             else
                dto.setSadmdivcode(str.trim());

             //S_STYEAR
             str = rs.getString("S_STYEAR");
             if (str == null)
                dto.setSstyear(null);
             else
                dto.setSstyear(str.trim());

             //S_VTCODE
             str = rs.getString("S_VTCODE");
             if (str == null)
                dto.setSvtcode(null);
             else
                dto.setSvtcode(str.trim());

             //S_VOUDATE
             str = rs.getString("S_VOUDATE");
             if (str == null)
                dto.setSvoudate(null);
             else
                dto.setSvoudate(str.trim());

             //S_VOUCHERNO
             str = rs.getString("S_VOUCHERNO");
             if (str == null)
                dto.setSvoucherno(null);
             else
                dto.setSvoucherno(str.trim());

             //S_ORIGINALVTCODE
             str = rs.getString("S_ORIGINALVTCODE");
             if (str == null)
                dto.setSoriginalvtcode(null);
             else
                dto.setSoriginalvtcode(str.trim());

             //S_ORIGINALVOUCHERNO
             str = rs.getString("S_ORIGINALVOUCHERNO");
             if (str == null)
                dto.setSoriginalvoucherno(null);
             else
                dto.setSoriginalvoucherno(str.trim());

             //S_FUNDTYPECODE
             str = rs.getString("S_FUNDTYPECODE");
             if (str == null)
                dto.setSfundtypecode(null);
             else
                dto.setSfundtypecode(str.trim());

             //S_FUNDTYPENAME
             str = rs.getString("S_FUNDTYPENAME");
             if (str == null)
                dto.setSfundtypename(null);
             else
                dto.setSfundtypename(str.trim());

             //S_PAYDICTATENO
             str = rs.getString("S_PAYDICTATENO");
             if (str == null)
                dto.setSpaydictateno(null);
             else
                dto.setSpaydictateno(str.trim());

             //S_PAYMSGNO
             str = rs.getString("S_PAYMSGNO");
             if (str == null)
                dto.setSpaymsgno(null);
             else
                dto.setSpaymsgno(str.trim());

             //S_PAYENTRUSTDATE
             str = rs.getString("S_PAYENTRUSTDATE");
             if (str == null)
                dto.setSpayentrustdate(null);
             else
                dto.setSpayentrustdate(str.trim());

             //S_PAYSNDBNKNO
             str = rs.getString("S_PAYSNDBNKNO");
             if (str == null)
                dto.setSpaysndbnkno(null);
             else
                dto.setSpaysndbnkno(str.trim());

             //N_SUMAMT
           dto.setNsumamt(rs.getBigDecimal("N_SUMAMT"));

             //S_AGENCYCODE
             str = rs.getString("S_AGENCYCODE");
             if (str == null)
                dto.setSagencycode(null);
             else
                dto.setSagencycode(str.trim());

             //S_AGENCYNAME
             str = rs.getString("S_AGENCYNAME");
             if (str == null)
                dto.setSagencyname(null);
             else
                dto.setSagencyname(str.trim());

             //S_PAYACCTNO
             str = rs.getString("S_PAYACCTNO");
             if (str == null)
                dto.setSpayacctno(null);
             else
                dto.setSpayacctno(str.trim());

             //S_PAYACCTNAME
             str = rs.getString("S_PAYACCTNAME");
             if (str == null)
                dto.setSpayacctname(null);
             else
                dto.setSpayacctname(str.trim());

             //S_PAYACCTBANKNAME
             str = rs.getString("S_PAYACCTBANKNAME");
             if (str == null)
                dto.setSpayacctbankname(null);
             else
                dto.setSpayacctbankname(str.trim());

             //S_PAYBANKCODE
             str = rs.getString("S_PAYBANKCODE");
             if (str == null)
                dto.setSpaybankcode(null);
             else
                dto.setSpaybankcode(str.trim());

             //S_PAYBANKNAME
             str = rs.getString("S_PAYBANKNAME");
             if (str == null)
                dto.setSpaybankname(null);
             else
                dto.setSpaybankname(str.trim());

             //S_BUSINESSTYPECODE
             str = rs.getString("S_BUSINESSTYPECODE");
             if (str == null)
                dto.setSbusinesstypecode(null);
             else
                dto.setSbusinesstypecode(str.trim());

             //S_BUSINESSTYPENAME
             str = rs.getString("S_BUSINESSTYPENAME");
             if (str == null)
                dto.setSbusinesstypename(null);
             else
                dto.setSbusinesstypename(str.trim());

             //S_PAYTYPECODE
             str = rs.getString("S_PAYTYPECODE");
             if (str == null)
                dto.setSpaytypecode(null);
             else
                dto.setSpaytypecode(str.trim());

             //S_PAYTYPENAME
             str = rs.getString("S_PAYTYPENAME");
             if (str == null)
                dto.setSpaytypename(null);
             else
                dto.setSpaytypename(str.trim());

             //S_XPAYDATE
             str = rs.getString("S_XPAYDATE");
             if (str == null)
                dto.setSxpaydate(null);
             else
                dto.setSxpaydate(str.trim());

             //N_XSUMAMT
           dto.setNxsumamt(rs.getBigDecimal("N_XSUMAMT"));

             //S_HOLD1
             str = rs.getString("S_HOLD1");
             if (str == null)
                dto.setShold1(null);
             else
                dto.setShold1(str.trim());

             //S_HOLD2
             str = rs.getString("S_HOLD2");
             if (str == null)
                dto.setShold2(null);
             else
                dto.setShold2(str.trim());

             //S_EXT1
             str = rs.getString("S_EXT1");
             if (str == null)
                dto.setSext1(null);
             else
                dto.setSext1(str.trim());

             //S_EXT2
             str = rs.getString("S_EXT2");
             if (str == null)
                dto.setSext2(null);
             else
                dto.setSext2(str.trim());

             //S_EXT3
             str = rs.getString("S_EXT3");
             if (str == null)
                dto.setSext3(null);
             else
                dto.setSext3(str.trim());

             //S_EXT4
             str = rs.getString("S_EXT4");
             if (str == null)
                dto.setSext4(null);
             else
                dto.setSext4(str.trim());

             //S_EXT5
             str = rs.getString("S_EXT5");
             if (str == null)
                dto.setSext5(null);
             else
                dto.setSext5(str.trim());



            results.add(dto);
            
            if(maxSize >0  && results.size() == maxSize)
            {
            	break ;
            }
            
         }
        
		return results ;
    }

  /**
   * populate resultSet as cclass
   */
  protected IDto[] getChildrenResults(ResultSet rs ,Class _dto) throws SQLException{

      throw new SQLException("本dto没有与其它dto相关联，无法应用此方法");
  }


     /**
     * Update a record in Database.
     */

    public void update(IDto _dto, Connection conn, boolean isLobSupport) throws SQLException
    {
        String msgValid = _dto.checkValid() ;
        if (msgValid != null)
            throw new SQLException("修改错误，"+msgValid) ;
        TfPaymentDetailsmainDto dto = (TfPaymentDetailsmainDto)_dto ;
        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            int pos = 1;
            //S_ORGCODE
            ps.setString(pos, dto.getSorgcode());
            pos++;

            //S_TRECODE
            ps.setString(pos, dto.getStrecode());
            pos++;

            //S_STATUS
            ps.setString(pos, dto.getSstatus());
            pos++;

            //S_DEMO
            ps.setString(pos, dto.getSdemo());
            pos++;

            //S_FILENAME
            ps.setString(pos, dto.getSfilename());
            pos++;

            //TS_SYSUPDATE
            //S_PACKAGENO
            ps.setString(pos, dto.getSpackageno());
            pos++;

            //S_ADMDIVCODE
            ps.setString(pos, dto.getSadmdivcode());
            pos++;

            //S_STYEAR
            ps.setString(pos, dto.getSstyear());
            pos++;

            //S_VTCODE
            ps.setString(pos, dto.getSvtcode());
            pos++;

            //S_VOUDATE
            ps.setString(pos, dto.getSvoudate());
            pos++;

            //S_VOUCHERNO
            ps.setString(pos, dto.getSvoucherno());
            pos++;

            //S_ORIGINALVTCODE
            ps.setString(pos, dto.getSoriginalvtcode());
            pos++;

            //S_ORIGINALVOUCHERNO
            ps.setString(pos, dto.getSoriginalvoucherno());
            pos++;

            //S_FUNDTYPECODE
            ps.setString(pos, dto.getSfundtypecode());
            pos++;

            //S_FUNDTYPENAME
            ps.setString(pos, dto.getSfundtypename());
            pos++;

            //S_PAYDICTATENO
            ps.setString(pos, dto.getSpaydictateno());
            pos++;

            //S_PAYMSGNO
            ps.setString(pos, dto.getSpaymsgno());
            pos++;

            //S_PAYENTRUSTDATE
            ps.setString(pos, dto.getSpayentrustdate());
            pos++;

            //S_PAYSNDBNKNO
            ps.setString(pos, dto.getSpaysndbnkno());
            pos++;

            //N_SUMAMT
            ps.setBigDecimal(pos, dto.getNsumamt());
            pos++;

            //S_AGENCYCODE
            ps.setString(pos, dto.getSagencycode());
            pos++;

            //S_AGENCYNAME
            ps.setString(pos, dto.getSagencyname());
            pos++;

            //S_PAYACCTNO
            ps.setString(pos, dto.getSpayacctno());
            pos++;

            //S_PAYACCTNAME
            ps.setString(pos, dto.getSpayacctname());
            pos++;

            //S_PAYACCTBANKNAME
            ps.setString(pos, dto.getSpayacctbankname());
            pos++;

            //S_PAYBANKCODE
            ps.setString(pos, dto.getSpaybankcode());
            pos++;

            //S_PAYBANKNAME
            ps.setString(pos, dto.getSpaybankname());
            pos++;

            //S_BUSINESSTYPECODE
            ps.setString(pos, dto.getSbusinesstypecode());
            pos++;

            //S_BUSINESSTYPENAME
            ps.setString(pos, dto.getSbusinesstypename());
            pos++;

            //S_PAYTYPECODE
            ps.setString(pos, dto.getSpaytypecode());
            pos++;

            //S_PAYTYPENAME
            ps.setString(pos, dto.getSpaytypename());
            pos++;

            //S_XPAYDATE
            ps.setString(pos, dto.getSxpaydate());
            pos++;

            //N_XSUMAMT
            ps.setBigDecimal(pos, dto.getNxsumamt());
            pos++;

            //S_HOLD1
            ps.setString(pos, dto.getShold1());
            pos++;

            //S_HOLD2
            ps.setString(pos, dto.getShold2());
            pos++;

            //S_EXT1
            ps.setString(pos, dto.getSext1());
            pos++;

            //S_EXT2
            ps.setString(pos, dto.getSext2());
            pos++;

            //S_EXT3
            ps.setString(pos, dto.getSext3());
            pos++;

            //S_EXT4
            ps.setString(pos, dto.getSext4());
            pos++;

            //S_EXT5
            ps.setString(pos, dto.getSext5());
            pos++;


           //I_VOUSRLNO
           ps.setLong(pos, dto.getIvousrlno().longValue());
           pos++;
           ps.executeUpdate();
        }
        finally
        {
           close(ps);
        }

        return ;
    }
    
    
    
      /**
     * Update batch record in Database.
     */

    public void update(IDto[] _dtos, Connection conn, boolean isLobSupport) throws SQLException
    {
     
     	 TfPaymentDetailsmainDto dto  ;
         for (int i= 0; i< _dtos.length ; i++)
         {
            dto  = (TfPaymentDetailsmainDto)_dtos[i] ; 
            String msgValid = dto.checkValid() ;
            if (msgValid != null)
                throw new SQLException("修改错误，"+msgValid) ;
         }

        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            
            for (int i= 0; i< _dtos.length ; i++)
            {
                dto  = (TfPaymentDetailsmainDto)_dtos[i] ; 
                int pos = 1;
                //S_ORGCODE
                 ps.setString(pos, dto.getSorgcode());
                pos++;

                //S_TRECODE
                 ps.setString(pos, dto.getStrecode());
                pos++;

                //S_STATUS
                 ps.setString(pos, dto.getSstatus());
                pos++;

                //S_DEMO
                 ps.setString(pos, dto.getSdemo());
                pos++;

                //S_FILENAME
                 ps.setString(pos, dto.getSfilename());
                pos++;

                //TS_SYSUPDATE
                 //S_PACKAGENO
                 ps.setString(pos, dto.getSpackageno());
                pos++;

                //S_ADMDIVCODE
                 ps.setString(pos, dto.getSadmdivcode());
                pos++;

                //S_STYEAR
                 ps.setString(pos, dto.getSstyear());
                pos++;

                //S_VTCODE
                 ps.setString(pos, dto.getSvtcode());
                pos++;

                //S_VOUDATE
                 ps.setString(pos, dto.getSvoudate());
                pos++;

                //S_VOUCHERNO
                 ps.setString(pos, dto.getSvoucherno());
                pos++;

                //S_ORIGINALVTCODE
                 ps.setString(pos, dto.getSoriginalvtcode());
                pos++;

                //S_ORIGINALVOUCHERNO
                 ps.setString(pos, dto.getSoriginalvoucherno());
                pos++;

                //S_FUNDTYPECODE
                 ps.setString(pos, dto.getSfundtypecode());
                pos++;

                //S_FUNDTYPENAME
                 ps.setString(pos, dto.getSfundtypename());
                pos++;

                //S_PAYDICTATENO
                 ps.setString(pos, dto.getSpaydictateno());
                pos++;

                //S_PAYMSGNO
                 ps.setString(pos, dto.getSpaymsgno());
                pos++;

                //S_PAYENTRUSTDATE
                 ps.setString(pos, dto.getSpayentrustdate());
                pos++;

                //S_PAYSNDBNKNO
                 ps.setString(pos, dto.getSpaysndbnkno());
                pos++;

                //N_SUMAMT
                 ps.setBigDecimal(pos, dto.getNsumamt());
                pos++;

                //S_AGENCYCODE
                 ps.setString(pos, dto.getSagencycode());
                pos++;

                //S_AGENCYNAME
                 ps.setString(pos, dto.getSagencyname());
                pos++;

                //S_PAYACCTNO
                 ps.setString(pos, dto.getSpayacctno());
                pos++;

                //S_PAYACCTNAME
                 ps.setString(pos, dto.getSpayacctname());
                pos++;

                //S_PAYACCTBANKNAME
                 ps.setString(pos, dto.getSpayacctbankname());
                pos++;

                //S_PAYBANKCODE
                 ps.setString(pos, dto.getSpaybankcode());
                pos++;

                //S_PAYBANKNAME
                 ps.setString(pos, dto.getSpaybankname());
                pos++;

                //S_BUSINESSTYPECODE
                 ps.setString(pos, dto.getSbusinesstypecode());
                pos++;

                //S_BUSINESSTYPENAME
                 ps.setString(pos, dto.getSbusinesstypename());
                pos++;

                //S_PAYTYPECODE
                 ps.setString(pos, dto.getSpaytypecode());
                pos++;

                //S_PAYTYPENAME
                 ps.setString(pos, dto.getSpaytypename());
                pos++;

                //S_XPAYDATE
                 ps.setString(pos, dto.getSxpaydate());
                pos++;

                //N_XSUMAMT
                 ps.setBigDecimal(pos, dto.getNxsumamt());
                pos++;

                //S_HOLD1
                 ps.setString(pos, dto.getShold1());
                pos++;

                //S_HOLD2
                 ps.setString(pos, dto.getShold2());
                pos++;

                //S_EXT1
                 ps.setString(pos, dto.getSext1());
                pos++;

                //S_EXT2
                 ps.setString(pos, dto.getSext2());
                pos++;

                //S_EXT3
                 ps.setString(pos, dto.getSext3());
                pos++;

                //S_EXT4
                 ps.setString(pos, dto.getSext4());
                pos++;

                //S_EXT5
                 ps.setString(pos, dto.getSext5());
                pos++;


               //I_VOUSRLNO
               ps.setLong(pos, dto.getIvousrlno().longValue());
               pos++;
		       ps.addBatch(); 
	       }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }

        return ;
    }



    /**
     * delete a record in Database.
     */
    public void delete(IPK _pk, Connection conn) throws SQLException
    {
       
      
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return ;
        //	throw new SQLException("删除错误，"+msgValid) ;
       }
       TfPaymentDetailsmainPK pk = (TfPaymentDetailsmainPK)_pk ;


       PreparedStatement ps = null;
       try {
           ps = conn.prepareStatement(SQL_DELETE);
           ps.setLong(1, pk.getIvousrlno().longValue());
           ps.executeUpdate();
        }
        finally
        {
            close(ps);
        }
    }


     /**
     * delete batch record in Database.
     */
    public void delete(IPK[] _pks, Connection conn) throws SQLException
    {
        TfPaymentDetailsmainPK pk ;
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TfPaymentDetailsmainPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 		
 		
        PreparedStatement ps = null;
        try
         {
            ps = conn.prepareStatement(SQL_DELETE);
             
       		for (int i= 0; i< pks.size() ; i++)
       		{
       			pk  = (TfPaymentDetailsmainPK)(pks.get(i)) ; 
                ps.setLong(1, pk.getIvousrlno().longValue());
		        ps.addBatch() ;
			}
            ps.executeBatch();
        }
        finally
        {
            close(ps);
        }      
    }



    /**
    * Check IChangerNumber or UpdateTimeStamp.
    */
    public void check(IDto _dto, Connection conn) throws SQLException
    {
                                                                                                                           		throw new SQLException("数据库表：TF_PAYMENT_DETAILSMAIN没有检查修改的字段");
    }


	/**
	* synchronize values between parent and children
	*/
    public void syncToChildren(IDto _dto)     throws SQLException{
       throw new SQLException("本记录没有与其它记录相关联，无法进行主记录与明细记录之间的数据同步。");
  }

	/**
	*get children
	*/
    public IDto[] findChildren (IDto _dto, Connection conn, boolean isLobSupport)     throws SQLException{
         throw new SQLException("本dto没有与其它dto相关联，无法得到子dto");
  }    
    
  /**
   *return the children IDto class
   * @return Class
   */
  public Class getChildrenClass(){
	  return null;
  };    



    /**
     * Close JDBC Statement.
     * @param stmt  Statement to be closed.
     */
    private void close(Statement stmt)
    {
        if (stmt != null)
        {
            try
            {
                stmt.close();
            }
            catch(SQLException    e)
            {}
        }
    }

    /**
     * Close JDBC ResultSet.
     * @param rs  ResultSet to be closed.
     */
    private void close(ResultSet rs)
    {
        if (rs != null)
        {
            try
            {
                rs.close();
            }catch(SQLException  e){}
        }
    }
}
