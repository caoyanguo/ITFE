    



package com.cfcc.itfe.persistence.dao;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.Statement;
import java.sql.SQLException;
import java.sql.Date;
import java.sql.Time;
import java.sql.Timestamp ;
import java.sql.Clob;

import com.cfcc.jaf.persistence.jaform.parent.* ;

import java.math.BigDecimal;

import java.util.List;
import java.util.ArrayList;
import java.util.HashMap;

import java.io.StringReader;
import java.io.Reader;
import java.io.IOException;

import com.cfcc.itfe.persistence.dto.TvPayoutmsgmainDto ;
import com.cfcc.itfe.persistence.pk.TvPayoutmsgmainPK ;


/**
 * <p>Title: DAO of table: TV_PAYOUTMSGMAIN</p>
 * <p>Description: Data Access Object  </p>
 * <p>Copyright: Copyright (c) 2005</p>
 * <p>Company: CFCC </p>
 * <p>Generation Time: 2014-12-25 15:29:03 </p>
 *  Do NOT change this file by hand.
 *  This java source file is generated by JAF_ORM .
 *  dao.vm timestamp: 2007-04-06 08:30:00
 ******************************************************
 * <p> <b>change record</b><br>
 *  make dao methods simply.<br>
 *  especially make update,delete methods simply.<br>
 *  remove "..withCheck" method,withCheck will impl in JAFOrmTemplate.<br>
 *  remove "delete(IDto _dto,..)" method.<br></p>
 * @author win7
 */

public class TvPayoutmsgmainDao  implements IDao
{


    /* SQL to insert data */
    private static final String SQL_INSERT =
        "INSERT INTO TV_PAYOUTMSGMAIN ("
          + "S_BIZNO,S_ORGCODE,S_COMMITDATE,S_ACCDATE,S_FILENAME"
          + ",S_TRECODE,S_PACKAGENO,S_PAYUNIT,S_PAYEEBANKNO,S_DEALNO"
          + ",S_TAXTICKETNO,S_GENTICKETDATE,S_PAYERACCT,S_PAYERNAME,S_PAYERADDR"
          + ",N_MONEY,S_RECBANKNO,S_RECACCT,S_RECNAME,S_TRIMFLAG"
          + ",S_BUDGETUNITCODE,S_UNITCODENAME,S_OFYEAR,S_BUDGETTYPE,S_STATUS"
          + ",S_USERCODE,S_ADDWORD,S_ORIVOUNO,S_ORIVOUDATE,S_BACKFLAG"
          + ",S_DEMO,TS_SYSUPDATE,S_ID,S_RECBANKNAME,S_PAYERBANKNAME"
          + ",S_FUNDTYPECODE,S_FUNDTYPENAME,S_PAYTYPECODE,S_PAYTYPENAME,S_CLEARBANKCODE"
          + ",S_CLEARBANKNAME,S_PAYSUMMARYCODE,S_PAYSUMMARYNAME,S_XAGENTBUSINESSNO,S_XPAYAMT"
          + ",S_XPAYDATE,S_IFMATCH,S_HOLD1,S_HOLD2,S_INPUTRECBANKNO"
          + ",S_INPUTUSERCODE,S_CHECKSERCODE,S_INPUTRECBANKNAME,S_CHECKSTATUS,S_BUSINESSTYPECODE"
          + ",S_BUSINESSTYPENAME"
        + ") VALUES ("
        + "?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,CURRENT TIMESTAMP ,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)";


    private static final String SQL_INSERT_WITH_RESULT = "SELECT * FROM FINAL TABLE( " + SQL_INSERT + " )";
    
    /* SQL to select data */
    private static final String SQL_SELECT =
        "SELECT "
        + "TV_PAYOUTMSGMAIN.S_BIZNO, TV_PAYOUTMSGMAIN.S_ORGCODE, TV_PAYOUTMSGMAIN.S_COMMITDATE, TV_PAYOUTMSGMAIN.S_ACCDATE, TV_PAYOUTMSGMAIN.S_FILENAME, "
        + "TV_PAYOUTMSGMAIN.S_TRECODE, TV_PAYOUTMSGMAIN.S_PACKAGENO, TV_PAYOUTMSGMAIN.S_PAYUNIT, TV_PAYOUTMSGMAIN.S_PAYEEBANKNO, TV_PAYOUTMSGMAIN.S_DEALNO, "
        + "TV_PAYOUTMSGMAIN.S_TAXTICKETNO, TV_PAYOUTMSGMAIN.S_GENTICKETDATE, TV_PAYOUTMSGMAIN.S_PAYERACCT, TV_PAYOUTMSGMAIN.S_PAYERNAME, TV_PAYOUTMSGMAIN.S_PAYERADDR, "
        + "TV_PAYOUTMSGMAIN.N_MONEY, TV_PAYOUTMSGMAIN.S_RECBANKNO, TV_PAYOUTMSGMAIN.S_RECACCT, TV_PAYOUTMSGMAIN.S_RECNAME, TV_PAYOUTMSGMAIN.S_TRIMFLAG, "
        + "TV_PAYOUTMSGMAIN.S_BUDGETUNITCODE, TV_PAYOUTMSGMAIN.S_UNITCODENAME, TV_PAYOUTMSGMAIN.S_OFYEAR, TV_PAYOUTMSGMAIN.S_BUDGETTYPE, TV_PAYOUTMSGMAIN.S_STATUS, "
        + "TV_PAYOUTMSGMAIN.S_USERCODE, TV_PAYOUTMSGMAIN.S_ADDWORD, TV_PAYOUTMSGMAIN.S_ORIVOUNO, TV_PAYOUTMSGMAIN.S_ORIVOUDATE, TV_PAYOUTMSGMAIN.S_BACKFLAG, "
        + "TV_PAYOUTMSGMAIN.S_DEMO, TV_PAYOUTMSGMAIN.TS_SYSUPDATE, TV_PAYOUTMSGMAIN.S_ID, TV_PAYOUTMSGMAIN.S_RECBANKNAME, TV_PAYOUTMSGMAIN.S_PAYERBANKNAME, "
        + "TV_PAYOUTMSGMAIN.S_FUNDTYPECODE, TV_PAYOUTMSGMAIN.S_FUNDTYPENAME, TV_PAYOUTMSGMAIN.S_PAYTYPECODE, TV_PAYOUTMSGMAIN.S_PAYTYPENAME, TV_PAYOUTMSGMAIN.S_CLEARBANKCODE, "
        + "TV_PAYOUTMSGMAIN.S_CLEARBANKNAME, TV_PAYOUTMSGMAIN.S_PAYSUMMARYCODE, TV_PAYOUTMSGMAIN.S_PAYSUMMARYNAME, TV_PAYOUTMSGMAIN.S_XAGENTBUSINESSNO, TV_PAYOUTMSGMAIN.S_XPAYAMT, "
        + "TV_PAYOUTMSGMAIN.S_XPAYDATE, TV_PAYOUTMSGMAIN.S_IFMATCH, TV_PAYOUTMSGMAIN.S_HOLD1, TV_PAYOUTMSGMAIN.S_HOLD2, TV_PAYOUTMSGMAIN.S_INPUTRECBANKNO, "
        + "TV_PAYOUTMSGMAIN.S_INPUTUSERCODE, TV_PAYOUTMSGMAIN.S_CHECKSERCODE, TV_PAYOUTMSGMAIN.S_INPUTRECBANKNAME, TV_PAYOUTMSGMAIN.S_CHECKSTATUS, TV_PAYOUTMSGMAIN.S_BUSINESSTYPECODE, "
        + "TV_PAYOUTMSGMAIN.S_BUSINESSTYPENAME "
        + "FROM TV_PAYOUTMSGMAIN "
        +" WHERE " 
        + "S_BIZNO = ?"
        ;
    /* SQL to select for update data */
    private static final String SQL_SELECT_FOR_UPDATE =
        "SELECT "
        + "TV_PAYOUTMSGMAIN.S_BIZNO, TV_PAYOUTMSGMAIN.S_ORGCODE, TV_PAYOUTMSGMAIN.S_COMMITDATE, TV_PAYOUTMSGMAIN.S_ACCDATE, TV_PAYOUTMSGMAIN.S_FILENAME, "
        + "TV_PAYOUTMSGMAIN.S_TRECODE, TV_PAYOUTMSGMAIN.S_PACKAGENO, TV_PAYOUTMSGMAIN.S_PAYUNIT, TV_PAYOUTMSGMAIN.S_PAYEEBANKNO, TV_PAYOUTMSGMAIN.S_DEALNO, "
        + "TV_PAYOUTMSGMAIN.S_TAXTICKETNO, TV_PAYOUTMSGMAIN.S_GENTICKETDATE, TV_PAYOUTMSGMAIN.S_PAYERACCT, TV_PAYOUTMSGMAIN.S_PAYERNAME, TV_PAYOUTMSGMAIN.S_PAYERADDR, "
        + "TV_PAYOUTMSGMAIN.N_MONEY, TV_PAYOUTMSGMAIN.S_RECBANKNO, TV_PAYOUTMSGMAIN.S_RECACCT, TV_PAYOUTMSGMAIN.S_RECNAME, TV_PAYOUTMSGMAIN.S_TRIMFLAG, "
        + "TV_PAYOUTMSGMAIN.S_BUDGETUNITCODE, TV_PAYOUTMSGMAIN.S_UNITCODENAME, TV_PAYOUTMSGMAIN.S_OFYEAR, TV_PAYOUTMSGMAIN.S_BUDGETTYPE, TV_PAYOUTMSGMAIN.S_STATUS, "
        + "TV_PAYOUTMSGMAIN.S_USERCODE, TV_PAYOUTMSGMAIN.S_ADDWORD, TV_PAYOUTMSGMAIN.S_ORIVOUNO, TV_PAYOUTMSGMAIN.S_ORIVOUDATE, TV_PAYOUTMSGMAIN.S_BACKFLAG, "
        + "TV_PAYOUTMSGMAIN.S_DEMO, TV_PAYOUTMSGMAIN.TS_SYSUPDATE, TV_PAYOUTMSGMAIN.S_ID, TV_PAYOUTMSGMAIN.S_RECBANKNAME, TV_PAYOUTMSGMAIN.S_PAYERBANKNAME, "
        + "TV_PAYOUTMSGMAIN.S_FUNDTYPECODE, TV_PAYOUTMSGMAIN.S_FUNDTYPENAME, TV_PAYOUTMSGMAIN.S_PAYTYPECODE, TV_PAYOUTMSGMAIN.S_PAYTYPENAME, TV_PAYOUTMSGMAIN.S_CLEARBANKCODE, "
        + "TV_PAYOUTMSGMAIN.S_CLEARBANKNAME, TV_PAYOUTMSGMAIN.S_PAYSUMMARYCODE, TV_PAYOUTMSGMAIN.S_PAYSUMMARYNAME, TV_PAYOUTMSGMAIN.S_XAGENTBUSINESSNO, TV_PAYOUTMSGMAIN.S_XPAYAMT, "
        + "TV_PAYOUTMSGMAIN.S_XPAYDATE, TV_PAYOUTMSGMAIN.S_IFMATCH, TV_PAYOUTMSGMAIN.S_HOLD1, TV_PAYOUTMSGMAIN.S_HOLD2, TV_PAYOUTMSGMAIN.S_INPUTRECBANKNO, "
        + "TV_PAYOUTMSGMAIN.S_INPUTUSERCODE, TV_PAYOUTMSGMAIN.S_CHECKSERCODE, TV_PAYOUTMSGMAIN.S_INPUTRECBANKNAME, TV_PAYOUTMSGMAIN.S_CHECKSTATUS, TV_PAYOUTMSGMAIN.S_BUSINESSTYPECODE, "
        + "TV_PAYOUTMSGMAIN.S_BUSINESSTYPENAME "
        + "FROM TV_PAYOUTMSGMAIN "
        +" WHERE " 
        + "S_BIZNO = ? FOR UPDATE"
        ;

      /* SQL to select data (SCROLLABLE)*/
     private static final String SQL_SELECT_BATCH_SCROLLABLE =
        "SELECT "
        + "  TV_PAYOUTMSGMAIN.S_BIZNO  , TV_PAYOUTMSGMAIN.S_ORGCODE  , TV_PAYOUTMSGMAIN.S_COMMITDATE  , TV_PAYOUTMSGMAIN.S_ACCDATE  , TV_PAYOUTMSGMAIN.S_FILENAME "
        + " , TV_PAYOUTMSGMAIN.S_TRECODE  , TV_PAYOUTMSGMAIN.S_PACKAGENO  , TV_PAYOUTMSGMAIN.S_PAYUNIT  , TV_PAYOUTMSGMAIN.S_PAYEEBANKNO  , TV_PAYOUTMSGMAIN.S_DEALNO "
        + " , TV_PAYOUTMSGMAIN.S_TAXTICKETNO  , TV_PAYOUTMSGMAIN.S_GENTICKETDATE  , TV_PAYOUTMSGMAIN.S_PAYERACCT  , TV_PAYOUTMSGMAIN.S_PAYERNAME  , TV_PAYOUTMSGMAIN.S_PAYERADDR "
        + " , TV_PAYOUTMSGMAIN.N_MONEY  , TV_PAYOUTMSGMAIN.S_RECBANKNO  , TV_PAYOUTMSGMAIN.S_RECACCT  , TV_PAYOUTMSGMAIN.S_RECNAME  , TV_PAYOUTMSGMAIN.S_TRIMFLAG "
        + " , TV_PAYOUTMSGMAIN.S_BUDGETUNITCODE  , TV_PAYOUTMSGMAIN.S_UNITCODENAME  , TV_PAYOUTMSGMAIN.S_OFYEAR  , TV_PAYOUTMSGMAIN.S_BUDGETTYPE  , TV_PAYOUTMSGMAIN.S_STATUS "
        + " , TV_PAYOUTMSGMAIN.S_USERCODE  , TV_PAYOUTMSGMAIN.S_ADDWORD  , TV_PAYOUTMSGMAIN.S_ORIVOUNO  , TV_PAYOUTMSGMAIN.S_ORIVOUDATE  , TV_PAYOUTMSGMAIN.S_BACKFLAG "
        + " , TV_PAYOUTMSGMAIN.S_DEMO  , TV_PAYOUTMSGMAIN.TS_SYSUPDATE  , TV_PAYOUTMSGMAIN.S_ID  , TV_PAYOUTMSGMAIN.S_RECBANKNAME  , TV_PAYOUTMSGMAIN.S_PAYERBANKNAME "
        + " , TV_PAYOUTMSGMAIN.S_FUNDTYPECODE  , TV_PAYOUTMSGMAIN.S_FUNDTYPENAME  , TV_PAYOUTMSGMAIN.S_PAYTYPECODE  , TV_PAYOUTMSGMAIN.S_PAYTYPENAME  , TV_PAYOUTMSGMAIN.S_CLEARBANKCODE "
        + " , TV_PAYOUTMSGMAIN.S_CLEARBANKNAME  , TV_PAYOUTMSGMAIN.S_PAYSUMMARYCODE  , TV_PAYOUTMSGMAIN.S_PAYSUMMARYNAME  , TV_PAYOUTMSGMAIN.S_XAGENTBUSINESSNO  , TV_PAYOUTMSGMAIN.S_XPAYAMT "
        + " , TV_PAYOUTMSGMAIN.S_XPAYDATE  , TV_PAYOUTMSGMAIN.S_IFMATCH  , TV_PAYOUTMSGMAIN.S_HOLD1  , TV_PAYOUTMSGMAIN.S_HOLD2  , TV_PAYOUTMSGMAIN.S_INPUTRECBANKNO "
        + " , TV_PAYOUTMSGMAIN.S_INPUTUSERCODE  , TV_PAYOUTMSGMAIN.S_CHECKSERCODE  , TV_PAYOUTMSGMAIN.S_INPUTRECBANKNAME  , TV_PAYOUTMSGMAIN.S_CHECKSTATUS  , TV_PAYOUTMSGMAIN.S_BUSINESSTYPECODE "
        + " , TV_PAYOUTMSGMAIN.S_BUSINESSTYPENAME "
        + "FROM TV_PAYOUTMSGMAIN ";



    /* SQL to select batch data */
    private static final String SQL_SELECT_BATCH =
        "SELECT "
        + "TV_PAYOUTMSGMAIN.S_BIZNO, TV_PAYOUTMSGMAIN.S_ORGCODE, TV_PAYOUTMSGMAIN.S_COMMITDATE, TV_PAYOUTMSGMAIN.S_ACCDATE, TV_PAYOUTMSGMAIN.S_FILENAME, "
        + "TV_PAYOUTMSGMAIN.S_TRECODE, TV_PAYOUTMSGMAIN.S_PACKAGENO, TV_PAYOUTMSGMAIN.S_PAYUNIT, TV_PAYOUTMSGMAIN.S_PAYEEBANKNO, TV_PAYOUTMSGMAIN.S_DEALNO, "
        + "TV_PAYOUTMSGMAIN.S_TAXTICKETNO, TV_PAYOUTMSGMAIN.S_GENTICKETDATE, TV_PAYOUTMSGMAIN.S_PAYERACCT, TV_PAYOUTMSGMAIN.S_PAYERNAME, TV_PAYOUTMSGMAIN.S_PAYERADDR, "
        + "TV_PAYOUTMSGMAIN.N_MONEY, TV_PAYOUTMSGMAIN.S_RECBANKNO, TV_PAYOUTMSGMAIN.S_RECACCT, TV_PAYOUTMSGMAIN.S_RECNAME, TV_PAYOUTMSGMAIN.S_TRIMFLAG, "
        + "TV_PAYOUTMSGMAIN.S_BUDGETUNITCODE, TV_PAYOUTMSGMAIN.S_UNITCODENAME, TV_PAYOUTMSGMAIN.S_OFYEAR, TV_PAYOUTMSGMAIN.S_BUDGETTYPE, TV_PAYOUTMSGMAIN.S_STATUS, "
        + "TV_PAYOUTMSGMAIN.S_USERCODE, TV_PAYOUTMSGMAIN.S_ADDWORD, TV_PAYOUTMSGMAIN.S_ORIVOUNO, TV_PAYOUTMSGMAIN.S_ORIVOUDATE, TV_PAYOUTMSGMAIN.S_BACKFLAG, "
        + "TV_PAYOUTMSGMAIN.S_DEMO, TV_PAYOUTMSGMAIN.TS_SYSUPDATE, TV_PAYOUTMSGMAIN.S_ID, TV_PAYOUTMSGMAIN.S_RECBANKNAME, TV_PAYOUTMSGMAIN.S_PAYERBANKNAME, "
        + "TV_PAYOUTMSGMAIN.S_FUNDTYPECODE, TV_PAYOUTMSGMAIN.S_FUNDTYPENAME, TV_PAYOUTMSGMAIN.S_PAYTYPECODE, TV_PAYOUTMSGMAIN.S_PAYTYPENAME, TV_PAYOUTMSGMAIN.S_CLEARBANKCODE, "
        + "TV_PAYOUTMSGMAIN.S_CLEARBANKNAME, TV_PAYOUTMSGMAIN.S_PAYSUMMARYCODE, TV_PAYOUTMSGMAIN.S_PAYSUMMARYNAME, TV_PAYOUTMSGMAIN.S_XAGENTBUSINESSNO, TV_PAYOUTMSGMAIN.S_XPAYAMT, "
        + "TV_PAYOUTMSGMAIN.S_XPAYDATE, TV_PAYOUTMSGMAIN.S_IFMATCH, TV_PAYOUTMSGMAIN.S_HOLD1, TV_PAYOUTMSGMAIN.S_HOLD2, TV_PAYOUTMSGMAIN.S_INPUTRECBANKNO, "
        + "TV_PAYOUTMSGMAIN.S_INPUTUSERCODE, TV_PAYOUTMSGMAIN.S_CHECKSERCODE, TV_PAYOUTMSGMAIN.S_INPUTRECBANKNAME, TV_PAYOUTMSGMAIN.S_CHECKSTATUS, TV_PAYOUTMSGMAIN.S_BUSINESSTYPECODE, "
        + "TV_PAYOUTMSGMAIN.S_BUSINESSTYPENAME "
        + "FROM TV_PAYOUTMSGMAIN " ;
        

   /* SQL to select batch data where condition  */
    private static final String SQL_SELECT_BATCH_WHERE =" ( "
        + "S_BIZNO = ?)"
        ;


    /* SQL to update data */
    private static final String SQL_UPDATE =
        "UPDATE TV_PAYOUTMSGMAIN SET "
        + "S_ORGCODE =?,S_COMMITDATE =?,S_ACCDATE =?,S_FILENAME =?,S_TRECODE =?, "
        + "S_PACKAGENO =?,S_PAYUNIT =?,S_PAYEEBANKNO =?,S_DEALNO =?,S_TAXTICKETNO =?, "
        + "S_GENTICKETDATE =?,S_PAYERACCT =?,S_PAYERNAME =?,S_PAYERADDR =?,N_MONEY =?, "
        + "S_RECBANKNO =?,S_RECACCT =?,S_RECNAME =?,S_TRIMFLAG =?,S_BUDGETUNITCODE =?, "
        + "S_UNITCODENAME =?,S_OFYEAR =?,S_BUDGETTYPE =?,S_STATUS =?,S_USERCODE =?, "
        + "S_ADDWORD =?,S_ORIVOUNO =?,S_ORIVOUDATE =?,S_BACKFLAG =?,S_DEMO =?, "
        + "TS_SYSUPDATE =CURRENT TIMESTAMP,S_ID =?,S_RECBANKNAME =?,S_PAYERBANKNAME =?,S_FUNDTYPECODE =?, "
        + "S_FUNDTYPENAME =?,S_PAYTYPECODE =?,S_PAYTYPENAME =?,S_CLEARBANKCODE =?,S_CLEARBANKNAME =?, "
        + "S_PAYSUMMARYCODE =?,S_PAYSUMMARYNAME =?,S_XAGENTBUSINESSNO =?,S_XPAYAMT =?,S_XPAYDATE =?, "
        + "S_IFMATCH =?,S_HOLD1 =?,S_HOLD2 =?,S_INPUTRECBANKNO =?,S_INPUTUSERCODE =?, "
        + "S_CHECKSERCODE =?,S_INPUTRECBANKNAME =?,S_CHECKSTATUS =?,S_BUSINESSTYPECODE =?,S_BUSINESSTYPENAME =? "
        + "WHERE "
        + "S_BIZNO = ?"
        ;

	/* SQL to update data, support LOB */
    private static final String SQL_UPDATE_LOB =
        "UPDATE TV_PAYOUTMSGMAIN SET "
        + "S_ORGCODE =?, S_COMMITDATE =?, S_ACCDATE =?, S_FILENAME =?, S_TRECODE =?,  "
        + "S_PACKAGENO =?, S_PAYUNIT =?, S_PAYEEBANKNO =?, S_DEALNO =?, S_TAXTICKETNO =?,  "
        + "S_GENTICKETDATE =?, S_PAYERACCT =?, S_PAYERNAME =?, S_PAYERADDR =?, N_MONEY =?,  "
        + "S_RECBANKNO =?, S_RECACCT =?, S_RECNAME =?, S_TRIMFLAG =?, S_BUDGETUNITCODE =?,  "
        + "S_UNITCODENAME =?, S_OFYEAR =?, S_BUDGETTYPE =?, S_STATUS =?, S_USERCODE =?,  "
        + "S_ADDWORD =?, S_ORIVOUNO =?, S_ORIVOUDATE =?, S_BACKFLAG =?, S_DEMO =?,  "
        + "TS_SYSUPDATE =CURRENT TIMESTAMP, S_ID =?, S_RECBANKNAME =?, S_PAYERBANKNAME =?, S_FUNDTYPECODE =?,  "
        + "S_FUNDTYPENAME =?, S_PAYTYPECODE =?, S_PAYTYPENAME =?, S_CLEARBANKCODE =?, S_CLEARBANKNAME =?,  "
        + "S_PAYSUMMARYCODE =?, S_PAYSUMMARYNAME =?, S_XAGENTBUSINESSNO =?, S_XPAYAMT =?, S_XPAYDATE =?,  "
        + "S_IFMATCH =?, S_HOLD1 =?, S_HOLD2 =?, S_INPUTRECBANKNO =?, S_INPUTUSERCODE =?,  "
        + "S_CHECKSERCODE =?, S_INPUTRECBANKNAME =?, S_CHECKSTATUS =?, S_BUSINESSTYPECODE =?, S_BUSINESSTYPENAME =? "
        + "WHERE "
        + "S_BIZNO = ?"
        ;	
	
    /* SQL to delete data */
    private static final String SQL_DELETE =
        "DELETE FROM TV_PAYOUTMSGMAIN " 
        + " WHERE "
        + "S_BIZNO = ?"
        ;


	/**
	*  批量查询 一次最多查询的参数数量
	*/
	
	public static final int FIND_BATCH_SIZE = 150  / 1;
	



   /**
   * Create a new record in Database.
   */
  public void create(IDto _dto,  Connection conn) throws SQLException
  {
     TvPayoutmsgmainDto dto = (TvPayoutmsgmainDto)_dto ;
     String msgValid = dto.checkValid() ;
     if (msgValid != null)
     	throw new SQLException("插入错误，"+msgValid) ;

     PreparedStatement ps = null;
     try
     {
         ps = conn.prepareStatement(SQL_INSERT);
          ps.setString(1, dto.getSbizno());

          ps.setString(2, dto.getSorgcode());

          ps.setString(3, dto.getScommitdate());

          ps.setString(4, dto.getSaccdate());

          ps.setString(5, dto.getSfilename());

          ps.setString(6, dto.getStrecode());

          ps.setString(7, dto.getSpackageno());

          ps.setString(8, dto.getSpayunit());

          ps.setString(9, dto.getSpayeebankno());

          ps.setString(10, dto.getSdealno());

          ps.setString(11, dto.getStaxticketno());

          ps.setString(12, dto.getSgenticketdate());

          ps.setString(13, dto.getSpayeracct());

          ps.setString(14, dto.getSpayername());

          ps.setString(15, dto.getSpayeraddr());

          ps.setBigDecimal(16, dto.getNmoney());

          ps.setString(17, dto.getSrecbankno());

          ps.setString(18, dto.getSrecacct());

          ps.setString(19, dto.getSrecname());

          ps.setString(20, dto.getStrimflag());

          ps.setString(21, dto.getSbudgetunitcode());

          ps.setString(22, dto.getSunitcodename());

          ps.setString(23, dto.getSofyear());

          ps.setString(24, dto.getSbudgettype());

          ps.setString(25, dto.getSstatus());

          ps.setString(26, dto.getSusercode());

          ps.setString(27, dto.getSaddword());

          ps.setString(28, dto.getSorivouno());

          ps.setString(29, dto.getSorivoudate());

          ps.setString(30, dto.getSbackflag());

          ps.setString(31, dto.getSdemo());

           ps.setString(32, dto.getSid());

          ps.setString(33, dto.getSrecbankname());

          ps.setString(34, dto.getSpayerbankname());

          ps.setString(35, dto.getSfundtypecode());

          ps.setString(36, dto.getSfundtypename());

          ps.setString(37, dto.getSpaytypecode());

          ps.setString(38, dto.getSpaytypename());

          ps.setString(39, dto.getSclearbankcode());

          ps.setString(40, dto.getSclearbankname());

          ps.setString(41, dto.getSpaysummarycode());

          ps.setString(42, dto.getSpaysummaryname());

          ps.setString(43, dto.getSxagentbusinessno());

          ps.setBigDecimal(44, dto.getSxpayamt());

          ps.setString(45, dto.getSxpaydate());

          ps.setString(46, dto.getSifmatch());

          ps.setString(47, dto.getShold1());

          ps.setString(48, dto.getShold2());

          ps.setString(49, dto.getSinputrecbankno());

          ps.setString(50, dto.getSinputusercode());

          ps.setString(51, dto.getSchecksercode());

          ps.setString(52, dto.getSinputrecbankname());

          ps.setString(53, dto.getScheckstatus());

          ps.setString(54, dto.getSbusinesstypecode());

          ps.setString(55, dto.getSbusinesstypename());

         ps.executeUpdate();
     }
     finally
     {
         close(ps);
     }
     return  ;
  }

   /**
   * Create a new record in Database. and return result, only used when having generated key
   */
   public IDto createWithResult(IDto _dto,  Connection conn) throws SQLException
   {
       TvPayoutmsgmainDto dto = (TvPayoutmsgmainDto)_dto ;
       String msgValid = dto.checkValid() ;
       if (msgValid != null)
           throw new SQLException("插入错误，"+msgValid) ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try
       {
           ps = conn.prepareStatement(SQL_INSERT_WITH_RESULT);
            ps.setString(1, dto.getSbizno());
            ps.setString(2, dto.getSorgcode());
            ps.setString(3, dto.getScommitdate());
            ps.setString(4, dto.getSaccdate());
            ps.setString(5, dto.getSfilename());
            ps.setString(6, dto.getStrecode());
            ps.setString(7, dto.getSpackageno());
            ps.setString(8, dto.getSpayunit());
            ps.setString(9, dto.getSpayeebankno());
            ps.setString(10, dto.getSdealno());
            ps.setString(11, dto.getStaxticketno());
            ps.setString(12, dto.getSgenticketdate());
            ps.setString(13, dto.getSpayeracct());
            ps.setString(14, dto.getSpayername());
            ps.setString(15, dto.getSpayeraddr());
            ps.setBigDecimal(16, dto.getNmoney());
            ps.setString(17, dto.getSrecbankno());
            ps.setString(18, dto.getSrecacct());
            ps.setString(19, dto.getSrecname());
            ps.setString(20, dto.getStrimflag());
            ps.setString(21, dto.getSbudgetunitcode());
            ps.setString(22, dto.getSunitcodename());
            ps.setString(23, dto.getSofyear());
            ps.setString(24, dto.getSbudgettype());
            ps.setString(25, dto.getSstatus());
            ps.setString(26, dto.getSusercode());
            ps.setString(27, dto.getSaddword());
            ps.setString(28, dto.getSorivouno());
            ps.setString(29, dto.getSorivoudate());
            ps.setString(30, dto.getSbackflag());
            ps.setString(31, dto.getSdemo());
             ps.setString(32, dto.getSid());
            ps.setString(33, dto.getSrecbankname());
            ps.setString(34, dto.getSpayerbankname());
            ps.setString(35, dto.getSfundtypecode());
            ps.setString(36, dto.getSfundtypename());
            ps.setString(37, dto.getSpaytypecode());
            ps.setString(38, dto.getSpaytypename());
            ps.setString(39, dto.getSclearbankcode());
            ps.setString(40, dto.getSclearbankname());
            ps.setString(41, dto.getSpaysummarycode());
            ps.setString(42, dto.getSpaysummaryname());
            ps.setString(43, dto.getSxagentbusinessno());
            ps.setBigDecimal(44, dto.getSxpayamt());
            ps.setString(45, dto.getSxpaydate());
            ps.setString(46, dto.getSifmatch());
            ps.setString(47, dto.getShold1());
            ps.setString(48, dto.getShold2());
            ps.setString(49, dto.getSinputrecbankno());
            ps.setString(50, dto.getSinputusercode());
            ps.setString(51, dto.getSchecksercode());
            ps.setString(52, dto.getSinputrecbankname());
            ps.setString(53, dto.getScheckstatus());
            ps.setString(54, dto.getSbusinesstypecode());
            ps.setString(55, dto.getSbusinesstypename());
           rs = ps.executeQuery();
           List results = getResults(rs);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }
        finally
        {
           close(ps);
           close(rs);
        }
   }

 	/**
	* Create batch new record in Database. it only create the first layer dto
	*/
    public void create(IDto[] _dtos, Connection conn)    throws SQLException
    {
        TvPayoutmsgmainDto dto  ;
     	for (int i= 0; i< _dtos.length ; i++)
 	    {
 		    dto  = (TvPayoutmsgmainDto)_dtos[i] ; 
    	    String msgValid = dto.checkValid() ;
        	if (msgValid != null)
     	    	throw new SQLException("插入错误，"+msgValid) ;
     	}
 	    PreparedStatement ps = null;
        try
        {
            ps = conn.prepareStatement(SQL_INSERT);
       
     	    for (int i= 0; i< _dtos.length ; i++)
     	    {
 	    	    dto  = (TvPayoutmsgmainDto)_dtos[i] ; 
  
               ps.setString(1, dto.getSbizno());
  
               ps.setString(2, dto.getSorgcode());
  
               ps.setString(3, dto.getScommitdate());
  
               ps.setString(4, dto.getSaccdate());
  
               ps.setString(5, dto.getSfilename());
  
               ps.setString(6, dto.getStrecode());
  
               ps.setString(7, dto.getSpackageno());
  
               ps.setString(8, dto.getSpayunit());
  
               ps.setString(9, dto.getSpayeebankno());
  
               ps.setString(10, dto.getSdealno());
  
               ps.setString(11, dto.getStaxticketno());
  
               ps.setString(12, dto.getSgenticketdate());
  
               ps.setString(13, dto.getSpayeracct());
  
               ps.setString(14, dto.getSpayername());
  
               ps.setString(15, dto.getSpayeraddr());
  
               ps.setBigDecimal(16, dto.getNmoney());
  
               ps.setString(17, dto.getSrecbankno());
  
               ps.setString(18, dto.getSrecacct());
  
               ps.setString(19, dto.getSrecname());
  
               ps.setString(20, dto.getStrimflag());
  
               ps.setString(21, dto.getSbudgetunitcode());
  
               ps.setString(22, dto.getSunitcodename());
  
               ps.setString(23, dto.getSofyear());
  
               ps.setString(24, dto.getSbudgettype());
  
               ps.setString(25, dto.getSstatus());
  
               ps.setString(26, dto.getSusercode());
  
               ps.setString(27, dto.getSaddword());
  
               ps.setString(28, dto.getSorivouno());
  
               ps.setString(29, dto.getSorivoudate());
  
               ps.setString(30, dto.getSbackflag());
  
               ps.setString(31, dto.getSdemo());
   
               ps.setString(32, dto.getSid());
  
               ps.setString(33, dto.getSrecbankname());
  
               ps.setString(34, dto.getSpayerbankname());
  
               ps.setString(35, dto.getSfundtypecode());
  
               ps.setString(36, dto.getSfundtypename());
  
               ps.setString(37, dto.getSpaytypecode());
  
               ps.setString(38, dto.getSpaytypename());
  
               ps.setString(39, dto.getSclearbankcode());
  
               ps.setString(40, dto.getSclearbankname());
  
               ps.setString(41, dto.getSpaysummarycode());
  
               ps.setString(42, dto.getSpaysummaryname());
  
               ps.setString(43, dto.getSxagentbusinessno());
  
               ps.setBigDecimal(44, dto.getSxpayamt());
  
               ps.setString(45, dto.getSxpaydate());
  
               ps.setString(46, dto.getSifmatch());
  
               ps.setString(47, dto.getShold1());
  
               ps.setString(48, dto.getShold2());
  
               ps.setString(49, dto.getSinputrecbankno());
  
               ps.setString(50, dto.getSinputusercode());
  
               ps.setString(51, dto.getSchecksercode());
  
               ps.setString(52, dto.getSinputrecbankname());
  
               ps.setString(53, dto.getScheckstatus());
  
               ps.setString(54, dto.getSbusinesstypecode());
  
               ps.setString(55, dto.getSbusinesstypename());
               ps.addBatch(); 
           }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }
        return  ;
   }




     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto find(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TvPayoutmsgmainPK pk = (TvPayoutmsgmainPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT);
           ps.setString(1, pk.getSbizno());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }

     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto findForUpdate(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TvPayoutmsgmainPK pk = (TvPayoutmsgmainPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT_FOR_UPDATE);
           ps.setString(1, pk.getSbizno());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }
  
     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto[] find(IPK[] _pks, Connection conn, boolean isLobSupport) throws SQLException
    {
        TvPayoutmsgmainPK pk ;
        
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TvPayoutmsgmainPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 
        PreparedStatement ps = null;
        ResultSet rs = null;
        
         
        List results = new ArrayList();  
		for (int iBegin = 0; iBegin < pks.size(); iBegin += FIND_BATCH_SIZE)   //
		{
        
            try {
          		int iFindBatchSize = pks.size() - iBegin ;
	    		if (iFindBatchSize >FIND_BATCH_SIZE )
		    		iFindBatchSize = FIND_BATCH_SIZE ;
		  
        	    StringBuffer sb = new StringBuffer() ;
            	sb.append(SQL_SELECT_BATCH).append(" WHERE ").append(SQL_SELECT_BATCH_WHERE);

	    		for (int i = iBegin+1; i < iBegin + iFindBatchSize; i++)
		    	{
			    	sb.append("OR").append(SQL_SELECT_BATCH_WHERE) ;
    			}
        	
                ps = conn.prepareStatement(sb.toString());

                for (int i = iBegin;i < iBegin + iFindBatchSize; i++)
                {
                    pk  = (TvPayoutmsgmainPK)(pks.get(i)) ; 
                   ps.setString((i-iBegin)*1+1, pk.getSbizno());

			
	            }
                rs = ps.executeQuery();
                results.addAll(getResults(rs,0,isLobSupport)); //
            }finally {
                close(rs);
                close(ps);
            }
        }
        if (results!=null && results.size() > 0)
        {
            TvPayoutmsgmainDto[] dtos = new TvPayoutmsgmainDto[0];
		    dtos = (TvPayoutmsgmainDto[]) results.toArray(dtos) ;
		    return dtos ;
        }
        else
        {
            return null;
        }
    }

  
  
  



    /**
	    * 得到查询语句
	    */
    public String getSelectSQL() 
    {
	        return SQL_SELECT_BATCH_SCROLLABLE ;
    }
   

	public List getResults(ResultSet rs) throws SQLException
    {
    	return getResults(rs, 0) ;
    }
  

    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize) throws SQLException
    {
		return getResults(rs,maxSize,false);
	}
    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize, boolean isLobSupport) throws SQLException
    {
        List results = new ArrayList();
        String str ;
        Reader reader;
        Clob clob;
        char[] chars ;
        
        while (rs.next())
         {
             TvPayoutmsgmainDto  dto = new  TvPayoutmsgmainDto ();
             //S_BIZNO
             str = rs.getString("S_BIZNO");
             if (str == null)
                dto.setSbizno(null);
             else
                dto.setSbizno(str.trim());

             //S_ORGCODE
             str = rs.getString("S_ORGCODE");
             if (str == null)
                dto.setSorgcode(null);
             else
                dto.setSorgcode(str.trim());

             //S_COMMITDATE
             str = rs.getString("S_COMMITDATE");
             if (str == null)
                dto.setScommitdate(null);
             else
                dto.setScommitdate(str.trim());

             //S_ACCDATE
             str = rs.getString("S_ACCDATE");
             if (str == null)
                dto.setSaccdate(null);
             else
                dto.setSaccdate(str.trim());

             //S_FILENAME
             str = rs.getString("S_FILENAME");
             if (str == null)
                dto.setSfilename(null);
             else
                dto.setSfilename(str.trim());

             //S_TRECODE
             str = rs.getString("S_TRECODE");
             if (str == null)
                dto.setStrecode(null);
             else
                dto.setStrecode(str.trim());

             //S_PACKAGENO
             str = rs.getString("S_PACKAGENO");
             if (str == null)
                dto.setSpackageno(null);
             else
                dto.setSpackageno(str.trim());

             //S_PAYUNIT
             str = rs.getString("S_PAYUNIT");
             if (str == null)
                dto.setSpayunit(null);
             else
                dto.setSpayunit(str.trim());

             //S_PAYEEBANKNO
             str = rs.getString("S_PAYEEBANKNO");
             if (str == null)
                dto.setSpayeebankno(null);
             else
                dto.setSpayeebankno(str.trim());

             //S_DEALNO
             str = rs.getString("S_DEALNO");
             if (str == null)
                dto.setSdealno(null);
             else
                dto.setSdealno(str.trim());

             //S_TAXTICKETNO
             str = rs.getString("S_TAXTICKETNO");
             if (str == null)
                dto.setStaxticketno(null);
             else
                dto.setStaxticketno(str.trim());

             //S_GENTICKETDATE
             str = rs.getString("S_GENTICKETDATE");
             if (str == null)
                dto.setSgenticketdate(null);
             else
                dto.setSgenticketdate(str.trim());

             //S_PAYERACCT
             str = rs.getString("S_PAYERACCT");
             if (str == null)
                dto.setSpayeracct(null);
             else
                dto.setSpayeracct(str.trim());

             //S_PAYERNAME
             str = rs.getString("S_PAYERNAME");
             if (str == null)
                dto.setSpayername(null);
             else
                dto.setSpayername(str.trim());

             //S_PAYERADDR
             str = rs.getString("S_PAYERADDR");
             if (str == null)
                dto.setSpayeraddr(null);
             else
                dto.setSpayeraddr(str.trim());

             //N_MONEY
           dto.setNmoney(rs.getBigDecimal("N_MONEY"));

             //S_RECBANKNO
             str = rs.getString("S_RECBANKNO");
             if (str == null)
                dto.setSrecbankno(null);
             else
                dto.setSrecbankno(str.trim());

             //S_RECACCT
             str = rs.getString("S_RECACCT");
             if (str == null)
                dto.setSrecacct(null);
             else
                dto.setSrecacct(str.trim());

             //S_RECNAME
             str = rs.getString("S_RECNAME");
             if (str == null)
                dto.setSrecname(null);
             else
                dto.setSrecname(str.trim());

             //S_TRIMFLAG
             str = rs.getString("S_TRIMFLAG");
             if (str == null)
                dto.setStrimflag(null);
             else
                dto.setStrimflag(str.trim());

             //S_BUDGETUNITCODE
             str = rs.getString("S_BUDGETUNITCODE");
             if (str == null)
                dto.setSbudgetunitcode(null);
             else
                dto.setSbudgetunitcode(str.trim());

             //S_UNITCODENAME
             str = rs.getString("S_UNITCODENAME");
             if (str == null)
                dto.setSunitcodename(null);
             else
                dto.setSunitcodename(str.trim());

             //S_OFYEAR
             str = rs.getString("S_OFYEAR");
             if (str == null)
                dto.setSofyear(null);
             else
                dto.setSofyear(str.trim());

             //S_BUDGETTYPE
             str = rs.getString("S_BUDGETTYPE");
             if (str == null)
                dto.setSbudgettype(null);
             else
                dto.setSbudgettype(str.trim());

             //S_STATUS
             str = rs.getString("S_STATUS");
             if (str == null)
                dto.setSstatus(null);
             else
                dto.setSstatus(str.trim());

             //S_USERCODE
             str = rs.getString("S_USERCODE");
             if (str == null)
                dto.setSusercode(null);
             else
                dto.setSusercode(str.trim());

             //S_ADDWORD
             str = rs.getString("S_ADDWORD");
             if (str == null)
                dto.setSaddword(null);
             else
                dto.setSaddword(str.trim());

             //S_ORIVOUNO
             str = rs.getString("S_ORIVOUNO");
             if (str == null)
                dto.setSorivouno(null);
             else
                dto.setSorivouno(str.trim());

             //S_ORIVOUDATE
             str = rs.getString("S_ORIVOUDATE");
             if (str == null)
                dto.setSorivoudate(null);
             else
                dto.setSorivoudate(str.trim());

             //S_BACKFLAG
             str = rs.getString("S_BACKFLAG");
             if (str == null)
                dto.setSbackflag(null);
             else
                dto.setSbackflag(str.trim());

             //S_DEMO
             str = rs.getString("S_DEMO");
             if (str == null)
                dto.setSdemo(null);
             else
                dto.setSdemo(str.trim());

             //TS_SYSUPDATE
           dto.setTssysupdate(rs.getTimestamp("TS_SYSUPDATE"));

             //S_ID
             str = rs.getString("S_ID");
             if (str == null)
                dto.setSid(null);
             else
                dto.setSid(str.trim());

             //S_RECBANKNAME
             str = rs.getString("S_RECBANKNAME");
             if (str == null)
                dto.setSrecbankname(null);
             else
                dto.setSrecbankname(str.trim());

             //S_PAYERBANKNAME
             str = rs.getString("S_PAYERBANKNAME");
             if (str == null)
                dto.setSpayerbankname(null);
             else
                dto.setSpayerbankname(str.trim());

             //S_FUNDTYPECODE
             str = rs.getString("S_FUNDTYPECODE");
             if (str == null)
                dto.setSfundtypecode(null);
             else
                dto.setSfundtypecode(str.trim());

             //S_FUNDTYPENAME
             str = rs.getString("S_FUNDTYPENAME");
             if (str == null)
                dto.setSfundtypename(null);
             else
                dto.setSfundtypename(str.trim());

             //S_PAYTYPECODE
             str = rs.getString("S_PAYTYPECODE");
             if (str == null)
                dto.setSpaytypecode(null);
             else
                dto.setSpaytypecode(str.trim());

             //S_PAYTYPENAME
             str = rs.getString("S_PAYTYPENAME");
             if (str == null)
                dto.setSpaytypename(null);
             else
                dto.setSpaytypename(str.trim());

             //S_CLEARBANKCODE
             str = rs.getString("S_CLEARBANKCODE");
             if (str == null)
                dto.setSclearbankcode(null);
             else
                dto.setSclearbankcode(str.trim());

             //S_CLEARBANKNAME
             str = rs.getString("S_CLEARBANKNAME");
             if (str == null)
                dto.setSclearbankname(null);
             else
                dto.setSclearbankname(str.trim());

             //S_PAYSUMMARYCODE
             str = rs.getString("S_PAYSUMMARYCODE");
             if (str == null)
                dto.setSpaysummarycode(null);
             else
                dto.setSpaysummarycode(str.trim());

             //S_PAYSUMMARYNAME
             str = rs.getString("S_PAYSUMMARYNAME");
             if (str == null)
                dto.setSpaysummaryname(null);
             else
                dto.setSpaysummaryname(str.trim());

             //S_XAGENTBUSINESSNO
             str = rs.getString("S_XAGENTBUSINESSNO");
             if (str == null)
                dto.setSxagentbusinessno(null);
             else
                dto.setSxagentbusinessno(str.trim());

             //S_XPAYAMT
           dto.setSxpayamt(rs.getBigDecimal("S_XPAYAMT"));

             //S_XPAYDATE
             str = rs.getString("S_XPAYDATE");
             if (str == null)
                dto.setSxpaydate(null);
             else
                dto.setSxpaydate(str.trim());

             //S_IFMATCH
             str = rs.getString("S_IFMATCH");
             if (str == null)
                dto.setSifmatch(null);
             else
                dto.setSifmatch(str.trim());

             //S_HOLD1
             str = rs.getString("S_HOLD1");
             if (str == null)
                dto.setShold1(null);
             else
                dto.setShold1(str.trim());

             //S_HOLD2
             str = rs.getString("S_HOLD2");
             if (str == null)
                dto.setShold2(null);
             else
                dto.setShold2(str.trim());

             //S_INPUTRECBANKNO
             str = rs.getString("S_INPUTRECBANKNO");
             if (str == null)
                dto.setSinputrecbankno(null);
             else
                dto.setSinputrecbankno(str.trim());

             //S_INPUTUSERCODE
             str = rs.getString("S_INPUTUSERCODE");
             if (str == null)
                dto.setSinputusercode(null);
             else
                dto.setSinputusercode(str.trim());

             //S_CHECKSERCODE
             str = rs.getString("S_CHECKSERCODE");
             if (str == null)
                dto.setSchecksercode(null);
             else
                dto.setSchecksercode(str.trim());

             //S_INPUTRECBANKNAME
             str = rs.getString("S_INPUTRECBANKNAME");
             if (str == null)
                dto.setSinputrecbankname(null);
             else
                dto.setSinputrecbankname(str.trim());

             //S_CHECKSTATUS
             str = rs.getString("S_CHECKSTATUS");
             if (str == null)
                dto.setScheckstatus(null);
             else
                dto.setScheckstatus(str.trim());

             //S_BUSINESSTYPECODE
             str = rs.getString("S_BUSINESSTYPECODE");
             if (str == null)
                dto.setSbusinesstypecode(null);
             else
                dto.setSbusinesstypecode(str.trim());

             //S_BUSINESSTYPENAME
             str = rs.getString("S_BUSINESSTYPENAME");
             if (str == null)
                dto.setSbusinesstypename(null);
             else
                dto.setSbusinesstypename(str.trim());



            results.add(dto);
            
            if(maxSize >0  && results.size() == maxSize)
            {
            	break ;
            }
            
         }
        
		return results ;
    }

  /**
   * populate resultSet as cclass
   */
  protected IDto[] getChildrenResults(ResultSet rs ,Class _dto) throws SQLException{

      throw new SQLException("本dto没有与其它dto相关联，无法应用此方法");
  }


     /**
     * Update a record in Database.
     */

    public void update(IDto _dto, Connection conn, boolean isLobSupport) throws SQLException
    {
        String msgValid = _dto.checkValid() ;
        if (msgValid != null)
            throw new SQLException("修改错误，"+msgValid) ;
        TvPayoutmsgmainDto dto = (TvPayoutmsgmainDto)_dto ;
        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            int pos = 1;
            //S_ORGCODE
            ps.setString(pos, dto.getSorgcode());
            pos++;

            //S_COMMITDATE
            ps.setString(pos, dto.getScommitdate());
            pos++;

            //S_ACCDATE
            ps.setString(pos, dto.getSaccdate());
            pos++;

            //S_FILENAME
            ps.setString(pos, dto.getSfilename());
            pos++;

            //S_TRECODE
            ps.setString(pos, dto.getStrecode());
            pos++;

            //S_PACKAGENO
            ps.setString(pos, dto.getSpackageno());
            pos++;

            //S_PAYUNIT
            ps.setString(pos, dto.getSpayunit());
            pos++;

            //S_PAYEEBANKNO
            ps.setString(pos, dto.getSpayeebankno());
            pos++;

            //S_DEALNO
            ps.setString(pos, dto.getSdealno());
            pos++;

            //S_TAXTICKETNO
            ps.setString(pos, dto.getStaxticketno());
            pos++;

            //S_GENTICKETDATE
            ps.setString(pos, dto.getSgenticketdate());
            pos++;

            //S_PAYERACCT
            ps.setString(pos, dto.getSpayeracct());
            pos++;

            //S_PAYERNAME
            ps.setString(pos, dto.getSpayername());
            pos++;

            //S_PAYERADDR
            ps.setString(pos, dto.getSpayeraddr());
            pos++;

            //N_MONEY
            ps.setBigDecimal(pos, dto.getNmoney());
            pos++;

            //S_RECBANKNO
            ps.setString(pos, dto.getSrecbankno());
            pos++;

            //S_RECACCT
            ps.setString(pos, dto.getSrecacct());
            pos++;

            //S_RECNAME
            ps.setString(pos, dto.getSrecname());
            pos++;

            //S_TRIMFLAG
            ps.setString(pos, dto.getStrimflag());
            pos++;

            //S_BUDGETUNITCODE
            ps.setString(pos, dto.getSbudgetunitcode());
            pos++;

            //S_UNITCODENAME
            ps.setString(pos, dto.getSunitcodename());
            pos++;

            //S_OFYEAR
            ps.setString(pos, dto.getSofyear());
            pos++;

            //S_BUDGETTYPE
            ps.setString(pos, dto.getSbudgettype());
            pos++;

            //S_STATUS
            ps.setString(pos, dto.getSstatus());
            pos++;

            //S_USERCODE
            ps.setString(pos, dto.getSusercode());
            pos++;

            //S_ADDWORD
            ps.setString(pos, dto.getSaddword());
            pos++;

            //S_ORIVOUNO
            ps.setString(pos, dto.getSorivouno());
            pos++;

            //S_ORIVOUDATE
            ps.setString(pos, dto.getSorivoudate());
            pos++;

            //S_BACKFLAG
            ps.setString(pos, dto.getSbackflag());
            pos++;

            //S_DEMO
            ps.setString(pos, dto.getSdemo());
            pos++;

            //TS_SYSUPDATE
            //S_ID
            ps.setString(pos, dto.getSid());
            pos++;

            //S_RECBANKNAME
            ps.setString(pos, dto.getSrecbankname());
            pos++;

            //S_PAYERBANKNAME
            ps.setString(pos, dto.getSpayerbankname());
            pos++;

            //S_FUNDTYPECODE
            ps.setString(pos, dto.getSfundtypecode());
            pos++;

            //S_FUNDTYPENAME
            ps.setString(pos, dto.getSfundtypename());
            pos++;

            //S_PAYTYPECODE
            ps.setString(pos, dto.getSpaytypecode());
            pos++;

            //S_PAYTYPENAME
            ps.setString(pos, dto.getSpaytypename());
            pos++;

            //S_CLEARBANKCODE
            ps.setString(pos, dto.getSclearbankcode());
            pos++;

            //S_CLEARBANKNAME
            ps.setString(pos, dto.getSclearbankname());
            pos++;

            //S_PAYSUMMARYCODE
            ps.setString(pos, dto.getSpaysummarycode());
            pos++;

            //S_PAYSUMMARYNAME
            ps.setString(pos, dto.getSpaysummaryname());
            pos++;

            //S_XAGENTBUSINESSNO
            ps.setString(pos, dto.getSxagentbusinessno());
            pos++;

            //S_XPAYAMT
            ps.setBigDecimal(pos, dto.getSxpayamt());
            pos++;

            //S_XPAYDATE
            ps.setString(pos, dto.getSxpaydate());
            pos++;

            //S_IFMATCH
            ps.setString(pos, dto.getSifmatch());
            pos++;

            //S_HOLD1
            ps.setString(pos, dto.getShold1());
            pos++;

            //S_HOLD2
            ps.setString(pos, dto.getShold2());
            pos++;

            //S_INPUTRECBANKNO
            ps.setString(pos, dto.getSinputrecbankno());
            pos++;

            //S_INPUTUSERCODE
            ps.setString(pos, dto.getSinputusercode());
            pos++;

            //S_CHECKSERCODE
            ps.setString(pos, dto.getSchecksercode());
            pos++;

            //S_INPUTRECBANKNAME
            ps.setString(pos, dto.getSinputrecbankname());
            pos++;

            //S_CHECKSTATUS
            ps.setString(pos, dto.getScheckstatus());
            pos++;

            //S_BUSINESSTYPECODE
            ps.setString(pos, dto.getSbusinesstypecode());
            pos++;

            //S_BUSINESSTYPENAME
            ps.setString(pos, dto.getSbusinesstypename());
            pos++;


           //S_BIZNO
           ps.setString(pos, dto.getSbizno());
           pos++;
           ps.executeUpdate();
        }
        finally
        {
           close(ps);
        }

        return ;
    }
    
    
    
      /**
     * Update batch record in Database.
     */

    public void update(IDto[] _dtos, Connection conn, boolean isLobSupport) throws SQLException
    {
     
     	 TvPayoutmsgmainDto dto  ;
         for (int i= 0; i< _dtos.length ; i++)
         {
            dto  = (TvPayoutmsgmainDto)_dtos[i] ; 
            String msgValid = dto.checkValid() ;
            if (msgValid != null)
                throw new SQLException("修改错误，"+msgValid) ;
         }

        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            
            for (int i= 0; i< _dtos.length ; i++)
            {
                dto  = (TvPayoutmsgmainDto)_dtos[i] ; 
                int pos = 1;
                //S_ORGCODE
                 ps.setString(pos, dto.getSorgcode());
                pos++;

                //S_COMMITDATE
                 ps.setString(pos, dto.getScommitdate());
                pos++;

                //S_ACCDATE
                 ps.setString(pos, dto.getSaccdate());
                pos++;

                //S_FILENAME
                 ps.setString(pos, dto.getSfilename());
                pos++;

                //S_TRECODE
                 ps.setString(pos, dto.getStrecode());
                pos++;

                //S_PACKAGENO
                 ps.setString(pos, dto.getSpackageno());
                pos++;

                //S_PAYUNIT
                 ps.setString(pos, dto.getSpayunit());
                pos++;

                //S_PAYEEBANKNO
                 ps.setString(pos, dto.getSpayeebankno());
                pos++;

                //S_DEALNO
                 ps.setString(pos, dto.getSdealno());
                pos++;

                //S_TAXTICKETNO
                 ps.setString(pos, dto.getStaxticketno());
                pos++;

                //S_GENTICKETDATE
                 ps.setString(pos, dto.getSgenticketdate());
                pos++;

                //S_PAYERACCT
                 ps.setString(pos, dto.getSpayeracct());
                pos++;

                //S_PAYERNAME
                 ps.setString(pos, dto.getSpayername());
                pos++;

                //S_PAYERADDR
                 ps.setString(pos, dto.getSpayeraddr());
                pos++;

                //N_MONEY
                 ps.setBigDecimal(pos, dto.getNmoney());
                pos++;

                //S_RECBANKNO
                 ps.setString(pos, dto.getSrecbankno());
                pos++;

                //S_RECACCT
                 ps.setString(pos, dto.getSrecacct());
                pos++;

                //S_RECNAME
                 ps.setString(pos, dto.getSrecname());
                pos++;

                //S_TRIMFLAG
                 ps.setString(pos, dto.getStrimflag());
                pos++;

                //S_BUDGETUNITCODE
                 ps.setString(pos, dto.getSbudgetunitcode());
                pos++;

                //S_UNITCODENAME
                 ps.setString(pos, dto.getSunitcodename());
                pos++;

                //S_OFYEAR
                 ps.setString(pos, dto.getSofyear());
                pos++;

                //S_BUDGETTYPE
                 ps.setString(pos, dto.getSbudgettype());
                pos++;

                //S_STATUS
                 ps.setString(pos, dto.getSstatus());
                pos++;

                //S_USERCODE
                 ps.setString(pos, dto.getSusercode());
                pos++;

                //S_ADDWORD
                 ps.setString(pos, dto.getSaddword());
                pos++;

                //S_ORIVOUNO
                 ps.setString(pos, dto.getSorivouno());
                pos++;

                //S_ORIVOUDATE
                 ps.setString(pos, dto.getSorivoudate());
                pos++;

                //S_BACKFLAG
                 ps.setString(pos, dto.getSbackflag());
                pos++;

                //S_DEMO
                 ps.setString(pos, dto.getSdemo());
                pos++;

                //TS_SYSUPDATE
                 //S_ID
                 ps.setString(pos, dto.getSid());
                pos++;

                //S_RECBANKNAME
                 ps.setString(pos, dto.getSrecbankname());
                pos++;

                //S_PAYERBANKNAME
                 ps.setString(pos, dto.getSpayerbankname());
                pos++;

                //S_FUNDTYPECODE
                 ps.setString(pos, dto.getSfundtypecode());
                pos++;

                //S_FUNDTYPENAME
                 ps.setString(pos, dto.getSfundtypename());
                pos++;

                //S_PAYTYPECODE
                 ps.setString(pos, dto.getSpaytypecode());
                pos++;

                //S_PAYTYPENAME
                 ps.setString(pos, dto.getSpaytypename());
                pos++;

                //S_CLEARBANKCODE
                 ps.setString(pos, dto.getSclearbankcode());
                pos++;

                //S_CLEARBANKNAME
                 ps.setString(pos, dto.getSclearbankname());
                pos++;

                //S_PAYSUMMARYCODE
                 ps.setString(pos, dto.getSpaysummarycode());
                pos++;

                //S_PAYSUMMARYNAME
                 ps.setString(pos, dto.getSpaysummaryname());
                pos++;

                //S_XAGENTBUSINESSNO
                 ps.setString(pos, dto.getSxagentbusinessno());
                pos++;

                //S_XPAYAMT
                 ps.setBigDecimal(pos, dto.getSxpayamt());
                pos++;

                //S_XPAYDATE
                 ps.setString(pos, dto.getSxpaydate());
                pos++;

                //S_IFMATCH
                 ps.setString(pos, dto.getSifmatch());
                pos++;

                //S_HOLD1
                 ps.setString(pos, dto.getShold1());
                pos++;

                //S_HOLD2
                 ps.setString(pos, dto.getShold2());
                pos++;

                //S_INPUTRECBANKNO
                 ps.setString(pos, dto.getSinputrecbankno());
                pos++;

                //S_INPUTUSERCODE
                 ps.setString(pos, dto.getSinputusercode());
                pos++;

                //S_CHECKSERCODE
                 ps.setString(pos, dto.getSchecksercode());
                pos++;

                //S_INPUTRECBANKNAME
                 ps.setString(pos, dto.getSinputrecbankname());
                pos++;

                //S_CHECKSTATUS
                 ps.setString(pos, dto.getScheckstatus());
                pos++;

                //S_BUSINESSTYPECODE
                 ps.setString(pos, dto.getSbusinesstypecode());
                pos++;

                //S_BUSINESSTYPENAME
                 ps.setString(pos, dto.getSbusinesstypename());
                pos++;


               //S_BIZNO
               ps.setString(pos, dto.getSbizno());
               pos++;
		       ps.addBatch(); 
	       }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }

        return ;
    }



    /**
     * delete a record in Database.
     */
    public void delete(IPK _pk, Connection conn) throws SQLException
    {
       
      
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return ;
        //	throw new SQLException("删除错误，"+msgValid) ;
       }
       TvPayoutmsgmainPK pk = (TvPayoutmsgmainPK)_pk ;


       PreparedStatement ps = null;
       try {
           ps = conn.prepareStatement(SQL_DELETE);
           ps.setString(1, pk.getSbizno());
           ps.executeUpdate();
        }
        finally
        {
            close(ps);
        }
    }


     /**
     * delete batch record in Database.
     */
    public void delete(IPK[] _pks, Connection conn) throws SQLException
    {
        TvPayoutmsgmainPK pk ;
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TvPayoutmsgmainPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 		
 		
        PreparedStatement ps = null;
        try
         {
            ps = conn.prepareStatement(SQL_DELETE);
             
       		for (int i= 0; i< pks.size() ; i++)
       		{
       			pk  = (TvPayoutmsgmainPK)(pks.get(i)) ; 
                ps.setString(1, pk.getSbizno());
		        ps.addBatch() ;
			}
            ps.executeBatch();
        }
        finally
        {
            close(ps);
        }      
    }



    /**
    * Check IChangerNumber or UpdateTimeStamp.
    */
    public void check(IDto _dto, Connection conn) throws SQLException
    {
                                                                                                                                                                     		throw new SQLException("数据库表：TV_PAYOUTMSGMAIN没有检查修改的字段");
    }


	/**
	* synchronize values between parent and children
	*/
    public void syncToChildren(IDto _dto)     throws SQLException{
       throw new SQLException("本记录没有与其它记录相关联，无法进行主记录与明细记录之间的数据同步。");
  }

	/**
	*get children
	*/
    public IDto[] findChildren (IDto _dto, Connection conn, boolean isLobSupport)     throws SQLException{
         throw new SQLException("本dto没有与其它dto相关联，无法得到子dto");
  }    
    
  /**
   *return the children IDto class
   * @return Class
   */
  public Class getChildrenClass(){
	  return null;
  };    



    /**
     * Close JDBC Statement.
     * @param stmt  Statement to be closed.
     */
    private void close(Statement stmt)
    {
        if (stmt != null)
        {
            try
            {
                stmt.close();
            }
            catch(SQLException    e)
            {}
        }
    }

    /**
     * Close JDBC ResultSet.
     * @param rs  ResultSet to be closed.
     */
    private void close(ResultSet rs)
    {
        if (rs != null)
        {
            try
            {
                rs.close();
            }catch(SQLException  e){}
        }
    }
}
