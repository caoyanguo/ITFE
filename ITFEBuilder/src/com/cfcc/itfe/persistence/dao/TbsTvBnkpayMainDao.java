    



package com.cfcc.itfe.persistence.dao;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.Statement;
import java.sql.SQLException;
import java.sql.Date;
import java.sql.Time;
import java.sql.Timestamp ;
import java.sql.Clob;

import com.cfcc.jaf.persistence.jaform.parent.* ;

import java.math.BigDecimal;

import java.util.List;
import java.util.ArrayList;
import java.util.HashMap;

import java.io.StringReader;
import java.io.Reader;
import java.io.IOException;

import com.cfcc.itfe.persistence.dto.TbsTvBnkpayMainDto ;
import com.cfcc.itfe.persistence.pk.TbsTvBnkpayMainPK ;


/**
 * <p>Title: DAO of table: TBS_TV_BNKPAY_MAIN</p>
 * <p>Description: Data Access Object  </p>
 * <p>Copyright: Copyright (c) 2005</p>
 * <p>Company: CFCC </p>
 * <p>Generation Time: 2014-12-25 15:28:57 </p>
 *  Do NOT change this file by hand.
 *  This java source file is generated by JAF_ORM .
 *  dao.vm timestamp: 2007-04-06 08:30:00
 ******************************************************
 * <p> <b>change record</b><br>
 *  make dao methods simply.<br>
 *  especially make update,delete methods simply.<br>
 *  remove "..withCheck" method,withCheck will impl in JAFOrmTemplate.<br>
 *  remove "delete(IDto _dto,..)" method.<br></p>
 * @author win7
 */

public class TbsTvBnkpayMainDao  implements IDao
{


    /* SQL to insert data */
    private static final String SQL_INSERT =
        "INSERT INTO TBS_TV_BNKPAY_MAIN ("
          + "I_VOUSRLNO,S_BOOKORGCODE,S_TRECODE,S_PACKNO,S_VOUNO"
          + ",S_BIZTYPE,F_ZEROSUMAMT,F_SMALLSUMAMT,S_SNDBNKNO,S_PAYEROPNBNKNO"
          + ",S_PAYERACCT,S_PAYERNAME,S_PAYERADDR,S_PAYEEACCT,S_PAYEENAME"
          + ",S_PAYEEADDR,S_AGENTBNKCODE,S_RCVBNKNO,S_PAYEEOPNBNKNO,S_SRLACCTBRIEFCODE"
          + ",S_ADDWORD,S_PAYDICTATENO,S_PAYMSGNO,D_PAYENTRUSTDATE,S_PAYSNDBNKNO"
          + ",D_VOUCHER,C_BDGKIND,I_OFYEAR,D_ACCEPT,D_ACCT"
          + ",S_ORIVOUNO,D_ORIVOUDATE,S_FILENAME,S_STATE,C_TRIMFLAG"
          + ",I_CHGNUM,S_INPUTERID,TS_SYSUPDATE,S_IFMATCH"
        + ") VALUES ("
        + "?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,0,?,CURRENT TIMESTAMP ,?)";


    private static final String SQL_INSERT_WITH_RESULT = "SELECT * FROM FINAL TABLE( " + SQL_INSERT + " )";
    
    /* SQL to select data */
    private static final String SQL_SELECT =
        "SELECT "
        + "TBS_TV_BNKPAY_MAIN.I_VOUSRLNO, TBS_TV_BNKPAY_MAIN.S_BOOKORGCODE, TBS_TV_BNKPAY_MAIN.S_TRECODE, TBS_TV_BNKPAY_MAIN.S_PACKNO, TBS_TV_BNKPAY_MAIN.S_VOUNO, "
        + "TBS_TV_BNKPAY_MAIN.S_BIZTYPE, TBS_TV_BNKPAY_MAIN.F_ZEROSUMAMT, TBS_TV_BNKPAY_MAIN.F_SMALLSUMAMT, TBS_TV_BNKPAY_MAIN.S_SNDBNKNO, TBS_TV_BNKPAY_MAIN.S_PAYEROPNBNKNO, "
        + "TBS_TV_BNKPAY_MAIN.S_PAYERACCT, TBS_TV_BNKPAY_MAIN.S_PAYERNAME, TBS_TV_BNKPAY_MAIN.S_PAYERADDR, TBS_TV_BNKPAY_MAIN.S_PAYEEACCT, TBS_TV_BNKPAY_MAIN.S_PAYEENAME, "
        + "TBS_TV_BNKPAY_MAIN.S_PAYEEADDR, TBS_TV_BNKPAY_MAIN.S_AGENTBNKCODE, TBS_TV_BNKPAY_MAIN.S_RCVBNKNO, TBS_TV_BNKPAY_MAIN.S_PAYEEOPNBNKNO, TBS_TV_BNKPAY_MAIN.S_SRLACCTBRIEFCODE, "
        + "TBS_TV_BNKPAY_MAIN.S_ADDWORD, TBS_TV_BNKPAY_MAIN.S_PAYDICTATENO, TBS_TV_BNKPAY_MAIN.S_PAYMSGNO, TBS_TV_BNKPAY_MAIN.D_PAYENTRUSTDATE, TBS_TV_BNKPAY_MAIN.S_PAYSNDBNKNO, "
        + "TBS_TV_BNKPAY_MAIN.D_VOUCHER, TBS_TV_BNKPAY_MAIN.C_BDGKIND, TBS_TV_BNKPAY_MAIN.I_OFYEAR, TBS_TV_BNKPAY_MAIN.D_ACCEPT, TBS_TV_BNKPAY_MAIN.D_ACCT, "
        + "TBS_TV_BNKPAY_MAIN.S_ORIVOUNO, TBS_TV_BNKPAY_MAIN.D_ORIVOUDATE, TBS_TV_BNKPAY_MAIN.S_FILENAME, TBS_TV_BNKPAY_MAIN.S_STATE, TBS_TV_BNKPAY_MAIN.C_TRIMFLAG, "
        + "TBS_TV_BNKPAY_MAIN.I_CHGNUM, TBS_TV_BNKPAY_MAIN.S_INPUTERID, TBS_TV_BNKPAY_MAIN.TS_SYSUPDATE, TBS_TV_BNKPAY_MAIN.S_IFMATCH "
        + "FROM TBS_TV_BNKPAY_MAIN "
        +" WHERE " 
        + "I_VOUSRLNO = ?"
        ;
    /* SQL to select for update data */
    private static final String SQL_SELECT_FOR_UPDATE =
        "SELECT "
        + "TBS_TV_BNKPAY_MAIN.I_VOUSRLNO, TBS_TV_BNKPAY_MAIN.S_BOOKORGCODE, TBS_TV_BNKPAY_MAIN.S_TRECODE, TBS_TV_BNKPAY_MAIN.S_PACKNO, TBS_TV_BNKPAY_MAIN.S_VOUNO, "
        + "TBS_TV_BNKPAY_MAIN.S_BIZTYPE, TBS_TV_BNKPAY_MAIN.F_ZEROSUMAMT, TBS_TV_BNKPAY_MAIN.F_SMALLSUMAMT, TBS_TV_BNKPAY_MAIN.S_SNDBNKNO, TBS_TV_BNKPAY_MAIN.S_PAYEROPNBNKNO, "
        + "TBS_TV_BNKPAY_MAIN.S_PAYERACCT, TBS_TV_BNKPAY_MAIN.S_PAYERNAME, TBS_TV_BNKPAY_MAIN.S_PAYERADDR, TBS_TV_BNKPAY_MAIN.S_PAYEEACCT, TBS_TV_BNKPAY_MAIN.S_PAYEENAME, "
        + "TBS_TV_BNKPAY_MAIN.S_PAYEEADDR, TBS_TV_BNKPAY_MAIN.S_AGENTBNKCODE, TBS_TV_BNKPAY_MAIN.S_RCVBNKNO, TBS_TV_BNKPAY_MAIN.S_PAYEEOPNBNKNO, TBS_TV_BNKPAY_MAIN.S_SRLACCTBRIEFCODE, "
        + "TBS_TV_BNKPAY_MAIN.S_ADDWORD, TBS_TV_BNKPAY_MAIN.S_PAYDICTATENO, TBS_TV_BNKPAY_MAIN.S_PAYMSGNO, TBS_TV_BNKPAY_MAIN.D_PAYENTRUSTDATE, TBS_TV_BNKPAY_MAIN.S_PAYSNDBNKNO, "
        + "TBS_TV_BNKPAY_MAIN.D_VOUCHER, TBS_TV_BNKPAY_MAIN.C_BDGKIND, TBS_TV_BNKPAY_MAIN.I_OFYEAR, TBS_TV_BNKPAY_MAIN.D_ACCEPT, TBS_TV_BNKPAY_MAIN.D_ACCT, "
        + "TBS_TV_BNKPAY_MAIN.S_ORIVOUNO, TBS_TV_BNKPAY_MAIN.D_ORIVOUDATE, TBS_TV_BNKPAY_MAIN.S_FILENAME, TBS_TV_BNKPAY_MAIN.S_STATE, TBS_TV_BNKPAY_MAIN.C_TRIMFLAG, "
        + "TBS_TV_BNKPAY_MAIN.I_CHGNUM, TBS_TV_BNKPAY_MAIN.S_INPUTERID, TBS_TV_BNKPAY_MAIN.TS_SYSUPDATE, TBS_TV_BNKPAY_MAIN.S_IFMATCH "
        + "FROM TBS_TV_BNKPAY_MAIN "
        +" WHERE " 
        + "I_VOUSRLNO = ? FOR UPDATE"
        ;

      /* SQL to select data (SCROLLABLE)*/
     private static final String SQL_SELECT_BATCH_SCROLLABLE =
        "SELECT "
        + "  TBS_TV_BNKPAY_MAIN.I_VOUSRLNO  , TBS_TV_BNKPAY_MAIN.S_BOOKORGCODE  , TBS_TV_BNKPAY_MAIN.S_TRECODE  , TBS_TV_BNKPAY_MAIN.S_PACKNO  , TBS_TV_BNKPAY_MAIN.S_VOUNO "
        + " , TBS_TV_BNKPAY_MAIN.S_BIZTYPE  , TBS_TV_BNKPAY_MAIN.F_ZEROSUMAMT  , TBS_TV_BNKPAY_MAIN.F_SMALLSUMAMT  , TBS_TV_BNKPAY_MAIN.S_SNDBNKNO  , TBS_TV_BNKPAY_MAIN.S_PAYEROPNBNKNO "
        + " , TBS_TV_BNKPAY_MAIN.S_PAYERACCT  , TBS_TV_BNKPAY_MAIN.S_PAYERNAME  , TBS_TV_BNKPAY_MAIN.S_PAYERADDR  , TBS_TV_BNKPAY_MAIN.S_PAYEEACCT  , TBS_TV_BNKPAY_MAIN.S_PAYEENAME "
        + " , TBS_TV_BNKPAY_MAIN.S_PAYEEADDR  , TBS_TV_BNKPAY_MAIN.S_AGENTBNKCODE  , TBS_TV_BNKPAY_MAIN.S_RCVBNKNO  , TBS_TV_BNKPAY_MAIN.S_PAYEEOPNBNKNO  , TBS_TV_BNKPAY_MAIN.S_SRLACCTBRIEFCODE "
        + " , TBS_TV_BNKPAY_MAIN.S_ADDWORD  , TBS_TV_BNKPAY_MAIN.S_PAYDICTATENO  , TBS_TV_BNKPAY_MAIN.S_PAYMSGNO  , TBS_TV_BNKPAY_MAIN.D_PAYENTRUSTDATE  , TBS_TV_BNKPAY_MAIN.S_PAYSNDBNKNO "
        + " , TBS_TV_BNKPAY_MAIN.D_VOUCHER  , TBS_TV_BNKPAY_MAIN.C_BDGKIND  , TBS_TV_BNKPAY_MAIN.I_OFYEAR  , TBS_TV_BNKPAY_MAIN.D_ACCEPT  , TBS_TV_BNKPAY_MAIN.D_ACCT "
        + " , TBS_TV_BNKPAY_MAIN.S_ORIVOUNO  , TBS_TV_BNKPAY_MAIN.D_ORIVOUDATE  , TBS_TV_BNKPAY_MAIN.S_FILENAME  , TBS_TV_BNKPAY_MAIN.S_STATE  , TBS_TV_BNKPAY_MAIN.C_TRIMFLAG "
        + " , TBS_TV_BNKPAY_MAIN.I_CHGNUM  , TBS_TV_BNKPAY_MAIN.S_INPUTERID  , TBS_TV_BNKPAY_MAIN.TS_SYSUPDATE  , TBS_TV_BNKPAY_MAIN.S_IFMATCH "
        + "FROM TBS_TV_BNKPAY_MAIN ";



    /* SQL to select batch data */
    private static final String SQL_SELECT_BATCH =
        "SELECT "
        + "TBS_TV_BNKPAY_MAIN.I_VOUSRLNO, TBS_TV_BNKPAY_MAIN.S_BOOKORGCODE, TBS_TV_BNKPAY_MAIN.S_TRECODE, TBS_TV_BNKPAY_MAIN.S_PACKNO, TBS_TV_BNKPAY_MAIN.S_VOUNO, "
        + "TBS_TV_BNKPAY_MAIN.S_BIZTYPE, TBS_TV_BNKPAY_MAIN.F_ZEROSUMAMT, TBS_TV_BNKPAY_MAIN.F_SMALLSUMAMT, TBS_TV_BNKPAY_MAIN.S_SNDBNKNO, TBS_TV_BNKPAY_MAIN.S_PAYEROPNBNKNO, "
        + "TBS_TV_BNKPAY_MAIN.S_PAYERACCT, TBS_TV_BNKPAY_MAIN.S_PAYERNAME, TBS_TV_BNKPAY_MAIN.S_PAYERADDR, TBS_TV_BNKPAY_MAIN.S_PAYEEACCT, TBS_TV_BNKPAY_MAIN.S_PAYEENAME, "
        + "TBS_TV_BNKPAY_MAIN.S_PAYEEADDR, TBS_TV_BNKPAY_MAIN.S_AGENTBNKCODE, TBS_TV_BNKPAY_MAIN.S_RCVBNKNO, TBS_TV_BNKPAY_MAIN.S_PAYEEOPNBNKNO, TBS_TV_BNKPAY_MAIN.S_SRLACCTBRIEFCODE, "
        + "TBS_TV_BNKPAY_MAIN.S_ADDWORD, TBS_TV_BNKPAY_MAIN.S_PAYDICTATENO, TBS_TV_BNKPAY_MAIN.S_PAYMSGNO, TBS_TV_BNKPAY_MAIN.D_PAYENTRUSTDATE, TBS_TV_BNKPAY_MAIN.S_PAYSNDBNKNO, "
        + "TBS_TV_BNKPAY_MAIN.D_VOUCHER, TBS_TV_BNKPAY_MAIN.C_BDGKIND, TBS_TV_BNKPAY_MAIN.I_OFYEAR, TBS_TV_BNKPAY_MAIN.D_ACCEPT, TBS_TV_BNKPAY_MAIN.D_ACCT, "
        + "TBS_TV_BNKPAY_MAIN.S_ORIVOUNO, TBS_TV_BNKPAY_MAIN.D_ORIVOUDATE, TBS_TV_BNKPAY_MAIN.S_FILENAME, TBS_TV_BNKPAY_MAIN.S_STATE, TBS_TV_BNKPAY_MAIN.C_TRIMFLAG, "
        + "TBS_TV_BNKPAY_MAIN.I_CHGNUM, TBS_TV_BNKPAY_MAIN.S_INPUTERID, TBS_TV_BNKPAY_MAIN.TS_SYSUPDATE, TBS_TV_BNKPAY_MAIN.S_IFMATCH "
        + "FROM TBS_TV_BNKPAY_MAIN " ;
        

   /* SQL to select batch data where condition  */
    private static final String SQL_SELECT_BATCH_WHERE =" ( "
        + "I_VOUSRLNO = ?)"
        ;


    /* SQL to update data */
    private static final String SQL_UPDATE =
        "UPDATE TBS_TV_BNKPAY_MAIN SET "
        + "S_BOOKORGCODE =?,S_TRECODE =?,S_PACKNO =?,S_VOUNO =?,S_BIZTYPE =?, "
        + "F_ZEROSUMAMT =?,F_SMALLSUMAMT =?,S_SNDBNKNO =?,S_PAYEROPNBNKNO =?,S_PAYERACCT =?, "
        + "S_PAYERNAME =?,S_PAYERADDR =?,S_PAYEEACCT =?,S_PAYEENAME =?,S_PAYEEADDR =?, "
        + "S_AGENTBNKCODE =?,S_RCVBNKNO =?,S_PAYEEOPNBNKNO =?,S_SRLACCTBRIEFCODE =?,S_ADDWORD =?, "
        + "S_PAYDICTATENO =?,S_PAYMSGNO =?,D_PAYENTRUSTDATE =?,S_PAYSNDBNKNO =?,D_VOUCHER =?, "
        + "C_BDGKIND =?,I_OFYEAR =?,D_ACCEPT =?,D_ACCT =?,S_ORIVOUNO =?, "
        + "D_ORIVOUDATE =?,S_FILENAME =?,S_STATE =?,C_TRIMFLAG =?,I_CHGNUM =I_CHGNUM+1, "
        + "S_INPUTERID =?,TS_SYSUPDATE =CURRENT TIMESTAMP,S_IFMATCH =? "
        + "WHERE "
        + "I_VOUSRLNO = ?"
        ;

	/* SQL to update data, support LOB */
    private static final String SQL_UPDATE_LOB =
        "UPDATE TBS_TV_BNKPAY_MAIN SET "
        + "S_BOOKORGCODE =?, S_TRECODE =?, S_PACKNO =?, S_VOUNO =?, S_BIZTYPE =?,  "
        + "F_ZEROSUMAMT =?, F_SMALLSUMAMT =?, S_SNDBNKNO =?, S_PAYEROPNBNKNO =?, S_PAYERACCT =?,  "
        + "S_PAYERNAME =?, S_PAYERADDR =?, S_PAYEEACCT =?, S_PAYEENAME =?, S_PAYEEADDR =?,  "
        + "S_AGENTBNKCODE =?, S_RCVBNKNO =?, S_PAYEEOPNBNKNO =?, S_SRLACCTBRIEFCODE =?, S_ADDWORD =?,  "
        + "S_PAYDICTATENO =?, S_PAYMSGNO =?, D_PAYENTRUSTDATE =?, S_PAYSNDBNKNO =?, D_VOUCHER =?,  "
        + "C_BDGKIND =?, I_OFYEAR =?, D_ACCEPT =?, D_ACCT =?, S_ORIVOUNO =?,  "
        + "D_ORIVOUDATE =?, S_FILENAME =?, S_STATE =?, C_TRIMFLAG =?, I_CHGNUM =I_CHGNUM+1,  "
        + "S_INPUTERID =?, TS_SYSUPDATE =CURRENT TIMESTAMP, S_IFMATCH =? "
        + "WHERE "
        + "I_VOUSRLNO = ?"
        ;	
	
    /* SQL to delete data */
    private static final String SQL_DELETE =
        "DELETE FROM TBS_TV_BNKPAY_MAIN " 
        + " WHERE "
        + "I_VOUSRLNO = ?"
        ;


	/**
	*  批量查询 一次最多查询的参数数量
	*/
	
	public static final int FIND_BATCH_SIZE = 150  / 1;
	



   /**
   * Create a new record in Database.
   */
  public void create(IDto _dto,  Connection conn) throws SQLException
  {
     TbsTvBnkpayMainDto dto = (TbsTvBnkpayMainDto)_dto ;
     String msgValid = dto.checkValid() ;
     if (msgValid != null)
     	throw new SQLException("插入错误，"+msgValid) ;

     PreparedStatement ps = null;
     try
     {
         ps = conn.prepareStatement(SQL_INSERT);
          if (dto.getIvousrlno()==null)
            ps.setNull(1, java.sql.Types.BIGINT);
         else
            ps.setLong(1, dto.getIvousrlno().longValue());
          ps.setString(2, dto.getSbookorgcode());

          ps.setString(3, dto.getStrecode());

          ps.setString(4, dto.getSpackno());

          ps.setString(5, dto.getSvouno());

          ps.setString(6, dto.getSbiztype());

          ps.setBigDecimal(7, dto.getFzerosumamt());

          ps.setBigDecimal(8, dto.getFsmallsumamt());

          ps.setString(9, dto.getSsndbnkno());

          ps.setString(10, dto.getSpayeropnbnkno());

          ps.setString(11, dto.getSpayeracct());

          ps.setString(12, dto.getSpayername());

          ps.setString(13, dto.getSpayeraddr());

          ps.setString(14, dto.getSpayeeacct());

          ps.setString(15, dto.getSpayeename());

          ps.setString(16, dto.getSpayeeaddr());

          ps.setString(17, dto.getSagentbnkcode());

          ps.setString(18, dto.getSrcvbnkno());

          ps.setString(19, dto.getSpayeeopnbnkno());

          ps.setString(20, dto.getSsrlacctbriefcode());

          ps.setString(21, dto.getSaddword());

          ps.setString(22, dto.getSpaydictateno());

          ps.setString(23, dto.getSpaymsgno());

          ps.setDate(24, dto.getDpayentrustdate());

          ps.setString(25, dto.getSpaysndbnkno());

          ps.setDate(26, dto.getDvoucher());

          ps.setString(27, dto.getCbdgkind());

          if (dto.getIofyear()==null)
            ps.setNull(28, java.sql.Types.INTEGER);
         else
            ps.setInt(28, dto.getIofyear().intValue());
          ps.setDate(29, dto.getDaccept());

          ps.setDate(30, dto.getDacct());

          ps.setString(31, dto.getSorivouno());

          ps.setDate(32, dto.getDorivoudate());

          ps.setString(33, dto.getSfilename());

          ps.setString(34, dto.getSstate());

          ps.setString(35, dto.getCtrimflag());

           ps.setString(36, dto.getSinputerid());

           ps.setString(37, dto.getSifmatch());

         ps.executeUpdate();
     }
     finally
     {
         close(ps);
     }
     return  ;
  }

   /**
   * Create a new record in Database. and return result, only used when having generated key
   */
   public IDto createWithResult(IDto _dto,  Connection conn) throws SQLException
   {
       TbsTvBnkpayMainDto dto = (TbsTvBnkpayMainDto)_dto ;
       String msgValid = dto.checkValid() ;
       if (msgValid != null)
           throw new SQLException("插入错误，"+msgValid) ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try
       {
           ps = conn.prepareStatement(SQL_INSERT_WITH_RESULT);
            if (dto.getIvousrlno()==null)
              ps.setNull(1, java.sql.Types.BIGINT);
           else
              ps.setLong(1, dto.getIvousrlno().longValue());
            ps.setString(2, dto.getSbookorgcode());
            ps.setString(3, dto.getStrecode());
            ps.setString(4, dto.getSpackno());
            ps.setString(5, dto.getSvouno());
            ps.setString(6, dto.getSbiztype());
            ps.setBigDecimal(7, dto.getFzerosumamt());
            ps.setBigDecimal(8, dto.getFsmallsumamt());
            ps.setString(9, dto.getSsndbnkno());
            ps.setString(10, dto.getSpayeropnbnkno());
            ps.setString(11, dto.getSpayeracct());
            ps.setString(12, dto.getSpayername());
            ps.setString(13, dto.getSpayeraddr());
            ps.setString(14, dto.getSpayeeacct());
            ps.setString(15, dto.getSpayeename());
            ps.setString(16, dto.getSpayeeaddr());
            ps.setString(17, dto.getSagentbnkcode());
            ps.setString(18, dto.getSrcvbnkno());
            ps.setString(19, dto.getSpayeeopnbnkno());
            ps.setString(20, dto.getSsrlacctbriefcode());
            ps.setString(21, dto.getSaddword());
            ps.setString(22, dto.getSpaydictateno());
            ps.setString(23, dto.getSpaymsgno());
            ps.setDate(24, dto.getDpayentrustdate());
            ps.setString(25, dto.getSpaysndbnkno());
            ps.setDate(26, dto.getDvoucher());
            ps.setString(27, dto.getCbdgkind());
            if (dto.getIofyear()==null)
              ps.setNull(28, java.sql.Types.INTEGER);
           else
              ps.setInt(28, dto.getIofyear().intValue());
            ps.setDate(29, dto.getDaccept());
            ps.setDate(30, dto.getDacct());
            ps.setString(31, dto.getSorivouno());
            ps.setDate(32, dto.getDorivoudate());
            ps.setString(33, dto.getSfilename());
            ps.setString(34, dto.getSstate());
            ps.setString(35, dto.getCtrimflag());
             ps.setString(36, dto.getSinputerid());
             ps.setString(37, dto.getSifmatch());
           rs = ps.executeQuery();
           List results = getResults(rs);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }
        finally
        {
           close(ps);
           close(rs);
        }
   }

 	/**
	* Create batch new record in Database. it only create the first layer dto
	*/
    public void create(IDto[] _dtos, Connection conn)    throws SQLException
    {
        TbsTvBnkpayMainDto dto  ;
     	for (int i= 0; i< _dtos.length ; i++)
 	    {
 		    dto  = (TbsTvBnkpayMainDto)_dtos[i] ; 
    	    String msgValid = dto.checkValid() ;
        	if (msgValid != null)
     	    	throw new SQLException("插入错误，"+msgValid) ;
     	}
 	    PreparedStatement ps = null;
        try
        {
            ps = conn.prepareStatement(SQL_INSERT);
       
     	    for (int i= 0; i< _dtos.length ; i++)
     	    {
 	    	    dto  = (TbsTvBnkpayMainDto)_dtos[i] ; 
  
               if (dto.getIvousrlno()==null)
                  ps.setNull(1, java.sql.Types.BIGINT);
               else
                  ps.setLong(1, dto.getIvousrlno().longValue());
  
               ps.setString(2, dto.getSbookorgcode());
  
               ps.setString(3, dto.getStrecode());
  
               ps.setString(4, dto.getSpackno());
  
               ps.setString(5, dto.getSvouno());
  
               ps.setString(6, dto.getSbiztype());
  
               ps.setBigDecimal(7, dto.getFzerosumamt());
  
               ps.setBigDecimal(8, dto.getFsmallsumamt());
  
               ps.setString(9, dto.getSsndbnkno());
  
               ps.setString(10, dto.getSpayeropnbnkno());
  
               ps.setString(11, dto.getSpayeracct());
  
               ps.setString(12, dto.getSpayername());
  
               ps.setString(13, dto.getSpayeraddr());
  
               ps.setString(14, dto.getSpayeeacct());
  
               ps.setString(15, dto.getSpayeename());
  
               ps.setString(16, dto.getSpayeeaddr());
  
               ps.setString(17, dto.getSagentbnkcode());
  
               ps.setString(18, dto.getSrcvbnkno());
  
               ps.setString(19, dto.getSpayeeopnbnkno());
  
               ps.setString(20, dto.getSsrlacctbriefcode());
  
               ps.setString(21, dto.getSaddword());
  
               ps.setString(22, dto.getSpaydictateno());
  
               ps.setString(23, dto.getSpaymsgno());
  
               ps.setDate(24, dto.getDpayentrustdate());
  
               ps.setString(25, dto.getSpaysndbnkno());
  
               ps.setDate(26, dto.getDvoucher());
  
               ps.setString(27, dto.getCbdgkind());
  
               if (dto.getIofyear()==null)
                  ps.setNull(28, java.sql.Types.INTEGER);
               else
                  ps.setInt(28, dto.getIofyear().intValue());
  
               ps.setDate(29, dto.getDaccept());
  
               ps.setDate(30, dto.getDacct());
  
               ps.setString(31, dto.getSorivouno());
  
               ps.setDate(32, dto.getDorivoudate());
  
               ps.setString(33, dto.getSfilename());
  
               ps.setString(34, dto.getSstate());
  
               ps.setString(35, dto.getCtrimflag());
   
               ps.setString(36, dto.getSinputerid());
   
               ps.setString(37, dto.getSifmatch());
               ps.addBatch(); 
           }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }
        return  ;
   }




     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto find(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TbsTvBnkpayMainPK pk = (TbsTvBnkpayMainPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT);
           if (pk.getIvousrlno()==null)
               ps.setNull(1, java.sql.Types.BIGINT);
           else
               ps.setLong(1, pk.getIvousrlno().longValue());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }

     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto findForUpdate(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TbsTvBnkpayMainPK pk = (TbsTvBnkpayMainPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT_FOR_UPDATE);
           if (pk.getIvousrlno()==null)
               ps.setNull(1, java.sql.Types.BIGINT);
           else
               ps.setLong(1, pk.getIvousrlno().longValue());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }
  
     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto[] find(IPK[] _pks, Connection conn, boolean isLobSupport) throws SQLException
    {
        TbsTvBnkpayMainPK pk ;
        
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TbsTvBnkpayMainPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 
        PreparedStatement ps = null;
        ResultSet rs = null;
        
         
        List results = new ArrayList();  
		for (int iBegin = 0; iBegin < pks.size(); iBegin += FIND_BATCH_SIZE)   //
		{
        
            try {
          		int iFindBatchSize = pks.size() - iBegin ;
	    		if (iFindBatchSize >FIND_BATCH_SIZE )
		    		iFindBatchSize = FIND_BATCH_SIZE ;
		  
        	    StringBuffer sb = new StringBuffer() ;
            	sb.append(SQL_SELECT_BATCH).append(" WHERE ").append(SQL_SELECT_BATCH_WHERE);

	    		for (int i = iBegin+1; i < iBegin + iFindBatchSize; i++)
		    	{
			    	sb.append("OR").append(SQL_SELECT_BATCH_WHERE) ;
    			}
        	
                ps = conn.prepareStatement(sb.toString());

                for (int i = iBegin;i < iBegin + iFindBatchSize; i++)
                {
                    pk  = (TbsTvBnkpayMainPK)(pks.get(i)) ; 
                   if (pk.getIvousrlno()==null)
                      ps.setNull((i-iBegin)*1+1, java.sql.Types.BIGINT);
                   else
                      ps.setLong((i-iBegin)*1+1, pk.getIvousrlno().longValue());

			
	            }
                rs = ps.executeQuery();
                results.addAll(getResults(rs,0,isLobSupport)); //
            }finally {
                close(rs);
                close(ps);
            }
        }
        if (results!=null && results.size() > 0)
        {
            TbsTvBnkpayMainDto[] dtos = new TbsTvBnkpayMainDto[0];
		    dtos = (TbsTvBnkpayMainDto[]) results.toArray(dtos) ;
		    return dtos ;
        }
        else
        {
            return null;
        }
    }

  
  
  



    /**
	    * 得到查询语句
	    */
    public String getSelectSQL() 
    {
	        return SQL_SELECT_BATCH_SCROLLABLE ;
    }
   

	public List getResults(ResultSet rs) throws SQLException
    {
    	return getResults(rs, 0) ;
    }
  

    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize) throws SQLException
    {
		return getResults(rs,maxSize,false);
	}
    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize, boolean isLobSupport) throws SQLException
    {
        List results = new ArrayList();
        String str ;
        Reader reader;
        Clob clob;
        char[] chars ;
        
        while (rs.next())
         {
             TbsTvBnkpayMainDto  dto = new  TbsTvBnkpayMainDto ();
             //I_VOUSRLNO
             str = rs.getString("I_VOUSRLNO");
             if(str!=null){
                dto.setIvousrlno(new Long(str));
             }

             //S_BOOKORGCODE
             str = rs.getString("S_BOOKORGCODE");
             if (str == null)
                dto.setSbookorgcode(null);
             else
                dto.setSbookorgcode(str.trim());

             //S_TRECODE
             str = rs.getString("S_TRECODE");
             if (str == null)
                dto.setStrecode(null);
             else
                dto.setStrecode(str.trim());

             //S_PACKNO
             str = rs.getString("S_PACKNO");
             if (str == null)
                dto.setSpackno(null);
             else
                dto.setSpackno(str.trim());

             //S_VOUNO
             str = rs.getString("S_VOUNO");
             if (str == null)
                dto.setSvouno(null);
             else
                dto.setSvouno(str.trim());

             //S_BIZTYPE
             str = rs.getString("S_BIZTYPE");
             if (str == null)
                dto.setSbiztype(null);
             else
                dto.setSbiztype(str.trim());

             //F_ZEROSUMAMT
           dto.setFzerosumamt(rs.getBigDecimal("F_ZEROSUMAMT"));

             //F_SMALLSUMAMT
           dto.setFsmallsumamt(rs.getBigDecimal("F_SMALLSUMAMT"));

             //S_SNDBNKNO
             str = rs.getString("S_SNDBNKNO");
             if (str == null)
                dto.setSsndbnkno(null);
             else
                dto.setSsndbnkno(str.trim());

             //S_PAYEROPNBNKNO
             str = rs.getString("S_PAYEROPNBNKNO");
             if (str == null)
                dto.setSpayeropnbnkno(null);
             else
                dto.setSpayeropnbnkno(str.trim());

             //S_PAYERACCT
             str = rs.getString("S_PAYERACCT");
             if (str == null)
                dto.setSpayeracct(null);
             else
                dto.setSpayeracct(str.trim());

             //S_PAYERNAME
             str = rs.getString("S_PAYERNAME");
             if (str == null)
                dto.setSpayername(null);
             else
                dto.setSpayername(str.trim());

             //S_PAYERADDR
             str = rs.getString("S_PAYERADDR");
             if (str == null)
                dto.setSpayeraddr(null);
             else
                dto.setSpayeraddr(str.trim());

             //S_PAYEEACCT
             str = rs.getString("S_PAYEEACCT");
             if (str == null)
                dto.setSpayeeacct(null);
             else
                dto.setSpayeeacct(str.trim());

             //S_PAYEENAME
             str = rs.getString("S_PAYEENAME");
             if (str == null)
                dto.setSpayeename(null);
             else
                dto.setSpayeename(str.trim());

             //S_PAYEEADDR
             str = rs.getString("S_PAYEEADDR");
             if (str == null)
                dto.setSpayeeaddr(null);
             else
                dto.setSpayeeaddr(str.trim());

             //S_AGENTBNKCODE
             str = rs.getString("S_AGENTBNKCODE");
             if (str == null)
                dto.setSagentbnkcode(null);
             else
                dto.setSagentbnkcode(str.trim());

             //S_RCVBNKNO
             str = rs.getString("S_RCVBNKNO");
             if (str == null)
                dto.setSrcvbnkno(null);
             else
                dto.setSrcvbnkno(str.trim());

             //S_PAYEEOPNBNKNO
             str = rs.getString("S_PAYEEOPNBNKNO");
             if (str == null)
                dto.setSpayeeopnbnkno(null);
             else
                dto.setSpayeeopnbnkno(str.trim());

             //S_SRLACCTBRIEFCODE
             str = rs.getString("S_SRLACCTBRIEFCODE");
             if (str == null)
                dto.setSsrlacctbriefcode(null);
             else
                dto.setSsrlacctbriefcode(str.trim());

             //S_ADDWORD
             str = rs.getString("S_ADDWORD");
             if (str == null)
                dto.setSaddword(null);
             else
                dto.setSaddword(str.trim());

             //S_PAYDICTATENO
             str = rs.getString("S_PAYDICTATENO");
             if (str == null)
                dto.setSpaydictateno(null);
             else
                dto.setSpaydictateno(str.trim());

             //S_PAYMSGNO
             str = rs.getString("S_PAYMSGNO");
             if (str == null)
                dto.setSpaymsgno(null);
             else
                dto.setSpaymsgno(str.trim());

             //D_PAYENTRUSTDATE
           dto.setDpayentrustdate(rs.getDate("D_PAYENTRUSTDATE"));

             //S_PAYSNDBNKNO
             str = rs.getString("S_PAYSNDBNKNO");
             if (str == null)
                dto.setSpaysndbnkno(null);
             else
                dto.setSpaysndbnkno(str.trim());

             //D_VOUCHER
           dto.setDvoucher(rs.getDate("D_VOUCHER"));

             //C_BDGKIND
             str = rs.getString("C_BDGKIND");
             if (str == null)
                dto.setCbdgkind(null);
             else
                dto.setCbdgkind(str.trim());

             //I_OFYEAR
             str = rs.getString("I_OFYEAR");
             if(str!=null){
                dto.setIofyear(new Integer(str));
             }

             //D_ACCEPT
           dto.setDaccept(rs.getDate("D_ACCEPT"));

             //D_ACCT
           dto.setDacct(rs.getDate("D_ACCT"));

             //S_ORIVOUNO
             str = rs.getString("S_ORIVOUNO");
             if (str == null)
                dto.setSorivouno(null);
             else
                dto.setSorivouno(str.trim());

             //D_ORIVOUDATE
           dto.setDorivoudate(rs.getDate("D_ORIVOUDATE"));

             //S_FILENAME
             str = rs.getString("S_FILENAME");
             if (str == null)
                dto.setSfilename(null);
             else
                dto.setSfilename(str.trim());

             //S_STATE
             str = rs.getString("S_STATE");
             if (str == null)
                dto.setSstate(null);
             else
                dto.setSstate(str.trim());

             //C_TRIMFLAG
             str = rs.getString("C_TRIMFLAG");
             if (str == null)
                dto.setCtrimflag(null);
             else
                dto.setCtrimflag(str.trim());

             //I_CHGNUM
             str = rs.getString("I_CHGNUM");
             if(str!=null){
                dto.setIchgnum(new Integer(str));
             }

             //S_INPUTERID
             str = rs.getString("S_INPUTERID");
             if (str == null)
                dto.setSinputerid(null);
             else
                dto.setSinputerid(str.trim());

             //TS_SYSUPDATE
           dto.setTssysupdate(rs.getTimestamp("TS_SYSUPDATE"));

             //S_IFMATCH
             str = rs.getString("S_IFMATCH");
             if (str == null)
                dto.setSifmatch(null);
             else
                dto.setSifmatch(str.trim());



            results.add(dto);
            
            if(maxSize >0  && results.size() == maxSize)
            {
            	break ;
            }
            
         }
        
		return results ;
    }

  /**
   * populate resultSet as cclass
   */
  protected IDto[] getChildrenResults(ResultSet rs ,Class _dto) throws SQLException{

      throw new SQLException("本dto没有与其它dto相关联，无法应用此方法");
  }


     /**
     * Update a record in Database.
     */

    public void update(IDto _dto, Connection conn, boolean isLobSupport) throws SQLException
    {
        String msgValid = _dto.checkValid() ;
        if (msgValid != null)
            throw new SQLException("修改错误，"+msgValid) ;
        TbsTvBnkpayMainDto dto = (TbsTvBnkpayMainDto)_dto ;
        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            int pos = 1;
            //S_BOOKORGCODE
            ps.setString(pos, dto.getSbookorgcode());
            pos++;

            //S_TRECODE
            ps.setString(pos, dto.getStrecode());
            pos++;

            //S_PACKNO
            ps.setString(pos, dto.getSpackno());
            pos++;

            //S_VOUNO
            ps.setString(pos, dto.getSvouno());
            pos++;

            //S_BIZTYPE
            ps.setString(pos, dto.getSbiztype());
            pos++;

            //F_ZEROSUMAMT
            ps.setBigDecimal(pos, dto.getFzerosumamt());
            pos++;

            //F_SMALLSUMAMT
            ps.setBigDecimal(pos, dto.getFsmallsumamt());
            pos++;

            //S_SNDBNKNO
            ps.setString(pos, dto.getSsndbnkno());
            pos++;

            //S_PAYEROPNBNKNO
            ps.setString(pos, dto.getSpayeropnbnkno());
            pos++;

            //S_PAYERACCT
            ps.setString(pos, dto.getSpayeracct());
            pos++;

            //S_PAYERNAME
            ps.setString(pos, dto.getSpayername());
            pos++;

            //S_PAYERADDR
            ps.setString(pos, dto.getSpayeraddr());
            pos++;

            //S_PAYEEACCT
            ps.setString(pos, dto.getSpayeeacct());
            pos++;

            //S_PAYEENAME
            ps.setString(pos, dto.getSpayeename());
            pos++;

            //S_PAYEEADDR
            ps.setString(pos, dto.getSpayeeaddr());
            pos++;

            //S_AGENTBNKCODE
            ps.setString(pos, dto.getSagentbnkcode());
            pos++;

            //S_RCVBNKNO
            ps.setString(pos, dto.getSrcvbnkno());
            pos++;

            //S_PAYEEOPNBNKNO
            ps.setString(pos, dto.getSpayeeopnbnkno());
            pos++;

            //S_SRLACCTBRIEFCODE
            ps.setString(pos, dto.getSsrlacctbriefcode());
            pos++;

            //S_ADDWORD
            ps.setString(pos, dto.getSaddword());
            pos++;

            //S_PAYDICTATENO
            ps.setString(pos, dto.getSpaydictateno());
            pos++;

            //S_PAYMSGNO
            ps.setString(pos, dto.getSpaymsgno());
            pos++;

            //D_PAYENTRUSTDATE
            ps.setDate(pos, dto.getDpayentrustdate());
            pos++;

            //S_PAYSNDBNKNO
            ps.setString(pos, dto.getSpaysndbnkno());
            pos++;

            //D_VOUCHER
            ps.setDate(pos, dto.getDvoucher());
            pos++;

            //C_BDGKIND
            ps.setString(pos, dto.getCbdgkind());
            pos++;

            //I_OFYEAR
            if (dto.getIofyear()==null)
                ps.setNull(pos, java.sql.Types.INTEGER);
            else
                ps.setInt(pos, dto.getIofyear().intValue());
            pos++;

            //D_ACCEPT
            ps.setDate(pos, dto.getDaccept());
            pos++;

            //D_ACCT
            ps.setDate(pos, dto.getDacct());
            pos++;

            //S_ORIVOUNO
            ps.setString(pos, dto.getSorivouno());
            pos++;

            //D_ORIVOUDATE
            ps.setDate(pos, dto.getDorivoudate());
            pos++;

            //S_FILENAME
            ps.setString(pos, dto.getSfilename());
            pos++;

            //S_STATE
            ps.setString(pos, dto.getSstate());
            pos++;

            //C_TRIMFLAG
            ps.setString(pos, dto.getCtrimflag());
            pos++;

            //I_CHGNUM
            //S_INPUTERID
            ps.setString(pos, dto.getSinputerid());
            pos++;

            //TS_SYSUPDATE
            //S_IFMATCH
            ps.setString(pos, dto.getSifmatch());
            pos++;


           //I_VOUSRLNO
           ps.setLong(pos, dto.getIvousrlno().longValue());
           pos++;
           ps.executeUpdate();
        }
        finally
        {
           close(ps);
        }

        return ;
    }
    
    
    
      /**
     * Update batch record in Database.
     */

    public void update(IDto[] _dtos, Connection conn, boolean isLobSupport) throws SQLException
    {
     
     	 TbsTvBnkpayMainDto dto  ;
         for (int i= 0; i< _dtos.length ; i++)
         {
            dto  = (TbsTvBnkpayMainDto)_dtos[i] ; 
            String msgValid = dto.checkValid() ;
            if (msgValid != null)
                throw new SQLException("修改错误，"+msgValid) ;
         }

        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            
            for (int i= 0; i< _dtos.length ; i++)
            {
                dto  = (TbsTvBnkpayMainDto)_dtos[i] ; 
                int pos = 1;
                //S_BOOKORGCODE
                 ps.setString(pos, dto.getSbookorgcode());
                pos++;

                //S_TRECODE
                 ps.setString(pos, dto.getStrecode());
                pos++;

                //S_PACKNO
                 ps.setString(pos, dto.getSpackno());
                pos++;

                //S_VOUNO
                 ps.setString(pos, dto.getSvouno());
                pos++;

                //S_BIZTYPE
                 ps.setString(pos, dto.getSbiztype());
                pos++;

                //F_ZEROSUMAMT
                 ps.setBigDecimal(pos, dto.getFzerosumamt());
                pos++;

                //F_SMALLSUMAMT
                 ps.setBigDecimal(pos, dto.getFsmallsumamt());
                pos++;

                //S_SNDBNKNO
                 ps.setString(pos, dto.getSsndbnkno());
                pos++;

                //S_PAYEROPNBNKNO
                 ps.setString(pos, dto.getSpayeropnbnkno());
                pos++;

                //S_PAYERACCT
                 ps.setString(pos, dto.getSpayeracct());
                pos++;

                //S_PAYERNAME
                 ps.setString(pos, dto.getSpayername());
                pos++;

                //S_PAYERADDR
                 ps.setString(pos, dto.getSpayeraddr());
                pos++;

                //S_PAYEEACCT
                 ps.setString(pos, dto.getSpayeeacct());
                pos++;

                //S_PAYEENAME
                 ps.setString(pos, dto.getSpayeename());
                pos++;

                //S_PAYEEADDR
                 ps.setString(pos, dto.getSpayeeaddr());
                pos++;

                //S_AGENTBNKCODE
                 ps.setString(pos, dto.getSagentbnkcode());
                pos++;

                //S_RCVBNKNO
                 ps.setString(pos, dto.getSrcvbnkno());
                pos++;

                //S_PAYEEOPNBNKNO
                 ps.setString(pos, dto.getSpayeeopnbnkno());
                pos++;

                //S_SRLACCTBRIEFCODE
                 ps.setString(pos, dto.getSsrlacctbriefcode());
                pos++;

                //S_ADDWORD
                 ps.setString(pos, dto.getSaddword());
                pos++;

                //S_PAYDICTATENO
                 ps.setString(pos, dto.getSpaydictateno());
                pos++;

                //S_PAYMSGNO
                 ps.setString(pos, dto.getSpaymsgno());
                pos++;

                //D_PAYENTRUSTDATE
                 ps.setDate(pos, dto.getDpayentrustdate());
                pos++;

                //S_PAYSNDBNKNO
                 ps.setString(pos, dto.getSpaysndbnkno());
                pos++;

                //D_VOUCHER
                 ps.setDate(pos, dto.getDvoucher());
                pos++;

                //C_BDGKIND
                 ps.setString(pos, dto.getCbdgkind());
                pos++;

                //I_OFYEAR
                 if (dto.getIofyear()==null)
                   ps.setNull(pos, java.sql.Types.INTEGER);
                else
                   ps.setInt(pos, dto.getIofyear().intValue());
                pos++ ;

                //D_ACCEPT
                 ps.setDate(pos, dto.getDaccept());
                pos++;

                //D_ACCT
                 ps.setDate(pos, dto.getDacct());
                pos++;

                //S_ORIVOUNO
                 ps.setString(pos, dto.getSorivouno());
                pos++;

                //D_ORIVOUDATE
                 ps.setDate(pos, dto.getDorivoudate());
                pos++;

                //S_FILENAME
                 ps.setString(pos, dto.getSfilename());
                pos++;

                //S_STATE
                 ps.setString(pos, dto.getSstate());
                pos++;

                //C_TRIMFLAG
                 ps.setString(pos, dto.getCtrimflag());
                pos++;

                //I_CHGNUM
                 //S_INPUTERID
                 ps.setString(pos, dto.getSinputerid());
                pos++;

                //TS_SYSUPDATE
                 //S_IFMATCH
                 ps.setString(pos, dto.getSifmatch());
                pos++;


               //I_VOUSRLNO
               ps.setLong(pos, dto.getIvousrlno().longValue());
               pos++;
		       ps.addBatch(); 
	       }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }

        return ;
    }



    /**
     * delete a record in Database.
     */
    public void delete(IPK _pk, Connection conn) throws SQLException
    {
       
      
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return ;
        //	throw new SQLException("删除错误，"+msgValid) ;
       }
       TbsTvBnkpayMainPK pk = (TbsTvBnkpayMainPK)_pk ;


       PreparedStatement ps = null;
       try {
           ps = conn.prepareStatement(SQL_DELETE);
           ps.setLong(1, pk.getIvousrlno().longValue());
           ps.executeUpdate();
        }
        finally
        {
            close(ps);
        }
    }


     /**
     * delete batch record in Database.
     */
    public void delete(IPK[] _pks, Connection conn) throws SQLException
    {
        TbsTvBnkpayMainPK pk ;
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TbsTvBnkpayMainPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 		
 		
        PreparedStatement ps = null;
        try
         {
            ps = conn.prepareStatement(SQL_DELETE);
             
       		for (int i= 0; i< pks.size() ; i++)
       		{
       			pk  = (TbsTvBnkpayMainPK)(pks.get(i)) ; 
                ps.setLong(1, pk.getIvousrlno().longValue());
		        ps.addBatch() ;
			}
            ps.executeBatch();
        }
        finally
        {
            close(ps);
        }      
    }



    /**
    * Check IChangerNumber or UpdateTimeStamp.
    */
    public void check(IDto _dto, Connection conn) throws SQLException
    {
                                                                                                                     
		TbsTvBnkpayMainDto dtoNew = (TbsTvBnkpayMainDto)_dto ;
		TbsTvBnkpayMainDto dtoOld =(TbsTvBnkpayMainDto) findForUpdate(_dto.getPK(), conn,false) ;
		if (dtoOld != null) {
			if (! dtoOld.getIchgnum().equals(dtoNew.getIchgnum()))
				throw new SQLException("数据库表：TBS_TV_BNKPAY_MAIN记录已经被修改!数据库中记录修改次数为:"+dtoOld.getIchgnum()+"; 当前记录修改次数为:"+dtoNew.getIchgnum()) ;
		}else{
			throw new SQLException("数据库表：TBS_TV_BNKPAY_MAIN记录已经被删除!");
		}
    }


	/**
	* synchronize values between parent and children
	*/
    public void syncToChildren(IDto _dto)     throws SQLException{
       throw new SQLException("本记录没有与其它记录相关联，无法进行主记录与明细记录之间的数据同步。");
  }

	/**
	*get children
	*/
    public IDto[] findChildren (IDto _dto, Connection conn, boolean isLobSupport)     throws SQLException{
         throw new SQLException("本dto没有与其它dto相关联，无法得到子dto");
  }    
    
  /**
   *return the children IDto class
   * @return Class
   */
  public Class getChildrenClass(){
	  return null;
  };    



    /**
     * Close JDBC Statement.
     * @param stmt  Statement to be closed.
     */
    private void close(Statement stmt)
    {
        if (stmt != null)
        {
            try
            {
                stmt.close();
            }
            catch(SQLException    e)
            {}
        }
    }

    /**
     * Close JDBC ResultSet.
     * @param rs  ResultSet to be closed.
     */
    private void close(ResultSet rs)
    {
        if (rs != null)
        {
            try
            {
                rs.close();
            }catch(SQLException  e){}
        }
    }
}
