    



package com.cfcc.itfe.persistence.dao;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.Statement;
import java.sql.SQLException;
import java.sql.Date;
import java.sql.Time;
import java.sql.Timestamp ;
import java.sql.Clob;

import com.cfcc.jaf.persistence.jaform.parent.* ;

import java.math.BigDecimal;

import java.util.List;
import java.util.ArrayList;
import java.util.HashMap;

import java.io.StringReader;
import java.io.Reader;
import java.io.IOException;

import com.cfcc.itfe.persistence.dto.TvFinIncomeonlineDto ;
import com.cfcc.itfe.persistence.pk.TvFinIncomeonlinePK ;


/**
 * <p>Title: DAO of table: TV_FIN_INCOMEONLINE</p>
 * <p>Description: Data Access Object  </p>
 * <p>Copyright: Copyright (c) 2005</p>
 * <p>Company: CFCC </p>
 * <p>Generation Time: 2014-12-25 15:29:02 </p>
 *  Do NOT change this file by hand.
 *  This java source file is generated by JAF_ORM .
 *  dao.vm timestamp: 2007-04-06 08:30:00
 ******************************************************
 * <p> <b>change record</b><br>
 *  make dao methods simply.<br>
 *  especially make update,delete methods simply.<br>
 *  remove "..withCheck" method,withCheck will impl in JAFOrmTemplate.<br>
 *  remove "delete(IDto _dto,..)" method.<br></p>
 * @author win7
 */

public class TvFinIncomeonlineDao  implements IDao
{


    /* SQL to insert data */
    private static final String SQL_INSERT =
        "INSERT INTO TV_FIN_INCOMEONLINE ("
          + "S_ORGCODE,S_APPLYDATE,S_PACKNO,S_TRECODE,S_TRENAME"
          + ",S_TAXORGCODE,S_PAYBNKNO,S_TRANO,S_ORIMSGNO,F_TRAAMT"
          + ",S_PAYEROPNBNKNO,PAYEROPBKNAME,S_HANDORGNAME,S_PAYACCT,S_TAXVOUNO"
          + ",S_BILLDATE,S_TAXPAYCODE,S_TAXPAYNAME,C_BUDGETTYPE,C_TRIMFLAG"
          + ",S_ETPCODE,S_ETPNAME,S_ETPTYPE,S_BDGSBTCODE,S_BDGSBTNAME"
          + ",S_LIMIT,S_TAXTYPECODE,S_TAXKINDNAME,C_BDGLEVEL,C_BDGLEVELNAME"
          + ",S_TAXSTARTDATE,S_TAXENDDATE,S_ASTFLAG,C_TAXTYPE,S_ACCT"
          + ",S_TRASTATE,S_REMARK,S_REMARK1,S_REMARK2,S_INPUTERID"
          + ",TS_SYSUPDATE,S_SEQ"
        + ") VALUES ("
        + "?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,CURRENT TIMESTAMP ,?)";


    private static final String SQL_INSERT_WITH_RESULT = "SELECT * FROM FINAL TABLE( " + SQL_INSERT + " )";
    
    /* SQL to select data */
    private static final String SQL_SELECT =
        "SELECT "
        + "TV_FIN_INCOMEONLINE.S_ORGCODE, TV_FIN_INCOMEONLINE.S_APPLYDATE, TV_FIN_INCOMEONLINE.S_PACKNO, TV_FIN_INCOMEONLINE.S_TRECODE, TV_FIN_INCOMEONLINE.S_TRENAME, "
        + "TV_FIN_INCOMEONLINE.S_TAXORGCODE, TV_FIN_INCOMEONLINE.S_PAYBNKNO, TV_FIN_INCOMEONLINE.S_TRANO, TV_FIN_INCOMEONLINE.S_ORIMSGNO, TV_FIN_INCOMEONLINE.F_TRAAMT, "
        + "TV_FIN_INCOMEONLINE.S_PAYEROPNBNKNO, TV_FIN_INCOMEONLINE.PAYEROPBKNAME, TV_FIN_INCOMEONLINE.S_HANDORGNAME, TV_FIN_INCOMEONLINE.S_PAYACCT, TV_FIN_INCOMEONLINE.S_TAXVOUNO, "
        + "TV_FIN_INCOMEONLINE.S_BILLDATE, TV_FIN_INCOMEONLINE.S_TAXPAYCODE, TV_FIN_INCOMEONLINE.S_TAXPAYNAME, TV_FIN_INCOMEONLINE.C_BUDGETTYPE, TV_FIN_INCOMEONLINE.C_TRIMFLAG, "
        + "TV_FIN_INCOMEONLINE.S_ETPCODE, TV_FIN_INCOMEONLINE.S_ETPNAME, TV_FIN_INCOMEONLINE.S_ETPTYPE, TV_FIN_INCOMEONLINE.S_BDGSBTCODE, TV_FIN_INCOMEONLINE.S_BDGSBTNAME, "
        + "TV_FIN_INCOMEONLINE.S_LIMIT, TV_FIN_INCOMEONLINE.S_TAXTYPECODE, TV_FIN_INCOMEONLINE.S_TAXKINDNAME, TV_FIN_INCOMEONLINE.C_BDGLEVEL, TV_FIN_INCOMEONLINE.C_BDGLEVELNAME, "
        + "TV_FIN_INCOMEONLINE.S_TAXSTARTDATE, TV_FIN_INCOMEONLINE.S_TAXENDDATE, TV_FIN_INCOMEONLINE.S_ASTFLAG, TV_FIN_INCOMEONLINE.C_TAXTYPE, TV_FIN_INCOMEONLINE.S_ACCT, "
        + "TV_FIN_INCOMEONLINE.S_TRASTATE, TV_FIN_INCOMEONLINE.S_REMARK, TV_FIN_INCOMEONLINE.S_REMARK1, TV_FIN_INCOMEONLINE.S_REMARK2, TV_FIN_INCOMEONLINE.S_INPUTERID, "
        + "TV_FIN_INCOMEONLINE.TS_SYSUPDATE, TV_FIN_INCOMEONLINE.S_SEQ "
        + "FROM TV_FIN_INCOMEONLINE "
        +" WHERE " 
        + "S_SEQ = ?"
        ;
    /* SQL to select for update data */
    private static final String SQL_SELECT_FOR_UPDATE =
        "SELECT "
        + "TV_FIN_INCOMEONLINE.S_ORGCODE, TV_FIN_INCOMEONLINE.S_APPLYDATE, TV_FIN_INCOMEONLINE.S_PACKNO, TV_FIN_INCOMEONLINE.S_TRECODE, TV_FIN_INCOMEONLINE.S_TRENAME, "
        + "TV_FIN_INCOMEONLINE.S_TAXORGCODE, TV_FIN_INCOMEONLINE.S_PAYBNKNO, TV_FIN_INCOMEONLINE.S_TRANO, TV_FIN_INCOMEONLINE.S_ORIMSGNO, TV_FIN_INCOMEONLINE.F_TRAAMT, "
        + "TV_FIN_INCOMEONLINE.S_PAYEROPNBNKNO, TV_FIN_INCOMEONLINE.PAYEROPBKNAME, TV_FIN_INCOMEONLINE.S_HANDORGNAME, TV_FIN_INCOMEONLINE.S_PAYACCT, TV_FIN_INCOMEONLINE.S_TAXVOUNO, "
        + "TV_FIN_INCOMEONLINE.S_BILLDATE, TV_FIN_INCOMEONLINE.S_TAXPAYCODE, TV_FIN_INCOMEONLINE.S_TAXPAYNAME, TV_FIN_INCOMEONLINE.C_BUDGETTYPE, TV_FIN_INCOMEONLINE.C_TRIMFLAG, "
        + "TV_FIN_INCOMEONLINE.S_ETPCODE, TV_FIN_INCOMEONLINE.S_ETPNAME, TV_FIN_INCOMEONLINE.S_ETPTYPE, TV_FIN_INCOMEONLINE.S_BDGSBTCODE, TV_FIN_INCOMEONLINE.S_BDGSBTNAME, "
        + "TV_FIN_INCOMEONLINE.S_LIMIT, TV_FIN_INCOMEONLINE.S_TAXTYPECODE, TV_FIN_INCOMEONLINE.S_TAXKINDNAME, TV_FIN_INCOMEONLINE.C_BDGLEVEL, TV_FIN_INCOMEONLINE.C_BDGLEVELNAME, "
        + "TV_FIN_INCOMEONLINE.S_TAXSTARTDATE, TV_FIN_INCOMEONLINE.S_TAXENDDATE, TV_FIN_INCOMEONLINE.S_ASTFLAG, TV_FIN_INCOMEONLINE.C_TAXTYPE, TV_FIN_INCOMEONLINE.S_ACCT, "
        + "TV_FIN_INCOMEONLINE.S_TRASTATE, TV_FIN_INCOMEONLINE.S_REMARK, TV_FIN_INCOMEONLINE.S_REMARK1, TV_FIN_INCOMEONLINE.S_REMARK2, TV_FIN_INCOMEONLINE.S_INPUTERID, "
        + "TV_FIN_INCOMEONLINE.TS_SYSUPDATE, TV_FIN_INCOMEONLINE.S_SEQ "
        + "FROM TV_FIN_INCOMEONLINE "
        +" WHERE " 
        + "S_SEQ = ? FOR UPDATE"
        ;

      /* SQL to select data (SCROLLABLE)*/
     private static final String SQL_SELECT_BATCH_SCROLLABLE =
        "SELECT "
        + "  TV_FIN_INCOMEONLINE.S_ORGCODE  , TV_FIN_INCOMEONLINE.S_APPLYDATE  , TV_FIN_INCOMEONLINE.S_PACKNO  , TV_FIN_INCOMEONLINE.S_TRECODE  , TV_FIN_INCOMEONLINE.S_TRENAME "
        + " , TV_FIN_INCOMEONLINE.S_TAXORGCODE  , TV_FIN_INCOMEONLINE.S_PAYBNKNO  , TV_FIN_INCOMEONLINE.S_TRANO  , TV_FIN_INCOMEONLINE.S_ORIMSGNO  , TV_FIN_INCOMEONLINE.F_TRAAMT "
        + " , TV_FIN_INCOMEONLINE.S_PAYEROPNBNKNO  , TV_FIN_INCOMEONLINE.PAYEROPBKNAME  , TV_FIN_INCOMEONLINE.S_HANDORGNAME  , TV_FIN_INCOMEONLINE.S_PAYACCT  , TV_FIN_INCOMEONLINE.S_TAXVOUNO "
        + " , TV_FIN_INCOMEONLINE.S_BILLDATE  , TV_FIN_INCOMEONLINE.S_TAXPAYCODE  , TV_FIN_INCOMEONLINE.S_TAXPAYNAME  , TV_FIN_INCOMEONLINE.C_BUDGETTYPE  , TV_FIN_INCOMEONLINE.C_TRIMFLAG "
        + " , TV_FIN_INCOMEONLINE.S_ETPCODE  , TV_FIN_INCOMEONLINE.S_ETPNAME  , TV_FIN_INCOMEONLINE.S_ETPTYPE  , TV_FIN_INCOMEONLINE.S_BDGSBTCODE  , TV_FIN_INCOMEONLINE.S_BDGSBTNAME "
        + " , TV_FIN_INCOMEONLINE.S_LIMIT  , TV_FIN_INCOMEONLINE.S_TAXTYPECODE  , TV_FIN_INCOMEONLINE.S_TAXKINDNAME  , TV_FIN_INCOMEONLINE.C_BDGLEVEL  , TV_FIN_INCOMEONLINE.C_BDGLEVELNAME "
        + " , TV_FIN_INCOMEONLINE.S_TAXSTARTDATE  , TV_FIN_INCOMEONLINE.S_TAXENDDATE  , TV_FIN_INCOMEONLINE.S_ASTFLAG  , TV_FIN_INCOMEONLINE.C_TAXTYPE  , TV_FIN_INCOMEONLINE.S_ACCT "
        + " , TV_FIN_INCOMEONLINE.S_TRASTATE  , TV_FIN_INCOMEONLINE.S_REMARK  , TV_FIN_INCOMEONLINE.S_REMARK1  , TV_FIN_INCOMEONLINE.S_REMARK2  , TV_FIN_INCOMEONLINE.S_INPUTERID "
        + " , TV_FIN_INCOMEONLINE.TS_SYSUPDATE  , TV_FIN_INCOMEONLINE.S_SEQ "
        + "FROM TV_FIN_INCOMEONLINE ";



    /* SQL to select batch data */
    private static final String SQL_SELECT_BATCH =
        "SELECT "
        + "TV_FIN_INCOMEONLINE.S_ORGCODE, TV_FIN_INCOMEONLINE.S_APPLYDATE, TV_FIN_INCOMEONLINE.S_PACKNO, TV_FIN_INCOMEONLINE.S_TRECODE, TV_FIN_INCOMEONLINE.S_TRENAME, "
        + "TV_FIN_INCOMEONLINE.S_TAXORGCODE, TV_FIN_INCOMEONLINE.S_PAYBNKNO, TV_FIN_INCOMEONLINE.S_TRANO, TV_FIN_INCOMEONLINE.S_ORIMSGNO, TV_FIN_INCOMEONLINE.F_TRAAMT, "
        + "TV_FIN_INCOMEONLINE.S_PAYEROPNBNKNO, TV_FIN_INCOMEONLINE.PAYEROPBKNAME, TV_FIN_INCOMEONLINE.S_HANDORGNAME, TV_FIN_INCOMEONLINE.S_PAYACCT, TV_FIN_INCOMEONLINE.S_TAXVOUNO, "
        + "TV_FIN_INCOMEONLINE.S_BILLDATE, TV_FIN_INCOMEONLINE.S_TAXPAYCODE, TV_FIN_INCOMEONLINE.S_TAXPAYNAME, TV_FIN_INCOMEONLINE.C_BUDGETTYPE, TV_FIN_INCOMEONLINE.C_TRIMFLAG, "
        + "TV_FIN_INCOMEONLINE.S_ETPCODE, TV_FIN_INCOMEONLINE.S_ETPNAME, TV_FIN_INCOMEONLINE.S_ETPTYPE, TV_FIN_INCOMEONLINE.S_BDGSBTCODE, TV_FIN_INCOMEONLINE.S_BDGSBTNAME, "
        + "TV_FIN_INCOMEONLINE.S_LIMIT, TV_FIN_INCOMEONLINE.S_TAXTYPECODE, TV_FIN_INCOMEONLINE.S_TAXKINDNAME, TV_FIN_INCOMEONLINE.C_BDGLEVEL, TV_FIN_INCOMEONLINE.C_BDGLEVELNAME, "
        + "TV_FIN_INCOMEONLINE.S_TAXSTARTDATE, TV_FIN_INCOMEONLINE.S_TAXENDDATE, TV_FIN_INCOMEONLINE.S_ASTFLAG, TV_FIN_INCOMEONLINE.C_TAXTYPE, TV_FIN_INCOMEONLINE.S_ACCT, "
        + "TV_FIN_INCOMEONLINE.S_TRASTATE, TV_FIN_INCOMEONLINE.S_REMARK, TV_FIN_INCOMEONLINE.S_REMARK1, TV_FIN_INCOMEONLINE.S_REMARK2, TV_FIN_INCOMEONLINE.S_INPUTERID, "
        + "TV_FIN_INCOMEONLINE.TS_SYSUPDATE, TV_FIN_INCOMEONLINE.S_SEQ "
        + "FROM TV_FIN_INCOMEONLINE " ;
        

   /* SQL to select batch data where condition  */
    private static final String SQL_SELECT_BATCH_WHERE =" ( "
        + "S_SEQ = ?)"
        ;


    /* SQL to update data */
    private static final String SQL_UPDATE =
        "UPDATE TV_FIN_INCOMEONLINE SET "
        + "S_ORGCODE =?,S_APPLYDATE =?,S_PACKNO =?,S_TRECODE =?,S_TRENAME =?, "
        + "S_TAXORGCODE =?,S_PAYBNKNO =?,S_TRANO =?,S_ORIMSGNO =?,F_TRAAMT =?, "
        + "S_PAYEROPNBNKNO =?,PAYEROPBKNAME =?,S_HANDORGNAME =?,S_PAYACCT =?,S_TAXVOUNO =?, "
        + "S_BILLDATE =?,S_TAXPAYCODE =?,S_TAXPAYNAME =?,C_BUDGETTYPE =?,C_TRIMFLAG =?, "
        + "S_ETPCODE =?,S_ETPNAME =?,S_ETPTYPE =?,S_BDGSBTCODE =?,S_BDGSBTNAME =?, "
        + "S_LIMIT =?,S_TAXTYPECODE =?,S_TAXKINDNAME =?,C_BDGLEVEL =?,C_BDGLEVELNAME =?, "
        + "S_TAXSTARTDATE =?,S_TAXENDDATE =?,S_ASTFLAG =?,C_TAXTYPE =?,S_ACCT =?, "
        + "S_TRASTATE =?,S_REMARK =?,S_REMARK1 =?,S_REMARK2 =?,S_INPUTERID =?, "
        + "TS_SYSUPDATE =CURRENT TIMESTAMP "
        + "WHERE "
        + "S_SEQ = ?"
        ;

	/* SQL to update data, support LOB */
    private static final String SQL_UPDATE_LOB =
        "UPDATE TV_FIN_INCOMEONLINE SET "
        + "S_ORGCODE =?, S_APPLYDATE =?, S_PACKNO =?, S_TRECODE =?, S_TRENAME =?,  "
        + "S_TAXORGCODE =?, S_PAYBNKNO =?, S_TRANO =?, S_ORIMSGNO =?, F_TRAAMT =?,  "
        + "S_PAYEROPNBNKNO =?, PAYEROPBKNAME =?, S_HANDORGNAME =?, S_PAYACCT =?, S_TAXVOUNO =?,  "
        + "S_BILLDATE =?, S_TAXPAYCODE =?, S_TAXPAYNAME =?, C_BUDGETTYPE =?, C_TRIMFLAG =?,  "
        + "S_ETPCODE =?, S_ETPNAME =?, S_ETPTYPE =?, S_BDGSBTCODE =?, S_BDGSBTNAME =?,  "
        + "S_LIMIT =?, S_TAXTYPECODE =?, S_TAXKINDNAME =?, C_BDGLEVEL =?, C_BDGLEVELNAME =?,  "
        + "S_TAXSTARTDATE =?, S_TAXENDDATE =?, S_ASTFLAG =?, C_TAXTYPE =?, S_ACCT =?,  "
        + "S_TRASTATE =?, S_REMARK =?, S_REMARK1 =?, S_REMARK2 =?, S_INPUTERID =?,  "
        + "TS_SYSUPDATE =CURRENT TIMESTAMP "
        + "WHERE "
        + "S_SEQ = ?"
        ;	
	
    /* SQL to delete data */
    private static final String SQL_DELETE =
        "DELETE FROM TV_FIN_INCOMEONLINE " 
        + " WHERE "
        + "S_SEQ = ?"
        ;


	/**
	*  批量查询 一次最多查询的参数数量
	*/
	
	public static final int FIND_BATCH_SIZE = 150  / 1;
	



   /**
   * Create a new record in Database.
   */
  public void create(IDto _dto,  Connection conn) throws SQLException
  {
     TvFinIncomeonlineDto dto = (TvFinIncomeonlineDto)_dto ;
     String msgValid = dto.checkValid() ;
     if (msgValid != null)
     	throw new SQLException("插入错误，"+msgValid) ;

     PreparedStatement ps = null;
     try
     {
         ps = conn.prepareStatement(SQL_INSERT);
          ps.setString(1, dto.getSorgcode());

          ps.setString(2, dto.getSapplydate());

          ps.setString(3, dto.getSpackno());

          ps.setString(4, dto.getStrecode());

          ps.setString(5, dto.getStrename());

          ps.setString(6, dto.getStaxorgcode());

          ps.setString(7, dto.getSpaybnkno());

          ps.setString(8, dto.getStrano());

          ps.setString(9, dto.getSorimsgno());

          ps.setBigDecimal(10, dto.getFtraamt());

          ps.setString(11, dto.getSpayeropnbnkno());

          ps.setString(12, dto.getPayeropbkname());

          ps.setString(13, dto.getShandorgname());

          ps.setString(14, dto.getSpayacct());

          ps.setString(15, dto.getStaxvouno());

          ps.setString(16, dto.getSbilldate());

          ps.setString(17, dto.getStaxpaycode());

          ps.setString(18, dto.getStaxpayname());

          ps.setString(19, dto.getCbudgettype());

          ps.setString(20, dto.getCtrimflag());

          ps.setString(21, dto.getSetpcode());

          ps.setString(22, dto.getSetpname());

          ps.setString(23, dto.getSetptype());

          ps.setString(24, dto.getSbdgsbtcode());

          ps.setString(25, dto.getSbdgsbtname());

          ps.setString(26, dto.getSlimit());

          ps.setString(27, dto.getStaxtypecode());

          ps.setString(28, dto.getStaxkindname());

          ps.setString(29, dto.getCbdglevel());

          ps.setString(30, dto.getCbdglevelname());

          ps.setString(31, dto.getStaxstartdate());

          ps.setString(32, dto.getStaxenddate());

          ps.setString(33, dto.getSastflag());

          ps.setString(34, dto.getCtaxtype());

          ps.setString(35, dto.getSacct());

          ps.setString(36, dto.getStrastate());

          ps.setString(37, dto.getSremark());

          ps.setString(38, dto.getSremark1());

          ps.setString(39, dto.getSremark2());

          ps.setString(40, dto.getSinputerid());

           ps.setString(41, dto.getSseq());

         ps.executeUpdate();
     }
     finally
     {
         close(ps);
     }
     return  ;
  }

   /**
   * Create a new record in Database. and return result, only used when having generated key
   */
   public IDto createWithResult(IDto _dto,  Connection conn) throws SQLException
   {
       TvFinIncomeonlineDto dto = (TvFinIncomeonlineDto)_dto ;
       String msgValid = dto.checkValid() ;
       if (msgValid != null)
           throw new SQLException("插入错误，"+msgValid) ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try
       {
           ps = conn.prepareStatement(SQL_INSERT_WITH_RESULT);
            ps.setString(1, dto.getSorgcode());
            ps.setString(2, dto.getSapplydate());
            ps.setString(3, dto.getSpackno());
            ps.setString(4, dto.getStrecode());
            ps.setString(5, dto.getStrename());
            ps.setString(6, dto.getStaxorgcode());
            ps.setString(7, dto.getSpaybnkno());
            ps.setString(8, dto.getStrano());
            ps.setString(9, dto.getSorimsgno());
            ps.setBigDecimal(10, dto.getFtraamt());
            ps.setString(11, dto.getSpayeropnbnkno());
            ps.setString(12, dto.getPayeropbkname());
            ps.setString(13, dto.getShandorgname());
            ps.setString(14, dto.getSpayacct());
            ps.setString(15, dto.getStaxvouno());
            ps.setString(16, dto.getSbilldate());
            ps.setString(17, dto.getStaxpaycode());
            ps.setString(18, dto.getStaxpayname());
            ps.setString(19, dto.getCbudgettype());
            ps.setString(20, dto.getCtrimflag());
            ps.setString(21, dto.getSetpcode());
            ps.setString(22, dto.getSetpname());
            ps.setString(23, dto.getSetptype());
            ps.setString(24, dto.getSbdgsbtcode());
            ps.setString(25, dto.getSbdgsbtname());
            ps.setString(26, dto.getSlimit());
            ps.setString(27, dto.getStaxtypecode());
            ps.setString(28, dto.getStaxkindname());
            ps.setString(29, dto.getCbdglevel());
            ps.setString(30, dto.getCbdglevelname());
            ps.setString(31, dto.getStaxstartdate());
            ps.setString(32, dto.getStaxenddate());
            ps.setString(33, dto.getSastflag());
            ps.setString(34, dto.getCtaxtype());
            ps.setString(35, dto.getSacct());
            ps.setString(36, dto.getStrastate());
            ps.setString(37, dto.getSremark());
            ps.setString(38, dto.getSremark1());
            ps.setString(39, dto.getSremark2());
            ps.setString(40, dto.getSinputerid());
             ps.setString(41, dto.getSseq());
           rs = ps.executeQuery();
           List results = getResults(rs);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }
        finally
        {
           close(ps);
           close(rs);
        }
   }

 	/**
	* Create batch new record in Database. it only create the first layer dto
	*/
    public void create(IDto[] _dtos, Connection conn)    throws SQLException
    {
        TvFinIncomeonlineDto dto  ;
     	for (int i= 0; i< _dtos.length ; i++)
 	    {
 		    dto  = (TvFinIncomeonlineDto)_dtos[i] ; 
    	    String msgValid = dto.checkValid() ;
        	if (msgValid != null)
     	    	throw new SQLException("插入错误，"+msgValid) ;
     	}
 	    PreparedStatement ps = null;
        try
        {
            ps = conn.prepareStatement(SQL_INSERT);
       
     	    for (int i= 0; i< _dtos.length ; i++)
     	    {
 	    	    dto  = (TvFinIncomeonlineDto)_dtos[i] ; 
  
               ps.setString(1, dto.getSorgcode());
  
               ps.setString(2, dto.getSapplydate());
  
               ps.setString(3, dto.getSpackno());
  
               ps.setString(4, dto.getStrecode());
  
               ps.setString(5, dto.getStrename());
  
               ps.setString(6, dto.getStaxorgcode());
  
               ps.setString(7, dto.getSpaybnkno());
  
               ps.setString(8, dto.getStrano());
  
               ps.setString(9, dto.getSorimsgno());
  
               ps.setBigDecimal(10, dto.getFtraamt());
  
               ps.setString(11, dto.getSpayeropnbnkno());
  
               ps.setString(12, dto.getPayeropbkname());
  
               ps.setString(13, dto.getShandorgname());
  
               ps.setString(14, dto.getSpayacct());
  
               ps.setString(15, dto.getStaxvouno());
  
               ps.setString(16, dto.getSbilldate());
  
               ps.setString(17, dto.getStaxpaycode());
  
               ps.setString(18, dto.getStaxpayname());
  
               ps.setString(19, dto.getCbudgettype());
  
               ps.setString(20, dto.getCtrimflag());
  
               ps.setString(21, dto.getSetpcode());
  
               ps.setString(22, dto.getSetpname());
  
               ps.setString(23, dto.getSetptype());
  
               ps.setString(24, dto.getSbdgsbtcode());
  
               ps.setString(25, dto.getSbdgsbtname());
  
               ps.setString(26, dto.getSlimit());
  
               ps.setString(27, dto.getStaxtypecode());
  
               ps.setString(28, dto.getStaxkindname());
  
               ps.setString(29, dto.getCbdglevel());
  
               ps.setString(30, dto.getCbdglevelname());
  
               ps.setString(31, dto.getStaxstartdate());
  
               ps.setString(32, dto.getStaxenddate());
  
               ps.setString(33, dto.getSastflag());
  
               ps.setString(34, dto.getCtaxtype());
  
               ps.setString(35, dto.getSacct());
  
               ps.setString(36, dto.getStrastate());
  
               ps.setString(37, dto.getSremark());
  
               ps.setString(38, dto.getSremark1());
  
               ps.setString(39, dto.getSremark2());
  
               ps.setString(40, dto.getSinputerid());
   
               ps.setString(41, dto.getSseq());
               ps.addBatch(); 
           }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }
        return  ;
   }




     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto find(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TvFinIncomeonlinePK pk = (TvFinIncomeonlinePK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT);
           ps.setString(1, pk.getSseq());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }

     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto findForUpdate(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TvFinIncomeonlinePK pk = (TvFinIncomeonlinePK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT_FOR_UPDATE);
           ps.setString(1, pk.getSseq());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }
  
     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto[] find(IPK[] _pks, Connection conn, boolean isLobSupport) throws SQLException
    {
        TvFinIncomeonlinePK pk ;
        
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TvFinIncomeonlinePK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 
        PreparedStatement ps = null;
        ResultSet rs = null;
        
         
        List results = new ArrayList();  
		for (int iBegin = 0; iBegin < pks.size(); iBegin += FIND_BATCH_SIZE)   //
		{
        
            try {
          		int iFindBatchSize = pks.size() - iBegin ;
	    		if (iFindBatchSize >FIND_BATCH_SIZE )
		    		iFindBatchSize = FIND_BATCH_SIZE ;
		  
        	    StringBuffer sb = new StringBuffer() ;
            	sb.append(SQL_SELECT_BATCH).append(" WHERE ").append(SQL_SELECT_BATCH_WHERE);

	    		for (int i = iBegin+1; i < iBegin + iFindBatchSize; i++)
		    	{
			    	sb.append("OR").append(SQL_SELECT_BATCH_WHERE) ;
    			}
        	
                ps = conn.prepareStatement(sb.toString());

                for (int i = iBegin;i < iBegin + iFindBatchSize; i++)
                {
                    pk  = (TvFinIncomeonlinePK)(pks.get(i)) ; 
                   ps.setString((i-iBegin)*1+1, pk.getSseq());

			
	            }
                rs = ps.executeQuery();
                results.addAll(getResults(rs,0,isLobSupport)); //
            }finally {
                close(rs);
                close(ps);
            }
        }
        if (results!=null && results.size() > 0)
        {
            TvFinIncomeonlineDto[] dtos = new TvFinIncomeonlineDto[0];
		    dtos = (TvFinIncomeonlineDto[]) results.toArray(dtos) ;
		    return dtos ;
        }
        else
        {
            return null;
        }
    }

  
  
  



    /**
	    * 得到查询语句
	    */
    public String getSelectSQL() 
    {
	        return SQL_SELECT_BATCH_SCROLLABLE ;
    }
   

	public List getResults(ResultSet rs) throws SQLException
    {
    	return getResults(rs, 0) ;
    }
  

    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize) throws SQLException
    {
		return getResults(rs,maxSize,false);
	}
    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize, boolean isLobSupport) throws SQLException
    {
        List results = new ArrayList();
        String str ;
        Reader reader;
        Clob clob;
        char[] chars ;
        
        while (rs.next())
         {
             TvFinIncomeonlineDto  dto = new  TvFinIncomeonlineDto ();
             //S_ORGCODE
             str = rs.getString("S_ORGCODE");
             if (str == null)
                dto.setSorgcode(null);
             else
                dto.setSorgcode(str.trim());

             //S_APPLYDATE
             str = rs.getString("S_APPLYDATE");
             if (str == null)
                dto.setSapplydate(null);
             else
                dto.setSapplydate(str.trim());

             //S_PACKNO
             str = rs.getString("S_PACKNO");
             if (str == null)
                dto.setSpackno(null);
             else
                dto.setSpackno(str.trim());

             //S_TRECODE
             str = rs.getString("S_TRECODE");
             if (str == null)
                dto.setStrecode(null);
             else
                dto.setStrecode(str.trim());

             //S_TRENAME
             str = rs.getString("S_TRENAME");
             if (str == null)
                dto.setStrename(null);
             else
                dto.setStrename(str.trim());

             //S_TAXORGCODE
             str = rs.getString("S_TAXORGCODE");
             if (str == null)
                dto.setStaxorgcode(null);
             else
                dto.setStaxorgcode(str.trim());

             //S_PAYBNKNO
             str = rs.getString("S_PAYBNKNO");
             if (str == null)
                dto.setSpaybnkno(null);
             else
                dto.setSpaybnkno(str.trim());

             //S_TRANO
             str = rs.getString("S_TRANO");
             if (str == null)
                dto.setStrano(null);
             else
                dto.setStrano(str.trim());

             //S_ORIMSGNO
             str = rs.getString("S_ORIMSGNO");
             if (str == null)
                dto.setSorimsgno(null);
             else
                dto.setSorimsgno(str.trim());

             //F_TRAAMT
           dto.setFtraamt(rs.getBigDecimal("F_TRAAMT"));

             //S_PAYEROPNBNKNO
             str = rs.getString("S_PAYEROPNBNKNO");
             if (str == null)
                dto.setSpayeropnbnkno(null);
             else
                dto.setSpayeropnbnkno(str.trim());

             //PAYEROPBKNAME
             str = rs.getString("PAYEROPBKNAME");
             if (str == null)
                dto.setPayeropbkname(null);
             else
                dto.setPayeropbkname(str.trim());

             //S_HANDORGNAME
             str = rs.getString("S_HANDORGNAME");
             if (str == null)
                dto.setShandorgname(null);
             else
                dto.setShandorgname(str.trim());

             //S_PAYACCT
             str = rs.getString("S_PAYACCT");
             if (str == null)
                dto.setSpayacct(null);
             else
                dto.setSpayacct(str.trim());

             //S_TAXVOUNO
             str = rs.getString("S_TAXVOUNO");
             if (str == null)
                dto.setStaxvouno(null);
             else
                dto.setStaxvouno(str.trim());

             //S_BILLDATE
             str = rs.getString("S_BILLDATE");
             if (str == null)
                dto.setSbilldate(null);
             else
                dto.setSbilldate(str.trim());

             //S_TAXPAYCODE
             str = rs.getString("S_TAXPAYCODE");
             if (str == null)
                dto.setStaxpaycode(null);
             else
                dto.setStaxpaycode(str.trim());

             //S_TAXPAYNAME
             str = rs.getString("S_TAXPAYNAME");
             if (str == null)
                dto.setStaxpayname(null);
             else
                dto.setStaxpayname(str.trim());

             //C_BUDGETTYPE
             str = rs.getString("C_BUDGETTYPE");
             if (str == null)
                dto.setCbudgettype(null);
             else
                dto.setCbudgettype(str.trim());

             //C_TRIMFLAG
             str = rs.getString("C_TRIMFLAG");
             if (str == null)
                dto.setCtrimflag(null);
             else
                dto.setCtrimflag(str.trim());

             //S_ETPCODE
             str = rs.getString("S_ETPCODE");
             if (str == null)
                dto.setSetpcode(null);
             else
                dto.setSetpcode(str.trim());

             //S_ETPNAME
             str = rs.getString("S_ETPNAME");
             if (str == null)
                dto.setSetpname(null);
             else
                dto.setSetpname(str.trim());

             //S_ETPTYPE
             str = rs.getString("S_ETPTYPE");
             if (str == null)
                dto.setSetptype(null);
             else
                dto.setSetptype(str.trim());

             //S_BDGSBTCODE
             str = rs.getString("S_BDGSBTCODE");
             if (str == null)
                dto.setSbdgsbtcode(null);
             else
                dto.setSbdgsbtcode(str.trim());

             //S_BDGSBTNAME
             str = rs.getString("S_BDGSBTNAME");
             if (str == null)
                dto.setSbdgsbtname(null);
             else
                dto.setSbdgsbtname(str.trim());

             //S_LIMIT
             str = rs.getString("S_LIMIT");
             if (str == null)
                dto.setSlimit(null);
             else
                dto.setSlimit(str.trim());

             //S_TAXTYPECODE
             str = rs.getString("S_TAXTYPECODE");
             if (str == null)
                dto.setStaxtypecode(null);
             else
                dto.setStaxtypecode(str.trim());

             //S_TAXKINDNAME
             str = rs.getString("S_TAXKINDNAME");
             if (str == null)
                dto.setStaxkindname(null);
             else
                dto.setStaxkindname(str.trim());

             //C_BDGLEVEL
             str = rs.getString("C_BDGLEVEL");
             if (str == null)
                dto.setCbdglevel(null);
             else
                dto.setCbdglevel(str.trim());

             //C_BDGLEVELNAME
             str = rs.getString("C_BDGLEVELNAME");
             if (str == null)
                dto.setCbdglevelname(null);
             else
                dto.setCbdglevelname(str.trim());

             //S_TAXSTARTDATE
             str = rs.getString("S_TAXSTARTDATE");
             if (str == null)
                dto.setStaxstartdate(null);
             else
                dto.setStaxstartdate(str.trim());

             //S_TAXENDDATE
             str = rs.getString("S_TAXENDDATE");
             if (str == null)
                dto.setStaxenddate(null);
             else
                dto.setStaxenddate(str.trim());

             //S_ASTFLAG
             str = rs.getString("S_ASTFLAG");
             if (str == null)
                dto.setSastflag(null);
             else
                dto.setSastflag(str.trim());

             //C_TAXTYPE
             str = rs.getString("C_TAXTYPE");
             if (str == null)
                dto.setCtaxtype(null);
             else
                dto.setCtaxtype(str.trim());

             //S_ACCT
             str = rs.getString("S_ACCT");
             if (str == null)
                dto.setSacct(null);
             else
                dto.setSacct(str.trim());

             //S_TRASTATE
             str = rs.getString("S_TRASTATE");
             if (str == null)
                dto.setStrastate(null);
             else
                dto.setStrastate(str.trim());

             //S_REMARK
             str = rs.getString("S_REMARK");
             if (str == null)
                dto.setSremark(null);
             else
                dto.setSremark(str.trim());

             //S_REMARK1
             str = rs.getString("S_REMARK1");
             if (str == null)
                dto.setSremark1(null);
             else
                dto.setSremark1(str.trim());

             //S_REMARK2
             str = rs.getString("S_REMARK2");
             if (str == null)
                dto.setSremark2(null);
             else
                dto.setSremark2(str.trim());

             //S_INPUTERID
             str = rs.getString("S_INPUTERID");
             if (str == null)
                dto.setSinputerid(null);
             else
                dto.setSinputerid(str.trim());

             //TS_SYSUPDATE
           dto.setTssysupdate(rs.getTimestamp("TS_SYSUPDATE"));

             //S_SEQ
             str = rs.getString("S_SEQ");
             if (str == null)
                dto.setSseq(null);
             else
                dto.setSseq(str.trim());



            results.add(dto);
            
            if(maxSize >0  && results.size() == maxSize)
            {
            	break ;
            }
            
         }
        
		return results ;
    }

  /**
   * populate resultSet as cclass
   */
  protected IDto[] getChildrenResults(ResultSet rs ,Class _dto) throws SQLException{

      throw new SQLException("本dto没有与其它dto相关联，无法应用此方法");
  }


     /**
     * Update a record in Database.
     */

    public void update(IDto _dto, Connection conn, boolean isLobSupport) throws SQLException
    {
        String msgValid = _dto.checkValid() ;
        if (msgValid != null)
            throw new SQLException("修改错误，"+msgValid) ;
        TvFinIncomeonlineDto dto = (TvFinIncomeonlineDto)_dto ;
        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            int pos = 1;
            //S_ORGCODE
            ps.setString(pos, dto.getSorgcode());
            pos++;

            //S_APPLYDATE
            ps.setString(pos, dto.getSapplydate());
            pos++;

            //S_PACKNO
            ps.setString(pos, dto.getSpackno());
            pos++;

            //S_TRECODE
            ps.setString(pos, dto.getStrecode());
            pos++;

            //S_TRENAME
            ps.setString(pos, dto.getStrename());
            pos++;

            //S_TAXORGCODE
            ps.setString(pos, dto.getStaxorgcode());
            pos++;

            //S_PAYBNKNO
            ps.setString(pos, dto.getSpaybnkno());
            pos++;

            //S_TRANO
            ps.setString(pos, dto.getStrano());
            pos++;

            //S_ORIMSGNO
            ps.setString(pos, dto.getSorimsgno());
            pos++;

            //F_TRAAMT
            ps.setBigDecimal(pos, dto.getFtraamt());
            pos++;

            //S_PAYEROPNBNKNO
            ps.setString(pos, dto.getSpayeropnbnkno());
            pos++;

            //PAYEROPBKNAME
            ps.setString(pos, dto.getPayeropbkname());
            pos++;

            //S_HANDORGNAME
            ps.setString(pos, dto.getShandorgname());
            pos++;

            //S_PAYACCT
            ps.setString(pos, dto.getSpayacct());
            pos++;

            //S_TAXVOUNO
            ps.setString(pos, dto.getStaxvouno());
            pos++;

            //S_BILLDATE
            ps.setString(pos, dto.getSbilldate());
            pos++;

            //S_TAXPAYCODE
            ps.setString(pos, dto.getStaxpaycode());
            pos++;

            //S_TAXPAYNAME
            ps.setString(pos, dto.getStaxpayname());
            pos++;

            //C_BUDGETTYPE
            ps.setString(pos, dto.getCbudgettype());
            pos++;

            //C_TRIMFLAG
            ps.setString(pos, dto.getCtrimflag());
            pos++;

            //S_ETPCODE
            ps.setString(pos, dto.getSetpcode());
            pos++;

            //S_ETPNAME
            ps.setString(pos, dto.getSetpname());
            pos++;

            //S_ETPTYPE
            ps.setString(pos, dto.getSetptype());
            pos++;

            //S_BDGSBTCODE
            ps.setString(pos, dto.getSbdgsbtcode());
            pos++;

            //S_BDGSBTNAME
            ps.setString(pos, dto.getSbdgsbtname());
            pos++;

            //S_LIMIT
            ps.setString(pos, dto.getSlimit());
            pos++;

            //S_TAXTYPECODE
            ps.setString(pos, dto.getStaxtypecode());
            pos++;

            //S_TAXKINDNAME
            ps.setString(pos, dto.getStaxkindname());
            pos++;

            //C_BDGLEVEL
            ps.setString(pos, dto.getCbdglevel());
            pos++;

            //C_BDGLEVELNAME
            ps.setString(pos, dto.getCbdglevelname());
            pos++;

            //S_TAXSTARTDATE
            ps.setString(pos, dto.getStaxstartdate());
            pos++;

            //S_TAXENDDATE
            ps.setString(pos, dto.getStaxenddate());
            pos++;

            //S_ASTFLAG
            ps.setString(pos, dto.getSastflag());
            pos++;

            //C_TAXTYPE
            ps.setString(pos, dto.getCtaxtype());
            pos++;

            //S_ACCT
            ps.setString(pos, dto.getSacct());
            pos++;

            //S_TRASTATE
            ps.setString(pos, dto.getStrastate());
            pos++;

            //S_REMARK
            ps.setString(pos, dto.getSremark());
            pos++;

            //S_REMARK1
            ps.setString(pos, dto.getSremark1());
            pos++;

            //S_REMARK2
            ps.setString(pos, dto.getSremark2());
            pos++;

            //S_INPUTERID
            ps.setString(pos, dto.getSinputerid());
            pos++;

            //TS_SYSUPDATE

           //S_SEQ
           ps.setString(pos, dto.getSseq());
           pos++;
           ps.executeUpdate();
        }
        finally
        {
           close(ps);
        }

        return ;
    }
    
    
    
      /**
     * Update batch record in Database.
     */

    public void update(IDto[] _dtos, Connection conn, boolean isLobSupport) throws SQLException
    {
     
     	 TvFinIncomeonlineDto dto  ;
         for (int i= 0; i< _dtos.length ; i++)
         {
            dto  = (TvFinIncomeonlineDto)_dtos[i] ; 
            String msgValid = dto.checkValid() ;
            if (msgValid != null)
                throw new SQLException("修改错误，"+msgValid) ;
         }

        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            
            for (int i= 0; i< _dtos.length ; i++)
            {
                dto  = (TvFinIncomeonlineDto)_dtos[i] ; 
                int pos = 1;
                //S_ORGCODE
                 ps.setString(pos, dto.getSorgcode());
                pos++;

                //S_APPLYDATE
                 ps.setString(pos, dto.getSapplydate());
                pos++;

                //S_PACKNO
                 ps.setString(pos, dto.getSpackno());
                pos++;

                //S_TRECODE
                 ps.setString(pos, dto.getStrecode());
                pos++;

                //S_TRENAME
                 ps.setString(pos, dto.getStrename());
                pos++;

                //S_TAXORGCODE
                 ps.setString(pos, dto.getStaxorgcode());
                pos++;

                //S_PAYBNKNO
                 ps.setString(pos, dto.getSpaybnkno());
                pos++;

                //S_TRANO
                 ps.setString(pos, dto.getStrano());
                pos++;

                //S_ORIMSGNO
                 ps.setString(pos, dto.getSorimsgno());
                pos++;

                //F_TRAAMT
                 ps.setBigDecimal(pos, dto.getFtraamt());
                pos++;

                //S_PAYEROPNBNKNO
                 ps.setString(pos, dto.getSpayeropnbnkno());
                pos++;

                //PAYEROPBKNAME
                 ps.setString(pos, dto.getPayeropbkname());
                pos++;

                //S_HANDORGNAME
                 ps.setString(pos, dto.getShandorgname());
                pos++;

                //S_PAYACCT
                 ps.setString(pos, dto.getSpayacct());
                pos++;

                //S_TAXVOUNO
                 ps.setString(pos, dto.getStaxvouno());
                pos++;

                //S_BILLDATE
                 ps.setString(pos, dto.getSbilldate());
                pos++;

                //S_TAXPAYCODE
                 ps.setString(pos, dto.getStaxpaycode());
                pos++;

                //S_TAXPAYNAME
                 ps.setString(pos, dto.getStaxpayname());
                pos++;

                //C_BUDGETTYPE
                 ps.setString(pos, dto.getCbudgettype());
                pos++;

                //C_TRIMFLAG
                 ps.setString(pos, dto.getCtrimflag());
                pos++;

                //S_ETPCODE
                 ps.setString(pos, dto.getSetpcode());
                pos++;

                //S_ETPNAME
                 ps.setString(pos, dto.getSetpname());
                pos++;

                //S_ETPTYPE
                 ps.setString(pos, dto.getSetptype());
                pos++;

                //S_BDGSBTCODE
                 ps.setString(pos, dto.getSbdgsbtcode());
                pos++;

                //S_BDGSBTNAME
                 ps.setString(pos, dto.getSbdgsbtname());
                pos++;

                //S_LIMIT
                 ps.setString(pos, dto.getSlimit());
                pos++;

                //S_TAXTYPECODE
                 ps.setString(pos, dto.getStaxtypecode());
                pos++;

                //S_TAXKINDNAME
                 ps.setString(pos, dto.getStaxkindname());
                pos++;

                //C_BDGLEVEL
                 ps.setString(pos, dto.getCbdglevel());
                pos++;

                //C_BDGLEVELNAME
                 ps.setString(pos, dto.getCbdglevelname());
                pos++;

                //S_TAXSTARTDATE
                 ps.setString(pos, dto.getStaxstartdate());
                pos++;

                //S_TAXENDDATE
                 ps.setString(pos, dto.getStaxenddate());
                pos++;

                //S_ASTFLAG
                 ps.setString(pos, dto.getSastflag());
                pos++;

                //C_TAXTYPE
                 ps.setString(pos, dto.getCtaxtype());
                pos++;

                //S_ACCT
                 ps.setString(pos, dto.getSacct());
                pos++;

                //S_TRASTATE
                 ps.setString(pos, dto.getStrastate());
                pos++;

                //S_REMARK
                 ps.setString(pos, dto.getSremark());
                pos++;

                //S_REMARK1
                 ps.setString(pos, dto.getSremark1());
                pos++;

                //S_REMARK2
                 ps.setString(pos, dto.getSremark2());
                pos++;

                //S_INPUTERID
                 ps.setString(pos, dto.getSinputerid());
                pos++;

                //TS_SYSUPDATE
 
               //S_SEQ
               ps.setString(pos, dto.getSseq());
               pos++;
		       ps.addBatch(); 
	       }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }

        return ;
    }



    /**
     * delete a record in Database.
     */
    public void delete(IPK _pk, Connection conn) throws SQLException
    {
       
      
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return ;
        //	throw new SQLException("删除错误，"+msgValid) ;
       }
       TvFinIncomeonlinePK pk = (TvFinIncomeonlinePK)_pk ;


       PreparedStatement ps = null;
       try {
           ps = conn.prepareStatement(SQL_DELETE);
           ps.setString(1, pk.getSseq());
           ps.executeUpdate();
        }
        finally
        {
            close(ps);
        }
    }


     /**
     * delete batch record in Database.
     */
    public void delete(IPK[] _pks, Connection conn) throws SQLException
    {
        TvFinIncomeonlinePK pk ;
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TvFinIncomeonlinePK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 		
 		
        PreparedStatement ps = null;
        try
         {
            ps = conn.prepareStatement(SQL_DELETE);
             
       		for (int i= 0; i< pks.size() ; i++)
       		{
       			pk  = (TvFinIncomeonlinePK)(pks.get(i)) ; 
                ps.setString(1, pk.getSseq());
		        ps.addBatch() ;
			}
            ps.executeBatch();
        }
        finally
        {
            close(ps);
        }      
    }



    /**
    * Check IChangerNumber or UpdateTimeStamp.
    */
    public void check(IDto _dto, Connection conn) throws SQLException
    {
                                                                                                                           		throw new SQLException("数据库表：TV_FIN_INCOMEONLINE没有检查修改的字段");
    }


	/**
	* synchronize values between parent and children
	*/
    public void syncToChildren(IDto _dto)     throws SQLException{
       throw new SQLException("本记录没有与其它记录相关联，无法进行主记录与明细记录之间的数据同步。");
  }

	/**
	*get children
	*/
    public IDto[] findChildren (IDto _dto, Connection conn, boolean isLobSupport)     throws SQLException{
         throw new SQLException("本dto没有与其它dto相关联，无法得到子dto");
  }    
    
  /**
   *return the children IDto class
   * @return Class
   */
  public Class getChildrenClass(){
	  return null;
  };    



    /**
     * Close JDBC Statement.
     * @param stmt  Statement to be closed.
     */
    private void close(Statement stmt)
    {
        if (stmt != null)
        {
            try
            {
                stmt.close();
            }
            catch(SQLException    e)
            {}
        }
    }

    /**
     * Close JDBC ResultSet.
     * @param rs  ResultSet to be closed.
     */
    private void close(ResultSet rs)
    {
        if (rs != null)
        {
            try
            {
                rs.close();
            }catch(SQLException  e){}
        }
    }
}
