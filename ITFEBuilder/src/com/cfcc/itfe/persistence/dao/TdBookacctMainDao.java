    



package com.cfcc.itfe.persistence.dao;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.Statement;
import java.sql.SQLException;
import java.sql.Date;
import java.sql.Time;
import java.sql.Timestamp ;
import java.sql.Clob;

import com.cfcc.jaf.persistence.jaform.parent.* ;

import java.math.BigDecimal;

import java.util.List;
import java.util.ArrayList;
import java.util.HashMap;

import java.io.StringReader;
import java.io.Reader;
import java.io.IOException;

import com.cfcc.itfe.persistence.dto.TdBookacctMainDto ;
import com.cfcc.itfe.persistence.pk.TdBookacctMainPK ;


/**
 * <p>Title: DAO of table: TD_BOOKACCT_MAIN</p>
 * <p>Description: Data Access Object  </p>
 * <p>Copyright: Copyright (c) 2005</p>
 * <p>Company: CFCC </p>
 * <p>Generation Time: 2014-12-25 15:28:57 </p>
 *  Do NOT change this file by hand.
 *  This java source file is generated by JAF_ORM .
 *  dao.vm timestamp: 2007-04-06 08:30:00
 ******************************************************
 * <p> <b>change record</b><br>
 *  make dao methods simply.<br>
 *  especially make update,delete methods simply.<br>
 *  remove "..withCheck" method,withCheck will impl in JAFOrmTemplate.<br>
 *  remove "delete(IDto _dto,..)" method.<br></p>
 * @author win7
 */

public class TdBookacctMainDao  implements IDao
{


    /* SQL to insert data */
    private static final String SQL_INSERT =
        "INSERT INTO TD_BOOKACCT_MAIN ("
          + "S_BOOKORGCODE,S_BOOKACCT,S_BOOKACCTNAME,S_BOOKACCTSHT,S_OFBOOKSBTCODE"
          + ",C_ACTGENSTYLE,D_OPENACCT,D_CANCELACCT,C_ACCTPROP,C_BOOKCLASS"
          + ",C_BALANCEPROP,C_BALANCEORT,C_BALANCECTRL,F_PREDATESWITCHBALANCE,C_INTERESTFLAG"
          + ",I_INTERESTTYPENO,D_STARTINTEREST,S_SPECIALORGCODE,I_SUBACCTNUM,S_OFTRECODE"
          + ",C_WAITRECKFLAG,TS_SYSUPDATE"
        + ") VALUES ("
        + "?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,CURRENT TIMESTAMP )";


    private static final String SQL_INSERT_WITH_RESULT = "SELECT * FROM FINAL TABLE( " + SQL_INSERT + " )";
    
    /* SQL to select data */
    private static final String SQL_SELECT =
        "SELECT "
        + "TD_BOOKACCT_MAIN.S_BOOKORGCODE, TD_BOOKACCT_MAIN.S_BOOKACCT, TD_BOOKACCT_MAIN.S_BOOKACCTNAME, TD_BOOKACCT_MAIN.S_BOOKACCTSHT, TD_BOOKACCT_MAIN.S_OFBOOKSBTCODE, "
        + "TD_BOOKACCT_MAIN.C_ACTGENSTYLE, TD_BOOKACCT_MAIN.D_OPENACCT, TD_BOOKACCT_MAIN.D_CANCELACCT, TD_BOOKACCT_MAIN.C_ACCTPROP, TD_BOOKACCT_MAIN.C_BOOKCLASS, "
        + "TD_BOOKACCT_MAIN.C_BALANCEPROP, TD_BOOKACCT_MAIN.C_BALANCEORT, TD_BOOKACCT_MAIN.C_BALANCECTRL, TD_BOOKACCT_MAIN.F_PREDATESWITCHBALANCE, TD_BOOKACCT_MAIN.C_INTERESTFLAG, "
        + "TD_BOOKACCT_MAIN.I_INTERESTTYPENO, TD_BOOKACCT_MAIN.D_STARTINTEREST, TD_BOOKACCT_MAIN.S_SPECIALORGCODE, TD_BOOKACCT_MAIN.I_SUBACCTNUM, TD_BOOKACCT_MAIN.S_OFTRECODE, "
        + "TD_BOOKACCT_MAIN.C_WAITRECKFLAG, TD_BOOKACCT_MAIN.TS_SYSUPDATE "
        + "FROM TD_BOOKACCT_MAIN "
        +" WHERE " 
        + "S_BOOKORGCODE = ? AND S_BOOKACCT = ?"
        ;
    /* SQL to select for update data */
    private static final String SQL_SELECT_FOR_UPDATE =
        "SELECT "
        + "TD_BOOKACCT_MAIN.S_BOOKORGCODE, TD_BOOKACCT_MAIN.S_BOOKACCT, TD_BOOKACCT_MAIN.S_BOOKACCTNAME, TD_BOOKACCT_MAIN.S_BOOKACCTSHT, TD_BOOKACCT_MAIN.S_OFBOOKSBTCODE, "
        + "TD_BOOKACCT_MAIN.C_ACTGENSTYLE, TD_BOOKACCT_MAIN.D_OPENACCT, TD_BOOKACCT_MAIN.D_CANCELACCT, TD_BOOKACCT_MAIN.C_ACCTPROP, TD_BOOKACCT_MAIN.C_BOOKCLASS, "
        + "TD_BOOKACCT_MAIN.C_BALANCEPROP, TD_BOOKACCT_MAIN.C_BALANCEORT, TD_BOOKACCT_MAIN.C_BALANCECTRL, TD_BOOKACCT_MAIN.F_PREDATESWITCHBALANCE, TD_BOOKACCT_MAIN.C_INTERESTFLAG, "
        + "TD_BOOKACCT_MAIN.I_INTERESTTYPENO, TD_BOOKACCT_MAIN.D_STARTINTEREST, TD_BOOKACCT_MAIN.S_SPECIALORGCODE, TD_BOOKACCT_MAIN.I_SUBACCTNUM, TD_BOOKACCT_MAIN.S_OFTRECODE, "
        + "TD_BOOKACCT_MAIN.C_WAITRECKFLAG, TD_BOOKACCT_MAIN.TS_SYSUPDATE "
        + "FROM TD_BOOKACCT_MAIN "
        +" WHERE " 
        + "S_BOOKORGCODE = ? AND S_BOOKACCT = ? FOR UPDATE"
        ;

      /* SQL to select data (SCROLLABLE)*/
     private static final String SQL_SELECT_BATCH_SCROLLABLE =
        "SELECT "
        + "  TD_BOOKACCT_MAIN.S_BOOKORGCODE  , TD_BOOKACCT_MAIN.S_BOOKACCT  , TD_BOOKACCT_MAIN.S_BOOKACCTNAME  , TD_BOOKACCT_MAIN.S_BOOKACCTSHT  , TD_BOOKACCT_MAIN.S_OFBOOKSBTCODE "
        + " , TD_BOOKACCT_MAIN.C_ACTGENSTYLE  , TD_BOOKACCT_MAIN.D_OPENACCT  , TD_BOOKACCT_MAIN.D_CANCELACCT  , TD_BOOKACCT_MAIN.C_ACCTPROP  , TD_BOOKACCT_MAIN.C_BOOKCLASS "
        + " , TD_BOOKACCT_MAIN.C_BALANCEPROP  , TD_BOOKACCT_MAIN.C_BALANCEORT  , TD_BOOKACCT_MAIN.C_BALANCECTRL  , TD_BOOKACCT_MAIN.F_PREDATESWITCHBALANCE  , TD_BOOKACCT_MAIN.C_INTERESTFLAG "
        + " , TD_BOOKACCT_MAIN.I_INTERESTTYPENO  , TD_BOOKACCT_MAIN.D_STARTINTEREST  , TD_BOOKACCT_MAIN.S_SPECIALORGCODE  , TD_BOOKACCT_MAIN.I_SUBACCTNUM  , TD_BOOKACCT_MAIN.S_OFTRECODE "
        + " , TD_BOOKACCT_MAIN.C_WAITRECKFLAG  , TD_BOOKACCT_MAIN.TS_SYSUPDATE "
        + "FROM TD_BOOKACCT_MAIN ";



    /* SQL to select batch data */
    private static final String SQL_SELECT_BATCH =
        "SELECT "
        + "TD_BOOKACCT_MAIN.S_BOOKORGCODE, TD_BOOKACCT_MAIN.S_BOOKACCT, TD_BOOKACCT_MAIN.S_BOOKACCTNAME, TD_BOOKACCT_MAIN.S_BOOKACCTSHT, TD_BOOKACCT_MAIN.S_OFBOOKSBTCODE, "
        + "TD_BOOKACCT_MAIN.C_ACTGENSTYLE, TD_BOOKACCT_MAIN.D_OPENACCT, TD_BOOKACCT_MAIN.D_CANCELACCT, TD_BOOKACCT_MAIN.C_ACCTPROP, TD_BOOKACCT_MAIN.C_BOOKCLASS, "
        + "TD_BOOKACCT_MAIN.C_BALANCEPROP, TD_BOOKACCT_MAIN.C_BALANCEORT, TD_BOOKACCT_MAIN.C_BALANCECTRL, TD_BOOKACCT_MAIN.F_PREDATESWITCHBALANCE, TD_BOOKACCT_MAIN.C_INTERESTFLAG, "
        + "TD_BOOKACCT_MAIN.I_INTERESTTYPENO, TD_BOOKACCT_MAIN.D_STARTINTEREST, TD_BOOKACCT_MAIN.S_SPECIALORGCODE, TD_BOOKACCT_MAIN.I_SUBACCTNUM, TD_BOOKACCT_MAIN.S_OFTRECODE, "
        + "TD_BOOKACCT_MAIN.C_WAITRECKFLAG, TD_BOOKACCT_MAIN.TS_SYSUPDATE "
        + "FROM TD_BOOKACCT_MAIN " ;
        

   /* SQL to select batch data where condition  */
    private static final String SQL_SELECT_BATCH_WHERE =" ( "
        + "S_BOOKORGCODE = ? AND S_BOOKACCT = ?)"
        ;


    /* SQL to update data */
    private static final String SQL_UPDATE =
        "UPDATE TD_BOOKACCT_MAIN SET "
        + "S_BOOKACCTNAME =?,S_BOOKACCTSHT =?,S_OFBOOKSBTCODE =?,C_ACTGENSTYLE =?,D_OPENACCT =?, "
        + "D_CANCELACCT =?,C_ACCTPROP =?,C_BOOKCLASS =?,C_BALANCEPROP =?,C_BALANCEORT =?, "
        + "C_BALANCECTRL =?,F_PREDATESWITCHBALANCE =?,C_INTERESTFLAG =?,I_INTERESTTYPENO =?,D_STARTINTEREST =?, "
        + "S_SPECIALORGCODE =?,I_SUBACCTNUM =?,S_OFTRECODE =?,C_WAITRECKFLAG =?,TS_SYSUPDATE =CURRENT TIMESTAMP "
        + "WHERE "
        + "S_BOOKORGCODE = ? AND S_BOOKACCT = ?"
        ;

	/* SQL to update data, support LOB */
    private static final String SQL_UPDATE_LOB =
        "UPDATE TD_BOOKACCT_MAIN SET "
        + "S_BOOKACCTNAME =?, S_BOOKACCTSHT =?, S_OFBOOKSBTCODE =?, C_ACTGENSTYLE =?, D_OPENACCT =?,  "
        + "D_CANCELACCT =?, C_ACCTPROP =?, C_BOOKCLASS =?, C_BALANCEPROP =?, C_BALANCEORT =?,  "
        + "C_BALANCECTRL =?, F_PREDATESWITCHBALANCE =?, C_INTERESTFLAG =?, I_INTERESTTYPENO =?, D_STARTINTEREST =?,  "
        + "S_SPECIALORGCODE =?, I_SUBACCTNUM =?, S_OFTRECODE =?, C_WAITRECKFLAG =?, TS_SYSUPDATE =CURRENT TIMESTAMP "
        + "WHERE "
        + "S_BOOKORGCODE = ? AND S_BOOKACCT = ?"
        ;	
	
    /* SQL to delete data */
    private static final String SQL_DELETE =
        "DELETE FROM TD_BOOKACCT_MAIN " 
        + " WHERE "
        + "S_BOOKORGCODE = ? AND S_BOOKACCT = ?"
        ;


	/**
	*  批量查询 一次最多查询的参数数量
	*/
	
	public static final int FIND_BATCH_SIZE = 150  / 2;
	



   /**
   * Create a new record in Database.
   */
  public void create(IDto _dto,  Connection conn) throws SQLException
  {
     TdBookacctMainDto dto = (TdBookacctMainDto)_dto ;
     String msgValid = dto.checkValid() ;
     if (msgValid != null)
     	throw new SQLException("插入错误，"+msgValid) ;

     PreparedStatement ps = null;
     try
     {
         ps = conn.prepareStatement(SQL_INSERT);
          ps.setString(1, dto.getSbookorgcode());

          ps.setString(2, dto.getSbookacct());

          ps.setString(3, dto.getSbookacctname());

          ps.setString(4, dto.getSbookacctsht());

          ps.setString(5, dto.getSofbooksbtcode());

          ps.setString(6, dto.getCactgenstyle());

          ps.setDate(7, dto.getDopenacct());

          ps.setDate(8, dto.getDcancelacct());

          ps.setString(9, dto.getCacctprop());

          ps.setString(10, dto.getCbookclass());

          ps.setString(11, dto.getCbalanceprop());

          ps.setString(12, dto.getCbalanceort());

          ps.setString(13, dto.getCbalancectrl());

          ps.setBigDecimal(14, dto.getFpredateswitchbalance());

          ps.setString(15, dto.getCinterestflag());

          if (dto.getIinteresttypeno()==null)
            ps.setNull(16, java.sql.Types.INTEGER);
         else
            ps.setInt(16, dto.getIinteresttypeno().intValue());
          ps.setDate(17, dto.getDstartinterest());

          ps.setString(18, dto.getSspecialorgcode());

          if (dto.getIsubacctnum()==null)
            ps.setNull(19, java.sql.Types.INTEGER);
         else
            ps.setInt(19, dto.getIsubacctnum().intValue());
          ps.setString(20, dto.getSoftrecode());

          ps.setString(21, dto.getCwaitreckflag());

          ps.executeUpdate();
     }
     finally
     {
         close(ps);
     }
     return  ;
  }

   /**
   * Create a new record in Database. and return result, only used when having generated key
   */
   public IDto createWithResult(IDto _dto,  Connection conn) throws SQLException
   {
       TdBookacctMainDto dto = (TdBookacctMainDto)_dto ;
       String msgValid = dto.checkValid() ;
       if (msgValid != null)
           throw new SQLException("插入错误，"+msgValid) ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try
       {
           ps = conn.prepareStatement(SQL_INSERT_WITH_RESULT);
            ps.setString(1, dto.getSbookorgcode());
            ps.setString(2, dto.getSbookacct());
            ps.setString(3, dto.getSbookacctname());
            ps.setString(4, dto.getSbookacctsht());
            ps.setString(5, dto.getSofbooksbtcode());
            ps.setString(6, dto.getCactgenstyle());
            ps.setDate(7, dto.getDopenacct());
            ps.setDate(8, dto.getDcancelacct());
            ps.setString(9, dto.getCacctprop());
            ps.setString(10, dto.getCbookclass());
            ps.setString(11, dto.getCbalanceprop());
            ps.setString(12, dto.getCbalanceort());
            ps.setString(13, dto.getCbalancectrl());
            ps.setBigDecimal(14, dto.getFpredateswitchbalance());
            ps.setString(15, dto.getCinterestflag());
            if (dto.getIinteresttypeno()==null)
              ps.setNull(16, java.sql.Types.INTEGER);
           else
              ps.setInt(16, dto.getIinteresttypeno().intValue());
            ps.setDate(17, dto.getDstartinterest());
            ps.setString(18, dto.getSspecialorgcode());
            if (dto.getIsubacctnum()==null)
              ps.setNull(19, java.sql.Types.INTEGER);
           else
              ps.setInt(19, dto.getIsubacctnum().intValue());
            ps.setString(20, dto.getSoftrecode());
            ps.setString(21, dto.getCwaitreckflag());
            rs = ps.executeQuery();
           List results = getResults(rs);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }
        finally
        {
           close(ps);
           close(rs);
        }
   }

 	/**
	* Create batch new record in Database. it only create the first layer dto
	*/
    public void create(IDto[] _dtos, Connection conn)    throws SQLException
    {
        TdBookacctMainDto dto  ;
     	for (int i= 0; i< _dtos.length ; i++)
 	    {
 		    dto  = (TdBookacctMainDto)_dtos[i] ; 
    	    String msgValid = dto.checkValid() ;
        	if (msgValid != null)
     	    	throw new SQLException("插入错误，"+msgValid) ;
     	}
 	    PreparedStatement ps = null;
        try
        {
            ps = conn.prepareStatement(SQL_INSERT);
       
     	    for (int i= 0; i< _dtos.length ; i++)
     	    {
 	    	    dto  = (TdBookacctMainDto)_dtos[i] ; 
  
               ps.setString(1, dto.getSbookorgcode());
  
               ps.setString(2, dto.getSbookacct());
  
               ps.setString(3, dto.getSbookacctname());
  
               ps.setString(4, dto.getSbookacctsht());
  
               ps.setString(5, dto.getSofbooksbtcode());
  
               ps.setString(6, dto.getCactgenstyle());
  
               ps.setDate(7, dto.getDopenacct());
  
               ps.setDate(8, dto.getDcancelacct());
  
               ps.setString(9, dto.getCacctprop());
  
               ps.setString(10, dto.getCbookclass());
  
               ps.setString(11, dto.getCbalanceprop());
  
               ps.setString(12, dto.getCbalanceort());
  
               ps.setString(13, dto.getCbalancectrl());
  
               ps.setBigDecimal(14, dto.getFpredateswitchbalance());
  
               ps.setString(15, dto.getCinterestflag());
  
               if (dto.getIinteresttypeno()==null)
                  ps.setNull(16, java.sql.Types.INTEGER);
               else
                  ps.setInt(16, dto.getIinteresttypeno().intValue());
  
               ps.setDate(17, dto.getDstartinterest());
  
               ps.setString(18, dto.getSspecialorgcode());
  
               if (dto.getIsubacctnum()==null)
                  ps.setNull(19, java.sql.Types.INTEGER);
               else
                  ps.setInt(19, dto.getIsubacctnum().intValue());
  
               ps.setString(20, dto.getSoftrecode());
  
               ps.setString(21, dto.getCwaitreckflag());
                ps.addBatch(); 
           }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }
        return  ;
   }




     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto find(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TdBookacctMainPK pk = (TdBookacctMainPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT);
           ps.setString(1, pk.getSbookorgcode());

           ps.setString(2, pk.getSbookacct());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }

     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto findForUpdate(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TdBookacctMainPK pk = (TdBookacctMainPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT_FOR_UPDATE);
           ps.setString(1, pk.getSbookorgcode());

           ps.setString(2, pk.getSbookacct());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }
  
     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto[] find(IPK[] _pks, Connection conn, boolean isLobSupport) throws SQLException
    {
        TdBookacctMainPK pk ;
        
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TdBookacctMainPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 
        PreparedStatement ps = null;
        ResultSet rs = null;
        
         
        List results = new ArrayList();  
		for (int iBegin = 0; iBegin < pks.size(); iBegin += FIND_BATCH_SIZE)   //
		{
        
            try {
          		int iFindBatchSize = pks.size() - iBegin ;
	    		if (iFindBatchSize >FIND_BATCH_SIZE )
		    		iFindBatchSize = FIND_BATCH_SIZE ;
		  
        	    StringBuffer sb = new StringBuffer() ;
            	sb.append(SQL_SELECT_BATCH).append(" WHERE ").append(SQL_SELECT_BATCH_WHERE);

	    		for (int i = iBegin+1; i < iBegin + iFindBatchSize; i++)
		    	{
			    	sb.append("OR").append(SQL_SELECT_BATCH_WHERE) ;
    			}
        	
                ps = conn.prepareStatement(sb.toString());

                for (int i = iBegin;i < iBegin + iFindBatchSize; i++)
                {
                    pk  = (TdBookacctMainPK)(pks.get(i)) ; 
                   ps.setString((i-iBegin)*2+1, pk.getSbookorgcode());

                   ps.setString((i-iBegin)*2+2, pk.getSbookacct());

			
	            }
                rs = ps.executeQuery();
                results.addAll(getResults(rs,0,isLobSupport)); //
            }finally {
                close(rs);
                close(ps);
            }
        }
        if (results!=null && results.size() > 0)
        {
            TdBookacctMainDto[] dtos = new TdBookacctMainDto[0];
		    dtos = (TdBookacctMainDto[]) results.toArray(dtos) ;
		    return dtos ;
        }
        else
        {
            return null;
        }
    }

  
  
  



    /**
	    * 得到查询语句
	    */
    public String getSelectSQL() 
    {
	        return SQL_SELECT_BATCH_SCROLLABLE ;
    }
   

	public List getResults(ResultSet rs) throws SQLException
    {
    	return getResults(rs, 0) ;
    }
  

    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize) throws SQLException
    {
		return getResults(rs,maxSize,false);
	}
    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize, boolean isLobSupport) throws SQLException
    {
        List results = new ArrayList();
        String str ;
        Reader reader;
        Clob clob;
        char[] chars ;
        
        while (rs.next())
         {
             TdBookacctMainDto  dto = new  TdBookacctMainDto ();
             //S_BOOKORGCODE
             str = rs.getString("S_BOOKORGCODE");
             if (str == null)
                dto.setSbookorgcode(null);
             else
                dto.setSbookorgcode(str.trim());

             //S_BOOKACCT
             str = rs.getString("S_BOOKACCT");
             if (str == null)
                dto.setSbookacct(null);
             else
                dto.setSbookacct(str.trim());

             //S_BOOKACCTNAME
             str = rs.getString("S_BOOKACCTNAME");
             if (str == null)
                dto.setSbookacctname(null);
             else
                dto.setSbookacctname(str.trim());

             //S_BOOKACCTSHT
             str = rs.getString("S_BOOKACCTSHT");
             if (str == null)
                dto.setSbookacctsht(null);
             else
                dto.setSbookacctsht(str.trim());

             //S_OFBOOKSBTCODE
             str = rs.getString("S_OFBOOKSBTCODE");
             if (str == null)
                dto.setSofbooksbtcode(null);
             else
                dto.setSofbooksbtcode(str.trim());

             //C_ACTGENSTYLE
             str = rs.getString("C_ACTGENSTYLE");
             if (str == null)
                dto.setCactgenstyle(null);
             else
                dto.setCactgenstyle(str.trim());

             //D_OPENACCT
           dto.setDopenacct(rs.getDate("D_OPENACCT"));

             //D_CANCELACCT
           dto.setDcancelacct(rs.getDate("D_CANCELACCT"));

             //C_ACCTPROP
             str = rs.getString("C_ACCTPROP");
             if (str == null)
                dto.setCacctprop(null);
             else
                dto.setCacctprop(str.trim());

             //C_BOOKCLASS
             str = rs.getString("C_BOOKCLASS");
             if (str == null)
                dto.setCbookclass(null);
             else
                dto.setCbookclass(str.trim());

             //C_BALANCEPROP
             str = rs.getString("C_BALANCEPROP");
             if (str == null)
                dto.setCbalanceprop(null);
             else
                dto.setCbalanceprop(str.trim());

             //C_BALANCEORT
             str = rs.getString("C_BALANCEORT");
             if (str == null)
                dto.setCbalanceort(null);
             else
                dto.setCbalanceort(str.trim());

             //C_BALANCECTRL
             str = rs.getString("C_BALANCECTRL");
             if (str == null)
                dto.setCbalancectrl(null);
             else
                dto.setCbalancectrl(str.trim());

             //F_PREDATESWITCHBALANCE
           dto.setFpredateswitchbalance(rs.getBigDecimal("F_PREDATESWITCHBALANCE"));

             //C_INTERESTFLAG
             str = rs.getString("C_INTERESTFLAG");
             if (str == null)
                dto.setCinterestflag(null);
             else
                dto.setCinterestflag(str.trim());

             //I_INTERESTTYPENO
             str = rs.getString("I_INTERESTTYPENO");
             if(str!=null){
                dto.setIinteresttypeno(new Integer(str));
             }

             //D_STARTINTEREST
           dto.setDstartinterest(rs.getDate("D_STARTINTEREST"));

             //S_SPECIALORGCODE
             str = rs.getString("S_SPECIALORGCODE");
             if (str == null)
                dto.setSspecialorgcode(null);
             else
                dto.setSspecialorgcode(str.trim());

             //I_SUBACCTNUM
             str = rs.getString("I_SUBACCTNUM");
             if(str!=null){
                dto.setIsubacctnum(new Integer(str));
             }

             //S_OFTRECODE
             str = rs.getString("S_OFTRECODE");
             if (str == null)
                dto.setSoftrecode(null);
             else
                dto.setSoftrecode(str.trim());

             //C_WAITRECKFLAG
             str = rs.getString("C_WAITRECKFLAG");
             if (str == null)
                dto.setCwaitreckflag(null);
             else
                dto.setCwaitreckflag(str.trim());

             //TS_SYSUPDATE
           dto.setTssysupdate(rs.getTimestamp("TS_SYSUPDATE"));



            results.add(dto);
            
            if(maxSize >0  && results.size() == maxSize)
            {
            	break ;
            }
            
         }
        
		return results ;
    }

  /**
   * populate resultSet as cclass
   */
  protected IDto[] getChildrenResults(ResultSet rs ,Class _dto) throws SQLException{

      throw new SQLException("本dto没有与其它dto相关联，无法应用此方法");
  }


     /**
     * Update a record in Database.
     */

    public void update(IDto _dto, Connection conn, boolean isLobSupport) throws SQLException
    {
        String msgValid = _dto.checkValid() ;
        if (msgValid != null)
            throw new SQLException("修改错误，"+msgValid) ;
        TdBookacctMainDto dto = (TdBookacctMainDto)_dto ;
        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            int pos = 1;
            //S_BOOKACCTNAME
            ps.setString(pos, dto.getSbookacctname());
            pos++;

            //S_BOOKACCTSHT
            ps.setString(pos, dto.getSbookacctsht());
            pos++;

            //S_OFBOOKSBTCODE
            ps.setString(pos, dto.getSofbooksbtcode());
            pos++;

            //C_ACTGENSTYLE
            ps.setString(pos, dto.getCactgenstyle());
            pos++;

            //D_OPENACCT
            ps.setDate(pos, dto.getDopenacct());
            pos++;

            //D_CANCELACCT
            ps.setDate(pos, dto.getDcancelacct());
            pos++;

            //C_ACCTPROP
            ps.setString(pos, dto.getCacctprop());
            pos++;

            //C_BOOKCLASS
            ps.setString(pos, dto.getCbookclass());
            pos++;

            //C_BALANCEPROP
            ps.setString(pos, dto.getCbalanceprop());
            pos++;

            //C_BALANCEORT
            ps.setString(pos, dto.getCbalanceort());
            pos++;

            //C_BALANCECTRL
            ps.setString(pos, dto.getCbalancectrl());
            pos++;

            //F_PREDATESWITCHBALANCE
            ps.setBigDecimal(pos, dto.getFpredateswitchbalance());
            pos++;

            //C_INTERESTFLAG
            ps.setString(pos, dto.getCinterestflag());
            pos++;

            //I_INTERESTTYPENO
            if (dto.getIinteresttypeno()==null)
                ps.setNull(pos, java.sql.Types.INTEGER);
            else
                ps.setInt(pos, dto.getIinteresttypeno().intValue());
            pos++;

            //D_STARTINTEREST
            ps.setDate(pos, dto.getDstartinterest());
            pos++;

            //S_SPECIALORGCODE
            ps.setString(pos, dto.getSspecialorgcode());
            pos++;

            //I_SUBACCTNUM
            if (dto.getIsubacctnum()==null)
                ps.setNull(pos, java.sql.Types.INTEGER);
            else
                ps.setInt(pos, dto.getIsubacctnum().intValue());
            pos++;

            //S_OFTRECODE
            ps.setString(pos, dto.getSoftrecode());
            pos++;

            //C_WAITRECKFLAG
            ps.setString(pos, dto.getCwaitreckflag());
            pos++;

            //TS_SYSUPDATE

           //S_BOOKORGCODE
           ps.setString(pos, dto.getSbookorgcode());
           pos++;
           //S_BOOKACCT
           ps.setString(pos, dto.getSbookacct());
           pos++;
           ps.executeUpdate();
        }
        finally
        {
           close(ps);
        }

        return ;
    }
    
    
    
      /**
     * Update batch record in Database.
     */

    public void update(IDto[] _dtos, Connection conn, boolean isLobSupport) throws SQLException
    {
     
     	 TdBookacctMainDto dto  ;
         for (int i= 0; i< _dtos.length ; i++)
         {
            dto  = (TdBookacctMainDto)_dtos[i] ; 
            String msgValid = dto.checkValid() ;
            if (msgValid != null)
                throw new SQLException("修改错误，"+msgValid) ;
         }

        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            
            for (int i= 0; i< _dtos.length ; i++)
            {
                dto  = (TdBookacctMainDto)_dtos[i] ; 
                int pos = 1;
                //S_BOOKACCTNAME
                 ps.setString(pos, dto.getSbookacctname());
                pos++;

                //S_BOOKACCTSHT
                 ps.setString(pos, dto.getSbookacctsht());
                pos++;

                //S_OFBOOKSBTCODE
                 ps.setString(pos, dto.getSofbooksbtcode());
                pos++;

                //C_ACTGENSTYLE
                 ps.setString(pos, dto.getCactgenstyle());
                pos++;

                //D_OPENACCT
                 ps.setDate(pos, dto.getDopenacct());
                pos++;

                //D_CANCELACCT
                 ps.setDate(pos, dto.getDcancelacct());
                pos++;

                //C_ACCTPROP
                 ps.setString(pos, dto.getCacctprop());
                pos++;

                //C_BOOKCLASS
                 ps.setString(pos, dto.getCbookclass());
                pos++;

                //C_BALANCEPROP
                 ps.setString(pos, dto.getCbalanceprop());
                pos++;

                //C_BALANCEORT
                 ps.setString(pos, dto.getCbalanceort());
                pos++;

                //C_BALANCECTRL
                 ps.setString(pos, dto.getCbalancectrl());
                pos++;

                //F_PREDATESWITCHBALANCE
                 ps.setBigDecimal(pos, dto.getFpredateswitchbalance());
                pos++;

                //C_INTERESTFLAG
                 ps.setString(pos, dto.getCinterestflag());
                pos++;

                //I_INTERESTTYPENO
                 if (dto.getIinteresttypeno()==null)
                   ps.setNull(pos, java.sql.Types.INTEGER);
                else
                   ps.setInt(pos, dto.getIinteresttypeno().intValue());
                pos++ ;

                //D_STARTINTEREST
                 ps.setDate(pos, dto.getDstartinterest());
                pos++;

                //S_SPECIALORGCODE
                 ps.setString(pos, dto.getSspecialorgcode());
                pos++;

                //I_SUBACCTNUM
                 if (dto.getIsubacctnum()==null)
                   ps.setNull(pos, java.sql.Types.INTEGER);
                else
                   ps.setInt(pos, dto.getIsubacctnum().intValue());
                pos++ ;

                //S_OFTRECODE
                 ps.setString(pos, dto.getSoftrecode());
                pos++;

                //C_WAITRECKFLAG
                 ps.setString(pos, dto.getCwaitreckflag());
                pos++;

                //TS_SYSUPDATE
 
               //S_BOOKORGCODE
               ps.setString(pos, dto.getSbookorgcode());
               pos++;
               //S_BOOKACCT
               ps.setString(pos, dto.getSbookacct());
               pos++;
		       ps.addBatch(); 
	       }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }

        return ;
    }



    /**
     * delete a record in Database.
     */
    public void delete(IPK _pk, Connection conn) throws SQLException
    {
       
      
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return ;
        //	throw new SQLException("删除错误，"+msgValid) ;
       }
       TdBookacctMainPK pk = (TdBookacctMainPK)_pk ;


       PreparedStatement ps = null;
       try {
           ps = conn.prepareStatement(SQL_DELETE);
           ps.setString(1, pk.getSbookorgcode());
           ps.setString(2, pk.getSbookacct());
           ps.executeUpdate();
        }
        finally
        {
            close(ps);
        }
    }


     /**
     * delete batch record in Database.
     */
    public void delete(IPK[] _pks, Connection conn) throws SQLException
    {
        TdBookacctMainPK pk ;
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TdBookacctMainPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 		
 		
        PreparedStatement ps = null;
        try
         {
            ps = conn.prepareStatement(SQL_DELETE);
             
       		for (int i= 0; i< pks.size() ; i++)
       		{
       			pk  = (TdBookacctMainPK)(pks.get(i)) ; 
                ps.setString(1, pk.getSbookorgcode());
                ps.setString(2, pk.getSbookacct());
		        ps.addBatch() ;
			}
            ps.executeBatch();
        }
        finally
        {
            close(ps);
        }      
    }



    /**
    * Check IChangerNumber or UpdateTimeStamp.
    */
    public void check(IDto _dto, Connection conn) throws SQLException
    {
                                                            		throw new SQLException("数据库表：TD_BOOKACCT_MAIN没有检查修改的字段");
    }


	/**
	* synchronize values between parent and children
	*/
    public void syncToChildren(IDto _dto)     throws SQLException{
       throw new SQLException("本记录没有与其它记录相关联，无法进行主记录与明细记录之间的数据同步。");
  }

	/**
	*get children
	*/
    public IDto[] findChildren (IDto _dto, Connection conn, boolean isLobSupport)     throws SQLException{
         throw new SQLException("本dto没有与其它dto相关联，无法得到子dto");
  }    
    
  /**
   *return the children IDto class
   * @return Class
   */
  public Class getChildrenClass(){
	  return null;
  };    



    /**
     * Close JDBC Statement.
     * @param stmt  Statement to be closed.
     */
    private void close(Statement stmt)
    {
        if (stmt != null)
        {
            try
            {
                stmt.close();
            }
            catch(SQLException    e)
            {}
        }
    }

    /**
     * Close JDBC ResultSet.
     * @param rs  ResultSet to be closed.
     */
    private void close(ResultSet rs)
    {
        if (rs != null)
        {
            try
            {
                rs.close();
            }catch(SQLException  e){}
        }
    }
}
