    



package com.cfcc.itfe.persistence.dao;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.Statement;
import java.sql.SQLException;
import java.sql.Date;
import java.sql.Time;
import java.sql.Timestamp ;
import java.sql.Clob;

import com.cfcc.jaf.persistence.jaform.parent.* ;

import java.math.BigDecimal;

import java.util.List;
import java.util.ArrayList;
import java.util.HashMap;

import java.io.StringReader;
import java.io.Reader;
import java.io.IOException;

import com.cfcc.itfe.persistence.dto.TvPayoutbackmsgMainDto ;
import com.cfcc.itfe.persistence.pk.TvPayoutbackmsgMainPK ;


/**
 * <p>Title: DAO of table: TV_PAYOUTBACKMSG_MAIN</p>
 * <p>Description: Data Access Object  </p>
 * <p>Copyright: Copyright (c) 2005</p>
 * <p>Company: CFCC </p>
 * <p>Generation Time: 2014-12-25 15:29:03 </p>
 *  Do NOT change this file by hand.
 *  This java source file is generated by JAF_ORM .
 *  dao.vm timestamp: 2007-04-06 08:30:00
 ******************************************************
 * <p> <b>change record</b><br>
 *  make dao methods simply.<br>
 *  especially make update,delete methods simply.<br>
 *  remove "..withCheck" method,withCheck will impl in JAFOrmTemplate.<br>
 *  remove "delete(IDto _dto,..)" method.<br></p>
 * @author win7
 */

public class TvPayoutbackmsgMainDao  implements IDao
{


    /* SQL to insert data */
    private static final String SQL_INSERT =
        "INSERT INTO TV_PAYOUTBACKMSG_MAIN ("
          + "S_BIZNO,S_ORGCODE,S_COMMITDATE,S_ACCDATE,S_FILENAME"
          + ",S_TRECODE,S_PACKAGENO,S_PAYUNIT,S_VOUNO,S_VOUDATE"
          + ",S_ORITRANO,S_ORIENTRUSTDATE,S_ORIVOUNO,S_ORIVOUDATE,S_PAYERACCT"
          + ",S_PAYERNAME,S_PAYERADDR,N_MONEY,S_PAYEEOPBKNO,S_PAYEEBANKNO"
          + ",S_PAYEEACCT,S_PAYEENAME,S_ADDWORD,S_BUDGETUNITCODE,S_UNITCODENAME"
          + ",S_OFYEAR,S_TRIMFLAG,S_BUDGETTYPE,S_STATUS,S_USERCODE"
          + ",S_BACKFLAG,S_ISRETURN,S_BCKREASON,S_DEMO,S_HOLD1"
          + ",S_HOLD2,S_HOLD3,S_HOLD4,TS_SYSUPDATE"
        + ") VALUES ("
        + "?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,CURRENT TIMESTAMP )";


    private static final String SQL_INSERT_WITH_RESULT = "SELECT * FROM FINAL TABLE( " + SQL_INSERT + " )";
    
    /* SQL to select data */
    private static final String SQL_SELECT =
        "SELECT "
        + "TV_PAYOUTBACKMSG_MAIN.S_BIZNO, TV_PAYOUTBACKMSG_MAIN.S_ORGCODE, TV_PAYOUTBACKMSG_MAIN.S_COMMITDATE, TV_PAYOUTBACKMSG_MAIN.S_ACCDATE, TV_PAYOUTBACKMSG_MAIN.S_FILENAME, "
        + "TV_PAYOUTBACKMSG_MAIN.S_TRECODE, TV_PAYOUTBACKMSG_MAIN.S_PACKAGENO, TV_PAYOUTBACKMSG_MAIN.S_PAYUNIT, TV_PAYOUTBACKMSG_MAIN.S_VOUNO, TV_PAYOUTBACKMSG_MAIN.S_VOUDATE, "
        + "TV_PAYOUTBACKMSG_MAIN.S_ORITRANO, TV_PAYOUTBACKMSG_MAIN.S_ORIENTRUSTDATE, TV_PAYOUTBACKMSG_MAIN.S_ORIVOUNO, TV_PAYOUTBACKMSG_MAIN.S_ORIVOUDATE, TV_PAYOUTBACKMSG_MAIN.S_PAYERACCT, "
        + "TV_PAYOUTBACKMSG_MAIN.S_PAYERNAME, TV_PAYOUTBACKMSG_MAIN.S_PAYERADDR, TV_PAYOUTBACKMSG_MAIN.N_MONEY, TV_PAYOUTBACKMSG_MAIN.S_PAYEEOPBKNO, TV_PAYOUTBACKMSG_MAIN.S_PAYEEBANKNO, "
        + "TV_PAYOUTBACKMSG_MAIN.S_PAYEEACCT, TV_PAYOUTBACKMSG_MAIN.S_PAYEENAME, TV_PAYOUTBACKMSG_MAIN.S_ADDWORD, TV_PAYOUTBACKMSG_MAIN.S_BUDGETUNITCODE, TV_PAYOUTBACKMSG_MAIN.S_UNITCODENAME, "
        + "TV_PAYOUTBACKMSG_MAIN.S_OFYEAR, TV_PAYOUTBACKMSG_MAIN.S_TRIMFLAG, TV_PAYOUTBACKMSG_MAIN.S_BUDGETTYPE, TV_PAYOUTBACKMSG_MAIN.S_STATUS, TV_PAYOUTBACKMSG_MAIN.S_USERCODE, "
        + "TV_PAYOUTBACKMSG_MAIN.S_BACKFLAG, TV_PAYOUTBACKMSG_MAIN.S_ISRETURN, TV_PAYOUTBACKMSG_MAIN.S_BCKREASON, TV_PAYOUTBACKMSG_MAIN.S_DEMO, TV_PAYOUTBACKMSG_MAIN.S_HOLD1, "
        + "TV_PAYOUTBACKMSG_MAIN.S_HOLD2, TV_PAYOUTBACKMSG_MAIN.S_HOLD3, TV_PAYOUTBACKMSG_MAIN.S_HOLD4, TV_PAYOUTBACKMSG_MAIN.TS_SYSUPDATE "
        + "FROM TV_PAYOUTBACKMSG_MAIN "
        +" WHERE " 
        + "S_BIZNO = ?"
        ;
    /* SQL to select for update data */
    private static final String SQL_SELECT_FOR_UPDATE =
        "SELECT "
        + "TV_PAYOUTBACKMSG_MAIN.S_BIZNO, TV_PAYOUTBACKMSG_MAIN.S_ORGCODE, TV_PAYOUTBACKMSG_MAIN.S_COMMITDATE, TV_PAYOUTBACKMSG_MAIN.S_ACCDATE, TV_PAYOUTBACKMSG_MAIN.S_FILENAME, "
        + "TV_PAYOUTBACKMSG_MAIN.S_TRECODE, TV_PAYOUTBACKMSG_MAIN.S_PACKAGENO, TV_PAYOUTBACKMSG_MAIN.S_PAYUNIT, TV_PAYOUTBACKMSG_MAIN.S_VOUNO, TV_PAYOUTBACKMSG_MAIN.S_VOUDATE, "
        + "TV_PAYOUTBACKMSG_MAIN.S_ORITRANO, TV_PAYOUTBACKMSG_MAIN.S_ORIENTRUSTDATE, TV_PAYOUTBACKMSG_MAIN.S_ORIVOUNO, TV_PAYOUTBACKMSG_MAIN.S_ORIVOUDATE, TV_PAYOUTBACKMSG_MAIN.S_PAYERACCT, "
        + "TV_PAYOUTBACKMSG_MAIN.S_PAYERNAME, TV_PAYOUTBACKMSG_MAIN.S_PAYERADDR, TV_PAYOUTBACKMSG_MAIN.N_MONEY, TV_PAYOUTBACKMSG_MAIN.S_PAYEEOPBKNO, TV_PAYOUTBACKMSG_MAIN.S_PAYEEBANKNO, "
        + "TV_PAYOUTBACKMSG_MAIN.S_PAYEEACCT, TV_PAYOUTBACKMSG_MAIN.S_PAYEENAME, TV_PAYOUTBACKMSG_MAIN.S_ADDWORD, TV_PAYOUTBACKMSG_MAIN.S_BUDGETUNITCODE, TV_PAYOUTBACKMSG_MAIN.S_UNITCODENAME, "
        + "TV_PAYOUTBACKMSG_MAIN.S_OFYEAR, TV_PAYOUTBACKMSG_MAIN.S_TRIMFLAG, TV_PAYOUTBACKMSG_MAIN.S_BUDGETTYPE, TV_PAYOUTBACKMSG_MAIN.S_STATUS, TV_PAYOUTBACKMSG_MAIN.S_USERCODE, "
        + "TV_PAYOUTBACKMSG_MAIN.S_BACKFLAG, TV_PAYOUTBACKMSG_MAIN.S_ISRETURN, TV_PAYOUTBACKMSG_MAIN.S_BCKREASON, TV_PAYOUTBACKMSG_MAIN.S_DEMO, TV_PAYOUTBACKMSG_MAIN.S_HOLD1, "
        + "TV_PAYOUTBACKMSG_MAIN.S_HOLD2, TV_PAYOUTBACKMSG_MAIN.S_HOLD3, TV_PAYOUTBACKMSG_MAIN.S_HOLD4, TV_PAYOUTBACKMSG_MAIN.TS_SYSUPDATE "
        + "FROM TV_PAYOUTBACKMSG_MAIN "
        +" WHERE " 
        + "S_BIZNO = ? FOR UPDATE"
        ;

      /* SQL to select data (SCROLLABLE)*/
     private static final String SQL_SELECT_BATCH_SCROLLABLE =
        "SELECT "
        + "  TV_PAYOUTBACKMSG_MAIN.S_BIZNO  , TV_PAYOUTBACKMSG_MAIN.S_ORGCODE  , TV_PAYOUTBACKMSG_MAIN.S_COMMITDATE  , TV_PAYOUTBACKMSG_MAIN.S_ACCDATE  , TV_PAYOUTBACKMSG_MAIN.S_FILENAME "
        + " , TV_PAYOUTBACKMSG_MAIN.S_TRECODE  , TV_PAYOUTBACKMSG_MAIN.S_PACKAGENO  , TV_PAYOUTBACKMSG_MAIN.S_PAYUNIT  , TV_PAYOUTBACKMSG_MAIN.S_VOUNO  , TV_PAYOUTBACKMSG_MAIN.S_VOUDATE "
        + " , TV_PAYOUTBACKMSG_MAIN.S_ORITRANO  , TV_PAYOUTBACKMSG_MAIN.S_ORIENTRUSTDATE  , TV_PAYOUTBACKMSG_MAIN.S_ORIVOUNO  , TV_PAYOUTBACKMSG_MAIN.S_ORIVOUDATE  , TV_PAYOUTBACKMSG_MAIN.S_PAYERACCT "
        + " , TV_PAYOUTBACKMSG_MAIN.S_PAYERNAME  , TV_PAYOUTBACKMSG_MAIN.S_PAYERADDR  , TV_PAYOUTBACKMSG_MAIN.N_MONEY  , TV_PAYOUTBACKMSG_MAIN.S_PAYEEOPBKNO  , TV_PAYOUTBACKMSG_MAIN.S_PAYEEBANKNO "
        + " , TV_PAYOUTBACKMSG_MAIN.S_PAYEEACCT  , TV_PAYOUTBACKMSG_MAIN.S_PAYEENAME  , TV_PAYOUTBACKMSG_MAIN.S_ADDWORD  , TV_PAYOUTBACKMSG_MAIN.S_BUDGETUNITCODE  , TV_PAYOUTBACKMSG_MAIN.S_UNITCODENAME "
        + " , TV_PAYOUTBACKMSG_MAIN.S_OFYEAR  , TV_PAYOUTBACKMSG_MAIN.S_TRIMFLAG  , TV_PAYOUTBACKMSG_MAIN.S_BUDGETTYPE  , TV_PAYOUTBACKMSG_MAIN.S_STATUS  , TV_PAYOUTBACKMSG_MAIN.S_USERCODE "
        + " , TV_PAYOUTBACKMSG_MAIN.S_BACKFLAG  , TV_PAYOUTBACKMSG_MAIN.S_ISRETURN  , TV_PAYOUTBACKMSG_MAIN.S_BCKREASON  , TV_PAYOUTBACKMSG_MAIN.S_DEMO  , TV_PAYOUTBACKMSG_MAIN.S_HOLD1 "
        + " , TV_PAYOUTBACKMSG_MAIN.S_HOLD2  , TV_PAYOUTBACKMSG_MAIN.S_HOLD3  , TV_PAYOUTBACKMSG_MAIN.S_HOLD4  , TV_PAYOUTBACKMSG_MAIN.TS_SYSUPDATE "
        + "FROM TV_PAYOUTBACKMSG_MAIN ";



    /* SQL to select batch data */
    private static final String SQL_SELECT_BATCH =
        "SELECT "
        + "TV_PAYOUTBACKMSG_MAIN.S_BIZNO, TV_PAYOUTBACKMSG_MAIN.S_ORGCODE, TV_PAYOUTBACKMSG_MAIN.S_COMMITDATE, TV_PAYOUTBACKMSG_MAIN.S_ACCDATE, TV_PAYOUTBACKMSG_MAIN.S_FILENAME, "
        + "TV_PAYOUTBACKMSG_MAIN.S_TRECODE, TV_PAYOUTBACKMSG_MAIN.S_PACKAGENO, TV_PAYOUTBACKMSG_MAIN.S_PAYUNIT, TV_PAYOUTBACKMSG_MAIN.S_VOUNO, TV_PAYOUTBACKMSG_MAIN.S_VOUDATE, "
        + "TV_PAYOUTBACKMSG_MAIN.S_ORITRANO, TV_PAYOUTBACKMSG_MAIN.S_ORIENTRUSTDATE, TV_PAYOUTBACKMSG_MAIN.S_ORIVOUNO, TV_PAYOUTBACKMSG_MAIN.S_ORIVOUDATE, TV_PAYOUTBACKMSG_MAIN.S_PAYERACCT, "
        + "TV_PAYOUTBACKMSG_MAIN.S_PAYERNAME, TV_PAYOUTBACKMSG_MAIN.S_PAYERADDR, TV_PAYOUTBACKMSG_MAIN.N_MONEY, TV_PAYOUTBACKMSG_MAIN.S_PAYEEOPBKNO, TV_PAYOUTBACKMSG_MAIN.S_PAYEEBANKNO, "
        + "TV_PAYOUTBACKMSG_MAIN.S_PAYEEACCT, TV_PAYOUTBACKMSG_MAIN.S_PAYEENAME, TV_PAYOUTBACKMSG_MAIN.S_ADDWORD, TV_PAYOUTBACKMSG_MAIN.S_BUDGETUNITCODE, TV_PAYOUTBACKMSG_MAIN.S_UNITCODENAME, "
        + "TV_PAYOUTBACKMSG_MAIN.S_OFYEAR, TV_PAYOUTBACKMSG_MAIN.S_TRIMFLAG, TV_PAYOUTBACKMSG_MAIN.S_BUDGETTYPE, TV_PAYOUTBACKMSG_MAIN.S_STATUS, TV_PAYOUTBACKMSG_MAIN.S_USERCODE, "
        + "TV_PAYOUTBACKMSG_MAIN.S_BACKFLAG, TV_PAYOUTBACKMSG_MAIN.S_ISRETURN, TV_PAYOUTBACKMSG_MAIN.S_BCKREASON, TV_PAYOUTBACKMSG_MAIN.S_DEMO, TV_PAYOUTBACKMSG_MAIN.S_HOLD1, "
        + "TV_PAYOUTBACKMSG_MAIN.S_HOLD2, TV_PAYOUTBACKMSG_MAIN.S_HOLD3, TV_PAYOUTBACKMSG_MAIN.S_HOLD4, TV_PAYOUTBACKMSG_MAIN.TS_SYSUPDATE "
        + "FROM TV_PAYOUTBACKMSG_MAIN " ;
        

   /* SQL to select batch data where condition  */
    private static final String SQL_SELECT_BATCH_WHERE =" ( "
        + "S_BIZNO = ?)"
        ;


    /* SQL to update data */
    private static final String SQL_UPDATE =
        "UPDATE TV_PAYOUTBACKMSG_MAIN SET "
        + "S_ORGCODE =?,S_COMMITDATE =?,S_ACCDATE =?,S_FILENAME =?,S_TRECODE =?, "
        + "S_PACKAGENO =?,S_PAYUNIT =?,S_VOUNO =?,S_VOUDATE =?,S_ORITRANO =?, "
        + "S_ORIENTRUSTDATE =?,S_ORIVOUNO =?,S_ORIVOUDATE =?,S_PAYERACCT =?,S_PAYERNAME =?, "
        + "S_PAYERADDR =?,N_MONEY =?,S_PAYEEOPBKNO =?,S_PAYEEBANKNO =?,S_PAYEEACCT =?, "
        + "S_PAYEENAME =?,S_ADDWORD =?,S_BUDGETUNITCODE =?,S_UNITCODENAME =?,S_OFYEAR =?, "
        + "S_TRIMFLAG =?,S_BUDGETTYPE =?,S_STATUS =?,S_USERCODE =?,S_BACKFLAG =?, "
        + "S_ISRETURN =?,S_BCKREASON =?,S_DEMO =?,S_HOLD1 =?,S_HOLD2 =?, "
        + "S_HOLD3 =?,S_HOLD4 =?,TS_SYSUPDATE =CURRENT TIMESTAMP "
        + "WHERE "
        + "S_BIZNO = ?"
        ;

	/* SQL to update data, support LOB */
    private static final String SQL_UPDATE_LOB =
        "UPDATE TV_PAYOUTBACKMSG_MAIN SET "
        + "S_ORGCODE =?, S_COMMITDATE =?, S_ACCDATE =?, S_FILENAME =?, S_TRECODE =?,  "
        + "S_PACKAGENO =?, S_PAYUNIT =?, S_VOUNO =?, S_VOUDATE =?, S_ORITRANO =?,  "
        + "S_ORIENTRUSTDATE =?, S_ORIVOUNO =?, S_ORIVOUDATE =?, S_PAYERACCT =?, S_PAYERNAME =?,  "
        + "S_PAYERADDR =?, N_MONEY =?, S_PAYEEOPBKNO =?, S_PAYEEBANKNO =?, S_PAYEEACCT =?,  "
        + "S_PAYEENAME =?, S_ADDWORD =?, S_BUDGETUNITCODE =?, S_UNITCODENAME =?, S_OFYEAR =?,  "
        + "S_TRIMFLAG =?, S_BUDGETTYPE =?, S_STATUS =?, S_USERCODE =?, S_BACKFLAG =?,  "
        + "S_ISRETURN =?, S_BCKREASON =?, S_DEMO =?, S_HOLD1 =?, S_HOLD2 =?,  "
        + "S_HOLD3 =?, S_HOLD4 =?, TS_SYSUPDATE =CURRENT TIMESTAMP "
        + "WHERE "
        + "S_BIZNO = ?"
        ;	
	
    /* SQL to delete data */
    private static final String SQL_DELETE =
        "DELETE FROM TV_PAYOUTBACKMSG_MAIN " 
        + " WHERE "
        + "S_BIZNO = ?"
        ;


	/**
	*  批量查询 一次最多查询的参数数量
	*/
	
	public static final int FIND_BATCH_SIZE = 150  / 1;
	



   /**
   * Create a new record in Database.
   */
  public void create(IDto _dto,  Connection conn) throws SQLException
  {
     TvPayoutbackmsgMainDto dto = (TvPayoutbackmsgMainDto)_dto ;
     String msgValid = dto.checkValid() ;
     if (msgValid != null)
     	throw new SQLException("插入错误，"+msgValid) ;

     PreparedStatement ps = null;
     try
     {
         ps = conn.prepareStatement(SQL_INSERT);
          ps.setString(1, dto.getSbizno());

          ps.setString(2, dto.getSorgcode());

          ps.setString(3, dto.getScommitdate());

          ps.setString(4, dto.getSaccdate());

          ps.setString(5, dto.getSfilename());

          ps.setString(6, dto.getStrecode());

          ps.setString(7, dto.getSpackageno());

          ps.setString(8, dto.getSpayunit());

          ps.setString(9, dto.getSvouno());

          ps.setString(10, dto.getSvoudate());

          ps.setString(11, dto.getSoritrano());

          ps.setString(12, dto.getSorientrustdate());

          ps.setString(13, dto.getSorivouno());

          ps.setString(14, dto.getSorivoudate());

          ps.setString(15, dto.getSpayeracct());

          ps.setString(16, dto.getSpayername());

          ps.setString(17, dto.getSpayeraddr());

          ps.setBigDecimal(18, dto.getNmoney());

          ps.setString(19, dto.getSpayeeopbkno());

          ps.setString(20, dto.getSpayeebankno());

          ps.setString(21, dto.getSpayeeacct());

          ps.setString(22, dto.getSpayeename());

          ps.setString(23, dto.getSaddword());

          ps.setString(24, dto.getSbudgetunitcode());

          ps.setString(25, dto.getSunitcodename());

          ps.setString(26, dto.getSofyear());

          ps.setString(27, dto.getStrimflag());

          ps.setString(28, dto.getSbudgettype());

          ps.setString(29, dto.getSstatus());

          ps.setString(30, dto.getSusercode());

          ps.setString(31, dto.getSbackflag());

          ps.setString(32, dto.getSisreturn());

          ps.setString(33, dto.getSbckreason());

          ps.setString(34, dto.getSdemo());

          ps.setString(35, dto.getShold1());

          ps.setString(36, dto.getShold2());

          ps.setString(37, dto.getShold3());

          ps.setString(38, dto.getShold4());

          ps.executeUpdate();
     }
     finally
     {
         close(ps);
     }
     return  ;
  }

   /**
   * Create a new record in Database. and return result, only used when having generated key
   */
   public IDto createWithResult(IDto _dto,  Connection conn) throws SQLException
   {
       TvPayoutbackmsgMainDto dto = (TvPayoutbackmsgMainDto)_dto ;
       String msgValid = dto.checkValid() ;
       if (msgValid != null)
           throw new SQLException("插入错误，"+msgValid) ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try
       {
           ps = conn.prepareStatement(SQL_INSERT_WITH_RESULT);
            ps.setString(1, dto.getSbizno());
            ps.setString(2, dto.getSorgcode());
            ps.setString(3, dto.getScommitdate());
            ps.setString(4, dto.getSaccdate());
            ps.setString(5, dto.getSfilename());
            ps.setString(6, dto.getStrecode());
            ps.setString(7, dto.getSpackageno());
            ps.setString(8, dto.getSpayunit());
            ps.setString(9, dto.getSvouno());
            ps.setString(10, dto.getSvoudate());
            ps.setString(11, dto.getSoritrano());
            ps.setString(12, dto.getSorientrustdate());
            ps.setString(13, dto.getSorivouno());
            ps.setString(14, dto.getSorivoudate());
            ps.setString(15, dto.getSpayeracct());
            ps.setString(16, dto.getSpayername());
            ps.setString(17, dto.getSpayeraddr());
            ps.setBigDecimal(18, dto.getNmoney());
            ps.setString(19, dto.getSpayeeopbkno());
            ps.setString(20, dto.getSpayeebankno());
            ps.setString(21, dto.getSpayeeacct());
            ps.setString(22, dto.getSpayeename());
            ps.setString(23, dto.getSaddword());
            ps.setString(24, dto.getSbudgetunitcode());
            ps.setString(25, dto.getSunitcodename());
            ps.setString(26, dto.getSofyear());
            ps.setString(27, dto.getStrimflag());
            ps.setString(28, dto.getSbudgettype());
            ps.setString(29, dto.getSstatus());
            ps.setString(30, dto.getSusercode());
            ps.setString(31, dto.getSbackflag());
            ps.setString(32, dto.getSisreturn());
            ps.setString(33, dto.getSbckreason());
            ps.setString(34, dto.getSdemo());
            ps.setString(35, dto.getShold1());
            ps.setString(36, dto.getShold2());
            ps.setString(37, dto.getShold3());
            ps.setString(38, dto.getShold4());
            rs = ps.executeQuery();
           List results = getResults(rs);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }
        finally
        {
           close(ps);
           close(rs);
        }
   }

 	/**
	* Create batch new record in Database. it only create the first layer dto
	*/
    public void create(IDto[] _dtos, Connection conn)    throws SQLException
    {
        TvPayoutbackmsgMainDto dto  ;
     	for (int i= 0; i< _dtos.length ; i++)
 	    {
 		    dto  = (TvPayoutbackmsgMainDto)_dtos[i] ; 
    	    String msgValid = dto.checkValid() ;
        	if (msgValid != null)
     	    	throw new SQLException("插入错误，"+msgValid) ;
     	}
 	    PreparedStatement ps = null;
        try
        {
            ps = conn.prepareStatement(SQL_INSERT);
       
     	    for (int i= 0; i< _dtos.length ; i++)
     	    {
 	    	    dto  = (TvPayoutbackmsgMainDto)_dtos[i] ; 
  
               ps.setString(1, dto.getSbizno());
  
               ps.setString(2, dto.getSorgcode());
  
               ps.setString(3, dto.getScommitdate());
  
               ps.setString(4, dto.getSaccdate());
  
               ps.setString(5, dto.getSfilename());
  
               ps.setString(6, dto.getStrecode());
  
               ps.setString(7, dto.getSpackageno());
  
               ps.setString(8, dto.getSpayunit());
  
               ps.setString(9, dto.getSvouno());
  
               ps.setString(10, dto.getSvoudate());
  
               ps.setString(11, dto.getSoritrano());
  
               ps.setString(12, dto.getSorientrustdate());
  
               ps.setString(13, dto.getSorivouno());
  
               ps.setString(14, dto.getSorivoudate());
  
               ps.setString(15, dto.getSpayeracct());
  
               ps.setString(16, dto.getSpayername());
  
               ps.setString(17, dto.getSpayeraddr());
  
               ps.setBigDecimal(18, dto.getNmoney());
  
               ps.setString(19, dto.getSpayeeopbkno());
  
               ps.setString(20, dto.getSpayeebankno());
  
               ps.setString(21, dto.getSpayeeacct());
  
               ps.setString(22, dto.getSpayeename());
  
               ps.setString(23, dto.getSaddword());
  
               ps.setString(24, dto.getSbudgetunitcode());
  
               ps.setString(25, dto.getSunitcodename());
  
               ps.setString(26, dto.getSofyear());
  
               ps.setString(27, dto.getStrimflag());
  
               ps.setString(28, dto.getSbudgettype());
  
               ps.setString(29, dto.getSstatus());
  
               ps.setString(30, dto.getSusercode());
  
               ps.setString(31, dto.getSbackflag());
  
               ps.setString(32, dto.getSisreturn());
  
               ps.setString(33, dto.getSbckreason());
  
               ps.setString(34, dto.getSdemo());
  
               ps.setString(35, dto.getShold1());
  
               ps.setString(36, dto.getShold2());
  
               ps.setString(37, dto.getShold3());
  
               ps.setString(38, dto.getShold4());
                ps.addBatch(); 
           }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }
        return  ;
   }




     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto find(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TvPayoutbackmsgMainPK pk = (TvPayoutbackmsgMainPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT);
           ps.setString(1, pk.getSbizno());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }

     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto findForUpdate(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TvPayoutbackmsgMainPK pk = (TvPayoutbackmsgMainPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT_FOR_UPDATE);
           ps.setString(1, pk.getSbizno());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }
  
     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto[] find(IPK[] _pks, Connection conn, boolean isLobSupport) throws SQLException
    {
        TvPayoutbackmsgMainPK pk ;
        
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TvPayoutbackmsgMainPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 
        PreparedStatement ps = null;
        ResultSet rs = null;
        
         
        List results = new ArrayList();  
		for (int iBegin = 0; iBegin < pks.size(); iBegin += FIND_BATCH_SIZE)   //
		{
        
            try {
          		int iFindBatchSize = pks.size() - iBegin ;
	    		if (iFindBatchSize >FIND_BATCH_SIZE )
		    		iFindBatchSize = FIND_BATCH_SIZE ;
		  
        	    StringBuffer sb = new StringBuffer() ;
            	sb.append(SQL_SELECT_BATCH).append(" WHERE ").append(SQL_SELECT_BATCH_WHERE);

	    		for (int i = iBegin+1; i < iBegin + iFindBatchSize; i++)
		    	{
			    	sb.append("OR").append(SQL_SELECT_BATCH_WHERE) ;
    			}
        	
                ps = conn.prepareStatement(sb.toString());

                for (int i = iBegin;i < iBegin + iFindBatchSize; i++)
                {
                    pk  = (TvPayoutbackmsgMainPK)(pks.get(i)) ; 
                   ps.setString((i-iBegin)*1+1, pk.getSbizno());

			
	            }
                rs = ps.executeQuery();
                results.addAll(getResults(rs,0,isLobSupport)); //
            }finally {
                close(rs);
                close(ps);
            }
        }
        if (results!=null && results.size() > 0)
        {
            TvPayoutbackmsgMainDto[] dtos = new TvPayoutbackmsgMainDto[0];
		    dtos = (TvPayoutbackmsgMainDto[]) results.toArray(dtos) ;
		    return dtos ;
        }
        else
        {
            return null;
        }
    }

  
  
  



    /**
	    * 得到查询语句
	    */
    public String getSelectSQL() 
    {
	        return SQL_SELECT_BATCH_SCROLLABLE ;
    }
   

	public List getResults(ResultSet rs) throws SQLException
    {
    	return getResults(rs, 0) ;
    }
  

    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize) throws SQLException
    {
		return getResults(rs,maxSize,false);
	}
    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize, boolean isLobSupport) throws SQLException
    {
        List results = new ArrayList();
        String str ;
        Reader reader;
        Clob clob;
        char[] chars ;
        
        while (rs.next())
         {
             TvPayoutbackmsgMainDto  dto = new  TvPayoutbackmsgMainDto ();
             //S_BIZNO
             str = rs.getString("S_BIZNO");
             if (str == null)
                dto.setSbizno(null);
             else
                dto.setSbizno(str.trim());

             //S_ORGCODE
             str = rs.getString("S_ORGCODE");
             if (str == null)
                dto.setSorgcode(null);
             else
                dto.setSorgcode(str.trim());

             //S_COMMITDATE
             str = rs.getString("S_COMMITDATE");
             if (str == null)
                dto.setScommitdate(null);
             else
                dto.setScommitdate(str.trim());

             //S_ACCDATE
             str = rs.getString("S_ACCDATE");
             if (str == null)
                dto.setSaccdate(null);
             else
                dto.setSaccdate(str.trim());

             //S_FILENAME
             str = rs.getString("S_FILENAME");
             if (str == null)
                dto.setSfilename(null);
             else
                dto.setSfilename(str.trim());

             //S_TRECODE
             str = rs.getString("S_TRECODE");
             if (str == null)
                dto.setStrecode(null);
             else
                dto.setStrecode(str.trim());

             //S_PACKAGENO
             str = rs.getString("S_PACKAGENO");
             if (str == null)
                dto.setSpackageno(null);
             else
                dto.setSpackageno(str.trim());

             //S_PAYUNIT
             str = rs.getString("S_PAYUNIT");
             if (str == null)
                dto.setSpayunit(null);
             else
                dto.setSpayunit(str.trim());

             //S_VOUNO
             str = rs.getString("S_VOUNO");
             if (str == null)
                dto.setSvouno(null);
             else
                dto.setSvouno(str.trim());

             //S_VOUDATE
             str = rs.getString("S_VOUDATE");
             if (str == null)
                dto.setSvoudate(null);
             else
                dto.setSvoudate(str.trim());

             //S_ORITRANO
             str = rs.getString("S_ORITRANO");
             if (str == null)
                dto.setSoritrano(null);
             else
                dto.setSoritrano(str.trim());

             //S_ORIENTRUSTDATE
             str = rs.getString("S_ORIENTRUSTDATE");
             if (str == null)
                dto.setSorientrustdate(null);
             else
                dto.setSorientrustdate(str.trim());

             //S_ORIVOUNO
             str = rs.getString("S_ORIVOUNO");
             if (str == null)
                dto.setSorivouno(null);
             else
                dto.setSorivouno(str.trim());

             //S_ORIVOUDATE
             str = rs.getString("S_ORIVOUDATE");
             if (str == null)
                dto.setSorivoudate(null);
             else
                dto.setSorivoudate(str.trim());

             //S_PAYERACCT
             str = rs.getString("S_PAYERACCT");
             if (str == null)
                dto.setSpayeracct(null);
             else
                dto.setSpayeracct(str.trim());

             //S_PAYERNAME
             str = rs.getString("S_PAYERNAME");
             if (str == null)
                dto.setSpayername(null);
             else
                dto.setSpayername(str.trim());

             //S_PAYERADDR
             str = rs.getString("S_PAYERADDR");
             if (str == null)
                dto.setSpayeraddr(null);
             else
                dto.setSpayeraddr(str.trim());

             //N_MONEY
           dto.setNmoney(rs.getBigDecimal("N_MONEY"));

             //S_PAYEEOPBKNO
             str = rs.getString("S_PAYEEOPBKNO");
             if (str == null)
                dto.setSpayeeopbkno(null);
             else
                dto.setSpayeeopbkno(str.trim());

             //S_PAYEEBANKNO
             str = rs.getString("S_PAYEEBANKNO");
             if (str == null)
                dto.setSpayeebankno(null);
             else
                dto.setSpayeebankno(str.trim());

             //S_PAYEEACCT
             str = rs.getString("S_PAYEEACCT");
             if (str == null)
                dto.setSpayeeacct(null);
             else
                dto.setSpayeeacct(str.trim());

             //S_PAYEENAME
             str = rs.getString("S_PAYEENAME");
             if (str == null)
                dto.setSpayeename(null);
             else
                dto.setSpayeename(str.trim());

             //S_ADDWORD
             str = rs.getString("S_ADDWORD");
             if (str == null)
                dto.setSaddword(null);
             else
                dto.setSaddword(str.trim());

             //S_BUDGETUNITCODE
             str = rs.getString("S_BUDGETUNITCODE");
             if (str == null)
                dto.setSbudgetunitcode(null);
             else
                dto.setSbudgetunitcode(str.trim());

             //S_UNITCODENAME
             str = rs.getString("S_UNITCODENAME");
             if (str == null)
                dto.setSunitcodename(null);
             else
                dto.setSunitcodename(str.trim());

             //S_OFYEAR
             str = rs.getString("S_OFYEAR");
             if (str == null)
                dto.setSofyear(null);
             else
                dto.setSofyear(str.trim());

             //S_TRIMFLAG
             str = rs.getString("S_TRIMFLAG");
             if (str == null)
                dto.setStrimflag(null);
             else
                dto.setStrimflag(str.trim());

             //S_BUDGETTYPE
             str = rs.getString("S_BUDGETTYPE");
             if (str == null)
                dto.setSbudgettype(null);
             else
                dto.setSbudgettype(str.trim());

             //S_STATUS
             str = rs.getString("S_STATUS");
             if (str == null)
                dto.setSstatus(null);
             else
                dto.setSstatus(str.trim());

             //S_USERCODE
             str = rs.getString("S_USERCODE");
             if (str == null)
                dto.setSusercode(null);
             else
                dto.setSusercode(str.trim());

             //S_BACKFLAG
             str = rs.getString("S_BACKFLAG");
             if (str == null)
                dto.setSbackflag(null);
             else
                dto.setSbackflag(str.trim());

             //S_ISRETURN
             str = rs.getString("S_ISRETURN");
             if (str == null)
                dto.setSisreturn(null);
             else
                dto.setSisreturn(str.trim());

             //S_BCKREASON
             str = rs.getString("S_BCKREASON");
             if (str == null)
                dto.setSbckreason(null);
             else
                dto.setSbckreason(str.trim());

             //S_DEMO
             str = rs.getString("S_DEMO");
             if (str == null)
                dto.setSdemo(null);
             else
                dto.setSdemo(str.trim());

             //S_HOLD1
             str = rs.getString("S_HOLD1");
             if (str == null)
                dto.setShold1(null);
             else
                dto.setShold1(str.trim());

             //S_HOLD2
             str = rs.getString("S_HOLD2");
             if (str == null)
                dto.setShold2(null);
             else
                dto.setShold2(str.trim());

             //S_HOLD3
             str = rs.getString("S_HOLD3");
             if (str == null)
                dto.setShold3(null);
             else
                dto.setShold3(str.trim());

             //S_HOLD4
             str = rs.getString("S_HOLD4");
             if (str == null)
                dto.setShold4(null);
             else
                dto.setShold4(str.trim());

             //TS_SYSUPDATE
           dto.setTssysupdate(rs.getTimestamp("TS_SYSUPDATE"));



            results.add(dto);
            
            if(maxSize >0  && results.size() == maxSize)
            {
            	break ;
            }
            
         }
        
		return results ;
    }

  /**
   * populate resultSet as cclass
   */
  protected IDto[] getChildrenResults(ResultSet rs ,Class _dto) throws SQLException{

      throw new SQLException("本dto没有与其它dto相关联，无法应用此方法");
  }


     /**
     * Update a record in Database.
     */

    public void update(IDto _dto, Connection conn, boolean isLobSupport) throws SQLException
    {
        String msgValid = _dto.checkValid() ;
        if (msgValid != null)
            throw new SQLException("修改错误，"+msgValid) ;
        TvPayoutbackmsgMainDto dto = (TvPayoutbackmsgMainDto)_dto ;
        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            int pos = 1;
            //S_ORGCODE
            ps.setString(pos, dto.getSorgcode());
            pos++;

            //S_COMMITDATE
            ps.setString(pos, dto.getScommitdate());
            pos++;

            //S_ACCDATE
            ps.setString(pos, dto.getSaccdate());
            pos++;

            //S_FILENAME
            ps.setString(pos, dto.getSfilename());
            pos++;

            //S_TRECODE
            ps.setString(pos, dto.getStrecode());
            pos++;

            //S_PACKAGENO
            ps.setString(pos, dto.getSpackageno());
            pos++;

            //S_PAYUNIT
            ps.setString(pos, dto.getSpayunit());
            pos++;

            //S_VOUNO
            ps.setString(pos, dto.getSvouno());
            pos++;

            //S_VOUDATE
            ps.setString(pos, dto.getSvoudate());
            pos++;

            //S_ORITRANO
            ps.setString(pos, dto.getSoritrano());
            pos++;

            //S_ORIENTRUSTDATE
            ps.setString(pos, dto.getSorientrustdate());
            pos++;

            //S_ORIVOUNO
            ps.setString(pos, dto.getSorivouno());
            pos++;

            //S_ORIVOUDATE
            ps.setString(pos, dto.getSorivoudate());
            pos++;

            //S_PAYERACCT
            ps.setString(pos, dto.getSpayeracct());
            pos++;

            //S_PAYERNAME
            ps.setString(pos, dto.getSpayername());
            pos++;

            //S_PAYERADDR
            ps.setString(pos, dto.getSpayeraddr());
            pos++;

            //N_MONEY
            ps.setBigDecimal(pos, dto.getNmoney());
            pos++;

            //S_PAYEEOPBKNO
            ps.setString(pos, dto.getSpayeeopbkno());
            pos++;

            //S_PAYEEBANKNO
            ps.setString(pos, dto.getSpayeebankno());
            pos++;

            //S_PAYEEACCT
            ps.setString(pos, dto.getSpayeeacct());
            pos++;

            //S_PAYEENAME
            ps.setString(pos, dto.getSpayeename());
            pos++;

            //S_ADDWORD
            ps.setString(pos, dto.getSaddword());
            pos++;

            //S_BUDGETUNITCODE
            ps.setString(pos, dto.getSbudgetunitcode());
            pos++;

            //S_UNITCODENAME
            ps.setString(pos, dto.getSunitcodename());
            pos++;

            //S_OFYEAR
            ps.setString(pos, dto.getSofyear());
            pos++;

            //S_TRIMFLAG
            ps.setString(pos, dto.getStrimflag());
            pos++;

            //S_BUDGETTYPE
            ps.setString(pos, dto.getSbudgettype());
            pos++;

            //S_STATUS
            ps.setString(pos, dto.getSstatus());
            pos++;

            //S_USERCODE
            ps.setString(pos, dto.getSusercode());
            pos++;

            //S_BACKFLAG
            ps.setString(pos, dto.getSbackflag());
            pos++;

            //S_ISRETURN
            ps.setString(pos, dto.getSisreturn());
            pos++;

            //S_BCKREASON
            ps.setString(pos, dto.getSbckreason());
            pos++;

            //S_DEMO
            ps.setString(pos, dto.getSdemo());
            pos++;

            //S_HOLD1
            ps.setString(pos, dto.getShold1());
            pos++;

            //S_HOLD2
            ps.setString(pos, dto.getShold2());
            pos++;

            //S_HOLD3
            ps.setString(pos, dto.getShold3());
            pos++;

            //S_HOLD4
            ps.setString(pos, dto.getShold4());
            pos++;

            //TS_SYSUPDATE

           //S_BIZNO
           ps.setString(pos, dto.getSbizno());
           pos++;
           ps.executeUpdate();
        }
        finally
        {
           close(ps);
        }

        return ;
    }
    
    
    
      /**
     * Update batch record in Database.
     */

    public void update(IDto[] _dtos, Connection conn, boolean isLobSupport) throws SQLException
    {
     
     	 TvPayoutbackmsgMainDto dto  ;
         for (int i= 0; i< _dtos.length ; i++)
         {
            dto  = (TvPayoutbackmsgMainDto)_dtos[i] ; 
            String msgValid = dto.checkValid() ;
            if (msgValid != null)
                throw new SQLException("修改错误，"+msgValid) ;
         }

        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            
            for (int i= 0; i< _dtos.length ; i++)
            {
                dto  = (TvPayoutbackmsgMainDto)_dtos[i] ; 
                int pos = 1;
                //S_ORGCODE
                 ps.setString(pos, dto.getSorgcode());
                pos++;

                //S_COMMITDATE
                 ps.setString(pos, dto.getScommitdate());
                pos++;

                //S_ACCDATE
                 ps.setString(pos, dto.getSaccdate());
                pos++;

                //S_FILENAME
                 ps.setString(pos, dto.getSfilename());
                pos++;

                //S_TRECODE
                 ps.setString(pos, dto.getStrecode());
                pos++;

                //S_PACKAGENO
                 ps.setString(pos, dto.getSpackageno());
                pos++;

                //S_PAYUNIT
                 ps.setString(pos, dto.getSpayunit());
                pos++;

                //S_VOUNO
                 ps.setString(pos, dto.getSvouno());
                pos++;

                //S_VOUDATE
                 ps.setString(pos, dto.getSvoudate());
                pos++;

                //S_ORITRANO
                 ps.setString(pos, dto.getSoritrano());
                pos++;

                //S_ORIENTRUSTDATE
                 ps.setString(pos, dto.getSorientrustdate());
                pos++;

                //S_ORIVOUNO
                 ps.setString(pos, dto.getSorivouno());
                pos++;

                //S_ORIVOUDATE
                 ps.setString(pos, dto.getSorivoudate());
                pos++;

                //S_PAYERACCT
                 ps.setString(pos, dto.getSpayeracct());
                pos++;

                //S_PAYERNAME
                 ps.setString(pos, dto.getSpayername());
                pos++;

                //S_PAYERADDR
                 ps.setString(pos, dto.getSpayeraddr());
                pos++;

                //N_MONEY
                 ps.setBigDecimal(pos, dto.getNmoney());
                pos++;

                //S_PAYEEOPBKNO
                 ps.setString(pos, dto.getSpayeeopbkno());
                pos++;

                //S_PAYEEBANKNO
                 ps.setString(pos, dto.getSpayeebankno());
                pos++;

                //S_PAYEEACCT
                 ps.setString(pos, dto.getSpayeeacct());
                pos++;

                //S_PAYEENAME
                 ps.setString(pos, dto.getSpayeename());
                pos++;

                //S_ADDWORD
                 ps.setString(pos, dto.getSaddword());
                pos++;

                //S_BUDGETUNITCODE
                 ps.setString(pos, dto.getSbudgetunitcode());
                pos++;

                //S_UNITCODENAME
                 ps.setString(pos, dto.getSunitcodename());
                pos++;

                //S_OFYEAR
                 ps.setString(pos, dto.getSofyear());
                pos++;

                //S_TRIMFLAG
                 ps.setString(pos, dto.getStrimflag());
                pos++;

                //S_BUDGETTYPE
                 ps.setString(pos, dto.getSbudgettype());
                pos++;

                //S_STATUS
                 ps.setString(pos, dto.getSstatus());
                pos++;

                //S_USERCODE
                 ps.setString(pos, dto.getSusercode());
                pos++;

                //S_BACKFLAG
                 ps.setString(pos, dto.getSbackflag());
                pos++;

                //S_ISRETURN
                 ps.setString(pos, dto.getSisreturn());
                pos++;

                //S_BCKREASON
                 ps.setString(pos, dto.getSbckreason());
                pos++;

                //S_DEMO
                 ps.setString(pos, dto.getSdemo());
                pos++;

                //S_HOLD1
                 ps.setString(pos, dto.getShold1());
                pos++;

                //S_HOLD2
                 ps.setString(pos, dto.getShold2());
                pos++;

                //S_HOLD3
                 ps.setString(pos, dto.getShold3());
                pos++;

                //S_HOLD4
                 ps.setString(pos, dto.getShold4());
                pos++;

                //TS_SYSUPDATE
 
               //S_BIZNO
               ps.setString(pos, dto.getSbizno());
               pos++;
		       ps.addBatch(); 
	       }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }

        return ;
    }



    /**
     * delete a record in Database.
     */
    public void delete(IPK _pk, Connection conn) throws SQLException
    {
       
      
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return ;
        //	throw new SQLException("删除错误，"+msgValid) ;
       }
       TvPayoutbackmsgMainPK pk = (TvPayoutbackmsgMainPK)_pk ;


       PreparedStatement ps = null;
       try {
           ps = conn.prepareStatement(SQL_DELETE);
           ps.setString(1, pk.getSbizno());
           ps.executeUpdate();
        }
        finally
        {
            close(ps);
        }
    }


     /**
     * delete batch record in Database.
     */
    public void delete(IPK[] _pks, Connection conn) throws SQLException
    {
        TvPayoutbackmsgMainPK pk ;
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TvPayoutbackmsgMainPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 		
 		
        PreparedStatement ps = null;
        try
         {
            ps = conn.prepareStatement(SQL_DELETE);
             
       		for (int i= 0; i< pks.size() ; i++)
       		{
       			pk  = (TvPayoutbackmsgMainPK)(pks.get(i)) ; 
                ps.setString(1, pk.getSbizno());
		        ps.addBatch() ;
			}
            ps.executeBatch();
        }
        finally
        {
            close(ps);
        }      
    }



    /**
    * Check IChangerNumber or UpdateTimeStamp.
    */
    public void check(IDto _dto, Connection conn) throws SQLException
    {
                                                                                                                  		throw new SQLException("数据库表：TV_PAYOUTBACKMSG_MAIN没有检查修改的字段");
    }


	/**
	* synchronize values between parent and children
	*/
    public void syncToChildren(IDto _dto)     throws SQLException{
       throw new SQLException("本记录没有与其它记录相关联，无法进行主记录与明细记录之间的数据同步。");
  }

	/**
	*get children
	*/
    public IDto[] findChildren (IDto _dto, Connection conn, boolean isLobSupport)     throws SQLException{
         throw new SQLException("本dto没有与其它dto相关联，无法得到子dto");
  }    
    
  /**
   *return the children IDto class
   * @return Class
   */
  public Class getChildrenClass(){
	  return null;
  };    



    /**
     * Close JDBC Statement.
     * @param stmt  Statement to be closed.
     */
    private void close(Statement stmt)
    {
        if (stmt != null)
        {
            try
            {
                stmt.close();
            }
            catch(SQLException    e)
            {}
        }
    }

    /**
     * Close JDBC ResultSet.
     * @param rs  ResultSet to be closed.
     */
    private void close(ResultSet rs)
    {
        if (rs != null)
        {
            try
            {
                rs.close();
            }catch(SQLException  e){}
        }
    }
}
