    



package com.cfcc.itfe.persistence.dao;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.Statement;
import java.sql.SQLException;
import java.sql.Date;
import java.sql.Time;
import java.sql.Timestamp ;
import java.sql.Clob;

import com.cfcc.jaf.persistence.jaform.parent.* ;

import java.math.BigDecimal;

import java.util.List;
import java.util.ArrayList;
import java.util.HashMap;

import java.io.StringReader;
import java.io.Reader;
import java.io.IOException;

import com.cfcc.itfe.persistence.dto.TbsTvInCorrhandbookDto ;
import com.cfcc.itfe.persistence.pk.TbsTvInCorrhandbookPK ;


/**
 * <p>Title: DAO of table: TBS_TV_IN_CORRHANDBOOK</p>
 * <p>Description: Data Access Object  </p>
 * <p>Copyright: Copyright (c) 2005</p>
 * <p>Company: CFCC </p>
 * <p>Generation Time: 2014-12-25 15:28:57 </p>
 *  Do NOT change this file by hand.
 *  This java source file is generated by JAF_ORM .
 *  dao.vm timestamp: 2007-04-06 08:30:00
 ******************************************************
 * <p> <b>change record</b><br>
 *  make dao methods simply.<br>
 *  especially make update,delete methods simply.<br>
 *  remove "..withCheck" method,withCheck will impl in JAFOrmTemplate.<br>
 *  remove "delete(IDto _dto,..)" method.<br></p>
 * @author win7
 */

public class TbsTvInCorrhandbookDao  implements IDao
{


    /* SQL to insert data */
    private static final String SQL_INSERT =
        "INSERT INTO TBS_TV_IN_CORRHANDBOOK ("
          + "I_VOUSRLNO,S_ELECVOUNO,S_CORRVOUNO,D_ACCEPT,D_ACCT"
          + ",D_VOUCHER,S_ORIPAYEETRECODE,S_ORIAIMTRECODE,S_ORITAXORGCODE,S_ORIBDGSBTCODE"
          + ",C_ORIBDGLEVEL,C_ORIBDGKIND,S_ORIASTFLAG,F_ORICORRAMT,S_CURPAYEETRECODE"
          + ",S_CURAIMTRECODE,S_CURTAXORGCODE,S_CURBDGSBTCODE,C_CURBDGLEVEL,C_CURBDGKIND"
          + ",S_CURASTFLAG,F_CURCORRAMT,S_REASONCODE,C_TRIMFLAG,S_PACKAGENO"
          + ",S_STATUS,S_FILENAME,S_BOOKORGCODE,TS_SYSUPDATE"
        + ") VALUES ("
        + "DEFAULT ,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,CURRENT TIMESTAMP )";


    private static final String SQL_INSERT_WITH_RESULT = "SELECT * FROM FINAL TABLE( " + SQL_INSERT + " )";
    
    /* SQL to select data */
    private static final String SQL_SELECT =
        "SELECT "
        + "TBS_TV_IN_CORRHANDBOOK.I_VOUSRLNO, TBS_TV_IN_CORRHANDBOOK.S_ELECVOUNO, TBS_TV_IN_CORRHANDBOOK.S_CORRVOUNO, TBS_TV_IN_CORRHANDBOOK.D_ACCEPT, TBS_TV_IN_CORRHANDBOOK.D_ACCT, "
        + "TBS_TV_IN_CORRHANDBOOK.D_VOUCHER, TBS_TV_IN_CORRHANDBOOK.S_ORIPAYEETRECODE, TBS_TV_IN_CORRHANDBOOK.S_ORIAIMTRECODE, TBS_TV_IN_CORRHANDBOOK.S_ORITAXORGCODE, TBS_TV_IN_CORRHANDBOOK.S_ORIBDGSBTCODE, "
        + "TBS_TV_IN_CORRHANDBOOK.C_ORIBDGLEVEL, TBS_TV_IN_CORRHANDBOOK.C_ORIBDGKIND, TBS_TV_IN_CORRHANDBOOK.S_ORIASTFLAG, TBS_TV_IN_CORRHANDBOOK.F_ORICORRAMT, TBS_TV_IN_CORRHANDBOOK.S_CURPAYEETRECODE, "
        + "TBS_TV_IN_CORRHANDBOOK.S_CURAIMTRECODE, TBS_TV_IN_CORRHANDBOOK.S_CURTAXORGCODE, TBS_TV_IN_CORRHANDBOOK.S_CURBDGSBTCODE, TBS_TV_IN_CORRHANDBOOK.C_CURBDGLEVEL, TBS_TV_IN_CORRHANDBOOK.C_CURBDGKIND, "
        + "TBS_TV_IN_CORRHANDBOOK.S_CURASTFLAG, TBS_TV_IN_CORRHANDBOOK.F_CURCORRAMT, TBS_TV_IN_CORRHANDBOOK.S_REASONCODE, TBS_TV_IN_CORRHANDBOOK.C_TRIMFLAG, TBS_TV_IN_CORRHANDBOOK.S_PACKAGENO, "
        + "TBS_TV_IN_CORRHANDBOOK.S_STATUS, TBS_TV_IN_CORRHANDBOOK.S_FILENAME, TBS_TV_IN_CORRHANDBOOK.S_BOOKORGCODE, TBS_TV_IN_CORRHANDBOOK.TS_SYSUPDATE "
        + "FROM TBS_TV_IN_CORRHANDBOOK "
        +" WHERE " 
        + "I_VOUSRLNO = ?"
        ;
    /* SQL to select for update data */
    private static final String SQL_SELECT_FOR_UPDATE =
        "SELECT "
        + "TBS_TV_IN_CORRHANDBOOK.I_VOUSRLNO, TBS_TV_IN_CORRHANDBOOK.S_ELECVOUNO, TBS_TV_IN_CORRHANDBOOK.S_CORRVOUNO, TBS_TV_IN_CORRHANDBOOK.D_ACCEPT, TBS_TV_IN_CORRHANDBOOK.D_ACCT, "
        + "TBS_TV_IN_CORRHANDBOOK.D_VOUCHER, TBS_TV_IN_CORRHANDBOOK.S_ORIPAYEETRECODE, TBS_TV_IN_CORRHANDBOOK.S_ORIAIMTRECODE, TBS_TV_IN_CORRHANDBOOK.S_ORITAXORGCODE, TBS_TV_IN_CORRHANDBOOK.S_ORIBDGSBTCODE, "
        + "TBS_TV_IN_CORRHANDBOOK.C_ORIBDGLEVEL, TBS_TV_IN_CORRHANDBOOK.C_ORIBDGKIND, TBS_TV_IN_CORRHANDBOOK.S_ORIASTFLAG, TBS_TV_IN_CORRHANDBOOK.F_ORICORRAMT, TBS_TV_IN_CORRHANDBOOK.S_CURPAYEETRECODE, "
        + "TBS_TV_IN_CORRHANDBOOK.S_CURAIMTRECODE, TBS_TV_IN_CORRHANDBOOK.S_CURTAXORGCODE, TBS_TV_IN_CORRHANDBOOK.S_CURBDGSBTCODE, TBS_TV_IN_CORRHANDBOOK.C_CURBDGLEVEL, TBS_TV_IN_CORRHANDBOOK.C_CURBDGKIND, "
        + "TBS_TV_IN_CORRHANDBOOK.S_CURASTFLAG, TBS_TV_IN_CORRHANDBOOK.F_CURCORRAMT, TBS_TV_IN_CORRHANDBOOK.S_REASONCODE, TBS_TV_IN_CORRHANDBOOK.C_TRIMFLAG, TBS_TV_IN_CORRHANDBOOK.S_PACKAGENO, "
        + "TBS_TV_IN_CORRHANDBOOK.S_STATUS, TBS_TV_IN_CORRHANDBOOK.S_FILENAME, TBS_TV_IN_CORRHANDBOOK.S_BOOKORGCODE, TBS_TV_IN_CORRHANDBOOK.TS_SYSUPDATE "
        + "FROM TBS_TV_IN_CORRHANDBOOK "
        +" WHERE " 
        + "I_VOUSRLNO = ? FOR UPDATE"
        ;

      /* SQL to select data (SCROLLABLE)*/
     private static final String SQL_SELECT_BATCH_SCROLLABLE =
        "SELECT "
        + "  TBS_TV_IN_CORRHANDBOOK.I_VOUSRLNO  , TBS_TV_IN_CORRHANDBOOK.S_ELECVOUNO  , TBS_TV_IN_CORRHANDBOOK.S_CORRVOUNO  , TBS_TV_IN_CORRHANDBOOK.D_ACCEPT  , TBS_TV_IN_CORRHANDBOOK.D_ACCT "
        + " , TBS_TV_IN_CORRHANDBOOK.D_VOUCHER  , TBS_TV_IN_CORRHANDBOOK.S_ORIPAYEETRECODE  , TBS_TV_IN_CORRHANDBOOK.S_ORIAIMTRECODE  , TBS_TV_IN_CORRHANDBOOK.S_ORITAXORGCODE  , TBS_TV_IN_CORRHANDBOOK.S_ORIBDGSBTCODE "
        + " , TBS_TV_IN_CORRHANDBOOK.C_ORIBDGLEVEL  , TBS_TV_IN_CORRHANDBOOK.C_ORIBDGKIND  , TBS_TV_IN_CORRHANDBOOK.S_ORIASTFLAG  , TBS_TV_IN_CORRHANDBOOK.F_ORICORRAMT  , TBS_TV_IN_CORRHANDBOOK.S_CURPAYEETRECODE "
        + " , TBS_TV_IN_CORRHANDBOOK.S_CURAIMTRECODE  , TBS_TV_IN_CORRHANDBOOK.S_CURTAXORGCODE  , TBS_TV_IN_CORRHANDBOOK.S_CURBDGSBTCODE  , TBS_TV_IN_CORRHANDBOOK.C_CURBDGLEVEL  , TBS_TV_IN_CORRHANDBOOK.C_CURBDGKIND "
        + " , TBS_TV_IN_CORRHANDBOOK.S_CURASTFLAG  , TBS_TV_IN_CORRHANDBOOK.F_CURCORRAMT  , TBS_TV_IN_CORRHANDBOOK.S_REASONCODE  , TBS_TV_IN_CORRHANDBOOK.C_TRIMFLAG  , TBS_TV_IN_CORRHANDBOOK.S_PACKAGENO "
        + " , TBS_TV_IN_CORRHANDBOOK.S_STATUS  , TBS_TV_IN_CORRHANDBOOK.S_FILENAME  , TBS_TV_IN_CORRHANDBOOK.S_BOOKORGCODE  , TBS_TV_IN_CORRHANDBOOK.TS_SYSUPDATE "
        + "FROM TBS_TV_IN_CORRHANDBOOK ";



    /* SQL to select batch data */
    private static final String SQL_SELECT_BATCH =
        "SELECT "
        + "TBS_TV_IN_CORRHANDBOOK.I_VOUSRLNO, TBS_TV_IN_CORRHANDBOOK.S_ELECVOUNO, TBS_TV_IN_CORRHANDBOOK.S_CORRVOUNO, TBS_TV_IN_CORRHANDBOOK.D_ACCEPT, TBS_TV_IN_CORRHANDBOOK.D_ACCT, "
        + "TBS_TV_IN_CORRHANDBOOK.D_VOUCHER, TBS_TV_IN_CORRHANDBOOK.S_ORIPAYEETRECODE, TBS_TV_IN_CORRHANDBOOK.S_ORIAIMTRECODE, TBS_TV_IN_CORRHANDBOOK.S_ORITAXORGCODE, TBS_TV_IN_CORRHANDBOOK.S_ORIBDGSBTCODE, "
        + "TBS_TV_IN_CORRHANDBOOK.C_ORIBDGLEVEL, TBS_TV_IN_CORRHANDBOOK.C_ORIBDGKIND, TBS_TV_IN_CORRHANDBOOK.S_ORIASTFLAG, TBS_TV_IN_CORRHANDBOOK.F_ORICORRAMT, TBS_TV_IN_CORRHANDBOOK.S_CURPAYEETRECODE, "
        + "TBS_TV_IN_CORRHANDBOOK.S_CURAIMTRECODE, TBS_TV_IN_CORRHANDBOOK.S_CURTAXORGCODE, TBS_TV_IN_CORRHANDBOOK.S_CURBDGSBTCODE, TBS_TV_IN_CORRHANDBOOK.C_CURBDGLEVEL, TBS_TV_IN_CORRHANDBOOK.C_CURBDGKIND, "
        + "TBS_TV_IN_CORRHANDBOOK.S_CURASTFLAG, TBS_TV_IN_CORRHANDBOOK.F_CURCORRAMT, TBS_TV_IN_CORRHANDBOOK.S_REASONCODE, TBS_TV_IN_CORRHANDBOOK.C_TRIMFLAG, TBS_TV_IN_CORRHANDBOOK.S_PACKAGENO, "
        + "TBS_TV_IN_CORRHANDBOOK.S_STATUS, TBS_TV_IN_CORRHANDBOOK.S_FILENAME, TBS_TV_IN_CORRHANDBOOK.S_BOOKORGCODE, TBS_TV_IN_CORRHANDBOOK.TS_SYSUPDATE "
        + "FROM TBS_TV_IN_CORRHANDBOOK " ;
        

   /* SQL to select batch data where condition  */
    private static final String SQL_SELECT_BATCH_WHERE =" ( "
        + "I_VOUSRLNO = ?)"
        ;


    /* SQL to update data */
    private static final String SQL_UPDATE =
        "UPDATE TBS_TV_IN_CORRHANDBOOK SET "
        + "S_ELECVOUNO =?,S_CORRVOUNO =?,D_ACCEPT =?,D_ACCT =?,D_VOUCHER =?, "
        + "S_ORIPAYEETRECODE =?,S_ORIAIMTRECODE =?,S_ORITAXORGCODE =?,S_ORIBDGSBTCODE =?,C_ORIBDGLEVEL =?, "
        + "C_ORIBDGKIND =?,S_ORIASTFLAG =?,F_ORICORRAMT =?,S_CURPAYEETRECODE =?,S_CURAIMTRECODE =?, "
        + "S_CURTAXORGCODE =?,S_CURBDGSBTCODE =?,C_CURBDGLEVEL =?,C_CURBDGKIND =?,S_CURASTFLAG =?, "
        + "F_CURCORRAMT =?,S_REASONCODE =?,C_TRIMFLAG =?,S_PACKAGENO =?,S_STATUS =?, "
        + "S_FILENAME =?,S_BOOKORGCODE =?,TS_SYSUPDATE =CURRENT TIMESTAMP "
        + "WHERE "
        + "I_VOUSRLNO = ?"
        ;

	/* SQL to update data, support LOB */
    private static final String SQL_UPDATE_LOB =
        "UPDATE TBS_TV_IN_CORRHANDBOOK SET "
        + "S_ELECVOUNO =?, S_CORRVOUNO =?, D_ACCEPT =?, D_ACCT =?, D_VOUCHER =?,  "
        + "S_ORIPAYEETRECODE =?, S_ORIAIMTRECODE =?, S_ORITAXORGCODE =?, S_ORIBDGSBTCODE =?, C_ORIBDGLEVEL =?,  "
        + "C_ORIBDGKIND =?, S_ORIASTFLAG =?, F_ORICORRAMT =?, S_CURPAYEETRECODE =?, S_CURAIMTRECODE =?,  "
        + "S_CURTAXORGCODE =?, S_CURBDGSBTCODE =?, C_CURBDGLEVEL =?, C_CURBDGKIND =?, S_CURASTFLAG =?,  "
        + "F_CURCORRAMT =?, S_REASONCODE =?, C_TRIMFLAG =?, S_PACKAGENO =?, S_STATUS =?,  "
        + "S_FILENAME =?, S_BOOKORGCODE =?, TS_SYSUPDATE =CURRENT TIMESTAMP "
        + "WHERE "
        + "I_VOUSRLNO = ?"
        ;	
	
    /* SQL to delete data */
    private static final String SQL_DELETE =
        "DELETE FROM TBS_TV_IN_CORRHANDBOOK " 
        + " WHERE "
        + "I_VOUSRLNO = ?"
        ;


	/**
	*  批量查询 一次最多查询的参数数量
	*/
	
	public static final int FIND_BATCH_SIZE = 150  / 1;
	



   /**
   * Create a new record in Database.
   */
  public void create(IDto _dto,  Connection conn) throws SQLException
  {
     TbsTvInCorrhandbookDto dto = (TbsTvInCorrhandbookDto)_dto ;
     String msgValid = dto.checkValid() ;
     if (msgValid != null)
     	throw new SQLException("插入错误，"+msgValid) ;

     PreparedStatement ps = null;
     try
     {
         ps = conn.prepareStatement(SQL_INSERT);
           ps.setString(1, dto.getSelecvouno());

          ps.setString(2, dto.getScorrvouno());

          ps.setDate(3, dto.getDaccept());

          ps.setDate(4, dto.getDacct());

          ps.setDate(5, dto.getDvoucher());

          ps.setString(6, dto.getSoripayeetrecode());

          ps.setString(7, dto.getSoriaimtrecode());

          ps.setString(8, dto.getSoritaxorgcode());

          ps.setString(9, dto.getSoribdgsbtcode());

          ps.setString(10, dto.getCoribdglevel());

          ps.setString(11, dto.getCoribdgkind());

          ps.setString(12, dto.getSoriastflag());

          ps.setBigDecimal(13, dto.getForicorramt());

          ps.setString(14, dto.getScurpayeetrecode());

          ps.setString(15, dto.getScuraimtrecode());

          ps.setString(16, dto.getScurtaxorgcode());

          ps.setString(17, dto.getScurbdgsbtcode());

          ps.setString(18, dto.getCcurbdglevel());

          ps.setString(19, dto.getCcurbdgkind());

          ps.setString(20, dto.getScurastflag());

          ps.setBigDecimal(21, dto.getFcurcorramt());

          ps.setString(22, dto.getSreasoncode());

          ps.setString(23, dto.getCtrimflag());

          ps.setString(24, dto.getSpackageno());

          ps.setString(25, dto.getSstatus());

          ps.setString(26, dto.getSfilename());

          ps.setString(27, dto.getSbookorgcode());

          ps.executeUpdate();
     }
     finally
     {
         close(ps);
     }
     return  ;
  }

   /**
   * Create a new record in Database. and return result, only used when having generated key
   */
   public IDto createWithResult(IDto _dto,  Connection conn) throws SQLException
   {
       TbsTvInCorrhandbookDto dto = (TbsTvInCorrhandbookDto)_dto ;
       String msgValid = dto.checkValid() ;
       if (msgValid != null)
           throw new SQLException("插入错误，"+msgValid) ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try
       {
           ps = conn.prepareStatement(SQL_INSERT_WITH_RESULT);
             ps.setString(1, dto.getSelecvouno());
            ps.setString(2, dto.getScorrvouno());
            ps.setDate(3, dto.getDaccept());
            ps.setDate(4, dto.getDacct());
            ps.setDate(5, dto.getDvoucher());
            ps.setString(6, dto.getSoripayeetrecode());
            ps.setString(7, dto.getSoriaimtrecode());
            ps.setString(8, dto.getSoritaxorgcode());
            ps.setString(9, dto.getSoribdgsbtcode());
            ps.setString(10, dto.getCoribdglevel());
            ps.setString(11, dto.getCoribdgkind());
            ps.setString(12, dto.getSoriastflag());
            ps.setBigDecimal(13, dto.getForicorramt());
            ps.setString(14, dto.getScurpayeetrecode());
            ps.setString(15, dto.getScuraimtrecode());
            ps.setString(16, dto.getScurtaxorgcode());
            ps.setString(17, dto.getScurbdgsbtcode());
            ps.setString(18, dto.getCcurbdglevel());
            ps.setString(19, dto.getCcurbdgkind());
            ps.setString(20, dto.getScurastflag());
            ps.setBigDecimal(21, dto.getFcurcorramt());
            ps.setString(22, dto.getSreasoncode());
            ps.setString(23, dto.getCtrimflag());
            ps.setString(24, dto.getSpackageno());
            ps.setString(25, dto.getSstatus());
            ps.setString(26, dto.getSfilename());
            ps.setString(27, dto.getSbookorgcode());
            rs = ps.executeQuery();
           List results = getResults(rs);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }
        finally
        {
           close(ps);
           close(rs);
        }
   }

 	/**
	* Create batch new record in Database. it only create the first layer dto
	*/
    public void create(IDto[] _dtos, Connection conn)    throws SQLException
    {
        TbsTvInCorrhandbookDto dto  ;
     	for (int i= 0; i< _dtos.length ; i++)
 	    {
 		    dto  = (TbsTvInCorrhandbookDto)_dtos[i] ; 
    	    String msgValid = dto.checkValid() ;
        	if (msgValid != null)
     	    	throw new SQLException("插入错误，"+msgValid) ;
     	}
 	    PreparedStatement ps = null;
        try
        {
            ps = conn.prepareStatement(SQL_INSERT);
       
     	    for (int i= 0; i< _dtos.length ; i++)
     	    {
 	    	    dto  = (TbsTvInCorrhandbookDto)_dtos[i] ; 
   
               ps.setString(1, dto.getSelecvouno());
  
               ps.setString(2, dto.getScorrvouno());
  
               ps.setDate(3, dto.getDaccept());
  
               ps.setDate(4, dto.getDacct());
  
               ps.setDate(5, dto.getDvoucher());
  
               ps.setString(6, dto.getSoripayeetrecode());
  
               ps.setString(7, dto.getSoriaimtrecode());
  
               ps.setString(8, dto.getSoritaxorgcode());
  
               ps.setString(9, dto.getSoribdgsbtcode());
  
               ps.setString(10, dto.getCoribdglevel());
  
               ps.setString(11, dto.getCoribdgkind());
  
               ps.setString(12, dto.getSoriastflag());
  
               ps.setBigDecimal(13, dto.getForicorramt());
  
               ps.setString(14, dto.getScurpayeetrecode());
  
               ps.setString(15, dto.getScuraimtrecode());
  
               ps.setString(16, dto.getScurtaxorgcode());
  
               ps.setString(17, dto.getScurbdgsbtcode());
  
               ps.setString(18, dto.getCcurbdglevel());
  
               ps.setString(19, dto.getCcurbdgkind());
  
               ps.setString(20, dto.getScurastflag());
  
               ps.setBigDecimal(21, dto.getFcurcorramt());
  
               ps.setString(22, dto.getSreasoncode());
  
               ps.setString(23, dto.getCtrimflag());
  
               ps.setString(24, dto.getSpackageno());
  
               ps.setString(25, dto.getSstatus());
  
               ps.setString(26, dto.getSfilename());
  
               ps.setString(27, dto.getSbookorgcode());
                ps.addBatch(); 
           }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }
        return  ;
   }




     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto find(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TbsTvInCorrhandbookPK pk = (TbsTvInCorrhandbookPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT);
           if (pk.getIvousrlno()==null)
               ps.setNull(1, java.sql.Types.BIGINT);
           else
               ps.setLong(1, pk.getIvousrlno().longValue());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }

     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto findForUpdate(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       TbsTvInCorrhandbookPK pk = (TbsTvInCorrhandbookPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT_FOR_UPDATE);
           if (pk.getIvousrlno()==null)
               ps.setNull(1, java.sql.Types.BIGINT);
           else
               ps.setLong(1, pk.getIvousrlno().longValue());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }
  
     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto[] find(IPK[] _pks, Connection conn, boolean isLobSupport) throws SQLException
    {
        TbsTvInCorrhandbookPK pk ;
        
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TbsTvInCorrhandbookPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 
        PreparedStatement ps = null;
        ResultSet rs = null;
        
         
        List results = new ArrayList();  
		for (int iBegin = 0; iBegin < pks.size(); iBegin += FIND_BATCH_SIZE)   //
		{
        
            try {
          		int iFindBatchSize = pks.size() - iBegin ;
	    		if (iFindBatchSize >FIND_BATCH_SIZE )
		    		iFindBatchSize = FIND_BATCH_SIZE ;
		  
        	    StringBuffer sb = new StringBuffer() ;
            	sb.append(SQL_SELECT_BATCH).append(" WHERE ").append(SQL_SELECT_BATCH_WHERE);

	    		for (int i = iBegin+1; i < iBegin + iFindBatchSize; i++)
		    	{
			    	sb.append("OR").append(SQL_SELECT_BATCH_WHERE) ;
    			}
        	
                ps = conn.prepareStatement(sb.toString());

                for (int i = iBegin;i < iBegin + iFindBatchSize; i++)
                {
                    pk  = (TbsTvInCorrhandbookPK)(pks.get(i)) ; 
                   if (pk.getIvousrlno()==null)
                      ps.setNull((i-iBegin)*1+1, java.sql.Types.BIGINT);
                   else
                      ps.setLong((i-iBegin)*1+1, pk.getIvousrlno().longValue());

			
	            }
                rs = ps.executeQuery();
                results.addAll(getResults(rs,0,isLobSupport)); //
            }finally {
                close(rs);
                close(ps);
            }
        }
        if (results!=null && results.size() > 0)
        {
            TbsTvInCorrhandbookDto[] dtos = new TbsTvInCorrhandbookDto[0];
		    dtos = (TbsTvInCorrhandbookDto[]) results.toArray(dtos) ;
		    return dtos ;
        }
        else
        {
            return null;
        }
    }

  
  
  



    /**
	    * 得到查询语句
	    */
    public String getSelectSQL() 
    {
	        return SQL_SELECT_BATCH_SCROLLABLE ;
    }
   

	public List getResults(ResultSet rs) throws SQLException
    {
    	return getResults(rs, 0) ;
    }
  

    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize) throws SQLException
    {
		return getResults(rs,maxSize,false);
	}
    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize, boolean isLobSupport) throws SQLException
    {
        List results = new ArrayList();
        String str ;
        Reader reader;
        Clob clob;
        char[] chars ;
        
        while (rs.next())
         {
             TbsTvInCorrhandbookDto  dto = new  TbsTvInCorrhandbookDto ();
             //I_VOUSRLNO
             str = rs.getString("I_VOUSRLNO");
             if(str!=null){
                dto.setIvousrlno(new Long(str));
             }

             //S_ELECVOUNO
             str = rs.getString("S_ELECVOUNO");
             if (str == null)
                dto.setSelecvouno(null);
             else
                dto.setSelecvouno(str.trim());

             //S_CORRVOUNO
             str = rs.getString("S_CORRVOUNO");
             if (str == null)
                dto.setScorrvouno(null);
             else
                dto.setScorrvouno(str.trim());

             //D_ACCEPT
           dto.setDaccept(rs.getDate("D_ACCEPT"));

             //D_ACCT
           dto.setDacct(rs.getDate("D_ACCT"));

             //D_VOUCHER
           dto.setDvoucher(rs.getDate("D_VOUCHER"));

             //S_ORIPAYEETRECODE
             str = rs.getString("S_ORIPAYEETRECODE");
             if (str == null)
                dto.setSoripayeetrecode(null);
             else
                dto.setSoripayeetrecode(str.trim());

             //S_ORIAIMTRECODE
             str = rs.getString("S_ORIAIMTRECODE");
             if (str == null)
                dto.setSoriaimtrecode(null);
             else
                dto.setSoriaimtrecode(str.trim());

             //S_ORITAXORGCODE
             str = rs.getString("S_ORITAXORGCODE");
             if (str == null)
                dto.setSoritaxorgcode(null);
             else
                dto.setSoritaxorgcode(str.trim());

             //S_ORIBDGSBTCODE
             str = rs.getString("S_ORIBDGSBTCODE");
             if (str == null)
                dto.setSoribdgsbtcode(null);
             else
                dto.setSoribdgsbtcode(str.trim());

             //C_ORIBDGLEVEL
             str = rs.getString("C_ORIBDGLEVEL");
             if (str == null)
                dto.setCoribdglevel(null);
             else
                dto.setCoribdglevel(str.trim());

             //C_ORIBDGKIND
             str = rs.getString("C_ORIBDGKIND");
             if (str == null)
                dto.setCoribdgkind(null);
             else
                dto.setCoribdgkind(str.trim());

             //S_ORIASTFLAG
             str = rs.getString("S_ORIASTFLAG");
             if (str == null)
                dto.setSoriastflag(null);
             else
                dto.setSoriastflag(str.trim());

             //F_ORICORRAMT
           dto.setForicorramt(rs.getBigDecimal("F_ORICORRAMT"));

             //S_CURPAYEETRECODE
             str = rs.getString("S_CURPAYEETRECODE");
             if (str == null)
                dto.setScurpayeetrecode(null);
             else
                dto.setScurpayeetrecode(str.trim());

             //S_CURAIMTRECODE
             str = rs.getString("S_CURAIMTRECODE");
             if (str == null)
                dto.setScuraimtrecode(null);
             else
                dto.setScuraimtrecode(str.trim());

             //S_CURTAXORGCODE
             str = rs.getString("S_CURTAXORGCODE");
             if (str == null)
                dto.setScurtaxorgcode(null);
             else
                dto.setScurtaxorgcode(str.trim());

             //S_CURBDGSBTCODE
             str = rs.getString("S_CURBDGSBTCODE");
             if (str == null)
                dto.setScurbdgsbtcode(null);
             else
                dto.setScurbdgsbtcode(str.trim());

             //C_CURBDGLEVEL
             str = rs.getString("C_CURBDGLEVEL");
             if (str == null)
                dto.setCcurbdglevel(null);
             else
                dto.setCcurbdglevel(str.trim());

             //C_CURBDGKIND
             str = rs.getString("C_CURBDGKIND");
             if (str == null)
                dto.setCcurbdgkind(null);
             else
                dto.setCcurbdgkind(str.trim());

             //S_CURASTFLAG
             str = rs.getString("S_CURASTFLAG");
             if (str == null)
                dto.setScurastflag(null);
             else
                dto.setScurastflag(str.trim());

             //F_CURCORRAMT
           dto.setFcurcorramt(rs.getBigDecimal("F_CURCORRAMT"));

             //S_REASONCODE
             str = rs.getString("S_REASONCODE");
             if (str == null)
                dto.setSreasoncode(null);
             else
                dto.setSreasoncode(str.trim());

             //C_TRIMFLAG
             str = rs.getString("C_TRIMFLAG");
             if (str == null)
                dto.setCtrimflag(null);
             else
                dto.setCtrimflag(str.trim());

             //S_PACKAGENO
             str = rs.getString("S_PACKAGENO");
             if (str == null)
                dto.setSpackageno(null);
             else
                dto.setSpackageno(str.trim());

             //S_STATUS
             str = rs.getString("S_STATUS");
             if (str == null)
                dto.setSstatus(null);
             else
                dto.setSstatus(str.trim());

             //S_FILENAME
             str = rs.getString("S_FILENAME");
             if (str == null)
                dto.setSfilename(null);
             else
                dto.setSfilename(str.trim());

             //S_BOOKORGCODE
             str = rs.getString("S_BOOKORGCODE");
             if (str == null)
                dto.setSbookorgcode(null);
             else
                dto.setSbookorgcode(str.trim());

             //TS_SYSUPDATE
           dto.setTssysupdate(rs.getTimestamp("TS_SYSUPDATE"));



            results.add(dto);
            
            if(maxSize >0  && results.size() == maxSize)
            {
            	break ;
            }
            
         }
        
		return results ;
    }

  /**
   * populate resultSet as cclass
   */
  protected IDto[] getChildrenResults(ResultSet rs ,Class _dto) throws SQLException{

      throw new SQLException("本dto没有与其它dto相关联，无法应用此方法");
  }


     /**
     * Update a record in Database.
     */

    public void update(IDto _dto, Connection conn, boolean isLobSupport) throws SQLException
    {
        String msgValid = _dto.checkValid() ;
        if (msgValid != null)
            throw new SQLException("修改错误，"+msgValid) ;
        TbsTvInCorrhandbookDto dto = (TbsTvInCorrhandbookDto)_dto ;
        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            int pos = 1;
            //S_ELECVOUNO
            ps.setString(pos, dto.getSelecvouno());
            pos++;

            //S_CORRVOUNO
            ps.setString(pos, dto.getScorrvouno());
            pos++;

            //D_ACCEPT
            ps.setDate(pos, dto.getDaccept());
            pos++;

            //D_ACCT
            ps.setDate(pos, dto.getDacct());
            pos++;

            //D_VOUCHER
            ps.setDate(pos, dto.getDvoucher());
            pos++;

            //S_ORIPAYEETRECODE
            ps.setString(pos, dto.getSoripayeetrecode());
            pos++;

            //S_ORIAIMTRECODE
            ps.setString(pos, dto.getSoriaimtrecode());
            pos++;

            //S_ORITAXORGCODE
            ps.setString(pos, dto.getSoritaxorgcode());
            pos++;

            //S_ORIBDGSBTCODE
            ps.setString(pos, dto.getSoribdgsbtcode());
            pos++;

            //C_ORIBDGLEVEL
            ps.setString(pos, dto.getCoribdglevel());
            pos++;

            //C_ORIBDGKIND
            ps.setString(pos, dto.getCoribdgkind());
            pos++;

            //S_ORIASTFLAG
            ps.setString(pos, dto.getSoriastflag());
            pos++;

            //F_ORICORRAMT
            ps.setBigDecimal(pos, dto.getForicorramt());
            pos++;

            //S_CURPAYEETRECODE
            ps.setString(pos, dto.getScurpayeetrecode());
            pos++;

            //S_CURAIMTRECODE
            ps.setString(pos, dto.getScuraimtrecode());
            pos++;

            //S_CURTAXORGCODE
            ps.setString(pos, dto.getScurtaxorgcode());
            pos++;

            //S_CURBDGSBTCODE
            ps.setString(pos, dto.getScurbdgsbtcode());
            pos++;

            //C_CURBDGLEVEL
            ps.setString(pos, dto.getCcurbdglevel());
            pos++;

            //C_CURBDGKIND
            ps.setString(pos, dto.getCcurbdgkind());
            pos++;

            //S_CURASTFLAG
            ps.setString(pos, dto.getScurastflag());
            pos++;

            //F_CURCORRAMT
            ps.setBigDecimal(pos, dto.getFcurcorramt());
            pos++;

            //S_REASONCODE
            ps.setString(pos, dto.getSreasoncode());
            pos++;

            //C_TRIMFLAG
            ps.setString(pos, dto.getCtrimflag());
            pos++;

            //S_PACKAGENO
            ps.setString(pos, dto.getSpackageno());
            pos++;

            //S_STATUS
            ps.setString(pos, dto.getSstatus());
            pos++;

            //S_FILENAME
            ps.setString(pos, dto.getSfilename());
            pos++;

            //S_BOOKORGCODE
            ps.setString(pos, dto.getSbookorgcode());
            pos++;

            //TS_SYSUPDATE

           //I_VOUSRLNO
           ps.setLong(pos, dto.getIvousrlno().longValue());
           pos++;
           ps.executeUpdate();
        }
        finally
        {
           close(ps);
        }

        return ;
    }
    
    
    
      /**
     * Update batch record in Database.
     */

    public void update(IDto[] _dtos, Connection conn, boolean isLobSupport) throws SQLException
    {
     
     	 TbsTvInCorrhandbookDto dto  ;
         for (int i= 0; i< _dtos.length ; i++)
         {
            dto  = (TbsTvInCorrhandbookDto)_dtos[i] ; 
            String msgValid = dto.checkValid() ;
            if (msgValid != null)
                throw new SQLException("修改错误，"+msgValid) ;
         }

        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            
            for (int i= 0; i< _dtos.length ; i++)
            {
                dto  = (TbsTvInCorrhandbookDto)_dtos[i] ; 
                int pos = 1;
                //S_ELECVOUNO
                 ps.setString(pos, dto.getSelecvouno());
                pos++;

                //S_CORRVOUNO
                 ps.setString(pos, dto.getScorrvouno());
                pos++;

                //D_ACCEPT
                 ps.setDate(pos, dto.getDaccept());
                pos++;

                //D_ACCT
                 ps.setDate(pos, dto.getDacct());
                pos++;

                //D_VOUCHER
                 ps.setDate(pos, dto.getDvoucher());
                pos++;

                //S_ORIPAYEETRECODE
                 ps.setString(pos, dto.getSoripayeetrecode());
                pos++;

                //S_ORIAIMTRECODE
                 ps.setString(pos, dto.getSoriaimtrecode());
                pos++;

                //S_ORITAXORGCODE
                 ps.setString(pos, dto.getSoritaxorgcode());
                pos++;

                //S_ORIBDGSBTCODE
                 ps.setString(pos, dto.getSoribdgsbtcode());
                pos++;

                //C_ORIBDGLEVEL
                 ps.setString(pos, dto.getCoribdglevel());
                pos++;

                //C_ORIBDGKIND
                 ps.setString(pos, dto.getCoribdgkind());
                pos++;

                //S_ORIASTFLAG
                 ps.setString(pos, dto.getSoriastflag());
                pos++;

                //F_ORICORRAMT
                 ps.setBigDecimal(pos, dto.getForicorramt());
                pos++;

                //S_CURPAYEETRECODE
                 ps.setString(pos, dto.getScurpayeetrecode());
                pos++;

                //S_CURAIMTRECODE
                 ps.setString(pos, dto.getScuraimtrecode());
                pos++;

                //S_CURTAXORGCODE
                 ps.setString(pos, dto.getScurtaxorgcode());
                pos++;

                //S_CURBDGSBTCODE
                 ps.setString(pos, dto.getScurbdgsbtcode());
                pos++;

                //C_CURBDGLEVEL
                 ps.setString(pos, dto.getCcurbdglevel());
                pos++;

                //C_CURBDGKIND
                 ps.setString(pos, dto.getCcurbdgkind());
                pos++;

                //S_CURASTFLAG
                 ps.setString(pos, dto.getScurastflag());
                pos++;

                //F_CURCORRAMT
                 ps.setBigDecimal(pos, dto.getFcurcorramt());
                pos++;

                //S_REASONCODE
                 ps.setString(pos, dto.getSreasoncode());
                pos++;

                //C_TRIMFLAG
                 ps.setString(pos, dto.getCtrimflag());
                pos++;

                //S_PACKAGENO
                 ps.setString(pos, dto.getSpackageno());
                pos++;

                //S_STATUS
                 ps.setString(pos, dto.getSstatus());
                pos++;

                //S_FILENAME
                 ps.setString(pos, dto.getSfilename());
                pos++;

                //S_BOOKORGCODE
                 ps.setString(pos, dto.getSbookorgcode());
                pos++;

                //TS_SYSUPDATE
 
               //I_VOUSRLNO
               ps.setLong(pos, dto.getIvousrlno().longValue());
               pos++;
		       ps.addBatch(); 
	       }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }

        return ;
    }



    /**
     * delete a record in Database.
     */
    public void delete(IPK _pk, Connection conn) throws SQLException
    {
       
      
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return ;
        //	throw new SQLException("删除错误，"+msgValid) ;
       }
       TbsTvInCorrhandbookPK pk = (TbsTvInCorrhandbookPK)_pk ;


       PreparedStatement ps = null;
       try {
           ps = conn.prepareStatement(SQL_DELETE);
           ps.setLong(1, pk.getIvousrlno().longValue());
           ps.executeUpdate();
        }
        finally
        {
            close(ps);
        }
    }


     /**
     * delete batch record in Database.
     */
    public void delete(IPK[] _pks, Connection conn) throws SQLException
    {
        TbsTvInCorrhandbookPK pk ;
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (TbsTvInCorrhandbookPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 		
 		
        PreparedStatement ps = null;
        try
         {
            ps = conn.prepareStatement(SQL_DELETE);
             
       		for (int i= 0; i< pks.size() ; i++)
       		{
       			pk  = (TbsTvInCorrhandbookPK)(pks.get(i)) ; 
                ps.setLong(1, pk.getIvousrlno().longValue());
		        ps.addBatch() ;
			}
            ps.executeBatch();
        }
        finally
        {
            close(ps);
        }      
    }



    /**
    * Check IChangerNumber or UpdateTimeStamp.
    */
    public void check(IDto _dto, Connection conn) throws SQLException
    {
                                                                                    		throw new SQLException("数据库表：TBS_TV_IN_CORRHANDBOOK没有检查修改的字段");
    }


	/**
	* synchronize values between parent and children
	*/
    public void syncToChildren(IDto _dto)     throws SQLException{
       throw new SQLException("本记录没有与其它记录相关联，无法进行主记录与明细记录之间的数据同步。");
  }

	/**
	*get children
	*/
    public IDto[] findChildren (IDto _dto, Connection conn, boolean isLobSupport)     throws SQLException{
         throw new SQLException("本dto没有与其它dto相关联，无法得到子dto");
  }    
    
  /**
   *return the children IDto class
   * @return Class
   */
  public Class getChildrenClass(){
	  return null;
  };    



    /**
     * Close JDBC Statement.
     * @param stmt  Statement to be closed.
     */
    private void close(Statement stmt)
    {
        if (stmt != null)
        {
            try
            {
                stmt.close();
            }
            catch(SQLException    e)
            {}
        }
    }

    /**
     * Close JDBC ResultSet.
     * @param rs  ResultSet to be closed.
     */
    private void close(ResultSet rs)
    {
        if (rs != null)
        {
            try
            {
                rs.close();
            }catch(SQLException  e){}
        }
    }
}
