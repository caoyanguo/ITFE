    



package com.cfcc.itfe.persistence.dao;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.Statement;
import java.sql.SQLException;
import java.sql.Date;
import java.sql.Time;
import java.sql.Timestamp ;
import java.sql.Clob;

import com.cfcc.jaf.persistence.jaform.parent.* ;

import java.math.BigDecimal;

import java.util.List;
import java.util.ArrayList;
import java.util.HashMap;

import java.io.StringReader;
import java.io.Reader;
import java.io.IOException;

import com.cfcc.itfe.persistence.dto.HtvPayoutmsgsubDto ;
import com.cfcc.itfe.persistence.pk.HtvPayoutmsgsubPK ;


/**
 * <p>Title: DAO of table: HTV_PAYOUTMSGSUB</p>
 * <p>Description: Data Access Object  </p>
 * <p>Copyright: Copyright (c) 2005</p>
 * <p>Company: CFCC </p>
 * <p>Generation Time: 2014-12-25 15:28:56 </p>
 *  Do NOT change this file by hand.
 *  This java source file is generated by JAF_ORM .
 *  dao.vm timestamp: 2007-04-06 08:30:00
 ******************************************************
 * <p> <b>change record</b><br>
 *  make dao methods simply.<br>
 *  especially make update,delete methods simply.<br>
 *  remove "..withCheck" method,withCheck will impl in JAFOrmTemplate.<br>
 *  remove "delete(IDto _dto,..)" method.<br></p>
 * @author win7
 */

public class HtvPayoutmsgsubDao  implements IDao
{


    /* SQL to insert data */
    private static final String SQL_INSERT =
        "INSERT INTO HTV_PAYOUTMSGSUB ("
          + "S_BIZNO,S_SEQNO,S_ACCDATE,S_ECNOMICSUBJECTCODE,S_BUDGETPRJCODE"
          + ",N_MONEY,S_FUNSUBJECTCODE,TS_SYSUPDATE,S_TAXTICKETNO,S_BGTTYPECODE"
          + ",S_BGTTYPENAME,S_PROCATCODE,S_PROCATNAME,S_AGENCYCODE,S_AGENCYNAME"
          + ",S_EXPFUNCNAME,S_EXPFUNCCODE1,S_EXPFUNCNAME1,S_EXPFUNCCODE2,S_EXPFUNCNAME2"
          + ",S_EXPFUNCCODE3,S_EXPFUNCNAME3,S_EXPECOCODE,S_EXPECONAME,S_EXPECOCODE1"
          + ",S_EXPECONAME1,S_EXPECOCODE2,S_EXPECONAME2,S_HOLD1,S_HOLD2"
          + ",S_HOLD3,S_HOLD4,S_PAYEEACCTNO,S_PAYEEACCTNAME,S_PAYEEACCTBANKNAME"
          + ",S_PAYSUMMARYCODE,S_PAYSUMMARYNAME,S_XPAYAMT,S_XPAYDATE,S_XAGENTBUSINESSNO"
          + ",S_XADDWORD,S_ID"
        + ") VALUES ("
        + "?,?,?,?,?,?,?,CURRENT TIMESTAMP ,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)";


    private static final String SQL_INSERT_WITH_RESULT = "SELECT * FROM FINAL TABLE( " + SQL_INSERT + " )";
    
    /* SQL to select data */
    private static final String SQL_SELECT =
        "SELECT "
        + "HTV_PAYOUTMSGSUB.S_BIZNO, HTV_PAYOUTMSGSUB.S_SEQNO, HTV_PAYOUTMSGSUB.S_ACCDATE, HTV_PAYOUTMSGSUB.S_ECNOMICSUBJECTCODE, HTV_PAYOUTMSGSUB.S_BUDGETPRJCODE, "
        + "HTV_PAYOUTMSGSUB.N_MONEY, HTV_PAYOUTMSGSUB.S_FUNSUBJECTCODE, HTV_PAYOUTMSGSUB.TS_SYSUPDATE, HTV_PAYOUTMSGSUB.S_TAXTICKETNO, HTV_PAYOUTMSGSUB.S_BGTTYPECODE, "
        + "HTV_PAYOUTMSGSUB.S_BGTTYPENAME, HTV_PAYOUTMSGSUB.S_PROCATCODE, HTV_PAYOUTMSGSUB.S_PROCATNAME, HTV_PAYOUTMSGSUB.S_AGENCYCODE, HTV_PAYOUTMSGSUB.S_AGENCYNAME, "
        + "HTV_PAYOUTMSGSUB.S_EXPFUNCNAME, HTV_PAYOUTMSGSUB.S_EXPFUNCCODE1, HTV_PAYOUTMSGSUB.S_EXPFUNCNAME1, HTV_PAYOUTMSGSUB.S_EXPFUNCCODE2, HTV_PAYOUTMSGSUB.S_EXPFUNCNAME2, "
        + "HTV_PAYOUTMSGSUB.S_EXPFUNCCODE3, HTV_PAYOUTMSGSUB.S_EXPFUNCNAME3, HTV_PAYOUTMSGSUB.S_EXPECOCODE, HTV_PAYOUTMSGSUB.S_EXPECONAME, HTV_PAYOUTMSGSUB.S_EXPECOCODE1, "
        + "HTV_PAYOUTMSGSUB.S_EXPECONAME1, HTV_PAYOUTMSGSUB.S_EXPECOCODE2, HTV_PAYOUTMSGSUB.S_EXPECONAME2, HTV_PAYOUTMSGSUB.S_HOLD1, HTV_PAYOUTMSGSUB.S_HOLD2, "
        + "HTV_PAYOUTMSGSUB.S_HOLD3, HTV_PAYOUTMSGSUB.S_HOLD4, HTV_PAYOUTMSGSUB.S_PAYEEACCTNO, HTV_PAYOUTMSGSUB.S_PAYEEACCTNAME, HTV_PAYOUTMSGSUB.S_PAYEEACCTBANKNAME, "
        + "HTV_PAYOUTMSGSUB.S_PAYSUMMARYCODE, HTV_PAYOUTMSGSUB.S_PAYSUMMARYNAME, HTV_PAYOUTMSGSUB.S_XPAYAMT, HTV_PAYOUTMSGSUB.S_XPAYDATE, HTV_PAYOUTMSGSUB.S_XAGENTBUSINESSNO, "
        + "HTV_PAYOUTMSGSUB.S_XADDWORD, HTV_PAYOUTMSGSUB.S_ID "
        + "FROM HTV_PAYOUTMSGSUB "
        +" WHERE " 
        + "S_BIZNO = ? AND S_SEQNO = ?"
        ;
    /* SQL to select for update data */
    private static final String SQL_SELECT_FOR_UPDATE =
        "SELECT "
        + "HTV_PAYOUTMSGSUB.S_BIZNO, HTV_PAYOUTMSGSUB.S_SEQNO, HTV_PAYOUTMSGSUB.S_ACCDATE, HTV_PAYOUTMSGSUB.S_ECNOMICSUBJECTCODE, HTV_PAYOUTMSGSUB.S_BUDGETPRJCODE, "
        + "HTV_PAYOUTMSGSUB.N_MONEY, HTV_PAYOUTMSGSUB.S_FUNSUBJECTCODE, HTV_PAYOUTMSGSUB.TS_SYSUPDATE, HTV_PAYOUTMSGSUB.S_TAXTICKETNO, HTV_PAYOUTMSGSUB.S_BGTTYPECODE, "
        + "HTV_PAYOUTMSGSUB.S_BGTTYPENAME, HTV_PAYOUTMSGSUB.S_PROCATCODE, HTV_PAYOUTMSGSUB.S_PROCATNAME, HTV_PAYOUTMSGSUB.S_AGENCYCODE, HTV_PAYOUTMSGSUB.S_AGENCYNAME, "
        + "HTV_PAYOUTMSGSUB.S_EXPFUNCNAME, HTV_PAYOUTMSGSUB.S_EXPFUNCCODE1, HTV_PAYOUTMSGSUB.S_EXPFUNCNAME1, HTV_PAYOUTMSGSUB.S_EXPFUNCCODE2, HTV_PAYOUTMSGSUB.S_EXPFUNCNAME2, "
        + "HTV_PAYOUTMSGSUB.S_EXPFUNCCODE3, HTV_PAYOUTMSGSUB.S_EXPFUNCNAME3, HTV_PAYOUTMSGSUB.S_EXPECOCODE, HTV_PAYOUTMSGSUB.S_EXPECONAME, HTV_PAYOUTMSGSUB.S_EXPECOCODE1, "
        + "HTV_PAYOUTMSGSUB.S_EXPECONAME1, HTV_PAYOUTMSGSUB.S_EXPECOCODE2, HTV_PAYOUTMSGSUB.S_EXPECONAME2, HTV_PAYOUTMSGSUB.S_HOLD1, HTV_PAYOUTMSGSUB.S_HOLD2, "
        + "HTV_PAYOUTMSGSUB.S_HOLD3, HTV_PAYOUTMSGSUB.S_HOLD4, HTV_PAYOUTMSGSUB.S_PAYEEACCTNO, HTV_PAYOUTMSGSUB.S_PAYEEACCTNAME, HTV_PAYOUTMSGSUB.S_PAYEEACCTBANKNAME, "
        + "HTV_PAYOUTMSGSUB.S_PAYSUMMARYCODE, HTV_PAYOUTMSGSUB.S_PAYSUMMARYNAME, HTV_PAYOUTMSGSUB.S_XPAYAMT, HTV_PAYOUTMSGSUB.S_XPAYDATE, HTV_PAYOUTMSGSUB.S_XAGENTBUSINESSNO, "
        + "HTV_PAYOUTMSGSUB.S_XADDWORD, HTV_PAYOUTMSGSUB.S_ID "
        + "FROM HTV_PAYOUTMSGSUB "
        +" WHERE " 
        + "S_BIZNO = ? AND S_SEQNO = ? FOR UPDATE"
        ;

      /* SQL to select data (SCROLLABLE)*/
     private static final String SQL_SELECT_BATCH_SCROLLABLE =
        "SELECT "
        + "  HTV_PAYOUTMSGSUB.S_BIZNO  , HTV_PAYOUTMSGSUB.S_SEQNO  , HTV_PAYOUTMSGSUB.S_ACCDATE  , HTV_PAYOUTMSGSUB.S_ECNOMICSUBJECTCODE  , HTV_PAYOUTMSGSUB.S_BUDGETPRJCODE "
        + " , HTV_PAYOUTMSGSUB.N_MONEY  , HTV_PAYOUTMSGSUB.S_FUNSUBJECTCODE  , HTV_PAYOUTMSGSUB.TS_SYSUPDATE  , HTV_PAYOUTMSGSUB.S_TAXTICKETNO  , HTV_PAYOUTMSGSUB.S_BGTTYPECODE "
        + " , HTV_PAYOUTMSGSUB.S_BGTTYPENAME  , HTV_PAYOUTMSGSUB.S_PROCATCODE  , HTV_PAYOUTMSGSUB.S_PROCATNAME  , HTV_PAYOUTMSGSUB.S_AGENCYCODE  , HTV_PAYOUTMSGSUB.S_AGENCYNAME "
        + " , HTV_PAYOUTMSGSUB.S_EXPFUNCNAME  , HTV_PAYOUTMSGSUB.S_EXPFUNCCODE1  , HTV_PAYOUTMSGSUB.S_EXPFUNCNAME1  , HTV_PAYOUTMSGSUB.S_EXPFUNCCODE2  , HTV_PAYOUTMSGSUB.S_EXPFUNCNAME2 "
        + " , HTV_PAYOUTMSGSUB.S_EXPFUNCCODE3  , HTV_PAYOUTMSGSUB.S_EXPFUNCNAME3  , HTV_PAYOUTMSGSUB.S_EXPECOCODE  , HTV_PAYOUTMSGSUB.S_EXPECONAME  , HTV_PAYOUTMSGSUB.S_EXPECOCODE1 "
        + " , HTV_PAYOUTMSGSUB.S_EXPECONAME1  , HTV_PAYOUTMSGSUB.S_EXPECOCODE2  , HTV_PAYOUTMSGSUB.S_EXPECONAME2  , HTV_PAYOUTMSGSUB.S_HOLD1  , HTV_PAYOUTMSGSUB.S_HOLD2 "
        + " , HTV_PAYOUTMSGSUB.S_HOLD3  , HTV_PAYOUTMSGSUB.S_HOLD4  , HTV_PAYOUTMSGSUB.S_PAYEEACCTNO  , HTV_PAYOUTMSGSUB.S_PAYEEACCTNAME  , HTV_PAYOUTMSGSUB.S_PAYEEACCTBANKNAME "
        + " , HTV_PAYOUTMSGSUB.S_PAYSUMMARYCODE  , HTV_PAYOUTMSGSUB.S_PAYSUMMARYNAME  , HTV_PAYOUTMSGSUB.S_XPAYAMT  , HTV_PAYOUTMSGSUB.S_XPAYDATE  , HTV_PAYOUTMSGSUB.S_XAGENTBUSINESSNO "
        + " , HTV_PAYOUTMSGSUB.S_XADDWORD  , HTV_PAYOUTMSGSUB.S_ID "
        + "FROM HTV_PAYOUTMSGSUB ";



    /* SQL to select batch data */
    private static final String SQL_SELECT_BATCH =
        "SELECT "
        + "HTV_PAYOUTMSGSUB.S_BIZNO, HTV_PAYOUTMSGSUB.S_SEQNO, HTV_PAYOUTMSGSUB.S_ACCDATE, HTV_PAYOUTMSGSUB.S_ECNOMICSUBJECTCODE, HTV_PAYOUTMSGSUB.S_BUDGETPRJCODE, "
        + "HTV_PAYOUTMSGSUB.N_MONEY, HTV_PAYOUTMSGSUB.S_FUNSUBJECTCODE, HTV_PAYOUTMSGSUB.TS_SYSUPDATE, HTV_PAYOUTMSGSUB.S_TAXTICKETNO, HTV_PAYOUTMSGSUB.S_BGTTYPECODE, "
        + "HTV_PAYOUTMSGSUB.S_BGTTYPENAME, HTV_PAYOUTMSGSUB.S_PROCATCODE, HTV_PAYOUTMSGSUB.S_PROCATNAME, HTV_PAYOUTMSGSUB.S_AGENCYCODE, HTV_PAYOUTMSGSUB.S_AGENCYNAME, "
        + "HTV_PAYOUTMSGSUB.S_EXPFUNCNAME, HTV_PAYOUTMSGSUB.S_EXPFUNCCODE1, HTV_PAYOUTMSGSUB.S_EXPFUNCNAME1, HTV_PAYOUTMSGSUB.S_EXPFUNCCODE2, HTV_PAYOUTMSGSUB.S_EXPFUNCNAME2, "
        + "HTV_PAYOUTMSGSUB.S_EXPFUNCCODE3, HTV_PAYOUTMSGSUB.S_EXPFUNCNAME3, HTV_PAYOUTMSGSUB.S_EXPECOCODE, HTV_PAYOUTMSGSUB.S_EXPECONAME, HTV_PAYOUTMSGSUB.S_EXPECOCODE1, "
        + "HTV_PAYOUTMSGSUB.S_EXPECONAME1, HTV_PAYOUTMSGSUB.S_EXPECOCODE2, HTV_PAYOUTMSGSUB.S_EXPECONAME2, HTV_PAYOUTMSGSUB.S_HOLD1, HTV_PAYOUTMSGSUB.S_HOLD2, "
        + "HTV_PAYOUTMSGSUB.S_HOLD3, HTV_PAYOUTMSGSUB.S_HOLD4, HTV_PAYOUTMSGSUB.S_PAYEEACCTNO, HTV_PAYOUTMSGSUB.S_PAYEEACCTNAME, HTV_PAYOUTMSGSUB.S_PAYEEACCTBANKNAME, "
        + "HTV_PAYOUTMSGSUB.S_PAYSUMMARYCODE, HTV_PAYOUTMSGSUB.S_PAYSUMMARYNAME, HTV_PAYOUTMSGSUB.S_XPAYAMT, HTV_PAYOUTMSGSUB.S_XPAYDATE, HTV_PAYOUTMSGSUB.S_XAGENTBUSINESSNO, "
        + "HTV_PAYOUTMSGSUB.S_XADDWORD, HTV_PAYOUTMSGSUB.S_ID "
        + "FROM HTV_PAYOUTMSGSUB " ;
        

   /* SQL to select batch data where condition  */
    private static final String SQL_SELECT_BATCH_WHERE =" ( "
        + "S_BIZNO = ? AND S_SEQNO = ?)"
        ;


    /* SQL to update data */
    private static final String SQL_UPDATE =
        "UPDATE HTV_PAYOUTMSGSUB SET "
        + "S_ACCDATE =?,S_ECNOMICSUBJECTCODE =?,S_BUDGETPRJCODE =?,N_MONEY =?,S_FUNSUBJECTCODE =?, "
        + "TS_SYSUPDATE =CURRENT TIMESTAMP,S_TAXTICKETNO =?,S_BGTTYPECODE =?,S_BGTTYPENAME =?,S_PROCATCODE =?, "
        + "S_PROCATNAME =?,S_AGENCYCODE =?,S_AGENCYNAME =?,S_EXPFUNCNAME =?,S_EXPFUNCCODE1 =?, "
        + "S_EXPFUNCNAME1 =?,S_EXPFUNCCODE2 =?,S_EXPFUNCNAME2 =?,S_EXPFUNCCODE3 =?,S_EXPFUNCNAME3 =?, "
        + "S_EXPECOCODE =?,S_EXPECONAME =?,S_EXPECOCODE1 =?,S_EXPECONAME1 =?,S_EXPECOCODE2 =?, "
        + "S_EXPECONAME2 =?,S_HOLD1 =?,S_HOLD2 =?,S_HOLD3 =?,S_HOLD4 =?, "
        + "S_PAYEEACCTNO =?,S_PAYEEACCTNAME =?,S_PAYEEACCTBANKNAME =?,S_PAYSUMMARYCODE =?,S_PAYSUMMARYNAME =?, "
        + "S_XPAYAMT =?,S_XPAYDATE =?,S_XAGENTBUSINESSNO =?,S_XADDWORD =?,S_ID =? "
        + "WHERE "
        + "S_BIZNO = ? AND S_SEQNO = ?"
        ;

	/* SQL to update data, support LOB */
    private static final String SQL_UPDATE_LOB =
        "UPDATE HTV_PAYOUTMSGSUB SET "
        + "S_ACCDATE =?, S_ECNOMICSUBJECTCODE =?, S_BUDGETPRJCODE =?, N_MONEY =?, S_FUNSUBJECTCODE =?,  "
        + "TS_SYSUPDATE =CURRENT TIMESTAMP, S_TAXTICKETNO =?, S_BGTTYPECODE =?, S_BGTTYPENAME =?, S_PROCATCODE =?,  "
        + "S_PROCATNAME =?, S_AGENCYCODE =?, S_AGENCYNAME =?, S_EXPFUNCNAME =?, S_EXPFUNCCODE1 =?,  "
        + "S_EXPFUNCNAME1 =?, S_EXPFUNCCODE2 =?, S_EXPFUNCNAME2 =?, S_EXPFUNCCODE3 =?, S_EXPFUNCNAME3 =?,  "
        + "S_EXPECOCODE =?, S_EXPECONAME =?, S_EXPECOCODE1 =?, S_EXPECONAME1 =?, S_EXPECOCODE2 =?,  "
        + "S_EXPECONAME2 =?, S_HOLD1 =?, S_HOLD2 =?, S_HOLD3 =?, S_HOLD4 =?,  "
        + "S_PAYEEACCTNO =?, S_PAYEEACCTNAME =?, S_PAYEEACCTBANKNAME =?, S_PAYSUMMARYCODE =?, S_PAYSUMMARYNAME =?,  "
        + "S_XPAYAMT =?, S_XPAYDATE =?, S_XAGENTBUSINESSNO =?, S_XADDWORD =?, S_ID =? "
        + "WHERE "
        + "S_BIZNO = ? AND S_SEQNO = ?"
        ;	
	
    /* SQL to delete data */
    private static final String SQL_DELETE =
        "DELETE FROM HTV_PAYOUTMSGSUB " 
        + " WHERE "
        + "S_BIZNO = ? AND S_SEQNO = ?"
        ;


	/**
	*  批量查询 一次最多查询的参数数量
	*/
	
	public static final int FIND_BATCH_SIZE = 150  / 2;
	



   /**
   * Create a new record in Database.
   */
  public void create(IDto _dto,  Connection conn) throws SQLException
  {
     HtvPayoutmsgsubDto dto = (HtvPayoutmsgsubDto)_dto ;
     String msgValid = dto.checkValid() ;
     if (msgValid != null)
     	throw new SQLException("插入错误，"+msgValid) ;

     PreparedStatement ps = null;
     try
     {
         ps = conn.prepareStatement(SQL_INSERT);
          ps.setString(1, dto.getSbizno());

          if (dto.getSseqno()==null)
            ps.setNull(2, java.sql.Types.INTEGER);
         else
            ps.setInt(2, dto.getSseqno().intValue());
          ps.setString(3, dto.getSaccdate());

          ps.setString(4, dto.getSecnomicsubjectcode());

          ps.setString(5, dto.getSbudgetprjcode());

          ps.setBigDecimal(6, dto.getNmoney());

          ps.setString(7, dto.getSfunsubjectcode());

           ps.setString(8, dto.getStaxticketno());

          ps.setString(9, dto.getSbgttypecode());

          ps.setString(10, dto.getSbgttypename());

          ps.setString(11, dto.getSprocatcode());

          ps.setString(12, dto.getSprocatname());

          ps.setString(13, dto.getSagencycode());

          ps.setString(14, dto.getSagencyname());

          ps.setString(15, dto.getSexpfuncname());

          ps.setString(16, dto.getSexpfunccode1());

          ps.setString(17, dto.getSexpfuncname1());

          ps.setString(18, dto.getSexpfunccode2());

          ps.setString(19, dto.getSexpfuncname2());

          ps.setString(20, dto.getSexpfunccode3());

          ps.setString(21, dto.getSexpfuncname3());

          ps.setString(22, dto.getSexpecocode());

          ps.setString(23, dto.getSexpeconame());

          ps.setString(24, dto.getSexpecocode1());

          ps.setString(25, dto.getSexpeconame1());

          ps.setString(26, dto.getSexpecocode2());

          ps.setString(27, dto.getSexpeconame2());

          ps.setString(28, dto.getShold1());

          ps.setString(29, dto.getShold2());

          ps.setString(30, dto.getShold3());

          ps.setString(31, dto.getShold4());

          ps.setString(32, dto.getSpayeeacctno());

          ps.setString(33, dto.getSpayeeacctname());

          ps.setString(34, dto.getSpayeeacctbankname());

          ps.setString(35, dto.getSpaysummarycode());

          ps.setString(36, dto.getSpaysummaryname());

          ps.setBigDecimal(37, dto.getSxpayamt());

          ps.setString(38, dto.getSxpaydate());

          ps.setString(39, dto.getSxagentbusinessno());

          ps.setString(40, dto.getSxaddword());

          ps.setString(41, dto.getSid());

         ps.executeUpdate();
     }
     finally
     {
         close(ps);
     }
     return  ;
  }

   /**
   * Create a new record in Database. and return result, only used when having generated key
   */
   public IDto createWithResult(IDto _dto,  Connection conn) throws SQLException
   {
       HtvPayoutmsgsubDto dto = (HtvPayoutmsgsubDto)_dto ;
       String msgValid = dto.checkValid() ;
       if (msgValid != null)
           throw new SQLException("插入错误，"+msgValid) ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try
       {
           ps = conn.prepareStatement(SQL_INSERT_WITH_RESULT);
            ps.setString(1, dto.getSbizno());
            if (dto.getSseqno()==null)
              ps.setNull(2, java.sql.Types.INTEGER);
           else
              ps.setInt(2, dto.getSseqno().intValue());
            ps.setString(3, dto.getSaccdate());
            ps.setString(4, dto.getSecnomicsubjectcode());
            ps.setString(5, dto.getSbudgetprjcode());
            ps.setBigDecimal(6, dto.getNmoney());
            ps.setString(7, dto.getSfunsubjectcode());
             ps.setString(8, dto.getStaxticketno());
            ps.setString(9, dto.getSbgttypecode());
            ps.setString(10, dto.getSbgttypename());
            ps.setString(11, dto.getSprocatcode());
            ps.setString(12, dto.getSprocatname());
            ps.setString(13, dto.getSagencycode());
            ps.setString(14, dto.getSagencyname());
            ps.setString(15, dto.getSexpfuncname());
            ps.setString(16, dto.getSexpfunccode1());
            ps.setString(17, dto.getSexpfuncname1());
            ps.setString(18, dto.getSexpfunccode2());
            ps.setString(19, dto.getSexpfuncname2());
            ps.setString(20, dto.getSexpfunccode3());
            ps.setString(21, dto.getSexpfuncname3());
            ps.setString(22, dto.getSexpecocode());
            ps.setString(23, dto.getSexpeconame());
            ps.setString(24, dto.getSexpecocode1());
            ps.setString(25, dto.getSexpeconame1());
            ps.setString(26, dto.getSexpecocode2());
            ps.setString(27, dto.getSexpeconame2());
            ps.setString(28, dto.getShold1());
            ps.setString(29, dto.getShold2());
            ps.setString(30, dto.getShold3());
            ps.setString(31, dto.getShold4());
            ps.setString(32, dto.getSpayeeacctno());
            ps.setString(33, dto.getSpayeeacctname());
            ps.setString(34, dto.getSpayeeacctbankname());
            ps.setString(35, dto.getSpaysummarycode());
            ps.setString(36, dto.getSpaysummaryname());
            ps.setBigDecimal(37, dto.getSxpayamt());
            ps.setString(38, dto.getSxpaydate());
            ps.setString(39, dto.getSxagentbusinessno());
            ps.setString(40, dto.getSxaddword());
            ps.setString(41, dto.getSid());
           rs = ps.executeQuery();
           List results = getResults(rs);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }
        finally
        {
           close(ps);
           close(rs);
        }
   }

 	/**
	* Create batch new record in Database. it only create the first layer dto
	*/
    public void create(IDto[] _dtos, Connection conn)    throws SQLException
    {
        HtvPayoutmsgsubDto dto  ;
     	for (int i= 0; i< _dtos.length ; i++)
 	    {
 		    dto  = (HtvPayoutmsgsubDto)_dtos[i] ; 
    	    String msgValid = dto.checkValid() ;
        	if (msgValid != null)
     	    	throw new SQLException("插入错误，"+msgValid) ;
     	}
 	    PreparedStatement ps = null;
        try
        {
            ps = conn.prepareStatement(SQL_INSERT);
       
     	    for (int i= 0; i< _dtos.length ; i++)
     	    {
 	    	    dto  = (HtvPayoutmsgsubDto)_dtos[i] ; 
  
               ps.setString(1, dto.getSbizno());
  
               if (dto.getSseqno()==null)
                  ps.setNull(2, java.sql.Types.INTEGER);
               else
                  ps.setInt(2, dto.getSseqno().intValue());
  
               ps.setString(3, dto.getSaccdate());
  
               ps.setString(4, dto.getSecnomicsubjectcode());
  
               ps.setString(5, dto.getSbudgetprjcode());
  
               ps.setBigDecimal(6, dto.getNmoney());
  
               ps.setString(7, dto.getSfunsubjectcode());
   
               ps.setString(8, dto.getStaxticketno());
  
               ps.setString(9, dto.getSbgttypecode());
  
               ps.setString(10, dto.getSbgttypename());
  
               ps.setString(11, dto.getSprocatcode());
  
               ps.setString(12, dto.getSprocatname());
  
               ps.setString(13, dto.getSagencycode());
  
               ps.setString(14, dto.getSagencyname());
  
               ps.setString(15, dto.getSexpfuncname());
  
               ps.setString(16, dto.getSexpfunccode1());
  
               ps.setString(17, dto.getSexpfuncname1());
  
               ps.setString(18, dto.getSexpfunccode2());
  
               ps.setString(19, dto.getSexpfuncname2());
  
               ps.setString(20, dto.getSexpfunccode3());
  
               ps.setString(21, dto.getSexpfuncname3());
  
               ps.setString(22, dto.getSexpecocode());
  
               ps.setString(23, dto.getSexpeconame());
  
               ps.setString(24, dto.getSexpecocode1());
  
               ps.setString(25, dto.getSexpeconame1());
  
               ps.setString(26, dto.getSexpecocode2());
  
               ps.setString(27, dto.getSexpeconame2());
  
               ps.setString(28, dto.getShold1());
  
               ps.setString(29, dto.getShold2());
  
               ps.setString(30, dto.getShold3());
  
               ps.setString(31, dto.getShold4());
  
               ps.setString(32, dto.getSpayeeacctno());
  
               ps.setString(33, dto.getSpayeeacctname());
  
               ps.setString(34, dto.getSpayeeacctbankname());
  
               ps.setString(35, dto.getSpaysummarycode());
  
               ps.setString(36, dto.getSpaysummaryname());
  
               ps.setBigDecimal(37, dto.getSxpayamt());
  
               ps.setString(38, dto.getSxpaydate());
  
               ps.setString(39, dto.getSxagentbusinessno());
  
               ps.setString(40, dto.getSxaddword());
  
               ps.setString(41, dto.getSid());
               ps.addBatch(); 
           }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }
        return  ;
   }




     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto find(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       HtvPayoutmsgsubPK pk = (HtvPayoutmsgsubPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT);
           ps.setString(1, pk.getSbizno());


           if (pk.getSseqno()==null)
               ps.setNull(2, java.sql.Types.INTEGER);
           else
               ps.setInt(2, pk.getSseqno().intValue());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }

     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto findForUpdate(IPK _pk, Connection conn, boolean isLobSupport) throws SQLException
    {
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return null ;
        //	throw new SQLException("查找错误，"+msgValid) ;
       }
        	
       HtvPayoutmsgsubPK pk = (HtvPayoutmsgsubPK)_pk ;

       PreparedStatement ps = null;
       ResultSet rs = null;
       try {
           ps = conn.prepareStatement(SQL_SELECT_FOR_UPDATE);
           ps.setString(1, pk.getSbizno());


           if (pk.getSseqno()==null)
               ps.setNull(2, java.sql.Types.INTEGER);
           else
               ps.setInt(2, pk.getSseqno().intValue());

           rs = ps.executeQuery();
           List results = getResults(rs,0,isLobSupport);
           if (results!=null && results.size() > 0)
               return (IDto)results.get(0);
           else
               return null;
        }finally {
           close(rs);
           close(ps);
        }
    }
  
     /**
     * Retrive a record from Database.
     * (the statement below is somewhat redundant, but doesnot matter)
     */
    public IDto[] find(IPK[] _pks, Connection conn, boolean isLobSupport) throws SQLException
    {
        HtvPayoutmsgsubPK pk ;
        
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (HtvPayoutmsgsubPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 
        PreparedStatement ps = null;
        ResultSet rs = null;
        
         
        List results = new ArrayList();  
		for (int iBegin = 0; iBegin < pks.size(); iBegin += FIND_BATCH_SIZE)   //
		{
        
            try {
          		int iFindBatchSize = pks.size() - iBegin ;
	    		if (iFindBatchSize >FIND_BATCH_SIZE )
		    		iFindBatchSize = FIND_BATCH_SIZE ;
		  
        	    StringBuffer sb = new StringBuffer() ;
            	sb.append(SQL_SELECT_BATCH).append(" WHERE ").append(SQL_SELECT_BATCH_WHERE);

	    		for (int i = iBegin+1; i < iBegin + iFindBatchSize; i++)
		    	{
			    	sb.append("OR").append(SQL_SELECT_BATCH_WHERE) ;
    			}
        	
                ps = conn.prepareStatement(sb.toString());

                for (int i = iBegin;i < iBegin + iFindBatchSize; i++)
                {
                    pk  = (HtvPayoutmsgsubPK)(pks.get(i)) ; 
                   ps.setString((i-iBegin)*2+1, pk.getSbizno());

                   if (pk.getSseqno()==null)
                      ps.setNull((i-iBegin)*2+ 2 , java.sql.Types.INTEGER);
                   else
                     ps.setInt((i-iBegin)*2+2, pk.getSseqno().intValue());

			
	            }
                rs = ps.executeQuery();
                results.addAll(getResults(rs,0,isLobSupport)); //
            }finally {
                close(rs);
                close(ps);
            }
        }
        if (results!=null && results.size() > 0)
        {
            HtvPayoutmsgsubDto[] dtos = new HtvPayoutmsgsubDto[0];
		    dtos = (HtvPayoutmsgsubDto[]) results.toArray(dtos) ;
		    return dtos ;
        }
        else
        {
            return null;
        }
    }

  
  
  



    /**
	    * 得到查询语句
	    */
    public String getSelectSQL() 
    {
	        return SQL_SELECT_BATCH_SCROLLABLE ;
    }
   

	public List getResults(ResultSet rs) throws SQLException
    {
    	return getResults(rs, 0) ;
    }
  

    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize) throws SQLException
    {
		return getResults(rs,maxSize,false);
	}
    /**
     * Populate the ResultSet.
     */
    public List getResults(ResultSet rs, int maxSize, boolean isLobSupport) throws SQLException
    {
        List results = new ArrayList();
        String str ;
        Reader reader;
        Clob clob;
        char[] chars ;
        
        while (rs.next())
         {
             HtvPayoutmsgsubDto  dto = new  HtvPayoutmsgsubDto ();
             //S_BIZNO
             str = rs.getString("S_BIZNO");
             if (str == null)
                dto.setSbizno(null);
             else
                dto.setSbizno(str.trim());

             //S_SEQNO
             str = rs.getString("S_SEQNO");
             if(str!=null){
                dto.setSseqno(new Integer(str));
             }

             //S_ACCDATE
             str = rs.getString("S_ACCDATE");
             if (str == null)
                dto.setSaccdate(null);
             else
                dto.setSaccdate(str.trim());

             //S_ECNOMICSUBJECTCODE
             str = rs.getString("S_ECNOMICSUBJECTCODE");
             if (str == null)
                dto.setSecnomicsubjectcode(null);
             else
                dto.setSecnomicsubjectcode(str.trim());

             //S_BUDGETPRJCODE
             str = rs.getString("S_BUDGETPRJCODE");
             if (str == null)
                dto.setSbudgetprjcode(null);
             else
                dto.setSbudgetprjcode(str.trim());

             //N_MONEY
           dto.setNmoney(rs.getBigDecimal("N_MONEY"));

             //S_FUNSUBJECTCODE
             str = rs.getString("S_FUNSUBJECTCODE");
             if (str == null)
                dto.setSfunsubjectcode(null);
             else
                dto.setSfunsubjectcode(str.trim());

             //TS_SYSUPDATE
           dto.setTssysupdate(rs.getTimestamp("TS_SYSUPDATE"));

             //S_TAXTICKETNO
             str = rs.getString("S_TAXTICKETNO");
             if (str == null)
                dto.setStaxticketno(null);
             else
                dto.setStaxticketno(str.trim());

             //S_BGTTYPECODE
             str = rs.getString("S_BGTTYPECODE");
             if (str == null)
                dto.setSbgttypecode(null);
             else
                dto.setSbgttypecode(str.trim());

             //S_BGTTYPENAME
             str = rs.getString("S_BGTTYPENAME");
             if (str == null)
                dto.setSbgttypename(null);
             else
                dto.setSbgttypename(str.trim());

             //S_PROCATCODE
             str = rs.getString("S_PROCATCODE");
             if (str == null)
                dto.setSprocatcode(null);
             else
                dto.setSprocatcode(str.trim());

             //S_PROCATNAME
             str = rs.getString("S_PROCATNAME");
             if (str == null)
                dto.setSprocatname(null);
             else
                dto.setSprocatname(str.trim());

             //S_AGENCYCODE
             str = rs.getString("S_AGENCYCODE");
             if (str == null)
                dto.setSagencycode(null);
             else
                dto.setSagencycode(str.trim());

             //S_AGENCYNAME
             str = rs.getString("S_AGENCYNAME");
             if (str == null)
                dto.setSagencyname(null);
             else
                dto.setSagencyname(str.trim());

             //S_EXPFUNCNAME
             str = rs.getString("S_EXPFUNCNAME");
             if (str == null)
                dto.setSexpfuncname(null);
             else
                dto.setSexpfuncname(str.trim());

             //S_EXPFUNCCODE1
             str = rs.getString("S_EXPFUNCCODE1");
             if (str == null)
                dto.setSexpfunccode1(null);
             else
                dto.setSexpfunccode1(str.trim());

             //S_EXPFUNCNAME1
             str = rs.getString("S_EXPFUNCNAME1");
             if (str == null)
                dto.setSexpfuncname1(null);
             else
                dto.setSexpfuncname1(str.trim());

             //S_EXPFUNCCODE2
             str = rs.getString("S_EXPFUNCCODE2");
             if (str == null)
                dto.setSexpfunccode2(null);
             else
                dto.setSexpfunccode2(str.trim());

             //S_EXPFUNCNAME2
             str = rs.getString("S_EXPFUNCNAME2");
             if (str == null)
                dto.setSexpfuncname2(null);
             else
                dto.setSexpfuncname2(str.trim());

             //S_EXPFUNCCODE3
             str = rs.getString("S_EXPFUNCCODE3");
             if (str == null)
                dto.setSexpfunccode3(null);
             else
                dto.setSexpfunccode3(str.trim());

             //S_EXPFUNCNAME3
             str = rs.getString("S_EXPFUNCNAME3");
             if (str == null)
                dto.setSexpfuncname3(null);
             else
                dto.setSexpfuncname3(str.trim());

             //S_EXPECOCODE
             str = rs.getString("S_EXPECOCODE");
             if (str == null)
                dto.setSexpecocode(null);
             else
                dto.setSexpecocode(str.trim());

             //S_EXPECONAME
             str = rs.getString("S_EXPECONAME");
             if (str == null)
                dto.setSexpeconame(null);
             else
                dto.setSexpeconame(str.trim());

             //S_EXPECOCODE1
             str = rs.getString("S_EXPECOCODE1");
             if (str == null)
                dto.setSexpecocode1(null);
             else
                dto.setSexpecocode1(str.trim());

             //S_EXPECONAME1
             str = rs.getString("S_EXPECONAME1");
             if (str == null)
                dto.setSexpeconame1(null);
             else
                dto.setSexpeconame1(str.trim());

             //S_EXPECOCODE2
             str = rs.getString("S_EXPECOCODE2");
             if (str == null)
                dto.setSexpecocode2(null);
             else
                dto.setSexpecocode2(str.trim());

             //S_EXPECONAME2
             str = rs.getString("S_EXPECONAME2");
             if (str == null)
                dto.setSexpeconame2(null);
             else
                dto.setSexpeconame2(str.trim());

             //S_HOLD1
             str = rs.getString("S_HOLD1");
             if (str == null)
                dto.setShold1(null);
             else
                dto.setShold1(str.trim());

             //S_HOLD2
             str = rs.getString("S_HOLD2");
             if (str == null)
                dto.setShold2(null);
             else
                dto.setShold2(str.trim());

             //S_HOLD3
             str = rs.getString("S_HOLD3");
             if (str == null)
                dto.setShold3(null);
             else
                dto.setShold3(str.trim());

             //S_HOLD4
             str = rs.getString("S_HOLD4");
             if (str == null)
                dto.setShold4(null);
             else
                dto.setShold4(str.trim());

             //S_PAYEEACCTNO
             str = rs.getString("S_PAYEEACCTNO");
             if (str == null)
                dto.setSpayeeacctno(null);
             else
                dto.setSpayeeacctno(str.trim());

             //S_PAYEEACCTNAME
             str = rs.getString("S_PAYEEACCTNAME");
             if (str == null)
                dto.setSpayeeacctname(null);
             else
                dto.setSpayeeacctname(str.trim());

             //S_PAYEEACCTBANKNAME
             str = rs.getString("S_PAYEEACCTBANKNAME");
             if (str == null)
                dto.setSpayeeacctbankname(null);
             else
                dto.setSpayeeacctbankname(str.trim());

             //S_PAYSUMMARYCODE
             str = rs.getString("S_PAYSUMMARYCODE");
             if (str == null)
                dto.setSpaysummarycode(null);
             else
                dto.setSpaysummarycode(str.trim());

             //S_PAYSUMMARYNAME
             str = rs.getString("S_PAYSUMMARYNAME");
             if (str == null)
                dto.setSpaysummaryname(null);
             else
                dto.setSpaysummaryname(str.trim());

             //S_XPAYAMT
           dto.setSxpayamt(rs.getBigDecimal("S_XPAYAMT"));

             //S_XPAYDATE
             str = rs.getString("S_XPAYDATE");
             if (str == null)
                dto.setSxpaydate(null);
             else
                dto.setSxpaydate(str.trim());

             //S_XAGENTBUSINESSNO
             str = rs.getString("S_XAGENTBUSINESSNO");
             if (str == null)
                dto.setSxagentbusinessno(null);
             else
                dto.setSxagentbusinessno(str.trim());

             //S_XADDWORD
             str = rs.getString("S_XADDWORD");
             if (str == null)
                dto.setSxaddword(null);
             else
                dto.setSxaddword(str.trim());

             //S_ID
             str = rs.getString("S_ID");
             if (str == null)
                dto.setSid(null);
             else
                dto.setSid(str.trim());



            results.add(dto);
            
            if(maxSize >0  && results.size() == maxSize)
            {
            	break ;
            }
            
         }
        
		return results ;
    }

  /**
   * populate resultSet as cclass
   */
  protected IDto[] getChildrenResults(ResultSet rs ,Class _dto) throws SQLException{

      throw new SQLException("本dto没有与其它dto相关联，无法应用此方法");
  }


     /**
     * Update a record in Database.
     */

    public void update(IDto _dto, Connection conn, boolean isLobSupport) throws SQLException
    {
        String msgValid = _dto.checkValid() ;
        if (msgValid != null)
            throw new SQLException("修改错误，"+msgValid) ;
        HtvPayoutmsgsubDto dto = (HtvPayoutmsgsubDto)_dto ;
        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            int pos = 1;
            //S_ACCDATE
            ps.setString(pos, dto.getSaccdate());
            pos++;

            //S_ECNOMICSUBJECTCODE
            ps.setString(pos, dto.getSecnomicsubjectcode());
            pos++;

            //S_BUDGETPRJCODE
            ps.setString(pos, dto.getSbudgetprjcode());
            pos++;

            //N_MONEY
            ps.setBigDecimal(pos, dto.getNmoney());
            pos++;

            //S_FUNSUBJECTCODE
            ps.setString(pos, dto.getSfunsubjectcode());
            pos++;

            //TS_SYSUPDATE
            //S_TAXTICKETNO
            ps.setString(pos, dto.getStaxticketno());
            pos++;

            //S_BGTTYPECODE
            ps.setString(pos, dto.getSbgttypecode());
            pos++;

            //S_BGTTYPENAME
            ps.setString(pos, dto.getSbgttypename());
            pos++;

            //S_PROCATCODE
            ps.setString(pos, dto.getSprocatcode());
            pos++;

            //S_PROCATNAME
            ps.setString(pos, dto.getSprocatname());
            pos++;

            //S_AGENCYCODE
            ps.setString(pos, dto.getSagencycode());
            pos++;

            //S_AGENCYNAME
            ps.setString(pos, dto.getSagencyname());
            pos++;

            //S_EXPFUNCNAME
            ps.setString(pos, dto.getSexpfuncname());
            pos++;

            //S_EXPFUNCCODE1
            ps.setString(pos, dto.getSexpfunccode1());
            pos++;

            //S_EXPFUNCNAME1
            ps.setString(pos, dto.getSexpfuncname1());
            pos++;

            //S_EXPFUNCCODE2
            ps.setString(pos, dto.getSexpfunccode2());
            pos++;

            //S_EXPFUNCNAME2
            ps.setString(pos, dto.getSexpfuncname2());
            pos++;

            //S_EXPFUNCCODE3
            ps.setString(pos, dto.getSexpfunccode3());
            pos++;

            //S_EXPFUNCNAME3
            ps.setString(pos, dto.getSexpfuncname3());
            pos++;

            //S_EXPECOCODE
            ps.setString(pos, dto.getSexpecocode());
            pos++;

            //S_EXPECONAME
            ps.setString(pos, dto.getSexpeconame());
            pos++;

            //S_EXPECOCODE1
            ps.setString(pos, dto.getSexpecocode1());
            pos++;

            //S_EXPECONAME1
            ps.setString(pos, dto.getSexpeconame1());
            pos++;

            //S_EXPECOCODE2
            ps.setString(pos, dto.getSexpecocode2());
            pos++;

            //S_EXPECONAME2
            ps.setString(pos, dto.getSexpeconame2());
            pos++;

            //S_HOLD1
            ps.setString(pos, dto.getShold1());
            pos++;

            //S_HOLD2
            ps.setString(pos, dto.getShold2());
            pos++;

            //S_HOLD3
            ps.setString(pos, dto.getShold3());
            pos++;

            //S_HOLD4
            ps.setString(pos, dto.getShold4());
            pos++;

            //S_PAYEEACCTNO
            ps.setString(pos, dto.getSpayeeacctno());
            pos++;

            //S_PAYEEACCTNAME
            ps.setString(pos, dto.getSpayeeacctname());
            pos++;

            //S_PAYEEACCTBANKNAME
            ps.setString(pos, dto.getSpayeeacctbankname());
            pos++;

            //S_PAYSUMMARYCODE
            ps.setString(pos, dto.getSpaysummarycode());
            pos++;

            //S_PAYSUMMARYNAME
            ps.setString(pos, dto.getSpaysummaryname());
            pos++;

            //S_XPAYAMT
            ps.setBigDecimal(pos, dto.getSxpayamt());
            pos++;

            //S_XPAYDATE
            ps.setString(pos, dto.getSxpaydate());
            pos++;

            //S_XAGENTBUSINESSNO
            ps.setString(pos, dto.getSxagentbusinessno());
            pos++;

            //S_XADDWORD
            ps.setString(pos, dto.getSxaddword());
            pos++;

            //S_ID
            ps.setString(pos, dto.getSid());
            pos++;


           //S_BIZNO
           ps.setString(pos, dto.getSbizno());
           pos++;
           //S_SEQNO
           ps.setInt(pos, dto.getSseqno().intValue());
           pos++;
           ps.executeUpdate();
        }
        finally
        {
           close(ps);
        }

        return ;
    }
    
    
    
      /**
     * Update batch record in Database.
     */

    public void update(IDto[] _dtos, Connection conn, boolean isLobSupport) throws SQLException
    {
     
     	 HtvPayoutmsgsubDto dto  ;
         for (int i= 0; i< _dtos.length ; i++)
         {
            dto  = (HtvPayoutmsgsubDto)_dtos[i] ; 
            String msgValid = dto.checkValid() ;
            if (msgValid != null)
                throw new SQLException("修改错误，"+msgValid) ;
         }

        PreparedStatement ps = null;
        try {
            if(isLobSupport){
                ps = conn.prepareStatement(SQL_UPDATE_LOB);
            }
            else{
                ps = conn.prepareStatement(SQL_UPDATE);
            }
            
            for (int i= 0; i< _dtos.length ; i++)
            {
                dto  = (HtvPayoutmsgsubDto)_dtos[i] ; 
                int pos = 1;
                //S_ACCDATE
                 ps.setString(pos, dto.getSaccdate());
                pos++;

                //S_ECNOMICSUBJECTCODE
                 ps.setString(pos, dto.getSecnomicsubjectcode());
                pos++;

                //S_BUDGETPRJCODE
                 ps.setString(pos, dto.getSbudgetprjcode());
                pos++;

                //N_MONEY
                 ps.setBigDecimal(pos, dto.getNmoney());
                pos++;

                //S_FUNSUBJECTCODE
                 ps.setString(pos, dto.getSfunsubjectcode());
                pos++;

                //TS_SYSUPDATE
                 //S_TAXTICKETNO
                 ps.setString(pos, dto.getStaxticketno());
                pos++;

                //S_BGTTYPECODE
                 ps.setString(pos, dto.getSbgttypecode());
                pos++;

                //S_BGTTYPENAME
                 ps.setString(pos, dto.getSbgttypename());
                pos++;

                //S_PROCATCODE
                 ps.setString(pos, dto.getSprocatcode());
                pos++;

                //S_PROCATNAME
                 ps.setString(pos, dto.getSprocatname());
                pos++;

                //S_AGENCYCODE
                 ps.setString(pos, dto.getSagencycode());
                pos++;

                //S_AGENCYNAME
                 ps.setString(pos, dto.getSagencyname());
                pos++;

                //S_EXPFUNCNAME
                 ps.setString(pos, dto.getSexpfuncname());
                pos++;

                //S_EXPFUNCCODE1
                 ps.setString(pos, dto.getSexpfunccode1());
                pos++;

                //S_EXPFUNCNAME1
                 ps.setString(pos, dto.getSexpfuncname1());
                pos++;

                //S_EXPFUNCCODE2
                 ps.setString(pos, dto.getSexpfunccode2());
                pos++;

                //S_EXPFUNCNAME2
                 ps.setString(pos, dto.getSexpfuncname2());
                pos++;

                //S_EXPFUNCCODE3
                 ps.setString(pos, dto.getSexpfunccode3());
                pos++;

                //S_EXPFUNCNAME3
                 ps.setString(pos, dto.getSexpfuncname3());
                pos++;

                //S_EXPECOCODE
                 ps.setString(pos, dto.getSexpecocode());
                pos++;

                //S_EXPECONAME
                 ps.setString(pos, dto.getSexpeconame());
                pos++;

                //S_EXPECOCODE1
                 ps.setString(pos, dto.getSexpecocode1());
                pos++;

                //S_EXPECONAME1
                 ps.setString(pos, dto.getSexpeconame1());
                pos++;

                //S_EXPECOCODE2
                 ps.setString(pos, dto.getSexpecocode2());
                pos++;

                //S_EXPECONAME2
                 ps.setString(pos, dto.getSexpeconame2());
                pos++;

                //S_HOLD1
                 ps.setString(pos, dto.getShold1());
                pos++;

                //S_HOLD2
                 ps.setString(pos, dto.getShold2());
                pos++;

                //S_HOLD3
                 ps.setString(pos, dto.getShold3());
                pos++;

                //S_HOLD4
                 ps.setString(pos, dto.getShold4());
                pos++;

                //S_PAYEEACCTNO
                 ps.setString(pos, dto.getSpayeeacctno());
                pos++;

                //S_PAYEEACCTNAME
                 ps.setString(pos, dto.getSpayeeacctname());
                pos++;

                //S_PAYEEACCTBANKNAME
                 ps.setString(pos, dto.getSpayeeacctbankname());
                pos++;

                //S_PAYSUMMARYCODE
                 ps.setString(pos, dto.getSpaysummarycode());
                pos++;

                //S_PAYSUMMARYNAME
                 ps.setString(pos, dto.getSpaysummaryname());
                pos++;

                //S_XPAYAMT
                 ps.setBigDecimal(pos, dto.getSxpayamt());
                pos++;

                //S_XPAYDATE
                 ps.setString(pos, dto.getSxpaydate());
                pos++;

                //S_XAGENTBUSINESSNO
                 ps.setString(pos, dto.getSxagentbusinessno());
                pos++;

                //S_XADDWORD
                 ps.setString(pos, dto.getSxaddword());
                pos++;

                //S_ID
                 ps.setString(pos, dto.getSid());
                pos++;


               //S_BIZNO
               ps.setString(pos, dto.getSbizno());
               pos++;
               //S_SEQNO
               ps.setInt(pos, dto.getSseqno().intValue());
               pos++;
		       ps.addBatch(); 
	       }
	
           ps.executeBatch();
        }
        finally
        {
           close(ps);
        }

        return ;
    }



    /**
     * delete a record in Database.
     */
    public void delete(IPK _pk, Connection conn) throws SQLException
    {
       
      
       String msgValid = _pk.checkValid() ;
       if (msgValid != null)
       {
       		return ;
        //	throw new SQLException("删除错误，"+msgValid) ;
       }
       HtvPayoutmsgsubPK pk = (HtvPayoutmsgsubPK)_pk ;


       PreparedStatement ps = null;
       try {
           ps = conn.prepareStatement(SQL_DELETE);
           ps.setString(1, pk.getSbizno());
           ps.setInt(2, pk.getSseqno().intValue());
           ps.executeUpdate();
        }
        finally
        {
            close(ps);
        }
    }


     /**
     * delete batch record in Database.
     */
    public void delete(IPK[] _pks, Connection conn) throws SQLException
    {
        HtvPayoutmsgsubPK pk ;
        List pks = new ArrayList();  
	  
		for (int i= 0; i< _pks.length ; i++)
 		{
 			pk  = (HtvPayoutmsgsubPK)_pks[i] ; 
	    	String msgValid = pk.checkValid() ;
    		if (msgValid == null)
    		{
	    		pks.add(pk) ;
     		}
 		}
 		
 		
        PreparedStatement ps = null;
        try
         {
            ps = conn.prepareStatement(SQL_DELETE);
             
       		for (int i= 0; i< pks.size() ; i++)
       		{
       			pk  = (HtvPayoutmsgsubPK)(pks.get(i)) ; 
                ps.setString(1, pk.getSbizno());
                ps.setInt(2, pk.getSseqno().intValue());
		        ps.addBatch() ;
			}
            ps.executeBatch();
        }
        finally
        {
            close(ps);
        }      
    }



    /**
    * Check IChangerNumber or UpdateTimeStamp.
    */
    public void check(IDto _dto, Connection conn) throws SQLException
    {
                                                                                                                        		throw new SQLException("数据库表：HTV_PAYOUTMSGSUB没有检查修改的字段");
    }


	/**
	* synchronize values between parent and children
	*/
    public void syncToChildren(IDto _dto)     throws SQLException{
       throw new SQLException("本记录没有与其它记录相关联，无法进行主记录与明细记录之间的数据同步。");
  }

	/**
	*get children
	*/
    public IDto[] findChildren (IDto _dto, Connection conn, boolean isLobSupport)     throws SQLException{
         throw new SQLException("本dto没有与其它dto相关联，无法得到子dto");
  }    
    
  /**
   *return the children IDto class
   * @return Class
   */
  public Class getChildrenClass(){
	  return null;
  };    



    /**
     * Close JDBC Statement.
     * @param stmt  Statement to be closed.
     */
    private void close(Statement stmt)
    {
        if (stmt != null)
        {
            try
            {
                stmt.close();
            }
            catch(SQLException    e)
            {}
        }
    }

    /**
     * Close JDBC ResultSet.
     * @param rs  ResultSet to be closed.
     */
    private void close(ResultSet rs)
    {
        if (rs != null)
        {
            try
            {
                rs.close();
            }catch(SQLException  e){}
        }
    }
}
